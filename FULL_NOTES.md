# 컴퓨터비전 PBL

---
### 참가 대회

### 대회 기록
- EDA - 2025.09.16
- 특이elev 별 기상 - 2025.09.26
- 기발한 아이디어 데이터 - 2025.09.26
- 전처리(XGBOOST) - 2025.09.27
- feature(XGBOOST) - 2025.09.27
### 논문
- Alex Net
- ResNet
- Attention Is All You Need
### 종합 시험
- AlexNet
- ResNet
- Attention is All you Need

---

# EDA - 2025.09.16

### 대회의 주제와 목표
---

**주제 :**<br>기후변화와 물 부족 문제에 대응하기 위해, 기상 데이터와 지점 특성 데이터를 활용하여 **지하수 수위를 예측**하는 것이 목표

✅ **핵심 목표**:
- 기온, 강수량, 풍속, 습도, 기압 등 **기상 데이터**와
- 지점의 고도, 수리전도도, 위치, 유역 등 **지형·수문학적 특성**을 결합하여
- 특정 지점의 **향후 지하수 수위 변화**를 정확히 추정하는 모델을 개발하는 것
---
### 제공 데이터 
---
- **12개_지하수_지점_정보.csv**
- **역할**: 지점(관측소)의 **고정 특성 데이터**
- **주요 변수**
- `code_new`: 지점 코드 (1\~12번)
- 위도·경도: 지점 위치
- `수리전도도(cm/sec)`: 지하수 이동 특성 (투수성)
- `표고(EL.m)`: 지점 고도
- `river`: 해당 지점이 속한 유역(영섬, 낙동강, 한강, 금강)
- **2014_2020_underwater_weather_train.csv**
- **역할**: **학습용 시계열 데이터** (2014년\~2020년)
- **주요 변수**
- `ymd`: 날짜·시간 (1시간 단위 관측)
- `code_new`: 지점 코드 (1\~12)
- `elev`: 지하수 수위 (목표 변수, m)
- `wtemp`: 수온
- `ec`: 전기전도도
- 기상 데이터 : `기온(°C)`, `강수량(mm)`, `풍속(m/s)`, `습도(%)`, `현지기압(hPa)`, `지면온도(°C)`
- **2021_2023_underwater_weather_test_inputs.csv**
- **역할**: **테스트 입력 데이터** (2021년\~2023년)
- **특징**:
- `elev`(지하수 수위)는 제공되지 않음
- 대신 기온, 강수량, 풍속, 습도, 기압 등 **기상 변수만 제공**됨
- 모델이 학습 데이터(2014\~2020)로 배운 패턴을 바탕으로 **2021\~2023 지하수 수위 예측**을 해야 함
---
### EDA 분석 
---
1. **결측치 탐색**


## 변수별 결측 비율 막대그래프: missing_ratio_bar.png

[image omitted: temporary Notion asset]
- **결과(대략값):**
- MSLP (hPa): 약 20% → 가장 큼
- RH (%): 약 18%
- Ground temp (°C): 약 13%
- Air temp (°C): 약 10%
- Wind (m/s): 약 9%
- ec: 약 3%
- wtemp, Precip (mm), ymd, code_new, elev, date/year/month/day: 0% (결측 없음)



## 애트리뷰트별 결측률(스케일 0–2%): 



- Air temp (°C) [ 기온 ]
[image omitted: temporary Notion asset]
- RH (%)  [상대습도 ]
[image omitted: temporary Notion asset]
- Ground temp (°C)   [ 지면온도 ]
[image omitted: temporary Notion asset]


- ec  [ 전기전도도 ]
[image omitted: temporary Notion asset]
- Wind (m/s)  [ 풍속 ]
[image omitted: temporary Notion asset]
- MSLP (hPa)  [ 해면기준 기압 ]
[image omitted: temporary Notion asset]



1. **수위(elev) 추세 분석**


## 전체 일별 평균 수위 시계열: elev_daily_mean.png

[image omitted: temporary Notion asset]
---
- 2014\~2020 전 기간의 일평균 지하수 수위는 전반적으로 평탄한 추세 
- 중심 수준은 대략 110m 부근
- 일상적 변동폭은 소폭(대략 ±3\~5m)으로 보이며, 간헐적으로 더 큰 하강 스파이크(최저 약 85\~90m 추정)가 몇 차례 나타남
- 2020년 하반기 무렵부터 단기간 변동성이 커지는 구간이 보이고, 이 시기에 매우 큰 상승 스파이크(약 200m 이상 추정)가 1회 관찰
- 큰 스파이크 구간을 제외하면 장기적인 상승·하강 추세는 뚜렷하지 않음
---



## 연도별 일별 평균 수위 시계열

- 2014
[image omitted: temporary Notion asset]
- 2015
[image omitted: temporary Notion asset]
- 2016
[image omitted: temporary Notion asset]
- 2017
[image omitted: temporary Notion asset]
- 2018
[image omitted: temporary Notion asset]
- 2019
[image omitted: temporary Notion asset]
- 2020
[image omitted: temporary Notion asset]
---
- 2014\~2019년은 모두 비슷한 범위(대략 100\~125m)에서 움직이며, 매년 여름\~초가을(대략 7–9월)에 수위가 낮아지고 이후 가을\~겨울에 회복되는 연간 주기가 반복
- 연간 진폭은 보통 ±3–5m 수준이며, 2016·2018년에 여름 하강 폭이 비교적 크게 나타남(일시적으로 90m대 하락).
- 장기 추세(지속 상승/하락)는 뚜렷하지 않음
- 2020년은 기본 패턴은 유사하나, 중하반기에 단 한 번 매우 큰 상승 스파이크가 있어 최대값이 200m대를 기록
---



## 지점별(12개) 소멀티플 일별 평균 수위: elev_by_station.png

[image omitted: temporary Notion asset]
---
- 12개 지점 모두에서 연 1회 반복되는 뚜렷한 주기성이 보임 (연간 사이클)
- 위상(타이밍): 저점은 대체로 7–9월, 고점은 대체로 12–2월에 반복(지점별로 약 2–8주 정도 시차)
- 주기의 위상(언제 낮고 언제 높아지는지)은 지점 간에 거의 유사하나, 몇 주\~한두 달 정도의 미세한 시차가 있음
- 진폭은 지점별로 크게 다름: 일부 지점은 작은 진동(소폭)이고, 몇 지점은 매년 비교적 큰 진동(대폭) 패턴이 반복
---



## 연도별(2014–2020) 각 지점의 월평균: all_years_months_by_station

[image omitted: temporary Notion asset]
---
- 12개 지점 모두 연 1회 계절 주기가 뚜렷
- 공통 위상: 1–3월(또는 11–12월) 저점 → 7–9월 고점으로 올라가는 형태가 해마다 반복
- 지점 간 위상 차: 피크/저점 시기는 대체로 같지만 약 1–2개월의 시차가 존재
- 진폭(연간 변동 폭)은 지점별로 상이: 2·3·8·9번 지점은 비교적 크고, 10·11번은 작음
- 연도 간 재현성: 2014–2020 각 해의 곡선이 거의 겹쳐져, 같은 달의 수준이 해마다 유사
---



## 월별 오버레이(2014–2020, 선=연도)



- 1월
[image omitted: temporary Notion asset]
- 3월
[image omitted: temporary Notion asset]
- 5월
[image omitted: temporary Notion asset]
- 7월
[image omitted: temporary Notion asset]
- 9월
[image omitted: temporary Notion asset]
- 11월
[image omitted: temporary Notion asset]


- 2월
[image omitted: temporary Notion asset]
- 4월
[image omitted: temporary Notion asset]
- 6월
[image omitted: temporary Notion asset]
- 8월
[image omitted: temporary Notion asset]
- 10월
[image omitted: temporary Notion asset]
- 12월
[image omitted: temporary Notion asset]



1. **변수 상관분석**


## 수위·기상 변수 간 상관 히트맵: results_graph/correlation_heatmap.png

[image omitted: temporary Notion asset]
---
- elev과 기상요인 동시상관은 전반적으로 매우 약함 
-  MSLP(hPa)와만 중간 수준의 음의 상관(r≈−0.37), 나머지는 거의 0
- 기상 변수 내부 상관은 뚜렷
- Air temp↔Ground temp 매우 강한 양의 상관(r≈0.95), Air temp↔MSLP 중간 음의 상관(r≈−0.53), Wind↔RH 중간 음의 상관(r≈−0.49).
- Precip(mm)은 다른 변수 및 elev과의 동시상관이 거의 없음 (\|r\|≤0.14).
---



## 지연 상관: elev(t)와 각 기상 변수 X(t−L)의 상관(L=0,1,2,3일) 히트맵

[image omitted: temporary Notion asset]
---
- 그래프 의미: 
오늘 수위(elev)가 당일(L0)·1\~3일 전(L1\~L3) 기상값과 얼마나 함께 움직였는지(상관계수 r)를 색과 숫자로 보여주는 표
- 가장 뚜렷한 관계: 기압(MSLP)과 보통 수준의 음의 상관(r≈−0.38)
- 그다음: ec r≈−0.28, 풍속 r≈−0.14, 수온(wtemp) r≈−0.11 정도의 약한 음의 상관
- 거의 없음: 상대습도 RH r≈+0.07, 기온 r≈−0.03, 지면온도 ≈0, 강수 r≈+0.01
- L0\~L3 사이 변화 거의 없음 → 3일 이내 단기 지연효과는 뚜렷하지 않음
---

1. **강수 분석**


## 연도별 강수량 & 24시간 롤링합 시계열

- 2014
[image omitted: temporary Notion asset]
- 2015
[image omitted: temporary Notion asset]
- 2016
[image omitted: temporary Notion asset]
- 2017
[image omitted: temporary Notion asset]
- 2018
[image omitted: temporary Notion asset]
- 2019
[image omitted: temporary Notion asset]
- 2020
[image omitted: temporary Notion asset]
---
- 공통 패턴: 모든 해에서 강수는 6–9월에 집중되고, 그 시기에 24시간 누적강수 피크가 반복적으로 나타남
- 이벤트 양상:
• 2014, 2016, 2018 — 소수의 매우 큰 스파이크(태풍·집중호우성)
• 2019 — 스파이크는 많지만 키가 낮은 편
• 2020 — 7–8월에 중간 규모 스파이크가 군집해 나타남
- 비수기(11–3월): 대부분 미약하거나 무강수 구간이 길게 이어짐
**elev와의 관계**
- 연중 위상: 강수 피크는 공통적으로 6–9월(장마/태풍)에 집중되는 반면, elev는 같은 시기에 낮고 겨울에 높아짐→ 계절 위상은 서로 반대
- 단기 반응: 강우 스파이크가 발생한 날·그 직후(0–3일) elev에서 일관된 동시 상승은 관찰되지 않음(동시·단기 상관 약함)
- 연도별 대비: 2014·2016·2018처럼 24h 누적 200\~300mm급 이벤트가 있어도 같은 시기 elev의 뚜렷한 동행 피크는 보이지 않음
-  2019년처럼 강수 피크가 약한 해에도 elev의 연 1주기(여름 저점·겨울 고점)는 유지
- 즉, 이 그래프들 기준에서 강수와 elev의 관계는 “즉시 동행”이라기보다, 계절 위상이 반대이고(여름 비↑ vs 수위↓), 단기(며칠 이내) 영향은 미약
---

1. **MSLP 분석**


## 연도별 MSLP 원시값 + 24시간 이동평균

- 2014
[image omitted: temporary Notion asset]
- 2015
[image omitted: temporary Notion asset]
- 2016
[image omitted: temporary Notion asset]
- 2017
[image omitted: temporary Notion asset]
- 2018
[image omitted: temporary Notion asset]
- 2019
[image omitted: temporary Notion asset]
- 2020
[image omitted: temporary Notion asset]
---
- **연 1주기 뚜렷:** 겨울(12–2월) 고기압, 여름(6–9월) 저기압의 U자형 패턴이 모든 해에서 반복
- **여름 저기압 이벤트:** 
- 2014·2016·2018년은 7–9월에 깊은 저기압 스파이크(태풍/장마성)가 두드러짐
- 2017은 중간, 2019·2020은 극단적 저기압은 비교적 적지만 계절 저점은 명확
- **변동성:** 수일 규모의 요동(대략 5\~15 hPa)이 상시 존재하며, 여름철에 변동 폭이 더 큼, 겨울은 상대적으로 안정적
- **연도별 특징(한 줄):**
- 2014/2016/2018: 여름 저기압 하강이 가장 깊고 짧은 급락 다수
- 2017: 여름 저기압 구간이 길게 이어지는 편
- 2019/2020: 여름 저점은 있으나 극저압 빈도는 낮고, 연말 고기압 회복이 뚜렷
---



## 연도별 표준화 시계열

- 2014
[image omitted: temporary Notion asset]
- 2015
[image omitted: temporary Notion asset]
- 2016
[image omitted: temporary Notion asset]
- 2017
[image omitted: temporary Notion asset]
- 2018
[image omitted: temporary Notion asset]
- 2019
[image omitted: temporary Notion asset]
- 2020
[image omitted: temporary Notion asset]
---
**그래프**
- 해마다 일일 중앙값(지점 합산)으로 만든 두 시계열을 표준화(z-score) 해서 겹쳐 그림
**결과**
- 공통 패턴: 겨울\~봄·가을 구간에서 두 선이 여러 주 단위로 같이 움직이는 구간이 자주 보임
→ 단기·시나틱(수일\~수주) 스케일의 **대기압-수위 역반응**이 존재
- **여름(7–9월)에는 분리:** 
- MSLP가 크게 올라가는데(저기압 심화) **elev는 대체로 하강**해 두 선이 벌어지는 구간이 반복
- 즉, 여름에는 증발산/양수 등 **비대기압 요인**이 지배적이라 역상관 신호가 희석
- **연도별 특징:**
- 2014–2018: 봄·가을에는 동조가 비교적 뚜렷하나, 여름엔 분리. 일부 기간의 **급격한 음의 스파이크**는 집계/이상치 영향으로 보임
- 2019: 두 선의 **동행 구간이 가장 안정적**(역상관 신호 선명)
- 2020: 전반 변동이 작고 중반 한 차례 **수위 단발성 스파이크**가 나타나 −MSLP와 괴리(대기압 외 요인/아티팩트 가능)
- **결론:** 연중 내내 강력한 역상관은 아니지만, **겨울·봄·가을엔 −MSLP와 수위가 꽤 동조**하고, **여름엔 분리**
-  이는 전체 상관(r≈−0.37)이 “계절에 따라 세기가 달라지는 역상관”임을 보여줍니다.
---

1. **계절성·주기성 분석**


## 일 평균 수위 시계열의 자기상관함수(ACF, 90일 래그)

[image omitted: temporary Notion asset]
---
- 일평균 지하수 수위의 자기상관(ACF) 그래프
- 축 뜻: x축은 시차(일), y축은 자기상관계수(−1\~1)
- 해석: 점/막대가 높을수록 “그 시차만큼 떨어진 과거 수위와 지금 수위가 비슷하다(연속성·기억이 강하다)”는 뜻, 0에 가까우면 유사성이 거의 없다는 뜻
- 0\~10일 구간에서 값이 크게 시작해 서서히 감소 → 단기(약 1\~2주) 지속성이 강함
- 30일 전후, 60일 부근에 작은 양의 봉우리 → 약한 월 단위(\~1개월) 성분 존재
- 20일 이후 전반적으로 작고 들쑥날쑥 → 90일 이내 뚜렷한 장주기는 보이지 않음
---



## FFT 기반 주기 스펙트럼(최대 400일 주기 범위의 피리어도그램)

[image omitted: temporary Notion asset]
---
**그래프 설명**
- 일평균 지하수 수위를 FFT로 분해한 피리어도그램
-  x축은 주기(일), y축은 해당 주기의 강도(Amplitude)로, 봉우리가 클수록 그 주기가 데이터에 강하게 존재
**결과**
- 가장 큰 피크가 280\~365일 부근에 있어 뚜렷한 연 1주기(계절성)가 확인
- 30\~60일, 90\~120일대에 약한 보조 피크가 있어 월·분기 수준의 약한 반복 성분이 있음
- 20일 이하의 짧은 주기는 잡음성 변동이 많아 지배적이지 않음
---

---

---

# 특이elev 별 기상 - 2025.09.26

## 개요
---
✅ 모든 주기 중 가장 변동이 심했던 **2020년 7\~9**월 사이의 elev에 대해서 당시 날짜의 기상과 비교
[image omitted: temporary Notion asset]
✅ 과거 관측은 기상청 날씨누리와 날씨 관련 기사(ex - 태풍, 가뭄 등)를 통해 진행

✅ 영섬권역, 한강권역, 낙동강권역, 금강권역 대표 도시 선정

- 영섬권역 - 광주
- 한강권역 - 서울
- 금강권역 - 대전
- 낙동강권역 - 대구
✅ 과거 관측에서 사용되는 데이터는 malab-5 서버에서
EDA →  results_eda → elev_results → month → 2020 
- 2020_07_by_day_station.png
- 2020_08_by_day_station.png
- 2020_09_by_day_station.png 사용 
---
## 2020.07
---
[image omitted: temporary Notion asset]
### ⛅ 기온/강수량


## 한강권역(서울) - station 3

[image omitted: temporary Notion asset]
1. ** 7월 10\~14일**
- **수위 그래프**: 약간의 상승 시작
- **기상 데이터**:
- 7/13: 일강수량 47.6mm
- 7/14: 일강수량 11.5mm
→ 이 구간에서 집중호우가 있었고, 바로 수위 상승으로 연결
2. ** 7월 19\~24일 **
- **수위 그래프**: 일부 지점에서 다시 변동 시작
- **기상 데이터**:
- 7/19: 29.1mm
- 7/20: 3.6mm
- 7/22: 11.5mm
- 7/23: 103.1mm (이달 최고치, 폭우)
- 이 날은 강수량이 압도적으로 많아서 여러 관측소 수위가 확 튀는 현상
- 7/24: 2.9mm
→ 중간 강도의 강우가 이어져 수위 변화 재차 발생
3. ** 7월 27\~31일 **
- **수위 그래프**: 월말에 다시 큰 폭 상승
- **기상 데이터**:
- 7/27: 2.2mm
- 7/29: 50.1mm
- 7/31: 1.5mm
→ 또 한 번 큰 비가 내려 수위가 전반적으로 다시 상승



## 영섬권역(광주) - station 1, 4, 5, 8, 10, 11, 12

[image omitted: temporary Notion asset]
1. ** 7월 10\~15일**
- **수위 그래프**:  S1·S4·S5·S8·S11·S12에서 급등 시작, S10은 반응이 작고 지연
- **기상 데이터**:
- 7/10: 68.1mm
- 7/12: 37.0mm
- 7/13: 121.2mm (이달 최고치, 폭우)
- 7/15: 9.8mm
→ 이 구간의 국지적 집중호우가 바로 수위 상승으로 연결됨
2. ** 7월 20\~24일 **
- **수위 그래프**: S8·S12는 두 번째 봉우리 뚜렷, S10은 계단식 상승, 나머지는 소폭 변동
- **기상 데이터**:
- 7/20: 6.1mm
- 7/21: 9.4mm
- 7/22: 19.7mm
- 7/23: 54.1mm
- 7/24: 21.9mm
→ 20\~21일은 약했고, 22\~24일 강우가 본격적인 두 번째 파동을 만듦
3. ** 7월 27\~30일 **
- **수위 그래프**: S1·S4·S8·S10·S12가 월말에 최고 수준까지 상승, S5는 중간 수준, S11은 파동 반복
- **기상 데이터**:
- 7/27: 8.9mm
- 7/28: 47.8mm
- 7/29: 115.5mm
- 7/30: 3.2mm
→ 29일 집중호우 영향으로 월말 피크 형성(지점별 0\~2일 시차 존재)


## station 10 다른 양상 

[image omitted: temporary Notion asset]
- 2020.7 수원 기상
[image omitted: temporary Notion asset]
[image omitted: temporary Notion asset]




## 낙동강권역(대구) - station 2, 7, 9

[image omitted: temporary Notion asset]
1. **7월 10\~15일**
- **수위 그래프**:  S2·S7·S9 모두 12\~15일 사이 급상승
- **기상 데이터**:
- 7/10: 26.2mm
- 7/12: 27.8mm
- 7/13: 54.5mm
- 7/15: 9.8mm
→ 초중반 누적 강우로 13\~15일 1차 피크 형성
2. ** 7월 21\~25일 **
- **수위 그래프**: 최대 강우(7/22–23)였지만 반응은 완만/지연
- **기상 데이터**:
- 7/21: 5.9mm
- 7/22: 45.6mm
- 7/23: 63.1mm (이달 최고치, 폭우)
- 7/24: 11.5mm
- 7/25: 13.7mm
→ 강우량은 최대였지만 다른 station보다는 최대치 강수량이 적음
3. ** 7월 27\~30일 **
- **수위 그래프**: 누적 강우(7/27–29)로 재피크: S2·S9는 30–31 고점, 7은 31일 반등
- **기상 데이터**:
- 7/27: 16.9mm
- 7/28: 14.6mm
- 7/29: 3.9mm
- 7/30: 36.0mm
→ 27\~29일 누적 강우 영향으로 말에 재피크



## 금강권역(대전) - station 6

[image omitted: temporary Notion asset]
1. **7월 10\~15일**
- **수위 그래프**: 13\~14일 급등 후 완만 하강
- **기상 데이터**:
- 7/10: 9.5mm
- 7/12: 43.8mm
- 7/13: 117.9mm
- 7/14: 6.1mm
- 7/15: 9.3mm
→ 첫 이벤트의 강우가 커서 1차 피크 형성
2. ** 7월 19\~25일 **
- **수위 그래프**: 23\~25일 재상승(2차 봉우리)
- **기상 데이터**:
- 7/19: 7.2mm
- 7/20: 20.2mm
- 7/22: 25.1mm
- 7/23: 52.8mm 
- 7/24: 13.5mm
- 7/25: 2.6mm
→ 중간 강도의 연속 강우로 재상승
3. ** 7월 27\~31일 **
- **수위 그래프**: 30\~31일 월말 최고치
- **기상 데이터**:
- 7/27: 2.6mm
- 7/28: 6.6mm
- 7/29: 67.9mm
- 7/30: 141.3mm (이달 최고치, 폭우)
- 7/31: 16.5mm
→ 말일 직전의 대규모 호우로 최종 피크 형성

---
## 2020.08
---
[image omitted: temporary Notion asset]
### ⛅ 기온/강수량


## 한강권역(서울) - station 3

[image omitted: temporary Notion asset]
1. **8월 1\~11일**
- **수위 그래프**: 초반 급상승 후 높은 수준 유지
- **기상 데이터**:
- 8/1: 39.1mm
- 8/2: 31.4mm
- 8/3: 102.6mm (이달 최고치, 폭우)
- 8/5: 18.5mm
- 8/6: 75.5mm
- 8/8: 19.1mm
- 8/9: 50.3mm
- 8/10: 44.6mm
- 8/11: 97.0mm
→ 연속 호우 영향으로 빠르게 올라 고수위를 유지
2. ** 8월 15일**
- **수위 그래프**: 고수위 유지하며 12\~15일 소봉우리
- **기상 데이터**:
- 8/15: 69.0mm
3. ** 8월 21\~22일**
- **수위 그래프**: 하강 추세 속 약한 반등
- **기상 데이터**:
- 8/21: 2.5mm
- 8/22: 42.6mm
→ 중간 강도의 강우로 일시적 반등
1. **8월 27\~30일**
- **수위 그래프: **말에 다시 소폭 상승
- **기상 데이터:**
- 8/28: 13.6mm
- 8/29: 13.6mm
- 8/30: 42.5mm
→ 누적 강우와 30일 비로 월말 재상승



## 영섬권역(광주) - station 1, 4, 5, 8, 10, 11, 12

[image omitted: temporary Notion asset]
1. **8월 5\~12일**
- **수위 그래프**: S1·5·8·11·12가 7–11일 1차 최고치, S10은 10–12일 고점, S4는 7–8일 피크 후 하강
- **기상 데이터**:
- 8/6: 38.8mm
- 8/7: 259.5mm (이달 최고치, 폭우)
- 8/8: 255.5mm
- 8/9: 17.6mm
- 8/10: 72.3mm
→ 초대형 연속 호우로 초순에 고수위 형성.
2. ** 8월 26\~31일**
- **수위 그래프**: 일부 지점 말일 소폭 추가 상승
- **기상 데이터**:
- 8/26: 33.6mm
- 8/29: 26.7mm
- 8/31: 10.0mm
→ 약한 비 영향으로 잔여 상승



## 낙동강권역(대구) - station 2, 7, 9

[image omitted: temporary Notion asset]
1. **8월 6\~11일**
- **수위 그래프**: 공통 급등→고수위. S2: 9–11 피크, S7: 6–10 고원→완만↓, S9: 6–10 고원 유지
- **기상 데이터**:
- 8/7: 84.7mm
- 8/8: 168.2mm (이달 최고치, 폭우)
- 8/10: 18.1mm
→ 초대형 연속 호우로 초순에 고수위 형성.
2. ** 8월 30일**
- **수위 그래프**: S9가 말일에 약한 반등, S2·S7은 영향 제한적
- **기상 데이터**:
- 8/30: 28.8mm
→ 단일 강우 영향으로 소폭 반응



## 금강권역(대전) - station 6

[image omitted: temporary Notion asset]
1. **8월 1\~12일**
- **수위 그래프**: 초순 급등→10\~12일 최대
- **기상 데이터**:
- 8/1: 17.5mm
- 8/4: 18.5mm
- 8/5: 24.4mm
- 8/6: 24.0mm
- 8/7: 34.9mm
- 8/8: 42.9mm
- 8/9: 32.1mm
- 8/10: 17.5mm
- 8/11: 59.3mm (이달 최고치, 폭우)
- 8/12: 17.5mm
→ 연속 호우가 0\~2일 시차로 반영되어 초순 고수위 형성, 10\~12일 정점
2. ** 8월 22일**
- **수위 그래프**: 하강 추세 속 약한 반등
- **기상 데이터**:
- 8/22: 11.8mm
→ 단일 강우로 일시적 회복
1. **8월 26\~31일**
- **수위 그래프**: 말에 소폭 재상승
- **기상 데이터**:
- 8/28: 27.4mm
- 8/29: 15.3mm
→ 28\~29일 강우 영향으로 말일에 약한 재상승

---
## 2020.09
---
[image omitted: temporary Notion asset]
### ⛅ 기온/강수량


## 한강권역(서울) - station 3

[image omitted: temporary Notion asset]
1. **9월 2\~3일**
- **수위 그래프**: 초반 소상승/유지
- **기상 데이터**:
- 9/2: 33.3mm
- 9/3: 29.8mm
→ 초순 강우로 초기 수위가 높게 유지됨
2. ** 9월 6\~7일**
- **수위 그래프**: 일시 반등 또는 고원 유지 후 하강 전환
- **기상 데이터**:
- 9/6: 6.4mm
- 9/7: 61.0mm
→ 7일 강우로 단기 반등
3. **9월 9\~12일**
- **수위 그래프**: 하강 지속, 변동 작음
- **기상 데이터**:
- 9/10: 3.9mm
- 9/12: 5.7mm
→ 약한 비로 영향 제한적
1. **9월 30일**
- **수위 그래프**: 비는 많았지만 같은 날 반등은 없음
- **기상 데이터**:
- 9/30: 38.3mm



## 영섬권역(광주) - station 1, 4, 5, 8, 10, 11, 12

[image omitted: temporary Notion asset]
1. **9월 2\~3일**
- **수위 그래프**: 초반 완만 상승/어깨 형성(S1·4·8·11·12), S10 상승 시작, S5는 미약
- **기상 데이터**:
- 9/2: 49.4mm
- 9/3: 17.0mm
→ 약한 비로 초기가동
2. ** 9월 6\~13일**
- **수위 그래프**: 대부분 6\~10일 월내 고점 또는 고원
- **기상 데이터**:
- 9/6: 11.3mm
- 9/7: 30.1mm
- 9/9: 25.3mm
- 9/11: 26.6mm
→ 중강도 강우가 여러 차례(6–7, 9, 11일) 들어오며 고수위 유지·확대
3. **9월 16\~18일**
- **수위 그래프**: 공통 하강 지속(소폭 요철), 뚜렷한 반등 없음
- **기상 데이터**:
- 9/16: 2.8mm
- 9/17: 2.0mm
- 9/18: 3.7mm
→ 약한 강우로 추세 변화 없음



## 낙동강권역(대구) - station 2, 7, 9

[image omitted: temporary Notion asset]
1. **9월 2\~12일**
- **수위 그래프**: 초순 고수위 형성
- S2는 2–3일 최고치 후 서서히 하강, S7은 5–6일·10일 이중 피크, S9는 4–5일 피크→6일 하강→9–11일 재상승(어깨)
- **기상 데이터**:
- 9/2: 44.7mm
- 9/3: 44.8mm
- 9/6: 18.3mm
- 9/7: 34.4mm
→ 초순의 연속 강우(특히 2일·6–7일)로 초반 피크/고원이 만들어지고, 9–12일의 약한 비로 높은 수준이 유지
2. ** 9월 16\~18일**
- **수위 그래프**: 세 지점 모두 하강 지속
- **기상 데이터**:
- 9/16: 1.3mm
- 9/17: 1.7mm
- 9/18: 1.9mm
→ 약한 비로 추세 변화 없음



## 금강권역(대전) - station 6

[image omitted: temporary Notion asset]
1. **9월 2\~3일**
- **수위 그래프**: 초반 소상승(어깨). 본격 피크는 4일 강우 이후 형성
- **기상 데이터**:
- 9/2: 26.2mm
- 9/3: 61.8mm
→ 초반 비로 기저 수위가 살짝 올라감
2. ** 9월 6\~12일**
- **수위 그래프**: 6\~8일 재상승해 소봉우리, 9\~12일 완만 하강·유지
- **기상 데이터**:
- 9/6: 22.8mm
- 9/7: 29.0mm
- 9/9: 9.9mm
- 9/12: 11.7mm
→ 중강도 강우가 연속해 두 번째 고원을 만들고, 약한 비로 높은 수준이 유지
3. **9월 16\~19일**
- **수위 그래프**: 하강 지속, 눈에 띄는 반등 없음
- **기상 데이터**:
- 9/16: 4.8mm
- 9/19: 4.1mm
→ 약한 강우로 추세 변화 없음

---
## 2020.7\~9 태풍
---

[image omitted: temporary Notion asset]


- 태풍 8\~9월 존재
[image omitted: temporary Notion asset]


[image omitted: temporary Notion asset]


▶️  <br>2020년 8\~9월에 태풍 **바비(8/22\~27), 마이삭(8/28\~9/3), 하이선(9/1\~7)**이 연속적으로 한반도에 영향을 줬지만, 정작 지하수위 일별 그래프에는 큰 폭의 변화가 뚜렷하게 나타나지 않음<br><br>
1. ** 태풍의 강수 패턴 차이**
- 바비, 마이삭, 하이선 모두 강풍 중심의 태풍이었고, 우리나라 남부·동해안을 중심으로 피해가 집중
- 강수량은 태풍의 이동 경로에 따라 국지적으로 집중되었고, 전국적으로 고르게 내린 폭우 X
1. **지하수위 반응의 지연 및 완충 효과**
- 지하수위는 즉각적으로 반응하지 않고 며칠에서 수주 단위로 지연 가능
- 또 토양의 특성(투수성, 지하수위 심도 등)에 따라 강수 유입이 완충되어, 단기 폭우가 바로 수위 급등으로 이어지지 않을 수 있음
1. **해당 지역의 기상 조건**
- 8월(바비 시점)과 9월(마이삭·하이선 시점) 초반에는 약간의 수위 상승이 관측되지만, 전체적으로는 빠르게 다시 하강 추세를 보임
- 태풍의 비구름대가 해당 지역을 스치면서 일시적 강수가 있었으나 누적량이 적거나 짧아 지하수위 유지에 크게 기여하지 못한 것으로 해석 가능
### 🍃 태풍의 저기압은 elev의 영향이 없을까?
[image omitted: temporary Notion asset]
**▶️ Shelbina 관측정 지하수위(파랑, 우측 y축) 와 기압(주황, 좌측 y축) 변화**
1. **태풍의 기압 변화 크기**
- 보통 태풍 중심부는 **940\~960 hPa**, 주변부는 **1010\~1015 hPa** 정도
- 즉, **40\~70 hPa 차이**가 발생
2. **Shelbina 그래프의 기압 변화 크기**
- 그래프 좌측 축은 Barometric pressure (ft of water) 단위
- 범위: 약 29.4 \~ 31.0 ft of water → 약 1.6 ft = 50 cm 수두 차이
- 1 hPa ≈ 1 cm 수두로 환산하면 → **약 50 hPa 기압 변화**
- 따라서, 이 그래프의 기압 변화 크기는 **실제 태풍이 올 때와 비슷한 수준**
1. **기압 변화에 따른 수위 변화**
- 같은 구간에서 지하수위(오른쪽 축, Depth to water level)는 **약 25.4 \~ 26.8 ft** → 총 변화 폭은 1.4 ft ≈ **43 cm**
- 그러나 개별 기압 파동에 동반되는 수위 반응은 대부분 **5\~10 cm 정도**에 불과
- 예: 18 cm 기압 하락 → 수위 약 6 cm 상승 (Barometric Efficiency ≈ 0.3, 30%)
---

---

# 기발한 아이디어 데이터 - 2025.09.26

## 지하수 수위와 연관된 데이터
---
### 💧 생수 공장·지하수 취수 시설 위치
> 생수 공장, 대형 취수장은 대규모 양수를 지속적으로 하므로 해당 지역 지하수 수위가 낮아질 가능성이 높음
**데이터 출처:**

### 🏭 대규모 공장·산업단지의 지하수 사용량
> 반도체, 발전소, 식음료 등 대형 산업단지는 공정 과정에서 대량의 지하수를 사용 <br>→ 주변 지하수위 패턴에 직접적 영향
**데이터 출처:**

### 🧑‍🌾 농업·관개용 지하수 취수량
> 농번기(특히 벼농사, 밭작물 집중 지역)에는 관개용 지하수 사용량이 급증 <br>→ 계절적 수위 변동이 크게 발생
**데이터 출처:**

### ♨️ 온천·스파 개발 지역
> 온천·스파는 지하수를 직접 이용 → 해당 지역은 수위뿐 아니라 수온까지 변화 가능성이 높음
**데이터 출처:**

---

---

# 전처리(XGBOOST) - 2025.09.27

## 전처리 방법 
---
### **1. 이벤트 태깅 (****`is_event`****)**
- **where** : 지점별로
- 최근 3타임 누적 강수가 상위 15% 이상(`PP_RAIN_Q=0.85`), 또는
- 기압 3타임 전 대비 하강량이 하위 15% 이하(`PP_PRES_Q=0.15`)면 
= 그 시점을 이벤트(폭우/급변)으로 표시


## 이유

- **왜 3타임 누적강수?**
한 번 “틱” 비는 노이즈일 수 있어요. 3칸 누적을 보면 “짧은 시간에 몰아치는 비(호우)”를 더 안정적으로 잡음
- **왜 상위 15%(q=0.85)?**
지점마다 비의 스케일이 달라서 절대값 임계치가 안 맞아요. 분위수로 자르면 각 지점에 맞게 “센 비만” 고르게 뽑힘
- **왜 기압 하강 하위 15%(q=0.15)?**
큰 비는 보통 **급격한 기압 하강**과 함께 와요. 강수 데이터가 비거나 약해도 기압으로 보완해서 이벤트를 안 놓치게 함

- **why **: 폭우/급변 시점은 수위가 급격히 움직여 피크 예측(NSE)에 특히 중요
- 전처리 단계에서 이벤트를 표시만 해두면, 이후 모델 입력에 넣어 피크 구간을 더 잘 학습시킬 수 있음
### **2. 수위 이상치 탐지 (****`is_outlier`****) – Hampel**
- **where** : 지점별 수위에 Hampel 필터 적용(창 크기 `K=5`, 임계 `THR=6.5`)
- 중앙값±6.5·(robust σ) 바깥의 진짜 튐만 이상치 후보로 잡음


## hampel 필터

### Hampel 필터
- **아이디어**: “평균·표준편차” 대신 중앙값(Median)과 MAD(중앙절대편차)로 이상치를 잡는 **robust(튀는값에 강한)** 필터
- **어떻게**: 윈도우(여기선 **K=5**) 안에서
1. 중앙값 `med` 계산
2. MAD 계산 → robust σ ≈ **1.4826 × MAD**
3. 현재 값이 `med ± THR × robust σ` 바깥이면 **이상치**로 표시
- **왜 좋음**: 평균/표준편차는 스파이크가 끼면 같이 흔들리지만, **중앙값·MAD는 스파이크에 둔감**해서 **진짜 튐**만 잘 잡음
### 왜 이 문제에서 Hampel 사용했는가
- 우리 데이터는 폭우 피크(자연 변화)와 센서 오류(비자연 튐)가 섞여 있음
- 폭우 피크는 **살리고**, 센서 스파이크만 **부드럽게 보정**해야 함
- Hampel은 자연스런 큰 변화(피크)에는 관대하고, **센서성 급튀기**만 골라내기에 딱 맞음
### 왜 임계값 THR=**6.5인가**
- 보통 시계열에선 **3\~4**도 쓰지만, 그러면 **폭우 피크까지 ‘이상치’로 오인**할 위험이 있음
- 우리는 피크(자연)를 거의 건드리지 않으려는 목적이라 **임계를 크게(6.5)** 잡아
**정말 말도 안 되는 급튀기**만 이상치로 보게 함
→ 결과: **피크는 보존(NSE 보호)**, **센서 튐만 완만 보정(KGE 안정)**

- **why** : 센서 오류 같은 비물리적 스파이크를 구분하려고.
- 임계를 높게(6.5) 둬서 폭우 피크(자연 변화)는 최대한 건드리지 않고, 센서성 튐만 걸러냄
### **3. 센서성 이상치 완만 보정 (****`PP_SMOOTH_SENSOR=True`****)**
- **where **: Hampel로 잡힌 이상치 중 **이벤트가 아닌** 구간만 대상으로, 
- 수위를 7타임 이동 중앙값으로 부드럽게 대체
- **why **: 센서 오류로 보이는 급격한 튐만 살짝 눌러 KGE 안정성(분산/상관)을 지키고,
- 폭우 피크는 그대로 보존해서 NSE를 해치지 않기 위함
- 7타임 이동 중앙값인 이유: 
- 7은 아주 짧은 국소 구간(양옆 3칸씩 + 자기 자신)이라 급튀기만 깎고 전체 모양은 거의 그대로 
### **4. 기상변수 결측 대치(****`is_imputed`****)**
- **where** : 기온/강수/풍속/습도/기압/지면온도 등 입력 기상변수의 결측을
- 연속 ≤ 2타임: 시간보간 (interpolate(method="time"))
- 연속 3\~10타임: (지점, 월) 평균값으로 채움
- 연속 \> 10타임: 대치하지 않고 그대로 남김
- 그리고 채운 위치는 `is_imputed=1`로 표기
- **why** : 짧은 구멍은 원래 흐름을 살리면서 자연스럽게 메우고,
- 좀 긴 구멍은 계절 스케일을 해치지 않도록 월평균으로 보수적으로 채움
- 아주 긴 구멍은 과도한 인공 신호를 만들지 않기 위해 그대로 둠
- 플래그를 남겨 두면 모델이 불확실 구간을 인지할 수 있음
---
## 전처리 결과
---
### baseline
```bash
 code_new     NSE     KGE  AVG(NSE,KGE)
        1  0.4954  0.6915        0.5935
        2  0.6810  0.7440        0.7125
        3  0.8207  0.8919        0.8563
        4  0.6296  0.7452        0.6874
        5  0.4015  0.6705        0.5360
        6  0.8092  0.8176        0.8134
        7  0.2953  0.7549        0.5251
        8  0.4263  0.6363        0.5313
        9  0.5976  0.7894        0.6935
       10 -4.9227  0.7492       -2.0867
       11  0.4811  0.6745        0.5778
       12  0.6391  0.7075        0.6733
MEAN(ALL)  0.1128  0.7394        0.4261
```
### After preprocessing
```bash
 code_new     NSE     KGE  AVG(NSE,KGE)
        1  0.4989  0.6952        0.5971
        2  0.6821  0.7478        0.7149
        3  0.8243  0.8975        0.8609
        4  0.6289  0.7478        0.6884
        5  0.4042  0.6769        0.5405
        6  0.8091  0.8242        0.8167
        7  0.3017  0.7607        0.5312
        8  0.4293  0.6426        0.5360
        9  0.5998  0.7999        0.6998
       10 -4.8770  0.7357       -2.0706
       11  0.4869  0.6885        0.5877
       12  0.6289  0.7090        0.6690
MEAN(ALL)  0.1181  0.7438        0.4310
```
### 결과 그래프 {toggle="true"}
- **station 1**
[image omitted: temporary Notion asset]
- **station 2**
[image omitted: temporary Notion asset]
- **station 3**
[image omitted: temporary Notion asset]
- **station 4**
[image omitted: temporary Notion asset]
- **station 5**
[image omitted: temporary Notion asset]
- **station 6**
[image omitted: temporary Notion asset]
- **station 7**
[image omitted: temporary Notion asset]
- **station 8**
[image omitted: temporary Notion asset]
- **station 9**
[image omitted: temporary Notion asset]
- **station 10**
[image omitted: temporary Notion asset]
- **station 11**
[image omitted: temporary Notion asset]
- **station 12**
[image omitted: temporary Notion asset]
---

---

# feature(XGBOOST) - 2025.09.27

## Feature 종류
---
### 원데이터에서의 Feature (시간 이동 스냅샷 형태)
- 원본 기상 6개: `temp, precip, wind, humidity, pressure, ground_temp`
- 다만 **앵커 윈도우 스냅샷**으로 들어감
→ `DAY_OFFSETS=[3, 8]`이면, 3일 전을 기준(anchor) 으로 총 6일(=144시간) 구간의 과거 원시값을 그대로 펼쳐서 사용
### 전처리/파생 Feature
- **WEATHER_LAGS = [1, 2, 3]**
- 각 변수의 **t-1, t-2, t-3 타임스텝 전 값**
- **시간 주기 피처 **(USE_TIME_FEATS=True):
- `hour_sin, hour_cos, dow_sin, dow_cos, month_sin, month_cos`
- 하루/주/연 단위 **계절·주기 위치**를 수치로 제공
- **지점 One-Hot **(USE_CODE_ONEHOT=True):
- `code_1, code_2, …` 형태
- 지점별 상수항/편차를 분리해 **지점 특성**을 모델이 쉽게 학습
- **전처리 플래그 **(INCLUDE_FLAGS_IN_X=True):
- `is_event`: 폭우/급격 기압하강 등 **이벤트 구간** 표시
- 기준: 지점별 3타임 누적 강수 상위 15%(q=0.85) 이상 또는 기압 3타임 변화 하위 15%(q=0.15) 이하
- `is_imputed`: **결측 대치**가 일어난 시점 표시
- ≤2일 연속은 시간보간, ≤10일은 (지점,월)평균으로 채움
- `is_outlier`: Hampel(k=5, thr=6.5)로 잡힌 **센서성 튐** 표시
- 전처리에서 수위는 7타임 이동중앙값으로 부드럽게 보정, 플래그는 남겨 불확실 구간 힌트 제공
---
## Feature 추가 결과
---
- baseline
```bash
 code_new     NSE     KGE  AVG(NSE,KGE)
        1  0.4954  0.6915        0.5935
        2  0.6810  0.7440        0.7125
        3  0.8207  0.8919        0.8563
        4  0.6296  0.7452        0.6874
        5  0.4015  0.6705        0.5360
        6  0.8092  0.8176        0.8134
        7  0.2953  0.7549        0.5251
        8  0.4263  0.6363        0.5313
        9  0.5976  0.7894        0.6935
       10 -4.9227  0.7492       -2.0867
       11  0.4811  0.6745        0.5778
       12  0.6391  0.7075        0.6733
MEAN(ALL)  0.1128  0.7394        0.4261
```
- preprocessing
```bash
 code_new     NSE     KGE  AVG(NSE,KGE)
        1  0.4989  0.6952        0.5971
        2  0.6821  0.7478        0.7149
        3  0.8243  0.8975        0.8609
        4  0.6289  0.7478        0.6884
        5  0.4042  0.6769        0.5405
        6  0.8091  0.8242        0.8167
        7  0.3017  0.7607        0.5312
        8  0.4293  0.6426        0.5360
        9  0.5998  0.7999        0.6998
       10 -4.8770  0.7357       -2.0706
       11  0.4869  0.6885        0.5877
       12  0.6289  0.7090        0.6690
MEAN(ALL)  0.1181  0.7438        0.4310
```
- feature
```bash
 code_new     NSE     KGE  AVG(NSE,KGE)
        1  0.5959  0.7431        0.6695
        2  0.6716  0.7460        0.7088
        3  0.8211  0.9304        0.8757
        4  0.6929  0.7891        0.7410
        5  0.5768  0.7378        0.6573
        6  0.8908  0.8823        0.8866
        7  0.3590  0.7808        0.5699
        8  0.6131  0.6950        0.6540
        9  0.6253  0.8120        0.7187
       10 -4.1437  0.7564       -1.6936
       11  0.5382  0.8047        0.6715
       12  0.6855  0.7474        0.7165
MEAN(ALL)  0.2439  0.7854        0.5146
```
### 결과 그래프 {toggle="true"}
- **station 1**
[image omitted: temporary Notion asset]
- **station 2**
[image omitted: temporary Notion asset]
- **station 3**
[image omitted: temporary Notion asset]
- **station 4**
[image omitted: temporary Notion asset]
- **station 5**
[image omitted: temporary Notion asset]
- **station 6**
[image omitted: temporary Notion asset]
- **station 7**
[image omitted: temporary Notion asset]
- **station 8**
[image omitted: temporary Notion asset]
- **station 9**
[image omitted: temporary Notion asset]
- **station 10**
[image omitted: temporary Notion asset]
- **station 11**
[image omitted: temporary Notion asset]
- **station 12**
[image omitted: temporary Notion asset]
---

---

# Alex Net

---
## 0. Abstract {toggle="true"}
### 원본
---
[image omitted: temporary Notion asset]
---
### 해석본
---
우리는 1.2백만 개의 고해상도 이미지를 포함한 ImageNet LSVRC-2010 대회의 1000개의 서로 다른 클래스를 분류하기 위해** 큰 규모의 깊은 합성곱 신경망(convolutional neural network)을 훈련**시켰습니다. 테스트 데이터에서 우리는 top-1과 top-5 오류율이 각각 37.5%와 17.0%를 달성했으며, 이는 이전의 최신 기술(state-of-the-art)보다 상당히 더 나은 성능입니다.** 이 신경망은 6천만 개의 매개변수와 65만 개의 뉴런으로 구성**되어 있으며, **다섯 개의 합성곱 층**(그중 일부는 최대 풀링 층이 뒤따름)과 **세 개의 완전 연결 층**, 그리고 최종적으로 1000방향 소프트맥스(softmax) 층으로 이루어져 있습니다. 학습 속도를 빠르게 하기 위해 우리는 포화되지 않는(non-saturating) 뉴런과 합성곱 연산의 매우 효율적인 GPU 구현을 사용했습니다. 완전 연결 층에서 과적합을 줄이기 위해, 우리는 “드롭아웃(dropout)”이라고 불리는 최근에 개발된 정규화 방법을 사용했으며, 이는 매우 효과적인 것으로 입증되었습니다. 우리는 또한 이 모델의 변형 버전을 ILSVRC-2012 대회에 출전시켰고, 테스트 데이터에서 top-5 오류율 15.3%를 달성했으며, 이는 두 번째로 좋은 참가자가 기록한 26.2%보다 우수한 결과였습니다.
---


## **다섯 개의 합성곱 층, 세 개의 완전 연결층, 그리고 최종적으로 1000방향 소프트맥스 층**

### **앞부분 — 합성곱층 5개 (특징 추출 단계)**
- 각 합성곱층은 **이미지의 특징을 점점 더 복잡하게 학습**
<table header-row="true">
<tr>
<td>층 번호</td>
<td>이름</td>
<td>주요 역할</td>
<td>특징</td>
</tr>
<tr>
<td>Conv1</td>
<td>1번째 합성곱층</td>
<td>색상·모서리 등 기본 패턴 추출</td>
<td>11×11 필터, stride=4</td>
</tr>
<tr>
<td>Conv2</td>
<td>2번째 합성곱층</td>
<td>곡선·윤곽선 등 중간 특징 감지</td>
<td>5×5 필터</td>
</tr>
<tr>
<td>Conv3</td>
<td>3번째 합성곱층</td>
<td>물체 일부 구조 감지</td>
<td>3×3 필터</td>
</tr>
<tr>
<td>Conv4</td>
<td>4번째 합성곱층</td>
<td>세부 패턴(눈·코 등) 인식</td>
<td>3×3 필터</td>
</tr>
<tr>
<td>Conv5</td>
<td>5번째 합성곱층</td>
<td>전체 물체 형태 감지</td>
<td>3×3 필터</td>
</tr>
</table>
💡 합성곱층 사이에는 **ReLU 활성화 함수**와 **풀링(Pooling)** 연산이 섞여 있음
→ 풀링은 “정보 요약” — 즉, 중요 정보만 남기고 크기를 줄이는 역할
### 중간 — Flatten (평탄화)
- 합성곱층의 마지막 출력은 2차원(예: 13×13×256) 형태의 특징 맵(feature map)
- 이걸 **1차원 벡터**로 펼쳐서 완전연결층에 입력
- 즉, “사진”이 “숫자열(feature vector)”로 바뀌는 구간이에요.
### 뒷부분 — 완전연결층 3개 (분류 단계)
<table header-row="true">
<tr>
<td>층 번호</td>
<td>이름</td>
<td>노드 수</td>
<td>역할</td>
</tr>
<tr>
<td>FC1</td>
<td>첫 번째 완전연결층</td>
<td>4096</td>
<td>복잡한 패턴 조합</td>
</tr>
<tr>
<td>FC2</td>
<td>두 번째 완전연결층</td>
<td>4096</td>
<td>고차원 특징 통합</td>
</tr>
<tr>
<td>FC3</td>
<td>세 번째 완전연결층</td>
<td>1000</td>
<td>Softmax로 1000개 클래스 확률 계산</td>
</tr>
</table>
→ 마지막 FC3의 1000개 노드가 ImageNet의 1000개 클래스(예: 고양이, 개, 비행기 등)에 대응하고,  → Softmax가 “이 이미지가 각 클래스일 확률”을 계산



## **학습 속도를 빠르게 하기 위해 우리는 포화되지 않는(non-saturating) 뉴런**

### 1️⃣ 먼저, “포화(saturation)”란?
- 활성화 함수의 출력이 **거의 일정하게 고정되어 버리는 현상**
- 즉, 입력값이 커지거나 작아져도 **출력이 거의 안 변하는 상태**
- 예를 들어 Sigmoid 함수:
[image omitted: temporary Notion asset]
- x가 아주 크면 → f(x) ≈ 1
- x가 아주 작으면 → f(x) ≈ 0
- 이 구간에서는 그래프가 평평해서, \*\*기울기(gradient)\*\*가 거의 0
→ 이걸 “**포화(saturation)**”라고 함
→ 기울기가 0이면 학습이 멈춰버림(= **기울기 소실 문제, vanishing gradient**)
### 2️⃣ 포화되지 않는 뉴런이란?
- 이와 반대로, **입력값이 커져도 기울기가 0으로 사라지지 않는 뉴런**
- 즉, 출력이 멈추지 않고 계속 변할 수 있어서 **학습이 계속 일어나는** 뉴런
- 대표적으로 ReLU(Rectified Linear Unit):
[image omitted: temporary Notion asset]
- x ≤ 0 → 출력 0 (여기선 멈춤)
- x \> 0 → 출력 = x (기울기 = 1, 즉 계속 학습 가능)
- x가 양수일 때는 포화되지 않음 → 학습이 잘 됨
- 그래서 ReLU를 “**non-saturating activation function**”이라고 부름

## 1. Introduction {toggle="true"}
### 원본
---
[image omitted: temporary Notion asset]
[image omitted: temporary Notion asset]
---
### 해석본
---
현재의 객체 인식 접근법들은 본질적으로 머신러닝 방법들을 사용합니다. 그들의 성능을 향상시키기 위해, 우리는 더 큰 데이터셋을 수집하고, 더 강력한 모델을 학습시키며, 과적합을 방지하기 위한 더 나은 기법들을 사용할 수 있습니다. 최근까지 라벨이 지정된 이미지 데이터셋들은 비교적 작았으며, 대략 수만 장의 이미지 수준이었습니다(예: NORB [16], Caltech-101/256 [8, 9], 그리고 CIFAR-10/100 [12]). 간단한 인식 과제들은 이러한 크기의 데이터셋으로도 꽤 잘 해결될 수 있으며, 특히 라벨을 유지하는 변환(label-preserving transformations)을 통해 데이터셋을 확장한 경우에는 더욱 그렇습니다. 예를 들어, MNIST 숫자 인식 과제에서 현재 최고 오류율(\<0.3%)은 인간의 성능에 근접합니다 [4]. 그러나 현실적인 환경에서의 객체들은 상당한 변동성을 보이므로, 그것들을 인식하도록 학습하기 위해서는 훨씬 더 큰 학습 세트가 필요합니다. 그리고 실제로, 작은 이미지 데이터셋의 한계점들은 널리 인식되어 왔습니다(예: Pinto et al. [21]). 하지만 라벨이 붙은 수백만 장의 이미지 데이터셋을 수집하는 것이 가능해진 것은 최근에 와서야였습니다. 새롭고 더 큰 데이터셋에는 완전하게 분할된 수십만 장의 이미지를 포함하는 LabelMe [23], 그리고 22,000개 이상의 범주에 걸쳐 1,500만 장 이상의 라벨이 붙은 고해상도 이미지를 포함하는 ImageNet [6]이 있습니다.

과거 단순한 인식 과제는 적은 데이터셋으로 가능, but 현실 세계 객체 인식은 더 큰 데이터셋 필요하고 이게 요즘 나온 ImageNet 데이터셋이 있다

수백만 장의 이미지로부터 수천 개의 객체를 학습하기 위해서는, 우리는 큰 학습 용량을 가진 모델이 필요합니다. 그러나 객체 인식 과제의 엄청난 복잡성은, 이 문제가 ImageNet만큼 큰 데이터셋으로도 완전히 명시될 수 없음을 의미합니다. 따라서, 우리 모델은 우리가 가지고 있지 않은 모든 데이터를 보완하기 위해 많은 사전 지식을 가지고 있어야 합니다. 합성곱 신경망(Convolutional Neural Networks, CNNs)은 이러한 종류의 모델 중 하나를 구성합니다 [16, 11, 13, 18, 15, 22, 26]. 이들의 용량은 깊이(depth)와 너비(breadth)를 조절함으로써 통제할 수 있으며, 또한 이미지의 본질에 대해 강력하고 대부분 올바른 가정을 합니다(즉, 통계의 정상성(stationarity of statistics)과 픽셀 의존성의 국소성(locality of pixel dependencies)). 따라서, 유사한 크기의 층을 가진 표준 피드포워드 신경망과 비교했을 때, CNN은 훨씬 더 적은 연결과 매개변수를 가지며, 그 결과 더 쉽게 학습될 수 있습니다. 반면에, 이론적으로 가능한 최고의 성능은 약간만 더 나쁠 가능성이 있습니다.

데이터셋이 많아지면 뭐해 그걸 돌릴 수 있는 모델이 필요한데 → CNN (CNN 특징 및 장단점)

CNN의 매력적인 특성들에도 불구하고, 그리고 그들의 국소적 아키텍처의 상대적인 효율성에도 불구하고, 여전히 고해상도 이미지에 대규모로 적용하기에는 비용이 지나치게 많이 들었습니다. 다행히도, 현재의 GPU들은 고도로 최적화된 2차원 합성곱(2D convolution) 구현과 결합되어, 흥미로울 정도로 큰 CNN을 학습할 수 있을 만큼 충분히 강력합니다. 또한, ImageNet과 같은 최근의 데이터셋들은 그러한 모델들을 심각한 과적합 없이 학습시키기에 충분한 수의 라벨이 지정된 예시들을 포함하고 있습니다.

CNN있으면 뭐해, 아직도 고해상도 이미지에 대규모로 적용하기에 비용이 많이 들어!
→ GPU를 사용하자 

이 논문의 구체적인 기여는 다음과 같습니다. 우리는 ILSVRC-2010과 ILSVRC-2012 대회 [2]에서 사용된 ImageNet의 하위집합들에 대해 지금까지 중 가장 큰 합성곱 신경망 중 하나를 학습시켰으며, 이 데이터셋들에서 지금까지 보고된 것 중 가장 뛰어난 결과를 달성했습니다. 우리는 2차원 합성곱과 합성곱 신경망 학습에 내재된 모든 다른 연산들에 대한 고도로 최적화된 GPU 구현을 작성하였으며, 이를 공개적으로 제공했습니다¹. 우리의 네트워크는 성능을 향상시키고 학습 시간을 단축시키는 여러 가지 새롭고 특이한 기능들을 포함하고 있으며, 이러한 내용은 3장에서 자세히 설명됩니다. 우리의 네트워크의 크기는 120만 개의 라벨이 붙은 학습 예제가 있음에도 불구하고 과적합을 중요한 문제로 만들었기 때문에, 우리는 과적합을 방지하기 위한 몇 가지 효과적인 기법들을 사용하였으며, 이는 4장에서 설명됩니다. 우리의 최종 네트워크는 다섯 개의 합성곱 층과 세 개의 완전 연결 층을 포함하고 있으며, 이러한 깊이는 중요한 것으로 보입니다. 우리는 어떤 합성곱 층이라도 제거할 경우(각 층은 모델의 전체 매개변수의 1% 미만만을 포함함) 성능이 저하된다는 것을 발견했습니다.

3장: 성능 향상 및 학습 단축시키는 여러 가지 새롭고 특이한 기능<br>4장: 과적합을 방지하기 위한 몇 가지 효과적인 기법<br>🌟 우리가 만든 네트워크는 5개의 합성곱 층과 3개의 완전 연결층이고 이건 중요해<br>       → 하나라도 없으면 성능 저하

결국, 네트워크의 크기는 주로 현재 GPU에서 사용 가능한 메모리의 양과 우리가 감수할 수 있는 학습 시간의 양에 의해 제한됩니다. 우리의 네트워크는 두 개의 GTX 580 3GB GPU에서 학습하는 데 5일에서 6일 사이가 걸립니다. 우리의 모든 실험은, 더 빠른 GPU와 더 큰 데이터셋이 사용 가능해지기를 단순히 기다림으로써 결과를 향상시킬 수 있음을 시사합니다.

네트워크의 크기는 현재 GPU에서 사용 가능한 메모리의 양과 우리가 감수할 수 있는 학습 시간의 양에 의해 제한돼!

---


## **라벨을 유지하는 변환(label-preserving transformations)**

> 이미지의 시각적 형태를 바꿔도 정답(라벨)이 바뀌지 않는 변환으로, **데이터 증강 시 모델이 다양한 입력에서도 같은 대상을 인식하도록 만드는 핵심 기법**



## **통계의 정상성(stationarity of statistics)과 픽셀 의존성의 국소성(locality of pixel dependencies)**

### 1️⃣ 통계의 정상성 (Stationarity of Statistics)
- 이미지 전체를 보면, **통계적인 특성(패턴, 질감, 색 변화 등)이 위치에 상관없이 비슷하다**는 뜻
- 즉,
- 고양이의 귀 부분이든
- 배경의 잔디 부분이든
“이미지의 한 구역에서 발견한 통계적 패턴이 다른 구역에서도 비슷하게 적용될 수 있다”는 의미
- 예를 들어,
- 모서리(edge), 선(line), 질감(texture) 같은 시각적 패턴은 이미지의 어디에 있든 **같은 방식으로 감지할 수 있어야** 함
- CNN에서는 이걸 반영하기 위해 **같은 필터(커널)를 이미지 전체에 공유**
→ 이를 가중치 공유(weight sharing)라고 함
### 2️⃣ 픽셀 의존성의 국소성 (Locality of Pixel Dependencies)
- 이미지에서 **한 픽셀은 멀리 떨어진 픽셀보다 가까운 픽셀들과 더 밀접하게 관련되어 있다**
- 즉, “주변 몇 픽셀 안에서 중요한 정보가 집중되어 있다”는 말
- 예를 들어,
- 눈 주변의 픽셀들은 **서로 강하게 연관**되어 있고,
- 눈과 발처럼 멀리 떨어진 픽셀들은 **서로 거의 영향이 없다**는 것
- CNN에서는
- 이 특성을 반영하기 위해 작은 크기의 필터(예: 3×3, 5×5)만 사용
→ 즉, CNN은 한 번에 전체 이미지를 보지 않고
→ 주변 몇 픽셀(국소 영역, local region)만 보면서 특징을 학습
**🧠 두 개념이 합쳐진 결과**
> “이미지의 국소적 패턴이 전체적으로 비슷한 통계적 구조를 가진다”는 전제 아래,
CNN은 **작고(local)**, **공유된(shared)** 필터를 사용해 전체 이미지를 효율적으로 분석한다

따라서, 유사한 크기의 층을 가진 표준 피드포워드 신경망과 비교했을 때, CNN은 훨씬 더 적은 연결과 매개변수를 가지며, 그 결과 더 쉽게 학습될 수 있습니다. 반면에, 이론적으로 가능한 최고의 성능은 약간만 더 나쁠 가능성이 있습니다.

### 첫 번째 포인트 — “CNN은 더 적은 연결과 매개변수를 가진다”
- 일반 신경망(Fully Connected Network)은모든 입력 픽셀이 모든 뉴런과 연결
-  예: 224×224×3 = 150,528개의 픽셀이면,
- 다음 층의 뉴런 하나마다 150,528개의 가중치(weight)가 필요
-  엄청난 수의 연결과 파라미터 생김
- **CNN**은
- “국소성(locality)” 때문에 작은 영역(예: 3×3)만 봄
- “정상성(stationarity)” 때문에 같은 필터(가중치)를 이미지 전체에 공유
- 즉, 3×3 필터 하나가 이미지 전체를 커버하니까, 전체 픽셀마다 새로운 가중치를 만들 필요가 없음
- **연결 수가 엄청나게 줄어듭니다.**
### 두 번째 포인트 — “그 결과 더 쉽게 학습될 수 있습니다”
파라미터가 적다는 건 곧:
- 학습해야 할 양이 적고,
- 오버피팅(overfitting) 위험이 줄고,
- 훈련이 훨씬 안정적이라는 뜻
= 즉, CNN은 **학습 효율이 높다**는 장점이 있습니다.
### 세 번째 포인트 — “이론적으로 가능한 최고의 성능은 약간만 더 나쁠 수 있다”
- 이건 “trade-off(절충)”에 대한 이야기
- CNN은 “통계의 정상성”과 “국소성”을 **가정**
- 즉,
> “이미지의 통계적 패턴은 전체적으로 비슷하다.”
“멀리 떨어진 픽셀끼리는 관계가 약하다.” 라고 가정하고 설계된 모델
- 하지만 실제 세상은 항상 이 가정에 완전히 맞지는 않음
- 예를 들어,
- 얼굴의 왼쪽 눈과 오른쪽 눈은 멀리 떨어져 있지만 강하게 관련
- CNN은 이 관계를 바로 포착하기 어려움
- 따라서 이런 **단순화된 가정 때문에, 이론적으로 완벽하게 표현할 수 있는 모든 경우 중 일부는 약간 손해를 본다**는 뜻
- 즉,
- CNN은 효율성(적은 파라미터, 빠른 학습)을 얻는 대신,
- 표현력(모든 경우를 다 모델링하는 능력)을 아주 조금 포기

## 2. The Dataset {toggle="true"}
### 원본
---
[image omitted: temporary Notion asset]
---
### 번역본
---
ImageNet은 약 22,000개의 범주에 속하는 1,500만 장이 넘는 라벨이 붙은 고해상도 이미지들로 구성된 데이터셋입니다. 이 이미지들은 웹에서 수집되었으며, Amazon의 Mechanical Turk 크라우드소싱 도구를 사용한 인간 라벨러들에 의해 라벨이 지정되었습니다. 2010년부터 Pascal Visual Object Challenge의 일환으로, ImageNet 대규모 시각적 인식 챌린지(ImageNet Large-Scale Visual Recognition Challenge, ILSVRC)라는 연례 대회가 열리고 있습니다. ILSVRC는 ImageNet의 하위집합을 사용하며, 각 1000개의 범주(category)마다 약 1000장의 이미지를 포함합니다. 전체적으로 약 120만 장의 학습 이미지, 5만 장의 검증 이미지, 그리고 15만 장의 테스트 이미지가 있습니다.

대회에서는 Category: 1000개 <br>각 Category마다: 1000장의 이미지<br>= 전체적으로, 약 120만장의 학습 이미지, 5만 장의 검증 이미지, 15만 장의 테스트 이미지

ILSVRC-2010은 테스트 세트의 라벨이 제공되는 유일한 ILSVRC 버전이므로, 우리는 대부분의 실험을 이 버전에서 수행했습니다. 또한 우리는 ILSVRC-2012 대회에도 우리의 모델을 출전시켰기 때문에, 6장에서 테스트 세트 라벨이 제공되지 않는 이 버전의 데이터셋에 대한 우리의 결과도 보고합니다. ImageNet에서는 일반적으로 두 가지 오류율을 보고하는 것이 관례입니다: top-1과 top-5. 여기서 top-5 오류율은 모델이 가장 가능성이 높다고 판단한 다섯 개의 라벨 중에 정답 라벨이 포함되지 않은 테스트 이미지의 비율을 의미합니다.

6장에서는 대회에 대한 실험 결과와 성능 보고가 나올꺼야<br>ImageNet에서는 일반적으로 두 가지 오류율을 보고해: Top-1, Top-5

ImageNet은 해상도가 다양한 이미지들로 구성되어 있지만, 우리의 시스템은 일정한 입력 차원을 필요로 합니다. 따라서 우리는 이미지를 256×256의 고정된 해상도로 다운샘플링했습니다. 직사각형 이미지를 주어졌을 때, 우리는 먼저 짧은 변의 길이가 256이 되도록 이미지를 리스케일한 후, 그 결과 이미지에서 중앙의 256×256 패치를 잘라냈습니다. 우리는 학습 세트 전체에서의 평균 활성(mean activity)을 각 픽셀에서 빼주는 것을 제외하고는, 이미지를 다른 어떤 방식으로도 전처리하지 않았습니다. 따라서 우리는 픽셀의 (중심화된) 원시 RGB 값들로 우리의 네트워크를 학습시켰습니다.

우리는 일정한 입력 차원을 필요로해 → 256 by 256 다운 샘플링<br><br>if) 직각사각형 이미지가 주어지면<br>1. 짧은 변의 길이가 256가 되도록 이미지 리스케일링<br>2. 그 결과 이미지에서 중앙 256 by 256 패치를 잘라냄<br>3. 해당 이미지에서 평균 활성(Mean Activity)을 각 픽셀에서 빼줌 = 픽셀을 0 근처로 중심화

---


## ** ImageNet에서는 일반적으로 두 가지 오류율을 보고하는 것이 관례입니다: top-1과 top-5**

**ImageNet에서 일반적으로 보고하는 두 가지 오류율**은 다음과 같음:
1. **Top-1 오류율**:
모델이 예측한 **가장 높은 확률의 단일 라벨이 정답이 아닐 확률**
→ 즉, “모델의 1순위 예측이 틀린 경우”를 의미합니다.
1. **Top-5 오류율**:
모델이 예측한 **상위 5개의 라벨 중 정답이 포함되지 않은 경우의 비율**
→ 즉, “모델의 5개 후보 중 정답이 없을 확률”입니다.
따라서 ImageNet에서는 모델의 인식 성능을 더 세밀하게 보기 위해 top-1(정확히 맞춘 경우)과 top-5(대체로 비슷한 후보 안에 있는 경우) 오류율을 함께 보고함



## **우리는 학습 세트 전체에서의 평균 활성(mean activity)을 각 픽셀에서 빼주는 것**

- 여기서 말하는 건 **“전체 이미지의 각 픽셀 위치별 평균”을 뺀 것**
- 즉, **픽셀 위치마다 따로 평균을 구한 뒤**, 그 값을 각 이미지의 같은 위치에서 빼준 것
구체적으로 설명:
1. **모든 학습 이미지를 모아서**, 각 픽셀 좌표별로 평균 RGB 값을 구함
- 예를 들어, (x=100, y=50) 위치의 모든 이미지에서 R, G, B 값을 평균 내면 → (120, 110, 100) 같은 평균이 나올 수 있음
2. 그다음, **각 이미지의 (100, 50) 픽셀 값에서 이 평균을 빼줌**
- 즉, 그 픽셀 위치의 “전역 평균”을 기준으로 0 근처로 맞춰주는 것
= 이걸 “per-pixel mean subtraction”이라고 함

## 3. The Architecture {toggle="true"}
### 원본
---
[image omitted: temporary Notion asset]
---
### 번역본
---
우리 네트워크의 구조는 그림 2에 요약되어 있습니다. 이 네트워크는 다섯 개의 합성곱 층과 세 개의 완전 연결 층을 포함하여 총 여덟 개의 학습된 층으로 이루어져 있습니다. 아래에서는 우리 네트워크 구조의 새롭거나 특이한 몇 가지 특징들을 설명합니다. 3.1절부터 3.4절까지는 우리가 판단한 중요도에 따라 정렬되어 있으며, 가장 중요한 항목이 먼저 제시됩니다.
---
## 3.1 ReLU Nonlinearity (Fig.1) {toggle="true"}
### 원본
---
[image omitted: temporary Notion asset]
---
### 번역본
---
뉴런의 출력 f를 입력 x의 함수로 모델링하는 표준적인 방법은
```math
f(x)=tanh(x)\quad또는\quad f(x)=(1+e^{−x})^{−1}
```
형태를 사용하는 것입니다. 그러나 경사하강법(gradient descent)을 이용한 학습 시간 측면에서, 이러한 포화형 비선형 함수(saturating nonlinearities)는 비포화형 비선형 함수(non-saturating nonlinearity)인 
```math
f(x)=max(0,x)
```
보다 훨씬 느립니다. Nair와 Hinton [20]을 따라, 우리는 이러한 비선형 함수를 사용하는 뉴런을 정류 선형 유닛(Rectified Linear Units, **ReLU**s)이라고 부릅니다. ReLU를 사용하는 심층 합성곱 신경망은 tanh 유닛을 사용하는 네트워크보다 몇 배나 빠르게 학습됩니다. 이는 그림 1에서 보여지는데, 해당 그림은 특정 4층 합성곱 신경망이 CIFAR-10 데이터셋에서 훈련 오류 25%에 도달하기 위해 필요한 반복 횟수를 나타냅니다. 이 그래프는 우리가 만약 전통적인 포화형 뉴런 모델을 사용했다면, 본 연구에서 이처럼 큰 신경망들을 실험할 수 없었을 것임을 보여줍니다.

포화형 비선형 함수(sigmoid, tanh)보다 비포화형 비선형 함수(ReLU)가 더 빠르게 학습

우리가 CNN에서 전통적인 뉴런 모델의 대안을 고려한 최초의 연구자는 아닙니다. 예를 들어, Jarrett 등 [11]은 그들의 방식의 대비 정규화(contrast normalization) 뒤에 지역 평균 풀링(local average pooling)을 적용했을 때, 비선형 함수
```math
f(x)=∣tanh⁡(x)∣
```
이 특히 잘 작동한다고 주장했습니다. 그러나 이 데이터셋(Caltech-101)에서는 주요 관심사가 과적합을 방지하는 것이기 때문에, 그들이 관찰한 효과는 우리가 ReLU를 사용할 때 보고한 훈련 세트를 더 빠르게 적합시키는 능력 향상과는 다른 것입니다.
더 빠른 학습은 큰 데이터셋에서 학습되는 대형 모델의 성능에 매우 큰 영향을 미칩니다.

이전 연구는 tanh 같은 함수로 과적합을 줄이는 데 초점을 뒀지만, AlexNet은 ReLU를 사용해 학습 속도를 대폭 높여 대규모 데이터셋에서 더 나은 성능을 얻었다는 점

그림 1: ReLU를 사용하는 4층 합성곱 신경망(실선)은 tanh 뉴런(점선)을 사용하는 동등한 네트워크보다 CIFAR-10에서 25%의 학습 오류율에 도달하는 속도가 6배 더 빠릅니다. 각 네트워크의 학습 속도를 가능한 한 빠르게 만들기 위해 학습률은 각각 독립적으로 선택되었습니다. 어떤 형태의 정규화도 사용되지 않았습니다. 여기에서 보여지는 효과의 크기는 **네트워크 구조에 따라 달라지지만, ReLU를 사용하는 네트워크들은 포화형 뉴런(saturating neurons)을 사용하는 네트워크보다 일관되게 몇 배 더 빠르게 학습**합니다.
---
## 3.2 Training on Multiple GPUs {toggle="true"}
### 원본
---
[image omitted: temporary Notion asset]
---
### 번역본
---
단일 GTX 580 GPU는 3GB의 메모리만을 가지고 있으며, 이는 그 위에서 학습시킬 수 있는 네트워크의 최대 크기를 제한합니다. 120만 개의 학습 예제는 하나의 GPU에 들어가지 않을 정도로 큰 네트워크를 학습시키기에 충분하다는 것이 밝혀졌습니다. 따라서 우리는 네트워크를 두 개의 GPU에 걸쳐 분산시켰습니다. 현재의 GPU들은 서로의 메모리에 직접 읽기와 쓰기를 수행할 수 있어, 호스트 머신의 메모리를 거치지 않고도 교차 GPU 병렬화에 특히 잘 적합합니다.

데이터셋이 많아질수록 그 데이터를 잘 학습하려면 큰 모델이 필요 <br>→ but, 그 큰 모델을 한 번에 GPU 한 대에 올리기엔 메모리가 부족 = 두개의 GPU로 분산<br>🌟 GPU끼리 통신 가능 

우리가 사용하는 병렬화 방식은 본질적으로 절반의 커널(또는 뉴런)을 각각의 GPU에 배치하는 것입니다. 여기에 하나의 추가적인 트릭이 있습니다. GPU들은 특정 층에서만 서로 통신합니다. 이는 예를 들어, 3번째 층의 커널들은 2번째 층의 모든 커널 맵으로부터 입력을 받는다는 것을 의미합니다. 그러나 4번째 층의 커널들은 동일한 GPU에 존재하는 3번째 층의 커널 맵으로부터만 입력을 받습니다. 연결 패턴을 선택하는 것은 교차 검증(cross-validation)의 문제이지만, 이러한 방식은 통신량이 계산량에 비해 허용 가능한 비율이 될 때까지 통신의 양을 정밀하게 조정할 수 있게 해줍니다.

GPU들으 항상 통신하는 것 X → 특정 층에서만 서로 통신 <br>통신 시간 많이 소요 → 필요한것들만 통신해 (통신량이 계산량에 비해 너무 많지 않도록)

결과적으로 생성된 아키텍처는 Cireşan 등 [5]이 사용한 “columnar” CNN의 구조와 다소 유사하지만, 우리의 컬럼들은 독립적이지 않습니다(그림 2 참조). 이 방식은 각 합성곱 층에서 절반의 커널만을 사용하여 하나의 GPU에서 학습된 네트워크와 비교했을 때, 우리의 top-1과 top-5 오류율을 각각 1.7%와 1.2% 감소시킵니다. 또한 두 개의 GPU를 사용하는 네트워크는 하나의 GPU를 사용하는 네트워크보다 학습 시간이 약간 더 짧습니다².

GPU를 2개 사용하지만 독립적인건 아니고, 일부는 공유 일부는 독립적인 구조야<br>이 구조 vs 네트워크 절반 구조 비교(GPU 1개만 쓰기)  : 해당 구조가 오류율 감소 <br>                                                                                               & 학습 시간도 짧음 

---


## **현재의 GPU들은 서로의 메모리에 직접 읽기와 쓰기를 수행할 수 있어, 호스트 머신의 메모리를 거치지 않고도 교차 GPU 병렬화에 특히 잘 적합합니다.**

- 요즘 GPU들은 **GPU ↔ GPU 간 데이터를 직접 주고받을 수 있음**
- 즉, **CPU 메모리(호스트 메모리)** 를 거치지 않고도 GPU끼리 데이터를 바로 교환 가능



## **우리가 사용하는 병렬화 방식은 본질적으로 절반의 커널(또는 뉴런)을 각각의 GPU에 배치하는 것입니다**

- 신경망의 각 층(특히 convolution layer)에 여러 **커널(필터)** 들이 있음
- 그 필터의 절반은 GPU1에, 나머지 절반은 GPU2에 둔 것
즉, “네트워크를 반으로 쪼개서” 두 GPU가 각각 절반씩 담당하게 만든 구조



## **GPU들은 특정 층에서만 서로 통신합니다.**

- 이건, GPU 간 데이터를 자주 주고받으면 속도가 느려지니까 
→ **필요한 층에서만 통신하게 제한**했다는 뜻
- 예시로 설명하면:
- 2번째 층 → 3번째 층 계산 시에는 **두 GPU의 정보가 섞임**
- 하지만 3번째 층 → 4번째 층으로 갈 때는 **각 GPU가 자기 쪽 데이터만 가지고 계산**
- 즉, 모든 층에서 GPU 간 통신을 하면 느려지니까, “
- 중간 몇 층만 연결해서 데이터 교환”하게 만든 것
- 이렇게 하면 **통신량(느린 부분)** 을 최소화하면서 **계산량(빠른 부분)** 은 유지 가능



## **연결 패턴을 선택하는 것은 교차 검증(cross-validation)의 문제이지만, 이러한 방식은 통신량이 계산량에 비해 허용 가능한 비율이 될 때까지 통신의 양을 정밀하게 조정할 수 있게 해줍니다.**

### 1. “연결 패턴을 선택하는 것은 교차 검증의 문제”
> “연결 패턴(connection pattern)”이란 **두 GPU가 어느 층에서, 어떤 식으로 서로 연결되어 데이터를 주고받을지**를 말함
- 예를 들어:
- GPU A의 3번째 층이 GPU B의 2번째 층에서 온 출력을 받을 수도 있고,
- 반대로 GPU B의 3번째 층은 GPU A와 독립적으로 계산할 수도 있음
이런 연결 구조를 어떻게 설계하느냐에 따라 학습 속도나 성능이 달라지기 때문에,
→ 그 “가장 좋은 연결 방식”을 **교차 검증(cross-validation)** 으로 정했다는 뜻
= 즉, 여러 방식으로 실험해 보고, **성능이 가장 좋은 구조를 선택했다**는 말
### 2. “이러한 방식은 통신량이 계산량에 비해 허용 가능한 비율이 될 때까지 통신의 양을 정밀하게 조정할 수 있게 해줍니다”
- 여기서 핵심은 **GPU 간 통신량과 연산량의 균형**
- GPU 간 데이터를 주고받는 건 **통신량(communication)** 입니다. → 느림
- GPU 내부에서 계산하는 건 **계산량(computation)** 입니다. → 빠름
- 따라서, 통신이 너무 많으면 속도가 느려지고,
- 너무 적으면 서로 필요한 정보를 주고받지 못해서 정확도가 떨어짐
- 그래서 논문에서는 **“통신량이 계산량에 비해 너무 많지 않도록”**,
- 즉 **속도가 느려지지 않으면서도 성능이 유지되는 최적 지점을** 찾기 위해 GPU 간 연결 방식을 조정했다고 말하는 것

## 3.3 Local Response Normalization {toggle="true"}
### 원본
---
[image omitted: temporary Notion asset]
---
### 번역본
---
[image omitted: temporary Notion asset]

ReLU는 원래 정규화를 안 해도 괜찮지만 Local normalization을 하니까 일반화에 도움
→ 한 위치에서 여러 커널들이 비슷한 특징에 강하게 반응할 수 있는데, 이때 RN은 너무 세게 반응하는 뉴런을 주변 뉴런들이 눌러준다<br><br>= 비슷한 특징을 중복해서 감지하지 않게 도와주는 경쟁 메커니즘<br>& 우리는 local normalization 중에서도 response normalization을 사용

이 방식은 Jarrett 등 [11]의 국소 대비 정규화(local contrast normalization) 방식과 약간의 유사성을 가지지만, 우리는 평균 활성(mean activity)을 빼지 않기 때문에 우리의 방식은 보다 정확하게 “밝기 정규화(brightness normalization)”라고 불릴 수 있습니다. 응답 정규화(response normalization)는 우리의 top-1 및 top-5 오류율을 각각 1.4%와 1.2% 감소시켰습니다. 우리는 또한 CIFAR-10 데이터셋에서 이 방식의 효율성을 검증했습니다. 네 개의 층으로 이루어진 CNN은 정규화를 사용하지 않았을 때 13%의 테스트 오류율을, 정규화를 사용했을 때 11%의 테스트 오류율을 달성했습니다³.

response normalization은 local contrast normalization 방식과 약간 유사하지만 <br>차이점 : 우리는 평균 활성을 빼지 않음 (brightness normalization)<br><br>그리고 response normalization을 하니까 성능이 더 좋아짐!

---


## **Local Normalization (LN 중에서도 Response normalization 사용)**

### 1. 앞 문장의 배경
> “ReLU는 입력 정규화를 하지 않아도 과적합을 잘 방지하지만, 우리는 추가적인 ‘국소 정규화(local normalization)’가 일반화에 도움이 된다는 걸 발견했다.”
-  ReLU는 이미 과적합 방지에 꽤 강한 특성을 가지고 있지만,
- 실험해 보니 **LN을 추가하면 더 잘 일반화(테스트 데이터에서도 성능 향상)** 되었다는 뜻
### 2. LN의 핵심 아이디어: “뉴런 간 억제(lateral inhibition)”
- 우리 뇌의 신경 세포는 비슷한 자극을 받으면 **서로 경쟁하면서 일부를 억제**
- 예를 들어, 여러 뉴런이 동시에 강하게 반응하면
→ 일부 뉴런만 강하게 남고
→ 나머지는 억제되어 신호가 과도하게 커지지 않아요.
- 이 아이디어를 CNN에도 적용한 게 LN
### 3. 식의 의미
[image omitted: temporary Notion asset]
각 항의 의미를 풀면 다음과 같음:
<table header-row="true">

<tr>
<td>기호</td>
<td>의미</td>
</tr>
<tr>
<td>a\^(i)_(x,y)</td>
<td>ReLU를 통과한 i번째 커널의 (x,y) 위치 출력값</td>
</tr>
<tr>
<td>b\^(i)_(x,y)</td>
<td>LRN을 적용한 후의 정규화된 출력값</td>
</tr>
<tr>
<td>N</td>
<td>한 층의 전체 커널(채널) 수</td>
</tr>
<tr>
<td>n</td>
<td>정규화에 참여할 이웃 커널 개수 (예: 자기 주변 5개)</td>
</tr>
<tr>
<td>k,α,β </td>
<td>하이퍼파라미터 (정규화 강도 조절)</td>
</tr>
</table>
- 즉, **한 픽셀 위치 (x,y)** 에 대해, 해당 위치의 **이웃 채널들(i 주변 n개)** 의 제곱값을 더해서
- 그 합으로 현재 뉴런의 출력을 나누어 **상대적으로 억제(normalize)** 하는 것
### 4. 쉽게 말하면
한 위치에서 여러 커널들이 비슷한 특징에 강하게 반응할 수 있는데,
이럴 때 LRN은 **“너무 세게 반응하는 뉴런을 주변 뉴런들이 눌러준다”** 고 보면 됌
→ 즉, 비슷한 특징을 중복해서 감지하지 않게 도와주는 “경쟁 메커니즘”
### 5. 마지막 문장의 숫자 설명
> “k=2, n=5, α=10⁻⁴, β=0.75를 사용했습니다.”
이건 실험으로 정한 하이퍼파라미터
- n=5 → 주변 5개 채널을 기준으로 정규화
- α, β → 정규화의 강도 조절
- k=2 → 분모가 0이 되는 걸 방지하는 안정화 상수



## **근데 왜 평균 활성을 빼는게 대조일까**

### 1. “밝기(brightness)” vs “대조(contrast)”의 개념 차이
<table header-row="true">
<tr>
<td>구분</td>
<td>정의</td>
<td>핵심 개념</td>
</tr>
<tr>
<td>**밝기 (Brightness)**</td>
<td>전체적으로 얼마나 밝은가 (절대적 세기)</td>
<td>평균값(mean)</td>
</tr>
<tr>
<td>**대조 (Contrast)**</td>
<td>주변과 얼마나 다른가 (상대적 차이)</td>
<td>평균으로부터의 편차(deviation from mean)</td>
</tr>
</table>
즉,
- **밝기 조절**은 “전체적으로 얼마나 밝은가”를 맞추는 거고,
- **대조 조절**은 “평균 밝기에서 얼마나 벗어나 있는가(차이)”를 강조하는 것
### 2. 평균을 빼는 이유 → “상대적 차이”만 남기기 위해
예를 들어, 이미지 한 구역의 픽셀 값이
```plain text
[110, 115, 120, 125, 130]
```
- 이고 평균이 120이라면,
- 이걸 단순히 쓰면 **밝기(120)** 의 영향이 그대로 남아 있음
그런데 평균을 빼면:
```plain text
[-10, -5, 0, +5, +10]
```
- 이렇게 “평균에서 얼마나 벗어났는가”만 남게 되죠.
- 이게 바로 **대조(contrast)** 입니다 — 즉, “상대적 밝기 차이”.
- 따라서,
- **평균을 빼는 순간 ‘절대 밝기’(brightness)는 제거되고, ‘상대적 밝기 차이’(contrast)만 남습니다.**

## 3.4 Overlapping Pooling {toggle="true"}
### 원본
---
[image omitted: temporary Notion asset]
---
### 번역본
---
[image omitted: temporary Notion asset]

pooling: 공간 크기를 줄이며 중요한 정보만 남기는 과정<br><br>두가지 pooling 방법:<br>1. non-overlapping polling: 안 겹치도록 (pool size =2x2, stride =2) → 윈도우 안 겹침
2. overlapping polling: 겹치도록(Alex: pool size=3x3, stride=2)→풀링 창이 1칸씩 겹침 <br><br>**우리는 overlapping polling 사용**<br>겹치면 다음과 같은 효과:<br>1. 공간적 연속성 유지 → 인접한 영역들이 조금씩 겹쳐서 정보 손실 덜함<br>2. 더 부드러운 요약 → 너무 급격하게 축소되지 않아 특징이 자연스럽게 이어짐<br>3. 과적합 완화 → 모델이 특정 위치의 특징에만 민감하게 반응하지 않게 됨<br><br>실험 결과도 overlapping polling이 더 좋았음!

---
## 3.5 Overall Architecture (Fig.2) {toggle="true"}
### 원본
---
[image omitted: temporary Notion asset]
[image omitted: temporary Notion asset]
---
### 번역본
---
이제 우리는 우리의 CNN의 전체 구조를 설명할 준비가 되었습니다. 그림 2에 나타난 것처럼, 이 네트워크는 가중치를 가진 여덟 개의 층으로 구성되어 있으며, 처음 다섯 개는 합성곱 층이고 나머지 세 개는 완전 연결 층입니다. 마지막 완전 연결 층의 출력은 1000개의 클래스 라벨에 대한 분포를 생성하는 **1000-way 소프트맥스(softmax)에 입력**됩니다. 우리의 네트워크는 다항 로지스틱 회귀(multinomial logistic regression) 목적함수를 최대화하며, 이는 예측 분포 하에서 정답 라벨의 로그 확률(log-probability)의 학습 사례 전체에 대한 평균을 최대화하는 것과 동일합니다.
두 번째, 네 번째, 그리고 다섯 번째 합성곱 층의 커널들은 이전 층에서 동일한 GPU에 존재하는 커널 맵들과만 연결되어 있습니다(그림 2 참조). 세 번째 합성곱 층의 커널들은 두 번째 층의 모든 커널 맵들과 연결되어 있습니다. 완전 연결 층의 뉴런들은 이전 층의 모든 뉴런들과 연결되어 있습니다. 응답 정규화(response-normalization) 층은 첫 번째와 두 번째 합성곱 층 뒤에 위치합니다.<br>섹션 3.4에서 설명된 종류의 최대 풀링(max-pooling) 층은 두 개의 응답 정규화 층과 다섯 번째 합성곱 층 뒤에 위치합니다. ReLU 비선형 함수는 모든 합성곱 층과 완전 연결 층의 출력에 적용됩니다.
첫 번째 합성곱 층은 224×224×3 크기의 입력 이미지를 11×11×3 크기의 커널 96개로 필터링하며, 스트라이드는 4픽셀입니다(이는 커널 맵에서 인접한 뉴런들의 수용 영역 중심 사이의 거리입니다). 두 번째 합성곱 층은 첫 번째 합성곱 층의 (응답 정규화되고 풀링된) 출력을 입력으로 받아, 5×5×48 크기의 커널 256개로 필터링합니다.
세 번째, 네 번째, 다섯 번째 합성곱 층들은 그 사이에 풀링이나 정규화 층이 없이 서로 연결되어 있습니다. 세 번째 합성곱 층은 두 번째 합성곱 층의 (정규화되고 풀링된) 출력에 연결된 3×3×256 크기의 커널 384개를 가지고 있습니다. 네 번째 합성곱 층은 3×3×192 크기의 커널 384개를, 다섯 번째 합성곱 층은 3×3×192 크기의 커널 256개를 가지고 있습니다. 완전 연결 층은 각각 4096개의 뉴런을 가지고 있습니다.
그림 2: 우리의 CNN 구조를 나타내는 그림으로, 두 개의 GPU 간의 역할 구분을 명확히 보여줍니다. 하나의 GPU는 그림의 상단에 있는 층 부분들을 실행하고, 다른 하나는 하단에 있는 층 부분들을 실행합니다. GPU들은 특정 층에서만 서로 통신합니다. 네트워크의 입력은 150,528차원이며, 나머지 층들에 있는 뉴런의 수는 각각 253,440–186,624–64,896–64,896–43,264–4096–4096–1000입니다.
---


## **우리의 네트워크는 다항 로지스틱 회귀(multinomial logistic regression) 목적함수를 최대화하며, 이는 예측 분포 하에서 정답 라벨의 로그 확률(log-probability)의 학습 사례 전체에 대한 평균을 최대화하는 것과 동일합니다.**

### 1. 핵심 문장
> “우리의 네트워크는 다항 로지스틱 회귀(multinomial logistic regression) 목적함수를 최대화하며, 이는 예측 분포 하에서 정답 라벨의 로그 확률(log-probability)의 학습 사례 전체에 대한 평균을 최대화하는 것과 동일합니다.”
- 이건 사실상 **“Softmax + Cross-Entropy 손실 함수”** 를 말하는 것
- AlexNet의 마지막 층은 **1000개 클래스에 대한 확률 분포를 출력하는 softmax 분류기**
### 2. 다항 로지스틱 회귀란?
- ‘다항(multinomial)’은 **클래스가 여러 개(2개 이상)** 라는 뜻
- 즉, 이건 **다중 분류(multi-class classification)** 문제
- 로지스틱 회귀(Logistic Regression)는 원래
- 0 또는 1 중 하나를 맞추는 **이진 분류(binary classification)** 에서
- 확률을 출력하도록 설계된 모델
- 그걸 여러 클래스(예: 1000개)로 확장한 게 **다항 로지스틱 회귀(multinomial logistic regression)** 이고,
- 이건 신경망에서는 **Softmax 함수**로 구현됩니다.
### 3. “예측 분포 하에서 정답 라벨의 로그 확률을 최대화한다”는 말의 의미
이 말은 수학적으로 보면,
> Softmax로 구한 각 클래스의 확률 중에서, 정답 클래스의 확률이 가장 높아지도록 학습한다는 뜻이에요.
즉,
[image omitted: temporary Notion asset]
- AlexNet이 말한 “목적함수를 **최대화(maximize)**”는 사실상 이 식의 **음수(-)** 를 취한 **Cross-Entropy 손실을 최소화**하는 것과 같은 의미



## **AlexNet의 실제 합성곱 층 구조**

<table header-row="true">
<tr>
<td>층</td>
<td>입력 크기</td>
<td>필터 크기</td>
<td>필터 개수</td>
<td>GPU 분할</td>
<td>비고</td>
</tr>
<tr>
<td>Conv1</td>
<td>224×224×3</td>
<td>11×11×3</td>
<td>96</td>
<td>GPU 1+2 전체</td>
<td>stride=4</td>
</tr>
<tr>
<td>**LRN + MaxPool**</td>
<td></td>
<td></td>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td>Conv2</td>
<td>27×27×96</td>
<td>5×5×48</td>
<td>256</td>
<td>GPU1:48입력 / GPU2:48입력</td>
<td>그룹별 연결 (partial connection)</td>
</tr>
<tr>
<td>**LRN + MaxPool**</td>
<td></td>
<td></td>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td>Conv3</td>
<td>13×13×256</td>
<td>3×3×256</td>
<td>384</td>
<td>GPU 1+2 전체</td>
<td>전체 연결</td>
</tr>
<tr>
<td>Conv4</td>
<td>13×13×192</td>
<td>3×3×192</td>
<td>384</td>
<td>GPU1↔GPU2 교차 연결</td>
<td>partial connection</td>
</tr>
<tr>
<td>Conv5</td>
<td>13×13×192</td>
<td>3×3×192</td>
<td>256</td>
<td>GPU1↔GPU2 교차 연결</td>
<td>partial connection</td>
</tr>
<tr>
<td>**MaxPool**</td>
<td></td>
<td></td>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td>FC6</td>
<td>4096</td>
<td></td>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td>FC7</td>
<td>4096</td>
<td></td>
<td></td>
<td></td>
<td></td>
</tr>
<tr>
<td>FC8</td>
<td>1000</td>
<td></td>
<td></td>
<td></td>
<td>Softmax</td>
</tr>
</table>

## 4. Reducing Overfitting {toggle="true"}
### 원본
---
[image omitted: temporary Notion asset]
---
### 번역본
---
우리의 신경망 구조는 6천만 개의 매개변수를 가지고 있습니다. ILSVRC의 1000개 클래스는 각 학습 예제가 이미지에서 라벨로의 매핑에 대해 10비트의 제약을 부여하지만, 이는 상당한 과적합(overfitting) 없이 그렇게 많은 매개변수를 학습하기에는 충분하지 않다는 것이 드러났습니다. 아래에서는 우리가 과적합에 맞서 싸우기 위해 사용한 두 가지 주요 방법을 설명합니다.

데이터가 주는 정보(라벨 1000개 ≈ 10비트)는 너무 적어서 6천만 개의 파라미터를 안정적으로 학습시키기 어렵기 때문에, AlexNet은 과적합을 막기 위해 **데이터 증강과 드롭아웃**을 적용했다

---


## **10비트의 제약을 부여하지만**

### 1. 우선, ‘비트(bit)’란?
비트는 정보량의 단위예요.
예를 들어,
- 1비트는 두 가지 선택지(예: 0 or 1)를 구분할 수 있는 정보량
- 2비트면 4가지(00, 01, 10, 11)
- 10비트면 2¹⁰ = 1024가지 구분이 가능하죠.
---
### 2. ImageNet에는 1000개의 클래스가 있음
ILSVRC(ImageNet) 데이터셋에는 **1000개의 카테고리(클래스)** 가 있어요.
그럼 “이미지 한 장의 라벨(label)”은
→ “1000가지 중 하나를 선택”하는 정보잖아요?
정보이론적으로는 이렇게 계산합니다:
정보량=log⁡2(1000)≈9.97≈10비트\\text\{정보량\} = \\log_2(1000) \\approx 9.97 \\approx 10\\text\{비트\}
정보량=log2(1000)≈9.97≈10비트
즉,
> “1000개 중 하나를 구분한다”는 건 “약 10비트의 정보를 전달한다”는 뜻이에요.
---
### 3. 그러니까 “10비트의 제약을 부여한다”는 건
> “이미지 한 장이, 모델의 파라미터를 조정하는 데 사용할 수 있는 정보량은 약 10비트뿐이다”
라는 뜻이에요.
즉, 모델이 아무리 복잡해도
이미지 하나가 정답을 알려주는 건 “1000개 중 하나가 맞다” 정도의 간단한 정보예요.
그 이상의 세부적인 피드백은 없죠.

## 4.1 Data Augmentation {toggle="true"}
### 원본
---
[image omitted: temporary Notion asset]
[image omitted: temporary Notion asset]
---
### 번역본
---
이미지 데이터에서 과적합을 줄이기 위한 가장 쉽고 일반적인 방법은 라벨을 보존하는 변환(label-preserving transformations)을 사용하여 데이터셋을 인위적으로 확장하는 것입니다(예: [25, 4, 5]). 우리는 **두 가지 형태의 데이터 증강(data augmentation)**을 사용하며, 이 두 방법 모두 원본 이미지로부터 매우 적은 계산만으로 변환된 이미지를 생성할 수 있기 때문에, 변환된 이미지를 디스크에 저장할 필요가 없습니다. 우리의 구현에서는, GPU가 이전 배치의 이미지를 학습하는 동안 CPU에서 Python 코드로 변환된 이미지가 생성됩니다. 따라서 이러한 데이터 증강 방식들은 사실상 계산 비용이 들지 않습니다.
**첫 번째 형태**의 데이터 증강(data augmentation)은 **이미지의 이동(translation)과 수평 반사(horizontal reflection)를 생성하는 것으로 구성**됩니다. 우리는 256×256 이미지로부터 무작위로 224×224 패치(그리고 그 수평 반사본)를 추출하고, 이러한 추출된 패치들로 우리의 네트워크를 학습시킵니다⁴. 이 방법은 우리의 학습 세트의 크기를 2048배 증가시키지만, 결과적으로 생성된 학습 예제들은 물론 서로 강하게 상호의존적입니다. 이 방식을 사용하지 않으면, 우리의 네트워크는 심각한 과적합을 겪게 되며, 이는 훨씬 더 작은 네트워크를 사용하도록 강요했을 것입니다. 테스트 시에는, 네트워크가 224×224 크기의 다섯 개의 패치(네 개의 모서리 패치와 하나의 중앙 패치) 및 그 수평 반사본(따라서 총 10개의 패치)을 추출하여, 네트워크의 소프트맥스(softmax) 층이 이 10개의 패치에 대해 생성한 예측값을 평균함으로써 최종 예측을 수행합니다.
[image omitted: temporary Notion asset]
---


## **우리의 구현에서는, GPU가 이전 배치의 이미지를 학습하는 동안 CPU에서 Python 코드로 변환된 이미지가 생성됩니다**

→ 즉, GPU가 현재 배치(batch)를 학습하는 동안
CPU가 다음 배치용 증강 이미지를 미리 만들어 놓습니다.
이렇게 하면 **시간 낭비 없이 병렬 처리(parallel processing)**가 가능하죠



## **이미지의 이동(translation)과 수평 반사(horizontal reflection)를 생성하는 것으로 구성**

### 1. 핵심 요약
> 첫 번째 데이터 증강 방식은 “이미지를 조금씩 움직이거나 좌우 반전(수평 반사) 시켜서,
원본 이미지를 다양한 형태로 바꾸는 것”이에요.
즉, **이미지의 라벨(정답)은 그대로 유지하면서, 입력 이미지를 다양하게 만들어서 과적합을 줄이려는 방법**입니다.
---
### 2. “256×256에서 224×224 패치를 무작위로 자른다”
AlexNet은 학습 전에 이미지를 **256×256 크기로 리사이즈**한 다음,
그 안에서 **224×224 크기의 일부분(패치)** 을 **랜덤하게 잘라서** 학습에 사용했어요.
즉,
- 한 번은 왼쪽 위를 잘라 쓰고,
- 또 한 번은 중앙,
- 또 한 번은 오른쪽 아래를 잘라 쓰는 식으로
→ 매번 다른 부분을 보게 만들어 **데이터 다양성을 인위적으로 늘린 거예요.**
---
### 3. “수평 반사(horizontal reflection)”란?
→ **이미지를 좌우로 뒤집는 것**, 즉 **거울에 비친 것처럼 반전**시키는 겁니다.
예를 들어,
고양이가 오른쪽을 보고 있는 사진을
→ 왼쪽을 보게 만들면
→ “다른 이미지처럼 보이지만 여전히 ‘고양이’인 이미지”가 됩니다.
이렇게 하면 하나의 이미지로부터
- 원본 224×224 패치 + 좌우 반전된 224×224 패치
= **두 장의 학습 데이터**를 만들 수 있어요.
---
### 4. “데이터셋의 크기를 2048배 증가시킨다”의 의미
각 이미지를 여러 위치(랜덤 크롭)와 반전 조합으로 자르면
엄청나게 많은 변형된 이미지가 만들어지죠.
즉, 원래 100만 장짜리 데이터셋이 **2048배나 많아진 효과**를 내는 거예요.
물론 실제로 2048배의 “서로 완전히 다른” 데이터가 생긴 건 아니고,
→ 서로 비슷하지만 약간씩 다른 “변형 이미지들”이 추가된 셈이에요.
그래서 논문도 “strongly interdependent (서로 강하게 상호의존적)”이라고 표현했죠.
---
### 5. “이걸 안 쓰면 과적합이 심해진다”
만약 이런 증강을 안 하면,
네트워크는 **훈련 이미지에만 완벽히 맞춰지고 새로운 이미지엔 잘 대응 못 하는 과적합** 현상이 생깁니다.
그래서 데이터 증강이 없었다면 AlexNet은 **더 작은 모델을 써야 했을 것**이라고 말한 거예요.
---
### 6. “테스트 시에는 10개의 패치를 사용한다”
훈련할 때는 랜덤 크롭을 하지만,
**테스트할 때는 랜덤 대신 일정한 규칙으로 10개 패치를 뽑아 평균**을 냈어요.
- 4개의 모서리(왼쪽 위, 오른쪽 위, 왼쪽 아래, 오른쪽 아래)
- 1개의 중앙 패치
- 그리고 각각의 좌우 반전본
→ 총 10개의 224×224 이미지를 만들어
→ 각 패치의 softmax 출력을 평균내서 최종 예측을 결정했습니다.
이렇게 하면 이미지 한 장을 여러 시각에서 본 것처럼 안정적인 예측이 가능해요.



## **RGB 채널 강도 변화**

### 1. 핵심 요약
> 첫 번째 증강이 “이미지를 자르거나 뒤집는” 공간적 변형(spatial transformation) 이었다면,
이번 두 번째 증강은 “**이미지 색깔을 살짝 바꾸는**” **색상 변형(color augmentation)** 입니다.
즉, 같은 고양이 사진이라도 조명이나 색감이 조금 다를 수 있잖아요?
→ 이런 색깔의 변화를 인위적으로 만들어서 **조명 변화에도 강한 네트워크**를 만드는 겁니다.
---
### 2. 어떻게 색을 바꾸는가?
그냥 무작위로 RGB를 바꾸는 게 아니라, **조명 변화의 통계적 특성**을 반영해서 바꿔요.
이를 위해 사용하는 게 바로 **PCA(주성분 분석, Principal Component Analysis)** 입니다.
### 단계 요약:
1. ImageNet 전체 학습 이미지의 RGB 픽셀들을 모읍니다.
2. 그 RGB 값들을 PCA로 분석해서, **색 공간의 주요 방향(principal components)** 3개를 찾습니다.
- 쉽게 말해 “색깔이 가장 많이 변하는 세 축”을 구한 거예요.
3. 각 축마다 그 중요도를 나타내는 **고유값(eigenvalue)** 이 있습니다.
4. 학습할 때마다, 각 축 방향으로 **조금씩 색을 변화시키는 노이즈**를 더합니다.
이렇게 하면 조명 조건이 달라도 잘 대응할 수 있게 돼요.
---
### 3. 수식 설명 (쉽게)
각 RGB 픽셀 Ixy=[IxyR,IxyG,IxyB]TI_\{xy\} = [I\^R_\{xy\}, I\^G_\{xy\}, I\^B_\{xy\}]\^TIxy=[IxyR,IxyG,IxyB]T 에 대해
아래 값을 더합니다:
[p1,p2,p3]⋅[α1λ1,α2λ2,α3λ3]T[p_1, p_2, p_3] \\cdot [\\alpha_1 \\lambda_1, \\alpha_2 \\lambda_2, \\alpha_3 \\lambda_3]\^T
[p1,p2,p3]⋅[α1λ1,α2λ2,α3λ3]T
여기서:
- p1,p2,p3p_1, p_2, p_3p1,p2,p3 → RGB 공간에서 구한 **3개의 주성분 축 방향(고유벡터)**
- λ1,λ2,λ3\\lambda_1, \\lambda_2, \\lambda_3λ1,λ2,λ3 → 각 주성분의 **고유값(중요도)**
- α1,α2,α3\\alpha_1, \\alpha_2, \\alpha_3α1,α2,α3 → 평균 0, 표준편차 0.1인 **랜덤 노이즈 값**
즉, RGB 픽셀마다 **주성분 방향으로 약간의 무작위 색 변화**를 더해주는 거예요.
---
### 4. 왜 이런 일을 하느냐?
> 조명 세기가 조금 바뀌거나 색조가 달라져도
같은 물체로 인식하게 하려는 목적이에요.
예를 들어,
- 낮에 찍은 고양이(밝은 노란색)
- 밤에 찍은 고양이(어두운 주황색)
둘 다 “고양이”로 인식해야 하죠.
이 PCA 색상 증강은 이런 “**조명·색 변화에 불변(invariant)**”한 특성을 학습하게 도와줍니다.
---
### 5. 성능 효과
이 단순한 색상 증강만으로 **Top-1 오류율을 약 1% 줄였다**고 논문에서 말해요.
즉, 실제로 모델의 일반화 성능을 확실히 개선한 겁니다.

## 4.2 Dropout {toggle="true"}
### 원본
---
[image omitted: temporary Notion asset]
---
### 번역본
---
여러 다른 모델들의 예측을 결합하는 것은 테스트 오류를 줄이는 매우 성공적인 방법입니다 [1, 3]. 그러나 이미 학습에 여러 날이 걸리는 대규모 신경망의 경우, 이러한 방법은 너무 비용이 많이 드는 것으로 보입니다. 그러나 학습 중에 약 두 배의 비용만 추가되는 매우 효율적인 모델 결합 방식이 존재합니다. 최근에 도입된 **“드롭아웃(dropout)” **[10]이라는 기법은 각 은닉 뉴런의 출력을 0.5의 확률로 0으로 설정하는 것으로 구성됩니다. 이렇게 “드롭아웃”된 뉴런들은 순전파(forward pass)에 기여하지 않으며, 역전파(backpropagation)에도 참여하지 않습니다.
따라서 입력이 주어질 때마다 신경망은 서로 다른 구조를 샘플링하게 되지만, 이 모든 구조들은 가중치를 공유합니다. 이 기법은 뉴런들의 복잡한 공동 적응(co-adaptation)을 감소시키는데, 이는 한 뉴런이 특정 다른 뉴런들의 존재에 의존할 수 없기 때문입니다. 따라서 뉴런은 여러 가지 다른 무작위 부분집합의 뉴런들과 함께 사용될 수 있는 보다 강건한 특징들을 학습하도록 강요됩니다.
테스트 시에는, 우리는 모든 뉴런을 사용하지만 그들의 출력을 0.5배로 줄입니다. 이는 지수적으로 많은 드롭아웃 네트워크들이 생성하는 예측 분포들의 기하평균(geometric mean)을 취하는 것에 대한 합리적인 근사치로 볼 수 있습니다.
우리는 그림 2의 처음 두 개의 완전 연결 층(fully-connected layers)에서 드롭아웃(dropout)을 사용합니다. 드롭아웃을 사용하지 않으면, 우리의 네트워크는 상당한 과적합을 보입니다. 드롭아웃은 수렴(converge)하는 데 필요한 반복 횟수를 대략 두 배로 증가시킵니다.
---


## **여러 다른 모델들의 예측을 결합하는 것은 테스트 오류를 줄이는 매우 성공적인 방법입니다**

### 1. “여러 다른 모델들의 예측을 결합하는 것은 테스트 오류를 줄이는 매우 성공적인 방법입니다.”
→ 여러 모델을 함께 쓰면 정확도가 올라간다는 뜻이에요.
이걸 **앙상블(ensemble)** 이라고 부릅니다.
예를 들어,
- 모델 A는 90% 정확도
- 모델 B는 91% 정확도
- 하지만 둘의 예측을 평균내면 93% 정확도가 될 수도 있습니다.
즉, **서로 다른 모델들의 장점을 합치면 더 좋은 결과를 낼 수 있다**는 거예요.
---
### 2. “그러나 이미 학습에 여러 날이 걸리는 대규모 신경망의 경우, 이러한 방법은 너무 비용이 많이 든다.”
→ AlexNet 같은 큰 모델은 **하나만 학습하는 데도 며칠이 걸리는데**,
그걸 여러 개 학습해야 한다면 현실적으로 **너무 오래 걸리고 계산 자원이 많이 든다**는 뜻이에요.
즉, “앙상블이 좋다는 건 알지만, 너무 비싸서 쓸 수 없다.”라는 말입니다.
---
### 3. “그러나 학습 중에 약 두 배의 비용만 추가되는 매우 효율적인 모델 결합 방식이 존재합니다.”
→ 그래서 “완전히 여러 모델을 따로 학습시키지 않아도,
**거의 같은 효과를 내는 효율적인 방법이 있다**”는 이야기를 이어서 하려는 거예요.
그게 바로 **드롭아웃(dropout)** 입니다.



## **따라서 입력이 주어질 때마다 신경망은 서로 다른 구조를 샘플링하게 되지만, 이 모든 구조들은 가중치를 공유합니다.**

### 1. “입력이 주어질 때마다 신경망은 서로 다른 구조를 샘플링하게 되지만, 이 모든 구조들은 가중치를 공유합니다.”
→ 드롭아웃을 쓰면, 매 학습 스텝마다 **일부 뉴런이 랜덤하게 꺼집니다.**
즉, 매번 **조금씩 다른 구조의 네트워크**가 만들어지는 거예요.
예를 들어,
- 첫 번째 배치에선 뉴런 A, C, D가 켜지고
- 다음 배치에선 B, D, E가 켜질 수도 있습니다.
하지만 이 모든 경우가 **하나의 네트워크 안에서 같은 가중치(weight)** 를 공유해요.
그래서 별도의 모델을 여러 개 학습시키는 게 아니라,
**한 모델 안에서 수많은 작은 네트워크들이 동시에 학습되는 효과**가 생깁니다.
---
### 2. “이 기법은 뉴런들의 복잡한 공동 적응(co-adaptation)을 감소시키는데,”
→ **공동 적응(co-adaptation)** 이란,
한 뉴런이 특정 다른 뉴런이 존재할 때만 잘 작동하는 현상이에요.
예를 들어,
뉴런 A가 “B가 활성화될 때만 의미 있는 출력을 내는” 식이면,
A는 B에 너무 의존하게 되고 독립적으로는 쓸모가 없게 돼요.
이런 의존 관계가 많아지면,
→ 학습 데이터에서는 잘 맞지만,
→ 새로운 데이터에서는 깨져버려서 **과적합(overfitting)** 이 생깁니다.
---
### 3. “이는 한 뉴런이 특정 다른 뉴런들의 존재에 의존할 수 없기 때문입니다.”
드롭아웃은 매번 랜덤하게 뉴런들을 꺼버리기 때문에,
A는 “이번엔 B가 있을 수도, 없을 수도 있음”이라는 조건에서 학습해야 합니다.
따라서 A는 **다른 뉴런이 없어도 스스로 유용한 특징(feature)** 을 뽑아야 하죠.
---
### 4. “따라서 뉴런은 여러 가지 다른 무작위 부분집합의 뉴런들과 함께 사용될 수 있는 보다 강건한 특징들을 학습하도록 강요됩니다.”
즉,
- 어떤 배치에서는 A가 B, C와 함께 학습되고
- 다른 배치에서는 D, E와 함께 학습되므로
A는 어떤 조합에서도 잘 작동해야 합니다.
결과적으로 **특정 뉴런 조합에 의존하지 않는, 더 일반적이고 견고한 표현(robust features)** 을 배우게 돼요.



## **테스트 시에는, 우리는 모든 뉴런을 사용하지만 그들의 출력을 0.5배로 줄입니다. **

### 1. “테스트 시에는, 우리는 모든 뉴런을 사용하지만 그들의 출력을 0.5배로 줄입니다.”
- 학습(훈련)할 때는 **뉴런을 50% 확률로 껐어요.**
→ 그래서 각 뉴런은 절반만의 확률로 작동했죠.
- 그런데 **테스트할 때는 뉴런을 끄면 안 되잖아요?**
→ 그러면 예측이 랜덤해져서 안정적인 결과를 못 얻어요.
그래서 **모든 뉴런을 켠 상태로 테스트**하되,
훈련 때 절반만 켜져 있었던 걸 보정하기 위해
**출력을 절반(×0.5)** 으로 줄여주는 거예요.
즉,
> 훈련할 땐 뉴런의 절반만 쓰고,
테스트할 땐 전부 쓰지만 각 뉴런의 기여도를 절반으로 줄인다.
이렇게 하면 훈련 시 평균 활성 수준과 맞춰집니다.
---
### 2. “이는 지수적으로 많은 드롭아웃 네트워크들이 생성하는 예측 분포들의 기하평균(geometric mean)을 취하는 것에 대한 합리적인 근사치로 볼 수 있습니다.”
이 문장은 수학적으로 드롭아웃의 “앙상블 효과”를 설명한 부분이에요.
즉, 드롭아웃은 매번 **랜덤한 뉴런 조합**으로
엄청나게 많은(지수적으로 많은) “작은 신경망들”을 만들어냅니다.
예를 들어 뉴런이 100개라면,
뉴런을 켜거나 끄는 조합이 `2^100` 가지예요 — 정말 많죠?
그러니까 드롭아웃 학습은 실제로
“2의 100제곱 개의 서로 다른 네트워크”를 동시에 훈련시키는 효과를 냅니다.
그런데 테스트할 때,
그렇게 많은 네트워크의 결과를 일일이 평균낼 수는 없으니까,
**출력을 0.5배로 줄이는 것**이
“그 수많은 드롭아웃 네트워크의 예측값을 평균낸 것과 거의 같다”고 보는 거예요.
수학적으로는 그 평균이 **기하평균(geometric mean)** 으로 표현됩니다.

## 5. Details of learning (Fig.3) {toggle="true"}
### 원본
---
[image omitted: temporary Notion asset]
[image omitted: temporary Notion asset]
---
### 번역본
---
[image omitted: temporary Notion asset]
우리는 각 층의 가중치를 평균이 0이고 표준편차가 0.01인 가우시안 분포로부터 초기화했습니다. 두 번째, 네 번째, 다섯 번째 합성곱 층과 완전 연결 은닉층의 뉴런 바이어스는 상수 1로 초기화했습니다. 이러한 초기화는 ReLU에 양의 입력을 제공함으로써 학습의 초기 단계를 가속화합니다. 나머지 층들의 뉴런 바이어스는 상수 0으로 초기화했습니다.
우리는 모든 층에 동일한 학습률(learning rate)을 사용했으며, 학습 과정 전반에 걸쳐 이를 수동으로 조정했습니다. 우리가 따른 경험적 기준(heuristic)은 현재의 학습률로 검증 오류율(validation error rate)이 더 이상 개선되지 않을 때 학습률을 10으로 나누는 것이었습니다. 학습률은 처음에 0.01로 설정되었으며, 종료되기 전까지 세 번 감소시켰습니다. 우리는 120만 장의 이미지로 구성된 학습 세트를 약 90회 순환(cycle)하며 네트워크를 학습시켰고, 이는 NVIDIA GTX 580 3GB GPU 두 개에서 5일에서 6일 정도가 걸렸습니다.
그림 3: 224×224×3 크기의 입력 이미지에 대해 첫 번째 합성곱 층이 학습한 11×11×3 크기의 96개의 합성곱 커널. 상단의 48개 커널은 GPU 1에서 학습되었으며, 하단의 48개 커널은 GPU 2에서 학습되었습니다. 자세한 내용은 6.1절을 참조하십시오.
---


## **우리는 배치 크기 128개의 예시, 모멘텀 0.9, 가중치 감쇠(weight decay) 0.0005를 사용하여 확률적 경사 하강법(SGD)으로 모델을 학습시켰습니다.**

### 1. 전체 문장의 핵심 요약
> “우리는 배치 크기 128, 모멘텀 0.9, 가중치 감쇠(weight decay) 0.0005를 사용해서
확률적 경사 하강법(SGD)으로 모델을 학습했다.”
이 말은 **모델을 학습할 때의 세부 설정값(하이퍼파라미터)** 을 소개한 거예요.
---
### 2. 등장하는 주요 개념 3가지
### (1) **배치 크기 128**
한 번의 학습 단계(Iteration)에서 **128개의 이미지**를 묶어서
평균 손실을 계산하고 가중치를 업데이트한다는 뜻이에요.
즉, **SGD**지만 실제로는 *mini-batch SGD*예요.
---
### (2) **모멘텀(momentum) = 0.9**
이건 가중치를 업데이트할 때
**이전 단계의 이동 방향을 일부 반영해서 관성 효과를 주는 기법**이에요.
예를 들어,
가중치가 계속 같은 방향으로 내려가고 있다면
그 방향으로 더 빠르게 가도록 “가속도”를 주는 거예요.
수식에서 모멘텀은 이렇게 표현돼요:
vi+1:=0.9⋅vi−ϵ⋅∂L∂wv_\{i+1\} := 0.9 \\cdot v_i - \\epsilon \\cdot \\frac\{\\partial L\}\{\\partial w\}
vi+1:=0.9⋅vi−ϵ⋅∂w∂L
여기서 viv_ivi는 이전 단계의 변화량(velocity)이고,
0.9가 그걸 90% 정도 이어받는다는 뜻이에요.
---
### (3) **가중치 감쇠(weight decay) = 0.0005**
→ 앞에서 봤던 정규화 기법이에요.
가중치가 너무 커지면 과적합이 생기니까
매 업데이트마다 살짝 줄여주는 항이 추가돼요.
수식에서는 이렇게 반영됩니다:
vi+1:=0.9⋅vi−0.0005⋅ϵ⋅wi−ϵ⋅∂L∂wv_\{i+1\} := 0.9 \\cdot v_i - 0.0005 \\cdot \\epsilon \\cdot w_i - \\epsilon \\cdot \\frac\{\\partial L\}\{\\partial w\}
vi+1:=0.9⋅vi−0.0005⋅ϵ⋅wi−ϵ⋅∂w∂L
즉,
- `0.0005 * ε * w_i` 부분이 가중치 감쇠 항이고,
- 그게 가중치가 커지지 않게 살짝 눌러주는 역할을 합니다.
---
### 3. “이 작은 양의 가중치 감쇠가 모델의 학습에 도움이 된다는 것을 발견했다”
→ 보통 weight decay는 “정규화 목적”으로만 쓰이는데,
AlexNet에서는 실제로 **학습 속도나 정확도도 좋아졌다**는 뜻이에요.
즉,
> “이건 단순히 규제(regularization) 효과만 있는 게 아니라,
모델이 더 잘 학습되게 하는 긍정적인 역할도 했다.”
---
### 4. 수식 요약
전체 업데이트는 두 단계로 진행돼요:
1️⃣ **속도(velocity) 갱신:**
vi+1=0.9vi−0.0005ϵwi−ϵ⟨∂L∂w⟩Div_\{i+1\} = 0.9v_i - 0.0005 \\epsilon w_i - \\epsilon \\left\\langle \\frac\{\\partial L\}\{\\partial w\} \\right\\rangle_\{D_i\}
vi+1=0.9vi−0.0005ϵwi−ϵ⟨∂w∂L⟩Di
2️⃣ **가중치 갱신:**
wi+1=wi+vi+1w_\{i+1\} = w_i + v_\{i+1\}
wi+1=wi+vi+1
여기서
- iii: 현재 반복(iteration)
- ϵ\\epsilonϵ: 학습률(learning rate)
- DiD_iDi: 현재 미니배치 데이터
- ⟨∂L∂w⟩Di\\langle \\frac\{\\partial L\}\{\\partial w\} \\rangle_\{D_i\}⟨∂w∂L⟩Di: 현재 배치에서의 평균 기울기



## **우리는 각 층의 가중치를 평균이 0이고 표준편차가 0.01인 가우시안 분포로부터 초기화했습니다.**

### 1. “각 층의 가중치를 평균이 0이고 표준편차가 0.01인 가우시안 분포로부터 초기화했습니다.”
→ 모든 합성곱층(conv)과 완전연결층(fc)의 **가중치(weight)** 를
무작위로, **정규분포 N(0, 0.01²)** 에서 뽑았다는 뜻이에요.
즉,
- 가중치 평균(mean) = 0
- 가중치 표준편차(std) = 0.01
이렇게 하면
너무 큰 값으로 시작하지 않아서 학습이 안정되고,
너무 작은 값으로 시작하지 않아서 신호가 소멸되지 않아요.
요약하면 →
> “모든 가중치는 0 근처의 작은 랜덤 값으로 시작한다.”
---
### 2. “두 번째, 네 번째, 다섯 번째 합성곱 층과 완전 연결 은닉층의 뉴런 바이어스는 상수 1로 초기화했습니다.”
→ 여기서 **바이어스(bias)** 는 각 뉴런이 가진 “기본 오프셋값”이에요.
일반적으로는 바이어스를 0으로 두는데,
여기서는 특정 층(2, 4, 5번째 conv층 + fully connected hidden layer)은 **1로 설정**했어요.
그 이유는 다음 문장에서 설명돼요 👇
---
### 3. “이러한 초기화는 ReLU에 양의 입력을 제공함으로써 학습의 초기 단계를 가속화합니다.”
ReLU 함수는
f(x)=max⁡(0,x)f(x) = \\max(0, x)
f(x)=max(0,x)
이죠.
즉, 입력이 0보다 작으면 **출력이 0**이 되어 뉴런이 “죽습니다(dead neuron)”.
그런데 초기 바이어스를 1로 주면,
→ 초기에 입력이 약간 음수더라도 +1+1+1 덕분에 ReLU 출력이 0이 되지 않아요.
→ 뉴런이 “활성화된 상태”에서 학습을 시작하게 되죠.
즉, **학습 초반에 더 많은 뉴런이 참여**하게 만들어
**학습 속도를 빠르게 하는 효과**가 있습니다.
요약하면 →
> “초기 바이어스를 1로 주면, ReLU 뉴런이 꺼지지 않고 초반 학습이 잘 진행된다.”
---
### 4. “나머지 층들의 뉴런 바이어스는 상수 0으로 초기화했습니다.”
→ 위에서 언급된 일부 층(2, 4, 5 conv + fc hidden)을 제외한
다른 층들은 **기본값인 0으로 설정**했다는 뜻이에요.
이건 특별히 조정할 필요가 없는 부분들이라 0으로 둔 겁니다.

## 6. Results (Table 1,2) {toggle="true"}
### 원본
---
[image omitted: temporary Notion asset]
---
### 번역본
---
우리의 ILSVRC-2010 결과는 표 1에 요약되어 있습니다. 우리의 네트워크는 테스트 세트에서 top-1과 top-5 오류율이 각각 37.5%와 17.0%⁵를 달성했습니다. ILSVRC-2010 대회 기간 동안 달성된 최고 성능은 서로 다른 특징(feature)들로 학습된 여섯 개의 희소 부호화(sparse-coding) 모델로부터 생성된 예측값들을 평균한 접근법으로, 각각 47.1%와 28.2%였습니다 [2]. 그 이후 발표된 최고 결과는 두 종류의 밀집 샘플링된 특징으로부터 계산된 피셔 벡터(Fisher Vectors, FVs)에 대해 학습된 두 개의 분류기의 예측값들을 평균한 접근법으로, 45.7%와 25.7%였습니다 [24].
우리는 또한 ILSVRC-2012 대회에 우리 모델을 출전시켰으며, 그 결과를 표 2에 보고합니다. ILSVRC-2012 테스트 세트의 라벨은 공개적으로 제공되지 않기 때문에, 우리가 시도한 모든 모델의 테스트 오류율을 보고할 수는 없습니다. 이 문단의 나머지 부분에서는 검증 오류율(validation error rate)과 테스트 오류율(test error rate)을 구분 없이 사용합니다. 왜냐하면 우리의 경험상 두 값의 차이는 0.1%를 넘지 않기 때문입니다(표 2 참조).
이 논문에서 설명한 CNN은 top-5 오류율 18.2%를 달성했습니다. 다섯 개의 유사한 CNN들의 예측값을 평균하면 오류율은 16.4%가 됩니다. 마지막 풀링 층 위에 여섯 번째 합성곱 층을 추가한 단일 CNN을 사용하여 ImageNet 2011년 가을 전체 릴리스(1500만 장의 이미지, 22,000개 범주)를 분류하도록 학습시킨 후, ILSVRC-2012에 대해 “미세 조정(fine-tuning)”을 수행하면 오류율은 16.6%가 됩니다.
전체 2011년 가을 릴리스에 대해 사전 학습된 두 개의 CNN과 앞서 언급한 다섯 개의 CNN의 예측값을 평균하면 오류율은 15.3%가 됩니다. 두 번째로 높은 성능을 기록한 대회 참가자는 서로 다른 유형의 밀집 샘플링된 특징들(densely-sampled features)로부터 계산된 피셔 벡터(Fisher Vectors, FVs)에 대해 학습된 여러 분류기의 예측값을 평균하는 접근법을 사용했으며, 오류율은 26.2%였습니다 [7].
<br>표 1: ILSVRC-2010 테스트 세트에서의 결과 비교. *이탤릭체*로 표시된 값은 다른 연구자들에 의해 달성된 최고 결과입니다.<br><br>표 2: ILSVRC-2012 검증 세트와 테스트 세트에서의 오류율 비교. *이탤릭체*로 표시된 값은 다른 연구자들에 의해 달성된 최고 결과입니다. 별표(\*)가 붙은 모델들은 ImageNet 2011년 가을 전체 릴리스를 분류하도록 “사전 학습(pre-trained)”되었습니다. 자세한 내용은 6절을 참조하십시오.
---
## 6.1 Qualitative Evaluations {toggle="true"}
### 원본
---
[image omitted: temporary Notion asset]
[image omitted: temporary Notion asset]
---
### 번역본
---
그림 4의 왼쪽 패널에서 우리는 여덟 개의 테스트 이미지에 대해 네트워크의 상위 5개 예측값(top-5 predictions)을 계산함으로써 네트워크가 무엇을 학습했는지를 정성적으로 평가합니다. 왼쪽 상단의 진드기(mite)와 같이 중심에 있지 않은 객체들도 네트워크에 의해 인식될 수 있음을 주목하십시오. 대부분의 상위 5개 라벨들은 타당해 보입니다. 예를 들어, 표범(leopard)의 경우 가능한 라벨로 고려된 것들은 모두 다른 종류의 고양이들입니다. 어떤 경우들(grille, cherry)에는 사진의 초점이 실제로 어디에 맞추어져 있는지에 대한 모호함이 존재합니다.
네트워크의 시각적 지식을 탐색하는 또 다른 방법은, 이미지가 마지막 4096차원 은닉층(hidden layer)에서 유도하는 특징 활성(feature activation)을 고려하는 것입니다. 두 이미지가 작은 유클리드 거리(Euclidean separation)를 가진 특징 활성 벡터를 생성한다면, 신경망의 높은 수준 계층들이 이 두 이미지를 유사하다고 판단한다고 말할 수 있습니다.
그림 4는 테스트 세트에서 선택된 다섯 개의 이미지와, 이 측정 기준에 따라 각각과 가장 유사한 학습 세트의 여섯 개 이미지를 보여줍니다. 주목할 점은, 픽셀 수준에서는 검색된 학습 이미지들이 첫 번째 열의 질의 이미지들과 L2 거리상으로 가깝지 않다는 것입니다. 예를 들어, 검색된 개(dog)들과 코끼리(elephant)들은 다양한 자세로 나타나 있습니다. 훨씬 더 많은 테스트 이미지에 대한 결과는 부록(supplementary material)에 제시되어 있습니다.
두 개의 4096차원 실수값 벡터(real-valued vectors) 사이의 유클리드 거리(Euclidean distance)를 사용하여 유사도를 계산하는 것은 비효율적입니다. 그러나 이러한 벡터들을 짧은 이진 코드(short binary codes)로 압축하도록 오토인코더(auto-encoder)를 학습시킴으로써 효율적으로 만들 수 있습니다. 이러한 방법은 이미지 라벨을 활용하지 않기 때문에 의미적으로 유사하지 않더라도 비슷한 에지(edge) 패턴을 가진 이미지를 검색하는 경향이 있는, 원시 픽셀(raw pixels)에 오토인코더를 적용하는 방식 [14]보다 훨씬 더 나은 이미지 검색 방법을 만들어낼 것입니다.
그림 3은 네트워크의 두 개의 데이터 연결 층(data-connected layers)에 의해 학습된 합성곱 커널(convolutional kernels)을 보여줍니다. 네트워크는 다양한 주파수(frequency)와 방향(orientation)에 선택적인 커널들뿐 아니라, 여러 가지 색상의 블롭(blobs)들도 학습했습니다. 3.5절에서 설명된 제한된 연결성(restricted connectivity)의 결과로, 두 GPU가 각각 특화된 형태를 보이는 것을 확인할 수 있습니다. GPU 1의 커널들은 주로 색상에 무관(color-agnostic)하며, GPU 2의 커널들은 주로 색상에 특화(color-specific)되어 있습니다. 이러한 형태의 특화(specialization)는 모든 실행(run)에서 발생하며, 특정한 무작위 가중치 초기화(random weight initialization)와는 독립적입니다(GPU의 번호 재지정은 예외).
그림 4: (왼쪽) 여덟 개의 ILSVRC-2010 테스트 이미지와, 우리 모델이 가장 가능성이 높다고 판단한 다섯 개의 라벨. 각 이미지 아래에는 정답 라벨이 적혀 있으며, 정답 라벨이 상위 다섯 개 안에 포함된 경우에는 해당 라벨에 할당된 확률이 빨간 막대로 표시되어 있습니다. (오른쪽) 첫 번째 열에는 다섯 개의 ILSVRC-2010 테스트 이미지가 있습니다. 나머지 열들은 테스트 이미지의 특징 벡터와 마지막 은닉층에서 유클리드 거리가 가장 작은 특징 벡터를 생성한 여섯 개의 학습 이미지를 보여줍니다.
---
## 7. Discussion {toggle="true"}
### 원본
---
[image omitted: temporary Notion asset]
---
### 번역본
---
우리의 결과는 크고 깊은 합성곱 신경망이 순수한 지도 학습(supervised learning)만을 사용하여 매우 어려운 데이터셋에서 기록적인 성능을 달성할 수 있음을 보여줍니다. **주목할 만한 점은, 하나의 합성곱 층을 제거하면 네트워크의 성능이 저하된다는 것입니다**. 예를 들어, 중간 층들 중 어느 하나를 제거하면 네트워크의 top-1 성능이 약 2% 감소합니다. 따라서 깊이(depth)는 우리의 결과를 달성하는 데 있어 실제로 중요한 요소입니다.
우리의 실험을 단순화하기 위해, 우리는 비지도 사전 학습(unsupervised pre-training)을 사용하지 않았습니다. 그러나 우리는 그것이 도움이 될 것이라고 예상합니다. 특히 라벨이 지정된 데이터의 양을 늘리지 않고도 네트워크의 크기를 크게 확장할 만큼 충분한 계산 능력을 확보할 수 있다면 더욱 그렇습니다. 지금까지 우리의 결과는 네트워크의 크기를 키우고 학습 시간을 늘릴수록 개선되어 왔지만, 인간 시각 시스템의 하측두(infero-temporal) 경로에 도달하기 위해서는 여전히 수많은 단계가 남아 있습니다. 궁극적으로 우리는 시간적 구조(temporal structure)가 정적 이미지에서는 결여되어 있거나 덜 명확한 유용한 정보를 제공하는 비디오 시퀀스에 대해 매우 크고 깊은 합성곱 신경망을 사용하고자 합니다.
---

---

# ResNet

---
## Abstract {toggle="true"}
더 깊은 신경망은 학습하기가 더 어렵습니다. 우리는 이전보다 훨씬 **더 깊은 네트워크를 학습하는 과정을 쉽게 만들기 위해 잔차(residual) 학습 프레임워크를 제안**합니다. 이 방법에서는 층들이 입력을 직접 변환하는 함수를 학습하도록 하는 대신, 입력 대비 변화량(잔차 함수) 를 학습하도록 층들을 재정의했습니다. 이를 통해 잔차 네트워크는 최적화가 쉬워지고, 깊이가 크게 증가해도 정확도가 향상된다는 실험적 근거를 제시합니다.
ImageNet 데이터셋에서 우리는 **152층**에 달하는 잔차 네트워크를 평가했으며, 이는 VGG 네트워크보다 8배 깊지만 더 낮은 연산 복잡도를 가집니다. 이러한 잔차 네트워크 앙상블은 ImageNet 테스트 세트에서 3.57% 오류율을 기록했으며, ILSVRC 2015 분류 과제에서 1위를 달성했습니다. 또한 CIFAR-10 데이터셋에서 100층 및 1000층 규모의 네트워크 분석 결과도 제시합니다.
**표현의 깊이는 다양한 시각 인식 작업에서 매우 중요**합니다. 우리의 매우 깊은 표현 덕분에 COCO 객체 검출 데이터셋에서는 28%의 상대적 성능 향상을 얻었습니다. 딥 잔차 네트워크는 ILSVRC 및 COCO 2015 대회에서 우리가 제출한 모델들의 기반이 되었으며, ImageNet detection, ImageNet localization, COCO detection, COCO segmentation 과제에서도 모두 1위를 차지했습니다.
## 1. Introduction {toggle="true"}
---
딥 컨볼루션 신경망은 이미지 분류에서 여러 돌파구를 만들어냈다. 이러한 네트워크는 저·중·고 수준의 특징들과 분류기를 end-to-end 방식(처음 입력부터 최종 출력까지를 하나의 모델로 **직접** 학습하는 방식)의 다층 구조로 자연스럽게 통합하며, 쌓인 layer(깊이)의 수가 많아질수록 특징의 “수준”은 더 풍부해질 수 있다. 최근 연구는 **네트워크 깊이가 매우 중요**하다는 사실을 보여주었고, 도전적인 ImageNet 데이터셋에서 최고 성능을 낸 모델들은 모두 깊이가 16층에서 30층에 이르는 “매우 깊은” 구조를 사용한다. 또한 많은 다른 시각 인식 과제에서도 매우 깊은 모델은 큰 성능 향상을 가져왔다.
이 깊이의 중요성으로 인해 자연스럽게 다음 질문이 제기된다:
더 나은 네트워크를 학습하는 것은 단순히 layer를 더 쌓는 것만으로 해결될까?
이 질문에 답하기 어려웠던 이유는 잘 알려진 **gradient vanishing / gradient explosion** 문제였고, 이는 학습 초기부터 수렴을 방해한다. 그러나 **normalized initialization**과 **Batch Normalization**이 도입되면서 이 문제는 상당 부분 해결되었고, 덕분에 수십 개 layer를 가진 네트워크도 **SGD + backpropagation**으로 학습을 시작할 수 있게 되었다.
하지만 더 깊은 네트워크가 학습을 시작할 수 있게 되자, 또 하나의 문제가 드러났다. 바로 **degradation problem**이다.
네트워크의 깊이가 증가하면 정확도는 어느 지점에서 포화되고(놀랍지 않을 수 있음), 이후 급격하게 떨어진다. 더욱 놀라운 점은, 이러한 성능 저하가 **overfitting 때문이 아니라는 것**이다. 이미 충분히 깊은 모델에 layer를 더 추가하면 오히려 **training error가 증가**한다는 사실이 보고되었고, 우리의 실험에서도 철저히 확인되었다. 그림 1은 이 전형적인 예를 보여준다.
[image omitted: temporary Notion asset]
이러한 training accuracy의 저하는 모든 구조가 똑같이 최적화되기 쉬운 것은 아니라는 사실을 의미한다.
얕은 네트워크와, 그 위에 layer를 더 추가한 더 깊은 네트워크를 생각해보자. 더 깊은 구조에는 구성적으로 하나의 해가 존재한다:
- 추가된 layer들은 **항등 함수(identity mapping)** 를 수행하고
- 나머지 layer들은 학습된 얕은 네트워크에서 그대로 복사한다.
이렇게 구성 가능한 해가 존재한다는 사실은, 더 깊은 네트워크가 얕은 네트워크보다 더 높은 training error를 내서는 안 된다는 의미다.
하지만 실험은, 우리가 사용하는 최적화 방법들이 이러한 해와 비슷한 수준의 solution조차 찾지 못한다는 것을 보여준다(혹은 현실적인 시간 안에는 찾지 못한다).
[image omitted: temporary Notion asset]
이 논문에서 우리는 이러한 degradation 문제를 해결하기 위해 **deep residual learning** 프레임워크를 제안한다. 여러 layer가 원하는 mapping을 직접 학습하도록 두는 대신, 우리는 이 layer들이 **residual mapping**을 학습하도록 한다.
원하는 mapping을 H(x)라고 하면, residual은 다음과 같다:
```math
F(x) = H(x) − x
```
즉, 원래의 mapping은 F(x) + x로 다시 표현된다.
이 residual mapping을 최적화하는 것이, 참조 정보가 없는 원래 mapping을 직접 최적화하는 것보다 더 쉽다는 것이 우리의 가설이다. 극단적인 경우, 최적의 함수가 identity mapping이라면, 여러 비선형 layer로 identity를 직접 학습하는 것보다 residual을 0으로 만드는 편이 훨씬 쉽다. **F(x) + x 구조는 shortcut connection을 가진 feedforward 네트워크로 구현**된다.
Shortcut connection은 하나 이상의 layer를 건너뛰는 연결이며, 우리의 경우 shortcut은 단순한 identity mapping을 수행하고, 그 출력은 쌓인 layer 출력과 더해진다.
이 identity shortcut은 파라미터를 추가하지 않으며 계산량도 증가시키지 않는다.
네트워크 전체는 여전히 기존의 **SGD + backpropagation**으로 end-to-end 학습이 가능하고, Caffe 같은 라이브러리에서 solver를 수정하지 않고 구현할 수 있다.
우리는 ImageNet에서 degradation 문제와 제안 방법을 검증하기 위한 실험을 수행했다. 그 결과:
1. 매우 깊은 residual network는 최적화가 쉽지만, 단순히 layer를 쌓은 plain network는 깊어질수록 training error가 증가한다.
2. residual network는 깊이가 크게 증가해도 정확도를 쉽게 향상시킬 수 있으며, 이전 네트워크보다 훨씬 좋은 결과를 낸다.
CIFAR-10에서도 같은 현상이 나타났으며, 이는 최적화의 어려움과 residual learning의 효과가 특정 데이터셋에 국한되지 않음을 보여준다. 우리는 100층이 넘는 모델을 성공적으로 학습했으며, 1000층 이상의 모델도 탐색했다.
ImageNet 분류에서는 매우 깊은 residual network로 뛰어난 성능을 얻었다. 152-layer ResNet은 ImageNet에서 발표된 모델 중 가장 깊은 네트워크이며, 복잡도는 VGG보다 낮다. 
우리의 앙상블은 ImageNet test set에서 top-5 error 3.57%를 달성해 ILSVRC 2015 분류 과제에서 1위를 차지했다. 또한 이러한 매우 깊은 표현은 다른 시각 인식 문제에서도 뛰어난 일반화 성능을 보였고, ImageNet detection, localization, COCO detection, COCO segmentation에서도 모두 1위를 기록했다. 이는 residual learning이 일반적으로 효과적이며, 비전뿐 아니라 다른 다양한 문제에도 적용 가능함을 보여준다.
---


## 깊어질수록 기울기 소실/폭발 생기는 이유

**역전파할 때 기울기가 레이어를 거칠 때마다 계속 곱해지기 때문이야.**
- 0.9 같은 숫자가 50번 곱해지면 거의 0 → **기울기 소실**
- 1.1 같은 숫자가 50번 곱해지면 엄청 커짐 → **기울기 폭발**
즉, 레이어가 많아지면 숫자를 계속 곱해야 해서 문제가 생김.



## normalized initialization과 Batch Normalization

## 1) **Normalized Initialization (정규화된 초기화)**
- **가중치를 처음 설정할 때, 각 층을 지나면서 신호(값)가 너무 커지거나 작아지지 않도록 ‘적당한 크기’로 초기화하는 방법**이야.
- 이런 초기화는 신호와 기울기가 **층을 많이 통과해도** 너무 작아지거나 커지지 않도록 “초기 세팅”을 잘해주는 역할을 해.
## 2) **Batch Normalization (BN)**
- **학습 중에 각 층의 출력값을 ‘정규화’해서 값이 너무 치우치지 않게 항상 안정적인 분포로 만들어주는 기법**이야.
- 방법:
- 미니배치의 평균을 빼고
- 표준편차로 나눠서
- 일정한 분포로 맞춰줌
- 이렇게 하면:
- 기울기 소실/폭발 완화
- 학습이 훨씬 안정적
- 깊은 네트워크도 잘 학습됨



## 더 깊은 네트워크는 얕은 네트워크보다 나쁘면 안 되는 이유

1. **얕은 네트워크가 이미 잘 학습되어 있음.**
2. 여기에 층 몇 개를 “그냥 x → x”만 하는 **항등 함수 층**으로 붙이면
→ 더 깊은 네트워크가 **얕은 네트워크와 똑같이 동작**할 수 있음.
3. 즉, 더 깊은 네트워크 안에는
**얕은 네트워크의 완벽한 복사본**이라는 해가 존재함.
4. 그래서 논리적으로는
**깊은 네트워크의 최소 성능 ≥ 얕은 네트워크 성능**이 되어야 맞음.
⚠️ 하지만 실제 plain 네트워크는 깊어지면 오히려 학습이 망함.
→ 즉, **항등 함수를 제대로 학습하지 못해서** 이런 문제가 생김.



## **degradation problem의 원인**

**원인: 네트워크가 “항등 함수(identity)”를 잘 학습하지 못해서 생기는 문제다.**
---
## 조금만 풀어서 말하면:
더 깊은 네트워크에는
“추가된 층들이 아무것도 하지 않도록(항등 함수) 만들기만 하면
원래 얕은 네트워크와 똑같이 동작할 수 있는 해”가 존재함.
그런데 실제 plain 네트워크는 이런 **항등 함수**를 제대로 만들지를 못함.
- 층이 많아질수록 작은 변화(= x를 그대로 통과시키는 것 같은 단순한 mapping)를
**SGD가 잘 못 찾는다.**
- 그래서 “원래 성능만이라도 유지하는 해”로 가지 못하고 **훨씬 나쁜 해로 빠짐 → training error 증가**
즉, 문제의 본질은:
# ✔ “여러 층이 연속된 비선형 변환을 거치면
‘아무것도 안 하기(Identity)’라는 단순한 행동조차 배우기 어렵다.”



## 왜 “identity를 직접 학습하는 것”보다 “residual을 0으로 만드는 것”이 더 쉬운가?

**항등 함수는 아주 정확한 형태를 요구하지만,<br>residual=0은 그냥 “아무것도 안 하기”라서 훨씬 만들기 쉽기 때문이다.**
---
## 조금만 풀어서:
### 1) Identity mapping을 직접 학습하려면
여러 개의 비선형 층들이
- 정확히 x를 그대로
- 변형 없이
- nonlinear 연산을 모두 통과해서
- 출력에서도 입력과 완전히 같게
만들어야 한다.
→ 이건 매우 섬세하고 어려운 작업.
---
### 2) 반대로 residual이 0이면
F(x) = 0
H(x) = F(x) + x = x
즉, **항등이 자동으로 만들어짐.**
여기서 학습해야 할 것은
- F(x)의 가중치를 모두 0 근처로 유지하기
딱 이것뿐.
→ 여러 층이 정밀하게 협력할 필요가 없음
→ 그냥 “변화시키지 말자”만 하면 됨

## 2. Related Work {toggle="true"}
### Residual Representations.
---
이미지 인식에서, VLAD는 사전(dictionary)에 대해 residual vector들을 인코딩하는 표현이고, Fisher Vector는 VLAD의 확률적 버전으로 볼 수 있다. 이 두 방법은 이미지 검색과 분류에서 강력한 얕은(shallow) 표현 방식이다.
벡터 양자화(vector quantization) 분야에서는, residual vector를 인코딩하는 것이 원래 벡터를 인코딩하는 것보다 더 효과적이라는 것이 알려져 있다.
저수준 비전과 컴퓨터 그래픽스에서는, 편미분방정식(PDE)을 푸는 데 사용되는 널리 알려진 Multigrid 방법이 존재한다. 이 방법은 시스템을 여러 스케일의 하위 문제(sub-problem)로 재구성하며, 각 하위 문제는 더 거친(coarser) 스케일과 더 세밀한(finer) 스케일 사이의 residual 해(solution) 를 담당한다.
Multigrid의 대안으로는 hierarchical basis preconditioning이 있는데, 이는 두 스케일 사이의 residual vector를 나타내는 변수들에 기반한다. 이러한 기법들은 residual의 특성을 고려하지 않는 기존 solver보다 훨씬 빠르게 수렴한다는 것이 알려져 있다. 이는 적절한 재구성(formulation) 또는 preconditioning이 최적화를 단순화할 수 있음을 시사한다.

Residual을 이용하는 방식은 여러 분야에서 이미 효과가 검증된 일반적인 최적화 전략이다

---


## VLAD, residual vector, Fisher Vector

### ✔ VLAD (Vector of Locally Aggregated Descriptors)
- **여러 지역 특징(SIFT 같은 것)들을 “사전(dictionary)”의 대표 벡터와 비교해서 그 차이(residual)를 모아 만든 큰 벡터 표현.**
→ 사전: 많은 이미지 특징들을 대표하는 “중심 벡터(centroids)”들의 모음
→ 이미지 전체를 하나의 큰 특징 벡터로 만드는 방법
---
### ✔ residual vector(잔차 벡터)
- **특징 벡터 − 가장 가까운 사전(dictionary) 중심점**
- 즉, “이 특징이 사전의 대표 특징과 얼마나 다른가?” 를 나타내는 벡터.
- VLAD = 각 클러스터에서 나온 residual vector들을 하나로 모아 만든 벡터.
---
### ✔ Fisher Vector
- VLAD와 비슷하지만 **사전을 확률모델(GMM)로 보고, 특징들이 그 확률모델에서 얼마나 벗어나는지(잔차)를 계산해서 합친 특징 표현.**
→ VLAD보다 더 고급 버전(확률 기반).
---



## 벡터 양자화(vector quantization) 분야

### ✔ 1. Vector Quantization(VQ)이란?
**특징(feature)을 가장 가까운 사전(dictionary) 벡터 하나로 “대신 표현(양자화)”하는 것.**
- 사전 만들기(K-means)
- 특징을 가장 가까운 중심점에 할당
- 특징은 “코드워드 번호”만 남음
→ **압축 + 군집화 방식**
- **VQ:** “어느 중심점에 속하냐?”만 사용
- **VLAD:** “어느 중심점에 속하냐 + 얼마나 다른가(residual)?”까지 사용



## Multigrid 방법

- 큰 문제(PDE 등)를 여러 “해상도(스케일)”로 나눠서 빠르게 푸는 방법.
- 즉, 거친 해상도에서 큰 틀을 먼저 맞추고 → 세밀한 해상도에서 마지막 디테일을 보정하는 방식.


## 좀 더 자세한 설명

### 1) \*\*Fine grid(촘촘한 격자)\*\*에서 일단 대충 해결
- 높은 해상도라 계산량 많음
- 하지만 오류 패턴을 확인할 수 있음
### 2) \*\*Coarse grid(거친 격자)\*\*로 문제를 옮김
- 해상도가 커져서 문제 크기가 확 줄어듦
- 큰 규모의 오류를 아주 빨리 고칠 수 있음
→ coarse grid에서는 큰 오류가 금방 사라짐
### 3) 다시 fine grid로 돌아와서 보정
- coarse grid에서 고친 큰 오류를 적용
- fine grid에서는 이제 작은 오류만 고치면 됨
이 과정을 반복하면 **엄청 빠르게 수렴**함.

**Multigrid는 coarse grid에서 풀 때 ‘잔차(residual)’를 이용한다.**
- fine grid에서 남은 오차(residual)를
- coarse grid에서 해결함
→ coarse grid는 큰 실수를 빨리 고쳐주는 역할
- 이 때문에 multigrid는 **residual-based optimization**의 대표 사례로 ResNet 논문에서도 인용



## hierarchical basis preconditioning

- 문제를 여러 스케일(큰 해상도 → 작은 해상도)로 나눠서, 각 스케일 사이에서 생기는 residual(오차)을 따로 다루어 전체 최적화를 훨씬 빠르게 만드는 기술.
- ↔ Multigrid랑 다른 점: 
- Multigird: → 여러 grid를 오가며 반복적으로 푸는 방법
- fine grid ↔ coarse grid **왔다 갔다** 하면서
- residual을 coarse grid에서 빠르게 해결하고
- fine grid에서 세밀하게 보정
- 즉, **문제를 실제로 여러 해상도에서 직접 solve하는 iterative method**
- hierarchical basis preconditioning: → 문제를 다시 표현
- 문제를 여러 스케일로 **재구성(basis decomposition)**
- 각 스케일별로 residual을 변수로 둠

### Shortcut Connections.
---
shortcut connection에 대한 실천적 기법과 이론은 오랫동안 연구되어 왔다.
🔽 초기 multi-layer perceptron(MLP) 연구에서는 입력을 출력으로 직접 연결하는 linear layer를 추가 <br>      하는 방식이 사용되었다.
🔽또한, 일부 연구에서는 vanishing/exploding gradient 문제를 완화하기 위해 중간 layer를 auxiliary <br>     classifier에 직접 연결하는 구조를 제안한 바 있다.
🔽다른 연구들에서는 layer의 반응(response), gradient, error를 안정화시키기 위해 shortcut <br>     connection을 이용해 중심화(centering)하는 방법이 소개되었다.
Inception 구조 또한 shortcut branch와 더 깊은 branch들을 함께 구성한 형태라고 볼 수 있다.
우리의 연구와 거의 동시에, highway network가 제안되었다.
highway network의 shortcut connection은 게이트(gate) 함수를 포함하며 이 게이트에는 학습 가능한 파라미터가 존재한다. 이는 우리의 **항등 shortcut(identity shortcut)** 과 다르다.
Highway network에서는 게이트가 닫히면(shortcut이 0에 가까워지면), layer는 residual function이 아닌 일반적인 변환을 수행하게 된다. 반면, 우리의 구조에서는 shortcut이 항상 열려 있으며, 모든 정보가 shortcut을 통해 흐른다. 네트워크는 반드시 residual function을 학습하도록 되어 있다.
또한 highway network는 매우 깊은(depth 100 이상) 구조에서의 정확도 향상을 보여주지 못했다는 한계가 있다.

이전에도 여러 종류의 shortcut 기법이 있었지만, 우리의 ResNet처럼 항등 shortcut을 항상 열어두는 단순한 residual 구조가 가장 안정적이고 깊은 네트워크에서도 효과적이다

---


## auxiliary classifier

- 메인 네트워크의 중간 레이어에 붙여주는 작은 보조 분류기다.
→ 중간에서도 loss를 발생시켜 gradient를 앞쪽 레이어까지 잘 전달하도록 돕는 장치
- 깊은 네트워크는
- 뒤쪽에서 오는 gradient가
- 앞쪽으로 갈수록 약해져서(= 기울기 소실)
- 학습이 잘 안 되는 문제가 있음.
- 그래서 다음처럼 함:
- 중간 레이어 → 작은 classifier 추가
→ 그 classifier에서 나온 loss도 역전파됨
→ 앞쪽 레이어까지 gradient를 밀어 넣어줌
→ 학습 안정화, 수렴 빨라짐



## layer의 반응(response), gradient, error를 안정화시키기 위해?

= 레이어에서 오가는 값들이 너무 커지거나 작아지지 않도록 안정적으로 유지하려는 것
딥러닝에서는 각 layer를 지날 때 다음 3가지가 계속 흘러다님:
- **response(반응)**: 레이어의 출력값
- **gradient(기울기)**: 역전파 때 전달되는 값
- **error(오차)**: loss에서 시작되는 error signal
- 이 값들이 **너무 커지거나 폭발하거나, 너무 작아져서 0에 가까워지면 **학습이 **불안정**해지고 **수렴이 안 되기 **때문에 이를 “안정화(stabilize)”하는 기법들이 많았다는 뜻


## 왜 응답/gradient/error를 안정화해야할까?

### 1) response가 불안정하면
- 한 레이어의 출력이 갑자기 너무 커지거나 너무 작아지면
- 다음 레이어가 제대로 학습하지 못함
### 2) gradient가 불안정하면
- 너무 작아지면 → **기울기 소실(vanishing gradient)**
- 너무 커지면 → **기울기 폭발(exploding gradient)**
- 네트워크가 학습 불가
### 3) error가 불안정하면
- 뒷부분에서 앞부분으로 error가 잘 전달되지 않음
- 특히 깊은 네트워크에서 학습이 안 됨
그래서 **값들의 크기와 분포를 적당히 유지**해야 함.




## highway network

### ✔ Highway Network (초간단 정의)
- 입력 x를 “얼마나 변환할지 vs 그대로 보낼지”를 Gate(문)로 조절하는 네트워크.
```plain text
output = H(x) * T(x) + x * C(x)
```
- H(x): 변환된 정보
- T(x): Transform gate (학습되는 파라미터 있음)
- C(x): Carry gate (학습되는 파라미터 있음)
- Highway Network에서는 입력 x를 shortcut(지름길)로 그대로 보낼 수도 있고, 아니면 안 보내고 변환된 값만 사용할 수도 있어. 
- 이 “얼마나 그대로 보낼지”를 gate(문)가 결정한다는 뜻
- 즉, gate가 닫히면 residual learning이 사라진다.
---
### ✔ ResNet의 Identity Shortcut (우리가 쓰는 것)
- **아무 조절 없이 입력 x를 그대로 더함.**
```plain text
output = F(x) + x
```
- gate 없음
- 파라미터 없음
- 그냥 x를 무조건 그대로 더함

## 3. Deep Residual Learning {toggle="true"}
### 3.1. Residual Learning {toggle="true"}
---
H(x)를 몇 개의 스택된 층들(전체 네트워크일 필요는 없음)이 맞춰야 하는 기본적인 매핑이라고 생각하자.\{=H(x)는 목표함수다\} 여기서 x는 이 층들 중 첫 번째 층에 입력되는 값을 나타낸다.
만약 여러 개의 비선형 층들이 복잡한 함수들을 점근적으로 근사할 수 있다고 가정한다면, 그들은 residual 함수들, 즉 H(x) − x 또한 점근적으로 근사할 수 있다고 가정하는 것과 동일하다(입력과 출력의 차원이 동일하다고 가정하면). 
= 복잡한 함수도 학습할 수 있으니까 그보다 쉬운 residual 함수는 껌이다
따라서 스택된 층들이 H(x)를 근사하도록 기대하는 대신, 우리는 이 층들이 **residual 함수 F(x) := H(x) − x 를 근사하도록 명시적으로 설정**한다. **그러면 원래의 함수는 F(x) + x **가 된다. 두 형태 모두 (가정에 따르면) 원하는 함수를 점근적으로 근사할 수 있어야 하지만, 학습의 용이성은 서로 다를 수 있다.
이러한 재공식화는 degradation 문제(그림 1, 왼쪽)에서의 직관적이지 않은 현상으로부터 동기부여된다. 도입부에서 논의했듯이, 추가된 층들이 identity mapping(항등 매핑)으로 구성될 수 있다면, 더 깊은 모델은 더 얕은 모델보다 훈련 오류가 더 커질 이유가 없다. 
degradation 문제는, 여러 비선형 층들로 identity mapping을 근사하는 데 solver가 어려움을 겪을 수 있음을 시사한다. residual learning 재공식화를 사용하면, 만약 identity mapping이 최적이라면, solver는 identity mapping에 접근하기 위해 여러 비선형 층들의 weight를 단순히 0에 가깝게 만들기만 하면 된다.
실제의 경우에는 identity mapping이 최적일 가능성은 낮지만, 우리의 재공식화는 문제를 precondition하는 데 도움이 될 수 있다. 만약 최적의 함수가 0 매핑보다는 identity mapping에 더 가깝다면, solver는 함수를 새롭고 독립적인 형태로 학습하기보다는 identity mapping을 기준으로 작은 변화(perturbation)를 찾는 것이 더 쉬울 것이다. 우리는 실험(그림 7)을 통해 학습된 residual 함수들이 일반적으로 작은 반응 값을 가진다는 것을 보여주며, 이는 identity mapping이 합리적인 preconditioning을 제공함을 시사한다.
---


## 함수를 새롭고 독립적인 형태로 학습하기보다는 identity mapping을 기준으로 작은 변화(perturbation)를 찾는 것이 더 쉬울 것

> 정답(H(x))이 입력(x)과 거의 비슷하다면, 그냥 x에서 조금만 고치는 함수(F(x))를 배우는 게처음부터 새 함수를 만드는 것보다 훨씬 쉽다.



## Preconditioning

- preconditioning = 문제를 더 쉽게 풀리도록 ‘미리 좋은 형태로 바꿔놓는 것’



## perturbation을 찾는다

- H(x)가 x와 비슷하면 F(x)는 **아주 작은 숫자들(작은 변화)** 이어야 함
- “perturbation을 찾는다” = 입력 x를 크게 바꾸지 않고, 조금만 수정해서 H(x)가 되게 만드는 작은 함수 F(x)를 학습



## residual 함수들이 일반적으로 작은 반응 값을 가진다는 것

> Conv 초기화와 BN 때문에 F(x)는 처음부터 작은 값이고, 그래서 residual 함수들이 전체 학습 과정 내내 '작은 반응값'을 갖게 된다.
- Normalized initialization 때문에 Conv 출력이 처음부터 작은 값으로 시작
- Batch Normalization(BN) 이 그 출력의 평균을 강제로 0으로 맞춤
- 결과적으로 F(x)는 학습 초기부터 0 근처의 매우 작은 값으로 시작

= 학습이 진행되어도 Residual F(x)는 입력을 크게 바꾸는 함수가 아니라 입력x에 작은 수정만 더하는 형태로 유지<br><br>= Residual 함수들이 일반적으로 작은 반응 값을 가짐




## residual 함수들이 작은 반응 값을 가지는 것에 대한 장점

**깊어질수록 더 커지는 문제(plain network의 degradation)를 해결**
plain network는 레이어가 깊어질수록 H(x)를 직접 만들기 때문에:
- 작은 오차가 축적 → 출력이 불안정
- SGD에서 최적점 찾기 어려움
- 깊어질수록 training error **증가**
하지만 ResNet은:
- 각 block이 “작은 변화(F(x))”만 담당
- identity가 전체 흐름을 유지
- 깊어져도 학습 난이도 증가가 적음
즉, **50층이든 150층이든 안정적으로 학습 가능**

### 3.2. Identity Mapping by Shortcuts {toggle="true"}
---
우리는 residual learning을 몇 개의 스택된 층마다 적용한다. 하나의 building block은 그림 2에 나타나 있다. 형식적으로, 이 논문에서 우리는 다음과 같이 정의되는 building block을 고려한다:
```plain text
y = F(x, {Wi}) + x   (식 1)
```
여기서 x와 y는 해당 층들이 받는 입력 벡터와 출력 벡터이다. F(x, \{Wi\}) 함수는 학습되어야 하는 residual mapping을 나타낸다. 그림 2의 예시처럼 두 개의 층을 가진 경우, F = W₂ σ(W₁ x) 이고
여기서 σ는 ReLU [29]를 나타내며, 단순화를 위해 bias는 표기에서 생략한다.
F + x 연산은 shortcut connection과 element-wise addition(요소별 덧셈)에 의해 수행된다. 우리는 addition 이후에 두 번째 비선형성(즉, σ(y))을 적용한다(그림 2 참고). 이 식 (1)의 shortcut connection은 **추가 파라미터나 추가 계산량을 발생시키지 않는다**. 이는 실제적으로 매력적일 뿐 아니라, plain 네트워크와 residual 네트워크를 비교할 때 중요한 점이다.
우리는 파라미터 수, 깊이, 폭, 계산 비용이 동일한 plain/residual 네트워크를 공정하게 비교할 수 있다(미미한 element-wise addition 계산량을 제외하고). 식 (1)에서 x와 F의 차원은 동일해야 한다.
만약 그렇지 않은 경우(예: input/output 채널 수가 달라지는 경우), shortcut connection에서 선형 투영 Wₛ 를 적용하여 차원을 맞출 수 있다:
```plain text
y = F(x, {Wi}) + Wₛ x   (식 2)
```
우리는 식 (1)에 제곱 행렬(square matrix) Wₛ를 사용하는 것도 가능하다. 그러나 실험을 통해 identity mapping이 degradation 문제를 해결하는 데 충분하며 경제적임을 보여줄 것이며, 따라서 Wₛ 는 **오직 차원을 맞춰야 할 때만 사용된다**.
Residual function F의 형태는 유연하다. 이 논문의 실험에서는 F가 두 개 또는 세 개의 층을 가진 경우(Fig. 5)를 사용하며, 더 많은 층을 사용하는 것도 가능하다.
하지만 만약 F가 단일 층만 가진다면, 식 (1)은 다음과 같은 선형 층과 유사하게 된다:
```plain text
y = W₁ x + x
```
이 경우에서는 특별한 장점을 관찰하지 못했다. 또한 위의 표기들은 단순성을 위해 fully-connected layer 기준으로 설명했지만, 동일한 개념이 convolutional layer에도 적용된다. F(x, \{Wi\}) 함수는 여러 개의 convolutional layer를 나타낼 수 있다. Element-wise addition은 두 개의 feature map에 대해 채널 단위로 수행된다.
---


## F가 단일 층만 가진다면 선형층과 유사한 이유

> 중간중간 비선형 활성화함수가 있어야 비선형적으로 좀 복잡한것도 학습할 수 있는데 단일 층이면 선형만 학습하고 x와 더해지는거니까 아무리 쌓아도 선형적으로밖에 학습을 못한다
- **비선형 활성화(ReLU 등)** 가 있어야
→ 네트워크가 **비선형 함수**를 학습할 수 있음
→ 깊어질수록 표현력 증가
- 그런데 **F(x)가 단일층**이면
→ 그 안에 ReLU 같은 비선형이 없음
→ 그래서 F(x) = W x (선형 변환)만 할 수 있음
- 이런 블록을 100개 쌓아도
→ 선형 × 선형 × 선형 … = 여전히 **선형**
→ 깊은 네트워크의 의미가 사라짐
→ residual learning의 이점도 사라짐



## Element-wise addition은 채널 단위로 수행된다

- Feature map의 shape이 (C, H, W) 라면,
```plain text
F(x): (C, H, W)
x:    (C, H, W)
```
- 이 둘을 더할 때는:
- 같은 위치 (h, w)
- 같은 channel c끼리 더해줌.
- 즉, **픽셀별(channel별)로 더한다는 뜻.**

### 3.3. Network Architectures {toggle="true"}
---


[image omitted: temporary Notion asset]


우리는 다양한 plain/residual 네트워크를 테스트했으며, 일관된 현상을 관찰하였다. 
논의를 위한 구체적인 예시를 제공하기 위해, 우리는 ImageNet용 두 가지 모델을 다음과 같이 설명한다. 먼저 **Plain Network**는 (그림 3 가운데) 주로 VGG 네트워크(그림 3 왼쪽)의 철학에 의해 영향을 받았다. 
이 네트워크의 convolution layer는 대부분 3×3 필터를 사용하며, 두 가지 단순한 설계 규칙을 따른다: 
1. 같은 출력 feature map 크기를 갖는 층들은 같은 수의 필터를 사용한다 
2. **feature map의 공간 크기가 절반으로 줄어들면, 각 층의 시간적 계산 복잡도를 유지하기 위해 필터 수는 두 배로 늘린다. **
다운샘플링은 stride 2를 갖는 convolution layer를 통해 직접 수행된다. 
네트워크의 마지막은 global average pooling layer와 softmax를 갖는 1000-way fully-connected layer로 끝난다. 그림 3 (가운데)의 모델에서 가중치를 갖는 층의 총 개수는 34개이다. 우리의 모델은 VGG 네트워크(그림 3 왼쪽)보다 더 적은 필터 수와 더 낮은 복잡도를 가진다는 점을 주목할 만하다. 우리의 34-layer baseline은 3.6 billion FLOPs(multiply-adds)를 가지며, 이는 VGG-19의 19.6 billion FLOPs의 18%에 불과하다.


**Residual Network**는 위에서 설명한 plain network를 기반으로 하며, 여기에 shortcut connection(그림 3 오른쪽)을 삽입하여 대응되는 residual 버전으로 만든 것이다. 
입력과 출력의 차원이 동일할 때(그림 3의 실선 shortcut), identity shortcut(식 1)은 그대로 사용할 수 있다. 차원이 증가하는 경우(그림 3의 점선 shortcut)에는 두 가지 선택지를 고려한다: 
1. shortcut이 여전히 identity mapping을 수행하지만, 차원을 증가시키기 위해 0을 패딩하여 추가한다. 이 옵션은 추가 파라미터를 전혀 도입하지 않는다. 
2. 식 (2)의 projection shortcut을 사용하여 차원을 맞추며, 이는 1×1 convolution으로 구현된다.
두 옵션 모두 shortcut이 두 가지 다른 크기의 feature map을 가로지를 때 stride 2로 수행된다.
---


## figure 안 내용 정리

1. 채널 수 = 특징 맵 개수
- 특징을 몇개 추출할지 결정하는 숫자
2. 3x3 = 필터(커널)의 크기
- 3x3크기의 창으로 입력을 훑어서 특징을 뽑는다는 뜻
3. 64 = 필터 개수 = 출력 채널 개수
- 출력 Feature map도 64개가 됨.
4. /2 = stride =2 → 공간 크기 절반으로 줄임
- 입력 크기를 1/2로 줄이는 설정



## global average pooling layer와 softmax를 갖는 1000-way fully-connected layer

### **1) Global Average Pooling (GAP)**
feature map(예: 7×7×512)의
**각 채널을 평균내서 1개 값만 남기는 층.**
→ 512개의 숫자만 남음.
---
### **2) 1000-way Fully-connected layer**
**[ ImageNet은 1000개의 클래스를 분류하는 문제이기 때문 ]**
남은 512개의 숫자를
**1000개의 클래스 점수로 변환하는 선형층.**
→ 출력 차원 = 1000
---
### **3) Softmax**
이 1000개의 점수를
**확률처럼 보이도록 정규화**하는 함수.



## FLOPs

**FLOPs = Floating Point Operations**
= 모델이 한 번 추론할 때 필요한 곱셈·덧셈 연산 횟수(계산량)



## VGG의 conv 레이어 수가 더 적어도 FLOPs가 훨씬 큰 이유

### 🔥 VGG-19
- 초반부터 **224 → 112 → 56 → 28 → 14 → 7**
- 여러 conv들이 **공간 크기가 큰 상태(224, 112, 56)** 에서 계속 실행됨
→ FLOPs 폭발적으로 커짐
### 🔥 ResNet-34
- 첫 레이어 바로 뒤에서 위치 크기 빠르게 감소
- 이후 block들은 **작은 feature map(56, 28, 14, 7)** 에서 반복 수행
→ conv 개수는 많아도 FLOPs는 훨씬 적음

### 3.4. Implementation {toggle="true"}
---
ImageNet 실험을 위한 우리의 구현은 [21, 40]의 방식을 따른다. 이미지는 scale augmentation을 위해 짧은 변(shorter side)을 256\~480 범위에서 무작위로 선택하여 리사이즈한다. 그 후 224×224 크기의 crop을 원본 이미지 또는 그 수평 뒤집기(horizontal flip)에서 무작위로 추출하고, per-pixel mean을 빼준다 [21]. 또한 [21]의 표준 컬러 증가(color augmentation) 기법을 사용한다.
각 convolution 뒤, activation(활성함수) 전에 batch normalization(BN) [16]을 적용하며, 이는 [16]의 방식을 따른 것이다. 가중치 초기화는 [12]의 방법을 사용하며, plain/residual 네트워크는 모두 처음부터(scratch) 학습한다.
최적화는 mini-batch 크기 256의 SGD를 사용하며, 초기 learning rate는 0.1이고, 에러가 더 이상 줄어들지 않는 plateau 지점에서 10배씩 감소시킨다. 전체 모델은 최대 60×10⁴ iterations까지 학습한다. weight decay는 0.0001, momentum은 0.9를 사용하며, [16]의 관례에 따라 dropout [13]은 사용하지 않는다.
테스트 단계에서는 비교 실험을 위해 [21]의 standard 10-crop testing을 사용한다. 최고 성능을 위해서는 [40, 12]에서 사용된 fully convolutional 형태를 채택하며, 여러 스케일에서 점수를 평균한다. 이때 이미지의 짧은 변은 \{224, 256, 384, 480, 640\} 중 하나가 되도록 리사이즈된다.
---


## Scale Augmentation

- 입력 이미지를 더 크게/더 작게 리사이즈해서 학습시키는 것



## per-pixel mean subtraction

- 이미지 밝기 편향을 제거해 입력을 0 중심으로 만들고, 학습을 더 빠르고 안정적으로 만들기 위해서
- 불필요한 밝기 차이를 제거 → 모델이 물체 자체에 집중할 수 있도록
- 평균을 빼면:
- 얼마나 밝은지는 줄어들고
- 형태/질감/색 대비 등 본질적인 특징에 더 집중하게 됨



## 왜 Conv → BN → ReLU 순서

### **0-mean, small-variance 입력이 ReLU와 아주 잘 맞는다**
- ReLU는 입력이 너무 크거나 편향되어 있으면:
- gradient가 불안정
- dead ReLU (출력이 항상 0되는 뉴런) 발생
- BN을 활성화 전에 두면 입력 값이 항상:
- 0 주변
- 적당한 scale
- 안정적인 분포를 갖게 되어 **ReLU가 건강하게 동작함.**



## 가중치 초기화는 [12]의 방법을 사용하며, plain/residual 네트워크는 모두 처음부터(scratch) 학습한다

### **1) 가중치 초기화는 [12] = He 초기화**
→ Conv weight들을 적절한 작은 랜덤값으로 초기화하는 방법.
### **2) scratch부터 학습한다**
→ 사전 학습(pretrained) 사용 안 하고
→ **완전 랜덤 초기값에서 처음부터 학습**한다는 뜻.



## 최적화는 mini-batch 크기 256의 SGD를 사용한다

### **1) SGD = Stochastic Gradient Descent**
→ 딥러닝 모델을 학습시키는 가장 기본적인 **최적화 알고리즘**
→ 경사(gradient)를 따라 가중치를 조금씩 업데이트하는 방식
---
### **2) mini-batch 크기 256**
→ 한 번의 업데이트(update)를 할 때
→ **이미지 256장을 묶어서** gradient를 계산한다는 뜻



## weight decay는 0.0001, momentum은 0.9를 사용하며, [16]의 관례에 따라 dropout [13]은 사용하지 않는다.

### ✔ 1) **Weight Decay = 가중치 L2 규제(L2 Regularization)**
- **가중치가 너무 커지지 않도록 페널티를 줘서 overfitting을 줄이는 기법.**
- 즉, **weight를 조금씩 줄여줌 → 모델이 더 일반화됨 → 과적합 방지**
---
### ✔ 2) “momentum = 0.9”
- SGD에 관성을 넣어서 이전 업데이트 방향을 그대로 조금 더 유지해주는 것.
→ 더 빠르게, 더 안정적으로 수렴.
---
### ✔ 3) “dropout을 사용하지 않는다”는 이유
- ResNet에서는 **Batch Normalization(BN)** 을 사용함.
- **BN과 Dropout을 같이 쓰면 성능이 오히려 나빠지는 경우가 많다.**
- 왜냐?
- BN은 미니배치 평균/분산으로 정규화하는데
- Dropout은 랜덤하게 뉴런을 꺼버려서 BN의 분포를 어지럽힘
- 둘이 서로 충돌할 수 있음
- 그래서 **BN이 있는 모델에서는 일반적으로 Dropout을 쓰지 않는다.**



## standard 10-crop testing

- 테스트할 때, **이미지 하나를 ****10개의 서로 다른 위치와 플립으로 잘라서**** 모두 모델에 넣고, ****그 결과를 평균하는 방식****.**
- 즉,
- 이미지 중앙 1개
- 네 귀퉁이 4개
→ 총 5개
그리고 이 5개 각각을 좌우 뒤집기
→ 5×2 = **10개 crop**
- 왜 하냐?
- 테스트 정확도를 더 안정적으로 만들기 위해서



## fully convolutional

- 완전연결층(FC layer)을 쓰지 않고, 전부 Convolution만 사용하는 CNN 구조
- 즉,
- 마지막에 FC(512→1000) 같은 고정 크기 레이어 대신
- **1×1 Convolution으로 대체해서**
- 어떤 크기의 이미지도 넣을 수 있게 만든 구조.
- 그 결과:
- 입력 이미지 크기가 달라도 그대로 예측 가능
- 여러 스케일(크기)에서 추론하기 쉬움
- 큰 이미지에서도 위치 정보 유지됨
**요약:**
**입력 크기의 제한을 없애서(=flexible input), 테스트에서 여러 해상도를 사용할 수 있도록 만든 CNN 형태.**



## 여러 스케일에서 점수를 평균화 (multi-scale testing)

### **Standard 10-crop testing (위치 기반) **
- 이미지 크기는 **고정** (224×224)
- 대신 **각기 다른 위치(중앙, 모서리, 플립)** 10개를 추출
- 10개 각각의 결과를 평균 → 최종 prediction
### **multi-scale testing (크기 기반)**
- 이미지 크기를 \{224, 256, 384, 480, 640\}로 다르게 넣음
- 각 스케일에서 나온 prediction을 평균 → 최종 prediction

## 4. Experiments {toggle="true"}
### 4.1. ImageNet Classification {toggle="true"}
---
[image omitted: temporary Notion asset]


[image omitted: temporary Notion asset]
[image omitted: temporary Notion asset]


[image omitted: temporary Notion asset]


우리는 1000개의 클래스들로 구성된 ImageNet 2012 분류 데이터셋 [35]에서 우리의 방법을 평가한다. 모델들은 128만 개의 학습 이미지에서 학습되며, 5만 개의 검증 이미지에서 평가된다. 또한 10만 개의 테스트 이미지에 대해 테스트 서버가 보고하는 최종 결과도 얻는다. 우리는 top-1과 top-5 오류율을 모두 평가한다.
**Plain Networks.** 
우리는 먼저 18-layer 및 34-layer plain 네트워크들을 평가한다. 34-layer plain 네트워크는 그림 3(가운데)에 있다. 18-layer plain 네트워크도 유사한 형태이다. 자세한 아키텍처는 표 1을 참조하라.
표 2의 결과는 더 깊은 34-layer plain 네트워크가 더 얕은 18-layer plain 네트워크보다 더 높은 검증 오류를 가진다는 것을 보여준다. 그 이유를 밝히기 위해, 그림 4(왼쪽)에서 학습 과정 동안 그들의 학습/검증 오류를 비교한다. 우리는** degradation 문제**를 관찰하였다 — 34-layer plain 네트워크는 전체 학습 절차 동안 더 높은 training error를 보이는데, 이는 18-layer plain 네트워크의 해 공간(solution space)이 34-layer의 해 공간의 부분집합임에도 불구하고 그렇다.
우리는 이러한 최적화 어려움이 gradient vanishing에 의해 발생했다고 보기는 어렵다고 주장한다. 이러한 plain 네트워크들은 BN[16]을 사용하여 학습되며, 이는 forward로 전파되는 신호들이 0이 아닌 분산을 갖도록 보장한다. 우리는 backward로 전파되는 gradient들도 건강한(norm이 정상적인) 값을 갖는다는 것을 확인하였다. 따라서 forward 신호나 backward 신호가 사라지지는 않는다. 실제로 34-layer plain 네트워크는 여전히 경쟁력 있는 정확도(표 3)를 달성할 수 있는데, 이는 solver가 어느 정도는 작동하고 있음을 시사한다. 우리는 깊은 plain 네트워크들이 지수적으로 낮은 수렴률을 가질 수 있으며, 이것이 training error 감소에 영향을 미친다고 추측한다(각주 3). 이러한 최적화 어려움의 원인은 향후 연구될 것이다.
**Residual Networks.** 
다음으로 우리는 18-layer 및 34-layer residual 네트워크(ResNets)를 평가한다. 기본 아키텍처는 위의 plain 네트워크와 동일하되, 그림 3(오른쪽)과 같이 각 3×3 필터 쌍마다 shortcut connection이 추가되어 있다. 첫 번째 비교(표 2 및 그림 4 오른쪽)에서는 모든 shortcut에 대해 identity mapping을 사용하고, 차원이 증가하는 경우 zero-padding(옵션 A)을 사용한다. 따라서 plain 네트워크 대비 추가 파라미터가 없다.
표 2와 그림 4에서 우리는 세 가지 주요 관찰을 얻는다. 
첫째, residual learning을 적용하면 상황이 반전된다 — 34-layer ResNet은 18-layer ResNet보다 더 좋다(top-1 기준 2.8% 향상). 더 중요한 점은, 34-layer ResNet이 상당히 낮은 training error를 보이며 검증 데이터에서도 일반화된다는 것이다. 이는 **degradation 문제가 이 설정에서 잘 해결**되었으며, 깊이가 증가함으로써 정확도 향상을 얻을 수 있음을 의미한다.
둘째, plain 네트워크와 비교하면, 34-layer ResNet은 top-1 에러를 3.5% 줄이며(표 2), 이는 성공적으로 낮아진 training error(그림 4 오른쪽 vs 왼쪽)에서 비롯된다. 이 비교는 극도로 깊은 시스템에서 residual learning의 효과를 검증한다. → plain보다도 좋다
마지막으로, 18-layer plain/residual 네트워크는 비슷한 정확도를 보이지만(표 2), 18-layer ResNet이 더 빠르게 수렴한다(그림 4 오른쪽 vs 왼쪽). 네트워크가 “지나치게 깊지 않은”(여기서는 18-layer) 경우에는 현재의 SGD solver가 plain 네트워크에 대해서도 여전히 좋은 해법을 찾을 수 있다. 이 경우 ResNet은 초기 단계에서 더 빠른 수렴을 제공함으로써 최적화를 돕는다.
**Identity vs. Projection Shortcuts.** 
우리는 파라미터가 없는 identity shortcut이 학습에 도움이 된다는 것을 보였다. 다음으로 projection shortcut(Eq.(2))을 조사한다. 표 3에서는 세 가지 옵션을 비교한다: 
(A) 차원이 증가할 때 zero-padding shortcut을 사용하며 모든 shortcut은 파라미터가 없음(표 2와 <br>      그림 4 오른쪽과 동일)
(B) 차원 증가 시 projection shortcut을 사용하고, 나머지는 identity
(C) 모든 shortcut을 projection으로 사용.
표 3은 세 옵션 모두 plain 네트워크보다 상당히 더 좋다는 것을 보여준다. B가 A보다 약간 더 좋은데, 이는 A에서 **zero-padding된 차원들은 실제 residual learning이 일어나지 않기 때문**이라고 본다. C는 B보다 근소하게 더 좋은데, 이는 여러(13개) projection shortcut이 도입하는 **추가 파라미터 때문**이라고 본다. 그러나 A/B/C의 **작은 차이는 degradation 문제를 해결하는 데 projection shortcut이 필수적이지 않다는 것을 나타낸다**. 따라서 우리는 이후 실험에서 모델 크기와 메모리/시간 복잡도를 줄이기 위해 옵션 C를 사용하지 않는다. Identity shortcut은 이후 소개하는 bottleneck 아키텍처에서 복잡도를 증가시키지 않는 데 특히 중요하다.
**Deeper Bottleneck Architectures.** 
이제 우리는 ImageNet을 위한 더 깊은 네트워크를 설명한다. 우리가 감당할 수 있는 학습 시간에 대한 우려 때문에, building block을 bottleneck 구조로 수정한다. 각 residual 함수 F는 2층이 아닌 3층(그림 5)으로 구성된다. 세 층은 1×1, 3×3, 1×1 convolution이며, 1×1 층들은 차원을 줄이고 늘리는 역할을 하여, 3×3 층이 더 작은 입력/출력 차원을 갖는 bottleneck 역할을 맡는다. 그림 5는 두 설계가 유사한 시간 복잡도를 갖는 예를 보여준다.
파라미터가 없는 identity shortcut은 bottleneck 아키텍처에서 특히 중요하다. 그림 5(오른쪽)에서 identity shortcut을 projection으로 대체하면, shortcut이 고차원 양 끝에 연결되므로 시간 복잡도와 모델 크기가 두 배가 된다. 따라서 bottleneck 설계에서는 identity shortcut이 더 효율적인 모델을 만든다.
**50-layer ResNet:** 우리는 34-layer 네트워크의 각 2층 블록을 이 3층 bottleneck 블록으로 대체하여 50-layer ResNet(표 1)을 만든다. 차원이 증가하는 경우 option B를 사용한다. 이 모델은 3.8 billion FLOPs를 가진다.
**101-layer 및 152-layer ResNet:** 우리는 더 많은 3층 블록을 사용하여 101-layer 및 152-layer ResNet을 구성한다(표 1). 주목할 만한 점은, 깊이가 상당히 증가했음에도 불구하고 152-layer ResNet(11.3 billion FLOPs)은 여전히 VGG-16/19(15.3/19.6 billion FLOPs)보다 낮은 복잡도를 가진다는 것이다.
50/101/152-layer ResNet들은 34-layer 모델보다 상당한 폭으로 더 정확하다(표 3 및 4). 우리는 degradation 문제를 관찰하지 않았으며, 깊이가 크게 증가함에 따라 의미 있는 정확도 향상을 얻는다. 깊이의 이점은 모든 평가 지표에서 확인된다(표 3 및 4).
**최첨단 기법들과의 비교:** 표 4에서 이전 최고 single-model 결과들과 비교한다. 우리의 34-layer ResNet baseline은 매우 경쟁력 있는 정확도를 달성했다. 우리의 152-layer ResNet은 단일 모델 top-5 검증 오류가 4.49%이다. 이 single-model 결과는 이전의 모든 ensemble 결과들을 능가한다(표 5). 우리는 깊이가 다른 6개의 모델을 결합하여 ensemble을 구성한다(논문 제출 시점에는 그중 2개만 152-layer였다). 이것은 테스트 세트에서 3.57% top-5 error를 달성했다(표 5). 이 entry는 ILSVRC 2015에서 1위를 차지했다.
---


## 왜 B가 A보다 좋은가?

### **A (zero-padding)**
- 채널을 늘릴 때 부족한 채널을 0으로 채워서 맞춤
- 그래서 그 채널들은 shortcut이 아무 정보도 안 보냄
- 즉, 그 채널들은 residual 학습이 제대로 안 됨
### **B (projection, 1×1 conv)**
- 1×1 conv로 모든 채널을 실제로 변환해서 shortcut으로 보냄
- 모든 채널이 학습에 참여함
- residual 구조가 완전히 유지됨 → 더 좋음



## 왜 projection shortcut을 쓰면 비용이 2배가 되냐?

**identity shortcut**
→ 그냥 입력을 그대로 더해줌
→ 계산량 0, 파라미터 0
**projection shortcut(1×1 conv)**
→ 입력 채널(예: 256채널) → 출력 채널(256채널)로 큰 1×1 convolution을 한 번 더 해야 함
→ 고비용 연산 + 많은 파라미터 추가
- bottleneck 블록은 원래도 1×1 conv가 2개 있는데,
- projection을 쓰면 고비용 1×1 conv가 하나 더 생김
→ 그래서 연산량과 모델 크기가 거의 두 배로 증가한다는 말이야.

### 4.2. CIFAR-10 and Analysis {toggle="true"}
---
[image omitted: temporary Notion asset]


[image omitted: temporary Notion asset]


[image omitted: temporary Notion asset]


우리는 50k개의 훈련 이미지와 10k개의 테스트 이미지(10개 클래스)로 구성된 CIFAR-10 데이터셋[20]에 대해 더 많은 연구를 진행하였다. 우리는 훈련 세트에서 학습하고 테스트 세트에서 평가한 실험을 제시한다. 우리의 초점은 최신 성능을 밀어붙이는 것이 아니라, **극도로 깊은 네트워크들의 동작 특성**에 있으므로 의도적으로 단순한 아키텍처를 사용한다.
plain 모델과 residual 모델의 아키텍처는 그림 3(가운데/오른쪽)의 형태를 따른다. 네트워크 입력은 32×32 이미지이며, 픽셀별 평균을 뺀다. 첫 번째 레이어는 3×3 합성곱이다. 그다음 특징 맵 크기가 \{32, 16, 8\}일 때 각각 3×3 합성곱으로 구성된 6n개의 레이어 스택을 사용하며, 각 크기마다 2n개의 레이어를 사용한다. 필터 개수는 순서대로 \{16, 32, 64\}이다. 서브샘플링은 stride 2의 합성곱으로 수행한다. 네트워크는 global average pooling, 10-way fully-connected layer, softmax로 끝난다. 총 6n+2개의 가중치를 가진 레이어가 있다. 
아키텍처 요약은 다음과 같다:
<table header-row="true">
<tr>
<td>출력 맵 크기</td>
<td>32×32</td>
<td>16×16</td>
<td>8×8</td>
</tr>
<tr>
<td># 레이어</td>
<td>1+2n</td>
<td>2n</td>
<td>2n</td>
</tr>
<tr>
<td># 필터</td>
<td>16</td>
<td>32</td>
<td>64</td>
</tr>
</table>
shortcut connection을 사용할 때는 3×3 레이어 쌍마다 연결되며(총 3n개의 shortcut), 이 데이터셋에서는 모든 경우 identity shortcut(옵션 A)를 사용한다. 따라서 residual 모델은 plain 모델과 깊이, 폭, 파라미터 수가 완전히 동일하다.
우리는 weight decay 0.0001, momentum 0.9를 사용하며, [12]의 weight initialization과 BN[16]을 사용하지만 dropout은 사용하지 않는다. 모델은 2개의 GPU에서 minibatch 크기 128로 훈련된다. 학습률은 0.1에서 시작하여 32k, 48k iteration에서 10배씩 줄이고, 64k iteration에서 학습을 종료한다. 이 스케줄은 45k/5k 훈련/검증 분할에서 결정되었다. 
훈련 시에는 [24]의 간단한 데이터 증강을 따른다: 각 변에 4픽셀 패딩 후 32×32 크롭을 패딩된 이미지 또는 수평 뒤집기에서 무작위로 샘플링한다. 테스트 시에는 원본 32×32 이미지 단일 뷰만 평가한다.
우리는 n = \{3, 5, 7, 9\}을 비교하여 20, 32, 44, 56-layer 네트워크를 얻었다. 그림 6(왼쪽)은 plain 네트워크의 동작을 보여준다. 깊은 plain 네트워크는 깊이가 증가할수록 학습 에러가 상승하는 현상을 보인다. 이 현상은 ImageNet(Fig. 4 왼쪽)이나 MNIST([41])에서도 나타나며, 이러한 최적화 어려움이 근본적인 문제임을 시사한다.
그림 6(가운데)은 ResNet의 동작을 보여준다. ImageNet 실험(Fig. 4 오른쪽)과 유사하게 ResNet은 최적화의 어려움을 극복하며, 깊이가 증가할수록 정확도 향상을 보여준다.
우리는 n = 18인 경우도 탐구했는데, 이는 110-layer ResNet이 된다. 이 경우 초기 학습률 0.1은 수렴을 시작하기에 약간 큰 것으로 나타났다. 그래서 학습 초반 약 400 iteration 동안 학습 오류가 80% 아래로 떨어질 때까지 학습률 0.01로 warm-up한 뒤 다시 0.1로 올려 학습을 지속했다. 나머지 학습 스케줄은 동일하다. 이 110-layer 네트워크는 잘 수렴한다(Fig. 6 가운데). 이 모델은 FitNet[34]이나 Highway[41] 같은 다른 얇고 깊은 모델보다 파라미터 수가 적지만(Table 6), 여전히 state-of-the-art 수준(6.43%)이다.
**Layer Response 분석.**
그림 7은 레이어 반응의 표준편차(std)를 보여준다. 이 반응은 각 3×3 layer의 출력이며, BN 이후이지만 ReLU/더하기 전에 측정한다. ResNet에서는 이 분석이 residual function의 반응 강도를 보여준다.
그림 7은 ResNet이 plain 네트워크에 비해 전반적으로 layer response가 더 작다는 것을 보여준다. 이는 residual function이 일반적으로 0에 더 가깝다는 우리의 기본 동기(Sec. 3.1)를 지지한다. 또한 그림에서 ResNet-20, 56, 110을 비교하면, 더 깊은 ResNet일수록 개별 레이어의 반응 크기가 더 작다. 즉, 레이어가 입력 신호를 수정하는 정도가 깊어질수록 줄어드는 경향이 있다.
**1000 레이어 이상 탐구.**
우리는 1000 레이어를 넘는 매우 깊은 모델도 탐구했다. n = 200을 설정하여 1202-layer 네트워크를 만들고, 앞서 설명한 방식으로 훈련했다. 우리의 방식은 최적화에 문제가 없었고, 이 1202-layer 네트워크는 학습 에러 \< 0.1% (Fig. 6 오른쪽)를 달성하였다. 테스트 에러도 비교적 괜찮은 7.93%(Table 6)이다.
하지만 이렇게 극단적으로 깊은 모델에는 여전히 해결되지 않은 문제가 있다. 이 1202-layer 모델의 테스트 결과는 110-layer 모델보다 좋지 않은데, 둘의 학습 오류는 거의 동일하다. 우리는 이를 **overfitting** 때문이라고 본다. CIFAR-10처럼 작은 데이터셋에서는 19.4M 파라미터의 1202-layer 모델이 지나치게 클 수 있다. maxout[9]이나 dropout[13] 같은 강한 regularization이 이 데이터셋에서 최상의 결과를 내는 데 자주 사용된다([9, 25, 24, 34]). 본 논문에서는 이러한 강한 regularization을 사용하지 않고, 최적화 문제에 집중하기 위해 설계적으로 깊고 얇은 네트워크를 regularization처럼 사용했을 뿐이다. 더 강한 regularization을 결합하면 성능이 개선될 수도 있으며, 이는 향후 연구로 남겨둔다.
---


## CIFAR-10용 ResNet에서 6n, 2n의 의미

1. CIFAR-10 ResNet은 **3개의 스테이지**로 구성됨
- 특징맵 크기: **32×32 → 16×16 → 8×8**
2. **각 스테이지마다 2n개의 3×3 합성곱 레이어**를 넣음
→ 스테이지가 3개니까
→ 전체 합성곱 레이어 수 = **2n × 3 = 6n**
3. 전체 네트워크 레이어 수는
**6n개의 conv + 처음 conv 1개 + 마지막 fc 1개**
= **6n + 2**
예:
- n=3 → 20층
- n=5 → 32층
- n=9 → 56층



## 출력 맵과 필터 수 헷갈림

### ✔ 1. 출력 맵 크기(Feature Map Size)
네트워크를 거치며 \*\*이미지 공간 크기(가로×세로)\*\*가 변하는 것.
- 처음 입력: **32×32**
- 다운샘플링(stride=2) 후: **16×16**
- 다시 다운샘플링 후: **8×8**
즉, **3단계**로 공간 크기가 줄어듦.
---
### ✔ 2. 필터 수(Number of Filters = 채널 수)
각 단계에서 \*\*특징의 종류(채널 수)\*\*를 말함.
CIFAR-10 ResNet에서 필터 수는 다음처럼 고정됨:
- 32×32 단계 → **16채널**
- 16×16 단계 → **32채널**
- 8×8 단계 → **64채널**
즉, **해상도가 줄어들면 필터 수(채널)는 늘어난다**.
= 정보를 잃지 않으면서 계산량을 유지하고 더 복잡한 특징을 학습하려고
해상도가 줄어들면 정보가 줄어듦 → 이를 대신 채널 수를 늘려서 정보를 보완



## 학습률을 점차 줄이는 이유

- **처음에는 크게 움직이며 빠르게 최적값 근처까지 가고, 나중에는 미세하게 조정해서 성능을 더 높이기 위해서**
- 즉, 미세조정(Fine-tuning)을 위해서



## 왜 110-layer에서는 LR=0.1이 너무 큰가?

- 네트워크가 엄청 깊어지면(110층) → 초기에는 weight가 아직 불안정함.
- 이때 학습률 0.1처럼 큰 값으로 시작하면 → gradient 업데이트가 너무 크게 튀어서 학습이 제대로 시작되지 못함(수렴X).
- 그래서 처음에는 작은 학습률(0.01)로 “예열(warm-up)”을 한 뒤 네트워크가 안정되면 다시 0.1로 올려서 본격적으로 학습함.

### 4.3. Object Detection on PASCAL and MS COCO {toggle="true"}
우리 방법은 다른 인식(task)들에서도 좋은 일반화 성능을 보인다.
표 7과 표 8은 PASCAL VOC 2007, 2012 [5]와 COCO [26]에서의 객체 탐지 기준(baseline) 결과를 보여준다.
우리는 탐지 방법으로 Faster R-CNN [32]을 사용하였다.
여기서 우리가 관심 있는 부분은 **기존의 VGG-16 [40]을 ResNet-101으로 교체했을 때 향상되는 성능**이다.
두 모델을 사용할 때의 탐지 파이프라인 구현은(appendix 참고) 완전히 동일하므로,
성능 향상은 오직 **더 나은 네트워크 표현력(표현 학습)** 때문이라고 볼 수 있다.
가장 주목할 점은 어려운 데이터셋인 COCO에서,
COCO의 표준 평가 지표(mAP@[.5, .95])가 **6.0% 절대 향상**, 즉 **28% 상대 향상**을 보였다는 점이다.
이 성능 향상은 전적으로 **학습된 표현(레프리젠테이션)이 더 좋아졌기 때문**이다.
딥 레지듀얼 네트워크를 기반으로 하여, 우리는 ILSVRC & COCO 2015 대회의 여러 트랙에서 1위를 차지했다:
ImageNet detection, ImageNet localization, COCO detection, COCO segmentation.
자세한 내용은 부록(appendix)에 나와 있다.
---
## 논문 정리 {toggle="true"}
# 1. 문제 상황: 깊어질수록 훈련이 더 어려워진다
---
### 1) 기존 깊은 CNN의 문제
- LeNet → AlexNet → VGG → 다 깊어짐
- 깊을수록 더 많은 특징을 학습 가능
- 하지만 실제로는 **깊게 만들면 훈련 오차가 더 커짐**
- 즉, “더 깊어서 overfitting”이 아니라
**“더 깊은데도 훈련이 더 안 됨(optimization difficulty)”**
논문 Fig.1 (CIFAR-10 plain network)
- 20층보다 56층에서 오히려 **훈련 오차가 더 높아짐**
(즉, Solver가 답을 못 찾음)
---
# 2. 핵심 아이디어: Residual Learning
---
### (가장 중요한 부분! 시험에 100% 나옴)
기존: H(x)를 직접 학습하려 함
ResNet: **F(x) = H(x) - x** 라는 “잔차(residual)”를 학습하고, 최종 출력은 **H(x) = F(x) + x**
### 왜 이렇게 하면 좋은가?
1. **항등함수(Identity Mapping)가 훨씬 배우기 쉽다.**
- 만약 어떤 블록이 아무것도 하지 않는 것이 최선이라면 F(x)=0으로 만들기만 하면 됨
- 반대로 기존 네트워크는 H(x)=x를 학습하려면 여러 비선형 레이어가 “항등함수”를 흉내내야 해서 어렵다
2. **초기화가 더 안정적**
3. **SGD가 훨씬 빠르고 잘 수렴**
### Shortcut Connection의 역할
- x를 그대로 다음 블록 출력에 더한다
- 이 shortcut은 **파라미터도 없음, 계산비용도 없음**
- 단순히 element-wise add
이게 “Residual Block”.
---


## 왜 비선형 레이어는 항등 함수를 흉내내기 어려운가?

# 1. 왜 일반적인 네트워크(비선형 레이어)는 “항등함수(H(x)=x)”를 흉내내기 어려운가?
### (1) 비선형 함수(ReLU, Conv, BN 등)가 개입 → “그대로 보내기”가 불가능해짐
보통 CNN 블록은:
x
→ Conv
→ BN
→ ReLU
→ Conv
→ BN
= F(x)
여기에서 **x 그대로 출력하는 H(x)=x를 만들고 싶으면**,
다음 조건들을 만족해야 함:
- Conv weight들이 정확히 항등행렬처럼 동작해야 함
- BN scale/shift가 정확히 특정 값이어야 함
- ReLU는 음수면 0이 되어버림 → 항등을 유지 못함
즉, **여러 파라미터가 아주 정확한 조합**으로 설정돼야 함.
**예시 (중요!)**
ReLU(x) = max(0, x)
→ 입력 x의 음수 부분은 무조건 0이 됨 → 항등 불가능
→ 이미 이 지점에서 H(x)=x를 정확히 만들 수 없음
그래서 기존 네트워크는 “항등함수”라는 매우 단순한 것을 만들기 위해
정말 복잡한 다중 비선형 레이어들이 완벽하게 조합돼야 한다.
→ **학습 난이도가 매우 높음**
# 2. 왜 Residual 구조에서는 항등함수가 쉽게 만들어지나?
### Residual block 출력
H(x) = F(x) + x
만약 최선의 해가 "그냥 x 그대로 출력"이라면?
→ F(x) = 0
→ H(x) = x
이때 F(x)=0이 되는 조건은 아주 간단함:
- Conv weight → 0
- BN scale → 0
- 다른 파라미터도 전부 0
즉, **아무것도 안 하면 자동으로 항등함수에 가까워짐**
그래서 Residual block은 “항등함수로 수렴하도록 만드는 것이 매우 쉽다”.



## 왜 초기화가 더 안정적인가

# 왜 초기화가 더 안정적인가？
초기화가 안정적이라는 말은
**초기 weight가 조금만 흔들려도 네트워크 출력이 폭주하지 않는다는 의미**야.
### (1) plain network에서의 초기화
Deep network가 아주 깊으면:
x → Conv → BN → ReLU → Conv → BN … (수십 \~ 수백 번 반복)
문제:
- 작은 초기 오차도 레이어를 거치며 증폭/감쇠될 수 있음
- 그래서 vanishing / exploding gradient 발생
- 즉 “초기화가 잘못되면 망함”
### (2) ResNet에서의 초기화
초기에는 보통 Conv weight ≈ 0 근처
→ 그러면 F(x) ≈ 0
→ H(x) ≈ x
즉, **아무리 깊어도 처음부터 거의 Identity Network**로 동작함.
그럼 어떤 일이 벌어질까?
- 깊이가 20층이든 200층이든 초기 출력이 서로 비슷함
- Gradient도 x를 그대로 통과하는 경로가 있으므로 사라지지 않음
- Forward/Backward가 안정적
그래서:
**“초기 상태부터 네트워크가 붕괴하지 않음 → 학습이 매우 안정적.”**



## 왜 SGD가 훨씬 빠르고 잘 수렴하는가

SGD가 빠르게 수렴한다는 말은
**Loss 곡면이 더 잘-conditioned 되어 있다는 의미**야.
Plain network는
H(x)를 직접 학습해야 하는 “어려운 최적화 문제”
반면 ResNet은
F(x) = H(x) - x 라는 “잔차(Residual)”만 학습
→ 대부분의 경우 “작은 값들”을 조정하는 문제로 바뀜
### 잔차(residual)는 보통 작은 값이기 때문
논문 Fig.7에서도 봤듯이
각 레이어의 Residual 출력은 대부분 **값이 작다**.
SGD는 “큰 변형”보다 “작은 변화”를 훨씬 잘 다룸
→ optimization이 훨씬 쉬워짐
→ gradient 흐름이 자연스러움
### 쉬운 예시 (비유)
기존:
- 목표: x를 10배 크게 만들려면
Conv + BN + ReLU 여러 레이어 조합 복잡하게 조정해야 함
→ 어려움
Residual:
- 목표: 기존 x보다 10%만 증가시키고 싶음 →
F(x)=0.1x 정도만 학습하면 됨
→ 훨씬 쉬움
즉, Residual을 학습하는 것은
\*\*미세한 보정(perturbation)\*\*을 하는 문제이고,
이런 문제는 **전통적인 최적화(SGD)에 훨씬 쉬움**.



## H(x), F(x)

### H(x): "원래 우리가 원하던 함수(목표 함수)"
CNN 블록이 해야 할 최종 변환을 H(x)라고 한다.
예:
- 특징을 더 추출하기
- 패턴을 더 잘 인코딩하기
- 더 깊은 레이어로 전달할 더 좋은 표현 만들기
즉, 우리가 원래 학습하고 싶은 “진짜 목적”이 H(x).
---
### F(x): “잔차(Residual function) = H(x) − x”
ResNet은 H(x)를 직접 학습하지 않는다.
대신에 H(x) - x = F(x)를 학습한다.
그리고 최종 출력은:
H(x) = F(x) + x
즉,
**입력 x를 그대로 보낸 뒤 거기에 “조금 수정한 것(F(x))”만 더하는 구조**
---
### 왜 이렇게 쪼개는가?
만약 “그대로 x가 최적”이라면?
→ F(x)=0이면 됨 → 학습 매우 쉬움
Plain network는 "H(x)=x"를 직접 만드는 건 매우 어렵다
(비선형 레이어들이 항등을 흉내내야 하므로)



## shortcut connection은 건너뛰는 연결인데 왜 X를 더하는가

shortcut = skip connection = 건너뛰는 연결
맞아.
**하지만 그 목적은 “이전 입력 x를 그대로 다음 블록에 전달하기 위해서”야.**
정의:
- shortcut connection은 어떤 레이어(혹은 여러 레이어)를 “통과하지 않고”
입력 x를 그대로 다음 출력 y에 더해주는 연결이다.
### 왜 x를 더하는가?
Block 출력:
**y = F(x) + x**
- F(x): 블록 내부 Conv/BN/ReLU가 만든 변화량
- x: shortcut이 전달한 원본 정보
즉,
**"원래 정보(x) + 수정본(F(x))" = 새로운 특징 H(x)**
이 방식이 Residual.

# 3. Residual Block 구조
---
### 기본 블록 (2-layer residual block)
(1) x → Conv → BN → ReLU
(2) → Conv → BN
(3) shortcut x + F(x)
(4) → ReLU
### Bottleneck 블록 (3-layer block)
ResNet-50/101/152에서 사용하는 구조:
1×1 → 3×3 → 1×1
- 첫 1×1 : channel 줄이기
- 3×3 : 연산
- 마지막 1×1 : channel 복원
= 연산량 절약 + 깊이 증가 가능
---
# 4. Shortcut의 2가지 종류
---
ResNet은 상황에 따라 shortcut을 다르게 적용함.
### (A) Identity Shortcut (파라미터 없음)
그냥 x를 그대로 더함
→ 채널 수가 같을 때 사용
**가장 이상적, 계산비용 없음**
### (B) Projection Shortcut (1×1 conv)
채널 수가 달라지면
Ws x 형태로 선형 변환 필요
→ 주로 stride=2 다운샘플링할 때 사용
---


## Projection shortcut은 왜 stride=2에서 필요한가

stride=2 → 다운샘플링
즉, **feature map의 크기가 줄어듦**
예: 56×56 → 28×28
이때 shortcut을 그냥 identity로 넘기면?
x는 여전히 56×56인데
F(x)은 28×28
shape이 다르므로 더할 수 없음.
그래서 shortcut도 함께 “크기를 줄여서” 맞춰줘야 한다.
그 방법이 바로 **1×1 Conv + stride=2**
→ Ws x : 1×1 conv로 채널/크기까지 맞춰줌
→ 그래서 F(x) + Ws x 가능.

# 5. 핵심 실험 결과 요약
---
## (1) ImageNet 실험 결과
### 34-layer plain vs 34-layer ResNet
- plain-34는 plain-18보다 오히려 성능 떨어짐
- ResNet-34는 ResNet-18보다 성능 좋아짐
→ **ResNet은 깊어질수록 성능 향상**
→ degradation 문제 완전 해결
### 깊은 모델 성능
- ResNet-50
- ResNet-101
- ResNet-152 → 가장 우수한 단일 모델 (top-5 4.49%)
### Ensemble
ResNet 여러 개 앙상블 → 3.57% error
ILSVRC 2015 우승
---
## (2) CIFAR-10 실험
### plain networks
- 깊어질수록 training error 증가 (대참사)
### ResNet
- 20, 32, 44, 56, 110-layer 모두 잘 학습
- 깊을수록 성능 증가
특히 110-layer 모델:
**6.43% (state-of-the-art 당시)**
### 1202-layer 초심도 실험
- 훈련은 성공 (train loss 거의 0)
- 테스트 성능은 110-layer보다 나쁨
→ 너무 깊어서 overfitting
---
# 6. Residual Function의 특징 분석(Fig.7)
---
- Residual block의 출력(잔차)은 대체로 아주 작은 값
- F(x)는 “미세 조정(perturbation)” 역할
- 깊어질수록 각 레이어의 수정량은 더 작아짐
→ Identity 기반으로 안정적으로 학습된다는 증거
---


## 깊어질수록 각 레이어의 수정량이 더 작아진다는 것이 무슨말인가

논문 Fig.7 보면
깊은 ResNet일수록 각 레이어의 F(x) 크기(std)가 매우 작다.
즉,
각 블록은 입력 x를 거의 그대로 전달하고
아주 약간(미세한 변화)만 준다.
이 말은:
**깊은 네트워크가 크고 복잡한 변환을 한 번에 하지 않고<br>아주 작은 수정들을 여러 번 반복하는 구조**라는 뜻.
이게 왜 중요하냐면:
- 훈련이 훨씬 쉽게 됨
- gradient가 안정적으로 흐름
- 네트워크 전체가 “항등함수 + 작은 변화” 의 연속이라 안정적

# 7. Object Detection / Segmentation 성능 향상
---
Faster R-CNN backbone을 VGG-16 → ResNet-101로 교체하니:
### PASCAL VOC
- mAP 상승
### COCO
- mAP +6% (mAP@[.5,.95])
→ 무려 **28% 상대 향상**
즉, **더 나은 feature representation** 덕분에 다른 모든 CV task도 향상됨
---

---

# Attention Is All You Need

## Abstract {toggle="true"}
---
지배적인 시퀀스 변환 모델들은 인코더와 디코더를 포함하는 복잡한 순환 신경망 또는 합성곱 신경망에 기반을 두고 있다. 가장 성능이 좋은 모델들은 또한 주의(attention) 메커니즘을 통해 인코더와 디코더를 연결한다. 우리는 Transformer라고 불리는 새로운 단순한 네트워크 구조를 제안하는데, 이는 전적으로 attention 메커니즘에만 기반하고, 순환과 합성 연산을 완전히 없앤다.
두 가지 기계 번역 과제에서의 실험은, 이 모델들이 품질 면에서 더 우수하면서도 더 높은 병렬성을 가지며, 학습에 필요한 시간도 상당히 더 적게 든다는 것을 보여준다.
우리 모델은 WMT 2014 영어→독일어 번역 과제에서 28.4 BLEU를 달성하여, 앙상블을 포함한 기존 최고 결과보다 2 BLEU 이상 향상시킨다. WMT 2014 영어→프랑스어 번역 과제에서는, 8개의 GPU로 3.5일 동안 학습한 후 단일 모델 기준의 새로운 최첨단 BLEU 점수 41.8을 기록하는데, 이는 문헌에서 보고된 최고 모델들의 학습 비용에 비해 극히 작은 수준이다.
우리는 Transformer가 다른 작업에도 잘 일반화됨을 보이기 위해, 대규모 및 소규모 학습 데이터 모두에서 영어 구성 구문 분석(English constituency parsing)에 성공적으로 적용함으로써 이를 보여준다.
---
<table header-row="true">

<tr>
<td>시대</td>
<td>구조</td>
<td>특징</td>
</tr>
<tr>
<td>옛날</td>
<td>RNN Encoder → RNN Decoder</td>
<td>느림, 병렬화 불가능</td>
</tr>
<tr>
<td>중간</td>
<td>RNN Encoder → RNN Decoder + Attention</td>
<td>성능 좋아짐</td>
</tr>
<tr>
<td>RNN 한계 극복</td>
<td>CNN Encoder → CNN Decoder (+ Attention)</td>
<td>빠르지만 구조 복잡</td>
</tr>
<tr>
<td>Transformer</td>
<td>**Self-Attention Encoder → Self-Attention Decoder**</td>
<td>RNN/CNN 없이 빠르고 정확</td>
</tr>
</table>
✅ **BLEU** = 기계 번역이 사람 번역과 얼마나 비슷한지를 수치로 나타낸 점수
                    ( 높을수록 더 좋은 번역 모델 )
## 1. Introduction {toggle="true"}
---
순환 신경망(Recurrent Neural Networks), 특히 장단기 메모리(Long Short-Term Memory) [13]와 게이트드 순환 신경망(Gated Recurrent Neural Networks) 은, 언어 모델링과 기계 번역과 같은 시퀀스 모델링 및 변환 문제에서 최첨단(state-of-the-art) 접근법으로 확고히 자리 잡아 왔다. 그 이후로 수많은 시도가 순환 언어 모델과 인코더-디코더 구조의 한계를 계속 확장해 왔다.
> RNN(LSTM/GRU)이 언어 모델과 번역에서 오랫동안 최고의 방법이었고, 많은 연구가 그것을 더 개선하려고 해왔다”라는 의미
순환 모델들은 일반적으로 입력 및 출력 시퀀스의 심볼 위치에 따라 계산을 분해한다. 위치들을 계산 시간의 단계와 정렬시키면서, 이들은 이전 은닉 상태 ht−1과 위치 t의 입력에 대한 함수로서 은닉 상태 hₜ의 시퀀스를 생성한다. 이러한 본질적으로 순차적인 특성은 학습 예제 내부의 병렬화를 불가능하게 하며, 이는 시퀀스 길이가 길어질 때 문제가 되는데, 그 이유는 메모리 제약이 예제 간 배치를 제한하기 때문이다.
> 1️⃣ RNN은 시퀀스를 앞에서부터 위치별로 순서대로 처리해야 한다
2️⃣ 각 위치는 이전 은닉 상태에 의존하므로 병렬 처리가 불가능하다
3️⃣ 그래서 시퀀스가 길어지면 계산이 매우 느려진다
4️⃣ 길어질수록 메모리를 많이 써서 batch 크기도 줄어든다
→ 결과적으로 학습 효율이 크게 떨어진다.
최근 연구들은 요인 분해 기법(factorization tricks)과 조건부 계산(conditional computation)을 통해 계산 효율성을 크게 개선했으며, 후자의 경우에는 모델 성능 또한 향상시켰다. 그러나 그럼에도 불구하고 순차적 계산이라는 근본적인 제약은 그대로 남아 있다.
주의(attention) 메커니즘은 다양한 작업에서 설득력 있는 시퀀스 모델링과 변환 모델의 필수적인 부분이 되었으며, 입력 또는 출력 시퀀스 내에서 위치 간의 거리에 관계없이 의존성을 모델링할 수 있게 한다. 하지만 몇 가지 경우 [27]를 제외하면, 이러한 attention 메커니즘은 순환 신경망과 함께 결합되어 사용된다.
이 연구에서 우리는** Transformer를 제안**하는데, 이 모델 구조는 순환을 배제(eschew)하고, 대신 입력과 출력 간의 전역적 의존성을 포착하기 위해 전적으로 attention 메커니즘에만 의존한다. Transformer는 매우 높은 병렬화를 가능하게 하며, 8개의 P100 GPU에서 단 12시간 학습한 후 번역 품질에서 새로운 최첨단 성능에 도달할 수 있다.
---


## **background: RNN/LSTM/GRU**

**RNN 문제** → 긴 문장에서 앞 정보가 금방 사라짐(기울기 소실)
**LSTM**        → 중요한 정보 오래 기억하게 만든 RNN
**GRU          **→ LSTM을 더 단순하고 빠르게 만든 RNN



## **입력 및 출력 시퀀스의 심볼 위치에 따라 계산을 분해**

### 1) **“입력 시퀀스의 심볼 위치에 따라 계산을 분해한다”**
- RNN 계열 모델은 시퀀스를 한꺼번에 처리하지 않고,
- 각 위치(1번, 2번 … t번)를 개별 연산 단계로 나누어 순차적으로 계산한다.
- 즉, 시퀀스 길이가 n이면 n번의 연산 단계가 존재한다.
---
### 2) **“위치들을 계산 시간의 단계(time steps)와 정렬시키면서”**
- 시퀀스의 1번째 토큰은 시간 단계 1, 2번째 토큰은 시간 단계 2 …
- 이렇게 시퀀스의 위치와 연산 순서가 1대1로 연결된다.
- 모델은 반드시 입력된 순서를 따라가야 한다는 의미이다.
---
### 3) **“이들은 이전 은닉 상태 ht−1과 위치 t의 입력을 이용해 ht를 생성한다”**
-  RNN의 핵심 정의를 서술한 것이다.
- 은닉 상태 ht는 다음 함수로 정의된다:
```math
h_{t}=f(h_{t−1},x_{t})
```
- ht−1 : 이전 단계까지의 누적 정보(메모리)
- xt : 현재 시점의 입력 토큰
- f : RNN/LSTM/GRU 셀의 변환 함수
- 즉, 현재 상태는 “이전 상태 + 현재 입력”의 조합으로 결정



## **은닉 상태**

은닉 상태 = RNN이 이전까지 읽은 모든 정보를 요약해 저장한 내부 기억
                  = RNN이 지금까지 본 입력들의 정보를 압축해서 가지고 있는 “현재 기억 상태”



## **그 이유는 메모리 제약이 예제 간 배치를 제한하기 때문**

시퀀스가 길면 필요한 메모리가 많아져서, 한 번에 여러 예제를 동시에 학습(batch)하기 어렵고 <br>→ 그래서 RNN 학습이 느려진다.



## **attention 메커니즘은 다양한 작업에서 설득력 있는 시퀀스 모델링과 변환 모델의 필수적인 부분**

- Attention은 시퀀스 처리 모델에서 매우 중요해졌다.
- 번역, 요약, 질문응답 등 거의 모든 모델이 쓴다.
- Attention을 쓰면, **단어들 사이의 거리와 상관없이 바로 연결**할 수 있다.
- RNN은 앞에서부터 한 단계씩만 보므로 멀리 떨어진 단어 관계 잡기 힘듦.
- 하지만 Attention은 문장 전체를 한 번에 보고 필요한 위치에 바로 집중할 수 있음

Attention은 멀리 떨어진 단어들 사이의 관계도 바로 연결해서 볼 수 있게 해주는 기술이고, 그래서 요즘 시퀀스 모델(번역, 요약 등)에서 거의 필수로 사용된다.


## 2. Background {toggle="true"}
---
순차적 계산을 줄이려는 목표는 Extended Neural GPU, ByteNet, 그리고 ConvS2S의 기반을 이루기도 하는데, 이들 모두는 합성곱 신경망을 기본 구성 요소로 사용하며, 모든 입력 및 출력 위치에 대해 은닉 표현을 병렬로 계산한다. 이러한 모델들에서, 임의의 두 입력 또는 출력 위치의 신호를 서로 관련짓기 위해 요구되는 연산 수는 그 위치들 사이의 거리에 따라 증가하며, ConvS2S의 경우 선형적으로, ByteNet의 경우 로그 형태로(logarithmically) 증가한다. **이는 먼 위치들 사이의 의존성을 학습하는 것을 더 어렵게 만든다. **
> 1️⃣ CNN 기반 모델(Extended Neural GPU, ByteNet, ConvS2S)은 RNN의 순차성을 줄이려고 나<br>      왔다.
         → 그래서 병렬 계산이 가능하다.
2️⃣ 하지만 CNN은 멀리 떨어진 두 단어를 연결하려면 여러 층을 거쳐야 한다.
3️⃣ 그래서 멀리 떨어진 단어 관계를 학습하기가 어렵다.
Transformer에서는 이러한 연산 수가 상수(constant)로 줄어드는데, 이는 attention 가중치가 적용된 위치들을 평균함으로 인해 효과적인 해상도가 감소하는 비용을 수반하며, 우리는 3.2절에서 설명한 멀티-헤드 어텐션(Multi-Head Attention)을 사용하여 이 효과를 상쇄한다.
> Transformer는 멀리 있는 단어도 한 번에 연결할 수 있지만 정보가 흐려지는 문제가 생기므로, 이를 Multi-Head Attention으로 보완한다.
Self-attention, 때때로 intra-attention이라고도 불리는 것은 하나의 시퀀스 내의 서로 다른 위치들을 서로 연관지어 그 시퀀스의 표현을 계산하는 attention 메커니즘이다. Self-attention은 읽기 이해, 추상적 요약, 텍스트 함의(textual entailment), 과제-독립적 문장 표현 학습을 포함한 다양한 작업들에서 성공적으로 사용되어 왔다.
> Self-attention은 한 문장 내부의 단어 관계를 계산해 표현을 만드는 방식이며, 여러 NLP 작업에서 매우 잘 동작한다.
End-to-end memory networks는 시퀀스 정렬된 순환 대신 순환적 attention 메커니즘에 기반하며, 단순한 언어의 질문응답 및 언어 모델링 작업에서 좋은 성능을 보이는 것으로 알려져 있다.
그러나 우리의 지식으로는, Transformer는 입력 및 출력의 표현을 계산하기 위해 시퀀스 정렬된 RNN이나 합성곱을 사용하지 않고 전적으로 self-attention에만 의존하는 최초의 변환(transduction) 모델이다. 다음 절들에서는 Transformer를 설명하고, self-attention의 동기를 제시하며, [17], [18], [9]와 같은 모델들에 비해 가지는 장점에 대해 논의할 것이다.
> ✅ End-to-End Memory Network는 RNN처럼 순서대로 처리하지 않고, attention을 반복해서            사용하는 모델이다.
✅ 하지만 Transformer는 그보다 더 나아가, RNN도 안 쓰고 CNN도 안 쓰고, 오직 self-attention<br>     만으로 입력·출력 표현을 계산하는 최초의 모델이다.
---


## **Transformer가 왜 CNN보다 멀리 떨어진 위치를 더 쉽게 연결할 수 있는가**

- Transformer는 멀리 떨어진 단어끼리 연결하는 데 필요한 연산 수가 ‘항상 1번’이다
- CNN처럼 거리만큼 늘어나지 않고 항상 한 번의 attention으로 연결 가능
- 그래서 연산량이 상수(constant) 라고 말한 것
- 하지만 attention은 여러 위치의 정보를 “평균”하므로, 정보가 흐릿해진다는 단점이 있다.
- 즉, 해상도(정확한 위치 정보)가 떨어질 수 있음
- 이 문제를 해결하려고 Multi-Head Attention을 쓴다.
- 여러 개의 attention head가 서로 다른 관점으로 정보를 보고 흐릿해진 부분을 보완함.



## **self-attention**

- “self-attention은 한 문장 안에서 단어들이 서로 중요도를 계산해서 서로를 참고하는 방식이다.”
예를 들면:
문장: *“The cat that you saw yesterday is mine”*
- “cat”은 “is”를 볼 수 있음
- “that”은 “saw”를 볼 수 있음
- “you”는 “saw”랑 연결될 수 있음
- “yesterday”도 “saw”랑 연결됨
이렇게 **문장 내부의 모든 위치가 서로 연결될 수 있고**,
그 연결을 통해 \*\*문장 전체 의미를 구성(표현 계산)\*\*하는 것이 self-attention.



## **End-to-end memory networks  vs  Transformer**

> Memory Network
→ “attention으로 메모리를 반복적으로 읽어서 답을 내는 작은 구조”
> Transformer
→ “attention만으로 인코더·디코더 전체를 구성한 대규모 깊은 신경망”

## 3. Model Architecture {toggle="true"}
---


[image omitted: temporary Notion asset]


대부분의 경쟁력 있는 신경 시퀀스 변환 모델들은 인코더-디코더 구조를 가진다. 여기에서 인코더는 심볼 표현들로 이루어진 입력 시퀀스 (x1, ..., xn)를 연속적인 표현들의 시퀀스 z = (z1, ..., zn)로 매핑한다. z가 주어지면, 디코더는 출력 시퀀스 (y1, ..., ym)를 한 번에 하나의 심볼씩 생성한다.
각 단계에서 모델은 **자가회귀(auto-regressive)** 방식으로 동작하며, 다음 심볼을 생성할 때 이전에 생성된 심볼들을 추가 입력으로 사용한다.
Transformer는 이러한 전체 구조를 따르며, 인코더와 디코더 모두에 대해 여러 층으로 쌓은(self-attention) 자기-주의와 위치별(point-wise) 완전연결층을 사용한다. 이는 각각 Figure 1의 왼쪽 절반과 오른쪽 절반에 나타나 있다.


---


## **인코더와 디코더**

### **1) 인코더(Encoder)**
- 입력 문장 `(x1, x2, …, xn)`을 받음
- 이걸 신경망이 이해할 수 있는 **연속적인 벡터 표현 z1, z2, …, zn**으로 바꾼다
→ 즉, **입력을 의미 벡터로 변환하는 역할**
### **2) 디코더(Decoder)**
- 인코더가 만든 표현 **z**를 보고
- 출력 문장 `(y1, y2, …, ym)`을 **한 단어씩 순서대로 생성**한다
→ y1 만들고 → y2 만들고 → y3 만들고 … 이런 식



## **자가회귀(auto-regressive) **

# ✔️ auto-regressive 방식이란?
**출력 문장을 만들 때,<br>이전에 만든 단어들을 다시 입력으로 넣어서 다음 단어를 만드는 방식**이다.
---
# ✔️ 문장 의미 정확하게 풀기
> “각 단계에서 모델은 자가회귀 방식으로 동작하며,
다음 심볼을 생성할 때 이전에 생성된 심볼들을 추가 입력으로 사용한다.”
→ 이 말은,
**y₂를 만들 때 y₁을 참고하고,<br>y₃를 만들 때 y₁·y₂를 참고하고,<br>y₄를 만들 때 y₁·y₂·y₃를 참고한다**
는 뜻이다.
디코더는 항상 **이전에 자신이 만든 출력들을 다시 입력으로 사용**한다.
---
# ✔️ 예시
입력: “I love NLP”
출력을 만드는 과정:
- y₁ = “나는”
- y₂ = “나는 좋아한다” (y₁을 참고)
- y₃ = “나는 NLP를 좋아한다” (y₁, y₂를 참고)
이렇게 **과거 출력 → 다음 출력 생성에 사용**됨.



## **마지막 문단의 정리**

Transformer는 그림처럼 인코더와 디코더 둘 다 여러 층(layer)로 구성되어 있고,<br>각 층은 두 가지 블록으로 이루어져 있다:
1. Self-Attention(자기-주의)
2. Point-wise Feed-Forward Network(위치별 완전연결층)
즉, 여러 층을 쌓아서 → Self-Attention → Feed Forward → Self-Attention → Feed Forward …<br>이런 구조로 인코더·디코더를 만든다는 말이다.



## **multi-head attention과 self attention 간의 관계**

> Transformer에서 “self-attention”이라고 말할 때 = Multi-Head Attention으로 수행하는 self-attention을 뜻한다.
# ✔️ Self-Attention = 개념
**한 시퀀스 안의 단어들이 서로를 바라보는 메커니즘**
- 단일 attention이든
- 다중 head attention이든
모두 self-attention이라는 “아이디어”를 구현하는 방식.
---
# ✔️ Multi-Head Attention = 구현 방식
Transformer에서는 **self-attention을 단일로 하지 않고, 여러 개(head)를 동시에 실행한다.**
즉, **Self-Attention을 Multi-Head로 여러 번 시켜서 → 더 풍부한 표현을 만든다.**



## **위치별 완전연결층 **

**위치별 완전연결층 = 각 단어 벡터에 똑같은 MLP를 독립적으로 적용하는 단계.**
(attention으로 섞은 정보를 “비선형 변환”으로 더 가공하는 역할)
### ✔️ 1) “단어 벡터에 똑같은 작은 MLP를 적용한다”는 말이 뭐야?
문장을 예로 들어보자:
**["나는", "오늘", "밥을", "먹었다"]**
각 단어는 이미 embedding된 하나의 벡터(512차원)라고 생각하면 돼.
그러면 Transformer는 이 단어벡터 각각에 아래 연산을 똑같이 적용한다:
```plain text
1) FC(Linear)
2) ReLU
3) FC(Linear)
```
즉,
- "나는" 벡터 → FC → ReLU → FC
- "오늘" 벡터 → FC → ReLU → FC
- "밥을" 벡터 → FC → ReLU → FC
- "먹었다" 벡터 → FC → ReLU → FC
**각 단어가 독립적으로 같은 네트워크를 통과하는 것.**
이걸 "위치(point)-별 feed-forward layer"라고 부른다.
### ✔️ 2) 왜 Feed Forward가 ‘완전 연결층(FC)’이야?
- Feed-Forward Network(피드포워드 신경망)라고 하면
- **입력 → FC → 활성함수 → FC 형태의 MLP 구조**를 의미해.
- 즉, **피드포워드층 = MLP = 완전연결층 기반 네트워크**다.



## **그림에 대한 완전한 설명**

# ✔️ 1) 입력단: Embedding + Positional Encoding
### ✓ Input Embedding
입력 단어들을 벡터로 바꾼 것.
### ✓ Positional Encoding
Self-Attention은 순서를 모르기 때문에
각 단어 위치 정보를 더해줌.
→ 이 합(+)이 아래 둥근 회로로 표현됨.
---
# ✔️ 2) 인코더(왼쪽 블록): N개의 동일한 층으로 구성
각 인코더 layer는 다음 2개로 구성:
---
## ① Multi-Head Self-Attention
- Query, Key, Value가 **모두 인코더 입력에서 옴.**
- 문장 전체를 보며 **단어끼리의 관계**를 파악.
예: “The cat chased the mouse”
→ “cat”은 “chased”와 연관, “mouse”는 “chased”와 연관.
**역할:** 문장 내부 관계를 이해하는 단계.
---
## ② Feed Forward Network (FFN)
- 각 단어 벡터를 **독립적으로**
- 512 → 2048 → 512 (Non-linear)로 변환
- Self-Attention이 계산한 의미를 더 정교하게 가공
**역할:** 단어 표현(embedding)을 더 깊게 변환해 의미를 강화.
---
## ③ Add & Norm
각 sub-layer(Self-Attention / FFN) 앞뒤에
**Residual Connection + LayerNorm**이 붙음.
그림에서 노란 박스가 이것.
**역할:** 안정적으로 학습되게 도와줌.
---
### 인코더 출력(맨 위의 N×)
→ 모든 단어의 “의미 벡터”들이 담긴 세트
→ 이게 디코더로 넘어감 (오른쪽 화살표)
---
# ✔️ 3) 디코더(오른쪽 블록): N개의 동일한 층
디코더의 각 layer는 **3개의 sub-layer**가 있음.
---
## ① Masked Multi-Head Self-Attention
- 디코더 출력 y₁, y₂, y₃…을 만듦
- 하지만 미래 단어(yᵢ+1)를 보면 안 됨 → **마스킹**
예:
y3을 생성할 때 y4, y5는 -∞ 처리 → 무시
**역할:** 이전에 만든 단어들 사이 관계 분석.
(자동회귀 구조 유지)
---
## ② Encoder–Decoder Attention
- Query = 디코더 내부 벡터
- Key, Value = 인코더 출력(입력 문장의 의미)
즉,
**디코더가 현재 만들 단어가 입력 문장의 어떤 단어와 관련 있는지 찾는다.**
예:
입력: “I love apples”
출력 만들기: “나는 사과를 좋아한다”
“사과를”을 생성할 때
→ encoder attention이 “apples”에 강하게 주의
**역할:** 번역/요약 등 “입력을 참고해서 출력 생성”하는 핵심 단계.
---
## ③ Feed-Forward Network (FFN)
- 인코더와 동일한 FFN
- 단어 벡터 자체를 비선형적으로 변환
---
## ④ Add & Norm
각 sub-layer 앞뒤에 동일하게 적용
(Residual + LayerNorm)
---
# ✔️ 4) 마지막 출력: Linear + Softmax
### ✓ Linear
디코더 마지막 벡터 → 단어 분포로 투영
### ✓ Softmax
가장 가능성 높은 다음 단어를 선택



## 잔차와 정규화가 존재하는 이유

### 잔차(Add)
- 깊은 네트워크에서 gradient가 사라지지 않게 함
- SubLayer가 잘못 학습돼도 최소한 원래 정보는 보존
### 정규화(Norm)
- 값의 스케일을 안정화
- Attention/FFN 출력이 폭주하는 것을 방지
- 학습 안정성 향상

### 3.1 Encoder and Decoder Stacks {toggle="true"}
---
**Encoder:**
인코더는 N = 6개의 동일한 층으로 이루어진 스택으로 구성된다. 각 층은 두 개의 서브레이어를 가진다. 첫 번째 서브레이어는 멀티-헤드 자기-어텐션(multi-head self-attention) 메커니즘이고, 두 번째는 위치별(position-wise) 완전연결(feed-forward) 네트워크이다.
우리는 두 서브레이어 각각을 둘러싸는 **잔차 연결(residual connection)** [11]을 사용하고, 그 뒤에 레이어 정규화(layer normalization) [1]를 적용한다. 즉, 각 서브레이어의 출력은 다음과 같다:
```math
LayerNorm(x + Sublayer(x))
```
여기서 Sublayer(x)는 해당 서브레이어가 구현하는 함수이다. 이러한 잔차 연결을 가능하게 하기 위해, 모델의 모든 서브레이어와 임베딩 층은 출력 차원이 d_model = 512가 되도록 한다.
**Decoder:**
디코더도 N = 6개의 동일한 층으로 구성된 스택이다. 디코더의 각 층에는 인코더 층과 동일한 두 개의 서브레이어에 더해, 세 번째 서브레이어가 추가된다. 이 서브레이어는 인코더 스택의 출력에 대해 멀티-헤드 어텐션을 수행한다.
인코더와 마찬가지로, 우리는 디코더의 각 서브레이어 주위에 잔차 연결을 적용하고, 그 뒤에 레이어 정규화를 수행한다.
또한 디코더 스택에서의 self-attention 서브레이어는 현재 위치가 이후 위치를 참조하지 못하도록(masking) 수정된다. 이 마스킹은 출력 임베딩이 한 위치(offset)만큼 이동되어 있다는 사실과 결합되어, 위치 i에 대한 예측이 i보다 작은 위치의 이미 알려진 출력에만 의존하도록 한다.
---


## **인코더 - 잔차 연결**

- 입력 x를 서브레이어 출력과 그대로 더해주는(skip) 연결이다.
```math
output=x+sublayer(x)
```
- 이렇게 **입력 x를 그대로 더해주는 것이 residual connection**이야.
- 왜 하냐면?
- 깊은 네트워크에서 **정보가 사라지지 않게**
- **학습이 더 안정적으로 되게**하는 효과가 있다.


## x를 더해주는게 왜 잔차지?

원래 신경망은 출력 H(x)H(x)H(x) 를 직접 학습하려고 했어.
근데 He et al.(ResNet)이 발견한 게 뭐냐면:
> H(x)를 직접 학습하는 것보다<br>(H(x) - x), 즉 ‘얼마나 변화(잔차)시킬지’만 학습하는 게 훨씬 쉽다.
즉,
```math
F(x)=H(x)−x
```
이 **F(x) = 잔차(residual)** 임.
그래서 실제 출력은:
```math
H(x)=F(x)+x
```
이게 바로 residual connection의 핵심 공식이다.




## **인코더 - 잔차 연결에서의 출력차원이 512여야하는 이유**

- Residual connection(잔차 연결)을 쓰려면, 더해지는 두 벡터의 차원이 반드시 같아야 한다.
- skip-connection에서 벡터끼리 더하려면 같은 크기여야 해서, 모든 서브레이어 출력과 임베딩을 512차원으로 맞춘다는 뜻이다.



## **디코더 -  self-attention이 2개인 이유**

- **Self-Attention**
→ 이미 생성된 이전 단어들을 참고해서
*이번 단어가 앞 단어들과 어떤 관계인지* 알기 위해 필요.
- **Encoder–Decoder Attention**
→ 입력 문장(원문)을 참고해서
*무엇을 번역해야 하는지* 알기 위해 필요.



## **디코더 - 마스킹 필요한 이유**

- 디코더는 **단어를 앞에서부터 하나씩 생성해야 한다.**
- 그런데 self-attention은 **원래 미래 단어도 볼 수 있음.**
- 그러면 y₃을 만들 때 y₄ 같은 \*\*미래 단어를 미리 보게 되어 부정행위(cheating)\*\*가 됨.
- 그래서 \*\*현재 위치 이후의 단어는 못 보게 마스킹(masking)\*\*을 넣는다.
- 마스킹 = **미래 단어를 완전히 차단하는 규칙**

### 3.2 Attention {toggle="true"}
---
[image omitted: temporary Notion asset]
어텐션 함수는 하나의 쿼리(query)와 키-값(key-value) 쌍들의 집합을 출력으로 매핑하는 함수로 설명될 수 있으며, 이때 쿼리, 키, 값, 그리고 출력은 모두 벡터이다. 
출력은 값들(values)의 가중합(weighted sum)으로 계산되며, 각 값에 할당되는 가중치는 쿼리와 해당 키의 호환성(comparability)을 나타내는 함수에 의해 계산된다.
---


## **쿼리/키/값**

- 값(Value) = 실제 정보(단어 벡터)
- 키(Key) = 값이 어떤 단어인지 표시하는 라벨
- 쿼리(Query) = 지금 “집중하고 싶은 단어”의 요청



## **attention 계산 방법**

attention은 다음처럼 계산함:
```plain text
1) Query와 Key를 비교해서 얼마나 관련 있는지 점수(score)를 준다.
2) 그 점수를 softmax로 가중치(weight)로 만든다.
3) 그 가중치로 Value들을 섞어서(가중합) 최종 출력 벡터를 만든다.
```
> 쿼리(Q)가 각 키(K)와 얼마나 관련 있는지 계산해 가중치로 만들고,
그 가중치를 Value들에 곱해 더해(가중합) attention 출력을 만든다는 뜻.



## **그림 설명 (왼쪽)**

### **① MatMul(Q, Kᵀ)**
- Query와 Key 내적
- “각 단어가 다른 단어와 얼마나 관련 있는지” 계산
### **② Scale (= 1/√dₖ)**
- 값이 너무 커지는 걸 방지하기 위해 √dₖ로 나눔
- softmax가 안정적으로 작동하게 함
### **③ Mask (optional)**
- 디코더에서 **미래 단어를 못 보게** 막을 때 사용
- 인코더에서는 사용하지 않음
### **④ Softmax**
- 관련도(score)를 확률처럼 정규화
- 중요도가 높은 단어는 큰 가중치, 낮으면 작은 가중치
### **⑤ MatMul(Softmax 결과, V)**
- Value들에 가중치를 곱해서 **가중합**을 구함
- 이것이 Attention 출력



## **그림 설명 (오른쪽)**

### **① Linear(3개): Q, K, V를 head마다 따로 만들기**
입력 X를
각 head마다:
- Wᵠʰ
- Wᵏʰ
- Wᵛʰ
이렇게 서로 다른 행렬로 변환해서
**8개의 Q, 8개의 K, 8개의 V를 만든다.**
### **② Scaled Dot-Product Attention (여러 개)**
왼쪽 그림의 구조를
**각 head마다 하나씩 돌리는 것.**
즉 8개의 독립적인 attention이 동시에 계산됨.
### **③ Concat**
각 head에서 나온 결과를 **그냥 이어붙인다.**
### **④ Linear**
마지막으로 concat한 벡터를
하나의 최종 벡터로 다시 선형 변환하여
Multi-Head Attention의 출력으로 만든다.

### 3.2.1 Scaled Dot-Product Attention {toggle="true"}
---
[image omitted: temporary Notion asset]
---


## **핵심**

- Attention(Q, K, V) = softmax( QKᵀ / √dₖ ) V
→ 이게 Transformer의 기본 attention 계산 공식이다.
- Q(쿼리), K(키), V(밸류)는
→ 입력 벡터를 서로 다른 행렬로 선형 변환해 얻는다.
→ 입력 단어 벡터 X에 서로 다른 가중치 행렬을 곱해서 Q, K, V라는 세 가지 다른 벡터로 바꾸는 것을 말한다.
- **QKᵀ**은 쿼리와 키의 “관련도(호환성)”를 계산하는 내적이다.
- **√dₖ로 나누는 이유**
→ dₖ(키 차원)가 커질수록 QKᵀ 값이 너무 커져 softmax가 죽기 때문에 값을 안정화시키려고 **스케일링**한다.
- **Additive attention과 Dot-product attention 비교**
- 둘 다 핵심 아이디어는 같음
- additive(MLP 사용)는 계산이 더 무거움
- dot-product(내적 사용)는 행렬 곱으로 구현 가능해서 **훨씬 빠름**
→ 그래서 Transformer는 dot-product attention 사용
- **dₖ가 작을 때는 둘 다 비슷하게 잘 작동**
→ dₖ가 커질수록 스케일링 없는 dot-product는 softmax가 망함
→ 그래서 Scaled Dot-Product Attention 사용

### 3.2.2 Multi-Head Attention {toggle="true"}
---
[image omitted: temporary Notion asset]
---


## 핵심

### **1) 단일 Attention 대신 여러 개(예: 8개) Attention을 병렬로 사용한다.**
- 이유: 한 가지 관점만 보는 것보다
**여러 관점에서 단어 관계를 동시에 보는 게 더 좋기 때문.**
---
### **2) 각 Head는 서로 다른 가중치(Wᵠ, Wᵏ, Wᵛ)를 사용해 입력을 Q,K,V로 변환한다.**
- 즉, 입력을 여러 버전(Qᵢ,Kᵢ,Vᵢ)으로 바꿔서
각 Head가 다른 관점에서 Attention을 계산한다.
---
### **3) 각 Head에서 Scaled Dot-Product Attention을 따로 계산한다.**
- Head 1 Attention
- Head 2 Attention
- …
- Head h Attention
→ **서로 독립적인 attention 학습**
---
### **4) 여러 Head 결과를 이어붙여(concatenate) 마지막으로 W⁰로 선형 변환한다.**
- Concat(head₁, …, head₈)
- 그다음 **W⁰**로 다시 projection
→ Multi-Head Attention 출력 완성
---
### **5) 계산량이 너무 커지지 않도록 각 Head의 차원은 작게(d_model / h) 한다.**
- 예: 모델 차원 512, Head 8개 → 각 Head 차원 64
- 그래서 **여러 Head를 써도 전체 계산량은 단일 Attention과 비슷함**
- 단일 Attention:
- Q, K, V 차원 = 512
- Attention 계산 비용 \~ 512 × 512
- Multi-Head Attention:
- Head가 8개
- 각 Head 차원 = 512 / 8 = 64
- Head 하나 비용 = 64 × 64
- Head 8개 비용 = 8 × (64×64) = 8×4096 = **32768**

### 3.2.3 Applications of Attention in our Model {toggle="true"}
---
Transformer는 멀티-헤드 어텐션을 세 가지 방식으로 사용한다:
- 인코더-디코더 어텐션 층에서는, query는 이전 디코더 층에서 오고, memory key와 value는 인코더의 출력에서 온다. 이를 통해 디코더의 모든 위치가 입력 시퀀스의 모든 위치에 주의를 기울일 수 있다. 이는 [38, 2, 9]와 같은 시퀀스-투-시퀀스 모델에서 전형적으로 사용되는 인코더-디코더 어텐션 메커니즘을 그대로 따른다.
- 인코더는 self-attention 층을 포함한다. self-attention 층에서는 key, value, query가 모두 동일한 곳, 즉 인코더 이전 층의 출력에서 온다. 인코더의 각 위치는 이전 인코더 층의 모든 위치에 주의를 기울일 수 있다.
- 디코더의 self-attention 층도 마찬가지로, 디코더의 각 위치가 자기 자신을 포함해 그 이전의 모든 위치에 주의를 기울일 수 있게 한다. 우리는 auto-regressive 성질을 유지하기 위해 디코더에서 왼쪽 방향으로의 정보 흐름을 막아야 한다. 이를 위해 scaled dot-product attention 내부에서, softmax 입력 중 불가능한 연결에 해당하는 값들을 마스킹하여( setting to −∞ ) 제거한다. 그림 2를 참고하라.
---


## **이를 통해 디코더의 모든 위치가 입력 시퀀스의 모든 위치에 주의를 기울일 수 있다**

→ 이 attention 구조 덕분에
**디코더의 각 단어는 입력 문장의 모든 단어를 직접 보고 관련도를 계산할 수 있다.**
- 예를 들어 번역:
- 입력: “I love apples”
- 출력: “나는 사과를 좋아한다”
- “사과를”을 생성할 때, 디코더는 “I / love / apples”  세 단어 모두를 보면서 어떤 단어가 현재 번역에 중요한지 판단할 수 있음.



## **인코더와 디코더의 핵심**

### ✔ 인코더 self-attention
→ 모든 단어가 서로를 자유롭게 볼 수 있음 (미래/과거 구분 없음)
### ✔ 디코더 self-attention
→ 현재 단어는 **앞 단어들만 볼 수 있음**
→ 미래 단어는 **마스킹(-∞)** 해서 접근 금지
→ 이유: auto-regressive(한 단어씩 생성하는 방식) 유지하려고

### 3.3 Position-wise Feed-Forward Networks {toggle="true"}
---
[image omitted: temporary Notion asset]
---


## 핵심

- 인코더/디코더의 각 층에는 attention 외에도 FFN(완전연결층 MLP)이 들어 있다.
- FFN은 각 단어(position-wise)마다 독립적으로 적용된다.
- 구조는 Linear → ReLU → Linear 의 2층 MLP이다.
- 각 층마다 FFN의 가중치는 다르다.
- FFN의 입력·출력 차원은 512(d_model) / 중간(hidden) 차원은 2048(d_ff)



## **FFN을 “Conv1D 두 개**

- FFN은 각 단어 벡터에 2개의 fully-connected layer를 적용하는데,
- 이것은 CNN에서 kernel size=1짜리 Conv1D를 두 번 적용하는 것과 수학적으로 완전히 동일하다.

### 3.4 Embeddings and Softmax {toggle="true"}
---
[image omitted: temporary Notion asset]
---


## 핵심

## ✔ 1. Embedding Layer는 뭐야?
- 단어는 문자열이라 컴퓨터가 이해 못 함 → 숫자로 바꿔야 함.
- 숫자 ID만으로는 의미 없음 → **단어를 벡터(512차원)로 변환해야 함.**
- 이 변환을 해주는 층이 **Embedding Layer**.
---
## ✔ 2. Embedding Matrix에는 뭐가 들어있어?
- `(단어 개수 × 벡터 차원)` 크기의 큰 행렬.
- 각 “행(row)”이 **특정 단어의 의미 벡터**.
- 예: embedding_matrix[42] = apple의 의미 벡터
- 처음엔 랜덤, 훈련하면서 “의미 있는 벡터”로 변함.
- 이 행렬이 바로 **가중치(weight)**.
---
## ✔ 3. 왜 embedding에도 가중치가 필요해?
- 단어 벡터가 학습 과정에서 의미를 배우기 때문.
- “apple은 과일”
- “cat은 동물”
- 의미를 배우려면 값이 업데이트되어야 함 → 파라미터(가중치)여야 함.
---
## ✔ 4. Input Embedding과 Output Embedding 가중치를 왜 공유함?
**같은 단어는 입력에서도, 출력에서도, softmax에서도 같은 의미여야 함.**
예:
입력 “apple” → 의미 벡터
출력 “apple” → 똑같은 의미 벡터
softmax에서 “apple” 확률 계산 → 같은 의미 벡터 사용
→ 그래서 **같은 weight를 쓰는 게 자연스럽고 효율적**.
---
## ✔ 5. 왜 softmax 직전 Linear Layer도 embedding weight를 공유함?
디코더 마지막 단계는:
```plain text
"이 벡터가 어떤 단어와 가장 비슷한가?"
```
를 판단하는 과정이므로
**단어 의미 벡터(embedding)와 비교하게 됨**.
그래서:
```plain text
단어 embedding = softmax weight = 단어 의미 벡터
```
→ softmax weight를 embedding과 공유하는 게 논리적으로 맞음.
---
## ✔ 6. Weight 공유의 장점
- 파라미터 3배 절약
- 훈련 안정성 증가
- 성능 향상됨
---
### ⭐ embedding에 √d_model을 곱하는 이유 (초간단)
1. **임베딩 벡터 값이 너무 작으면 → Q·K도 너무 작아짐**
2. 그러면 softmax가 \*\*모두 비슷한 값(평평함)\*\*이 되어
어떤 단어에 주의를 줘야 할지 학습이 안 됨.
3. 그래서 **임베딩 벡터 크기를 키우려고**
√d_model(예: √512 ≈ 22.6)을 곱해서 스케일 업함.
4. 이렇게 하면
- attention 점수(Q·K)가 적당한 크기가 되고
- softmax가 정상적으로 잘 동작한다.



## input embedding과 self-attention의 차이

Input embedding은 ‘단어 자체 의미’이고,
Self-attention은 그 단어가 문장 안의 다른 단어들과 어떤 관계를 맺는지 계산해
‘문맥을 이해한 새로운 의미 벡터’를 만들어주는 단계다.



## 디코더 과정

## **1) Output Embedding**
→ 지금까지 만든 단어들을 **512차원 벡터로 바꿔준다.**
(단어 ID → 의미 벡터)
---
## **2) Masked Self-Attention**
→ 이 벡터들끼리 **문맥 관계를 계산한다.**
→ 미래 단어는 보면 안 되므로 **마스킹**
→ 여기서 만든 문맥 벡터가 **쿼리(Q)** 가 된다.
---
## **3) Encoder–Decoder Attention**
→ Self-Attention에서 만든 **문맥(Q)** 을 들고
→ 인코더 출력(Key/Value)과 비교해서
→ **입력 문장의 어떤 단어를 참고해야 하는지** 결정한다.
---
# ✔ 2) FFN (Feed Forward Network)
**역할:**
- Attention 단계가 가져온 “문맥 정보”를
**단어 생성에 알맞은 형태로 가공**함
- Non-linear 변환을 통해 더 복잡한 표현을 만들고
- Linear(단어 확률 계산)가 잘 작동하도록
**표현을 다듬는 작업**
> Attention 결과를 ‘단어 생성 가능한 상태’로 정리하는 단계
---
# ✔ 3) Linear + Softmax
**역할:**
- **FFN에서 정리된 벡터를 vocab 크기(예: 30,000)로 변환**
- **Softmax로 확률 계산**
- **가장 높은 확률의 단어 1개를 실제로 출력**
> 실제 단어 생성 단계

### 3.5 Positional Encoding {toggle="true"}
---
[image omitted: temporary Notion asset]
---


## 핵심

# ⭐ 초간단 정리
### ✔ Transformer는 RNN/CNN이 없어서 “순서 정보”를 스스로 알 수 없음
→ 단어가 몇 번째인지 정보를 따로 넣어줘야 함.
### ✔ 그래서 단어 벡터(embedding)에 **Positional Encoding(PE)** 를 더함
→ “이 단어는 문장에서 몇 번째다”라는 위치 정보 제공.
### ✔ Positional Encoding은 sin/cos 함수를 이용해 만듦
이유:
1. **상대적 거리 정보가 자연스럽게 표현됨**
sin/cos는 주기성을 가지고 있어서:
pos+k 의 PE가<br>pos의 PE와 일정한 선형관계를 유지함
→ attention은 상대 거리까지 학습 가능
2. **처음 본 길이의 문장에도 잘 일반화됨(확장성 좋음)**
sin/cos는 수학적 함수라 훈련 범위를 넘어도 잘 작동함<br>(= extrapolation 가능)
학습된 positional embedding(lookup table)도 가능하지만<br>sin/cos 방식이 긴 문장 처리에 유리해서 채택됨.
### ✔ 이렇게 위치 정보를 넣어주면
Self-Attention이 문장 전체를 보면서도
단어 순서를 제대로 이해할 수 있게 됨.

## 4. Why Self-Attention {toggle="true"}
---
[image omitted: temporary Notion asset]
[image omitted: temporary Notion asset]
---


## 표 1 설명

## ✔ 기준 1 — Complexity per layer (한 층에서 드는 계산량)
왜 비교하는가?
→ 모델이 얼마나 빠르게 학습/추론할 수 있는지 보기 위함
표를 보면:
<table header-row="true">
<tr>
<td>Layer</td>
<td>Complexity</td>
</tr>
<tr>
<td>Self-Attention</td>
<td>O(n² · d)</td>
</tr>
<tr>
<td>RNN</td>
<td>O(n · d²)</td>
</tr>
<tr>
<td>CNN</td>
<td>O(k · n · d²)</td>
</tr>
</table>

### self-attention이 O(n²)인 이유:
모든 단어가 모든 단어와 dot product(M×M)를 계산하기 때문 → n×n
### RNN이 O(n)인 이유:
순서대로 한 번만 읽음
### CNN이 O(k·n)인 이유:
각 위치마다 커널 크기 k만큼만 본다
---
## ✔ 기준 2 — Sequential Operations (얼마나 병렬화 가능한가?)
이게 매우 중요함.
<table header-row="true">
<tr>
<td>Layer</td>
<td>Sequential Ops</td>
</tr>
<tr>
<td>Self-Attention</td>
<td>**O(1)** (완전 병렬)</td>
</tr>
<tr>
<td>RNN</td>
<td>**O(n)** (직렬, 병렬 불가능)</td>
</tr>
<tr>
<td>CNN</td>
<td>**O(1)** (병렬)</td>
</tr>
</table>

### 왜 self-attention은 O(1)?
모든 단어가 동시에 attention score를 계산할 수 있기 때문.
순서가 필요 없음.
### 왜 RNN은 O(n)?
한 단어를 읽기 전에는 이전 단어를 반드시 읽어야 함 → 직렬적
---
## ✔ 기준 3 — Maximum Path Length (두 단어가 정보 전달되기까지 거쳐야 하는 단계 수)
이건 **long-range dependency(장기 의존성)** 학습에서 핵심임.
<table header-row="true">
<tr>
<td>Layer</td>
<td>Maximum Path Length</td>
</tr>
<tr>
<td>Self-Attention</td>
<td>**O(1)**</td>
</tr>
<tr>
<td>RNN</td>
<td>**O(n)**</td>
</tr>
<tr>
<td>CNN</td>
<td>**O(logk(n))**</td>
</tr>
<tr>
<td>Restricted SA</td>
<td>O(n/r)</td>
</tr>
</table>

### path length = 두 위치 사이의 정보 전달” 거리
예:
문장:
“The animal that I saw yesterday was huge.”
“animal”과 “was”는 거리가 멀다 → 장기 의존성
### RNN에서는:
`animal → that → I → saw → yesterday → was`
5단계나 거침 → O(n)
### CNN에서는:
커널이 작으면 여러 층을 거쳐야 함 → logk(n)
### self-attention에서는:
바로 한 번에 연결 가능 → **O(1)**
### 결론 1
> self-attention은 long-range dependency 학습이 가장 쉬움
### 결론 2
> 병렬성(O(1)) + 짧은 path length(O(1)) 때문에 Transformer가 훨씬 빠름



## 핵심 요약

1. **Self-Attention, RNN, CNN을 비교하는 이유**
→ Transformer는 self-attention만 쓰기 때문에
기존 레이어들과 성능 차이를 세 기준으로 비교해야 한다.
(계산량, 병렬성, long-range 의존성 학습 능력)
---
1. **기준 1: 계산 복잡도**
- Self-attention: O(n²·d)
- RNN: O(n·d²)
- CNN: O(k·n·d²)
→ self-attention은 계산량이 많아 보이지만 병렬화가 잘 된다.
---
1. **기준 2: Sequential Operations(병렬성)**
- Self-attention: O(1) → 완전 병렬
- RNN: O(n) → 단어 순서대로 처리해야 해서 느림
- CNN: O(1) → 병렬 가능
→ self-attention은 RNN보다 훨씬 빠르게 학습 가능.
---
1. **기준 3: Maximum Path Length(장기 의존성 학습)**
- Self-attention: O(1) → 모든 단어가 즉시 서로 볼 수 있음
- RNN: O(n) → 멀리 떨어진 단어까지 정보 전달이 오래 걸림
- CNN: O(logk(n)) → kernel이 작아 멀리 보려면 여러 층 필요
→ self-attention은 long-range dependency(장기 문맥) 학습에 가장 유리하다.
---
1. **Restricted Self-Attention(제한된 attention)**
- 긴 문장을 더 빠르게 처리하려면
attention을 주변 r개의 단어로 제한할 수도 있음
- 이 경우 path length는 O(n/r)로 늘어나지만 계산량 줄음.
---
1. **CNN의 단점(본문 후반 설명)**
- CNN은 한 번에 볼 수 있는 범위(receptive field)가 작음
- kernel이 작으면 멀리 떨어진 두 단어를 연결하기 위해
여러 층을 쌓아야 해서 path length가 길어진다.
- dilated CNN을 써도 self-attention만큼 효율적이지 않다.
---
1. **Self-Attention의 추가 장점: 해석 가능성**
- attention score를 보면 모델이 어떤 단어를 참고하는지 눈으로 확인 가능
- multi-head는 문법적/의미적 관계 같은 다른 패턴을 각자 학습하는 경향이 있음
→ 모델이 왜 그 예측을 했는지 분석 가능(interpretable)

Self-attention은 RNN/CNN보다 병렬성도 높고, 멀리 떨어진 단어 관계도 즉시 학습할 수 있으며, 모델 해석까지 쉬워서 sequence modeling에 매우 유리하다.


## 5. Training {toggle="true"}
---
이 절에서는 우리의 모델들을 위한 훈련 방식(training regime)을 설명한다.
---
### 5.1 Training Data and Batching {toggle="true"}
---
우리는 약 450만 개의 문장 쌍으로 이루어진 표준 WMT 2014 English–German 데이터셋에서 훈련을 수행하였다. 문장들은 byte-pair encoding [3]을 사용해 인코딩되었으며, 약 37000개의 source–target 공유 vocabulary를 가진다. English–French의 경우, 훨씬 더 큰 규모인 3600만 개 문장으로 이루어진 WMT 2014 English–French 데이터셋을 사용하였고, 토큰은 32000개의 word-piece vocabulary [38]로 분할하였다.
문장 쌍들은 시퀀스 길이가 비슷하도록 묶어 batch를 구성하였다. 각 training batch는 약 25000개의 source token과 25000개의 target token을 포함하는 문장 쌍 집합으로 구성되었다.
---


## BPE (Byte-pair encoding)

- **인코딩 = 텍스트 → 토큰 ID로 변환**
- **BPE = 단어를 subword 단위로 똑똑하게 쪼개는 알고리즘**
# ⭐ 왜 굳이 이런 인코딩을 쓸까?
단어 전체를 쓰면:
- “running”, “runner”, “runs”, “run”
모두 다른 단어임 → 단어장이 너무 커짐
문자 단위로 쓰면:
- 너무 길고 의미 파악이 어렵고 학습이 힘듦
그래서 중간 해결책이 **subword(BPE)** 임.
---
# ⭐ BPE의 장점
### 1) 모르는 단어(OOV)가 거의 없음
새로운 단어가 나와도 subword로 쪼개면 됨.
예:
```plain text
hyperoptimization → hyper + optimize + ation


```
### 2) 단어장 크기(=Vocabulary size)를 적당히 유지
너무 커지지도, 너무 작지도 않음.
### 3) 의미 단위에 맞게 쪼개져서 학습 효율 증가
비슷한 단어끼리 subword를 공유함.
- **BPE 인코딩 = 입력 문장을 토큰으로 쪼개는 전처리**
- **Transformer 인코딩 = 인코더가 토큰의 의미를 계산하는 내부 과정**



## BPE VS word-piece vocabulary

<table header-row="true">
<tr>
<td>구분</td>
<td>BPE</td>
<td>WordPiece</td>
</tr>
<tr>
<td>만든 곳</td>
<td>Sennrich et al.</td>
<td>Google</td>
</tr>
<tr>
<td>방식</td>
<td>가장 빈도 높은 글자쌍 병합</td>
<td>likelihood 기반 병합</td>
</tr>
<tr>
<td>출력 형태</td>
<td>subword</td>
<td>subword (## 사용)</td>
</tr>
<tr>
<td>장점</td>
<td>간단, 빠름</td>
<td>더 자연스러운 분해</td>
</tr>
</table>
<table header-row="true">
<tr>
<td>방법</td>
<td>병합 기준</td>
<td>비유</td>
</tr>
<tr>
<td>**BPE**</td>
<td>단순 빈도(자주 보이는 글자 쌍)</td>
<td>많이 보이면 그냥 합침</td>
</tr>
<tr>
<td>**WordPiece**</td>
<td>언어 모델 확률(likelihood)이 증가하는 조합</td>
<td>“합쳤을 때 더 말이 되는 subword인지”로 판단</td>
</tr>
</table>



## batch를 만들 때 문장 길이가 비슷한 것끼리 묶는 이유

Transformer는 패딩 비용이 크기 때문에
길이가 비슷한 문장을 묶어야 GPU 효율이 올라감.

### 5.2 Hardware and Schedule {toggle="true"}
---
우리는 하나의 머신에서 8개의 NVIDIA P100 GPU를 사용하여 모델들을 훈련하였다. 논문 전반에서 설명한 하이퍼파라미터를 사용하는 base model의 경우, 각 training step은 약 0.4초가 걸렸다. 우리는 base model을 총 100,000 step, 즉 약 12시간 동안 훈련하였다.
table 3의 마지막 줄에 설명된 big model의 경우, step당 걸리는 시간은 1.0초였으며, big model은 300,000 step(약 3.5일) 동안 훈련되었다.
---


## **Base Model의 하이퍼파라미터(논문의 기본 값)**

<table header-row="true">
<tr>
<td>이름</td>
<td>값</td>
<td>설명</td>
</tr>
<tr>
<td>**N (Encoder/Decoder layer 수)**</td>
<td>6</td>
<td>인코더 6층, 디코더 6층</td>
</tr>
<tr>
<td>**d_model**</td>
<td>512</td>
<td>임베딩 차원</td>
</tr>
<tr>
<td>**d_ff**</td>
<td>2048</td>
<td>FFN 내부 차원</td>
</tr>
<tr>
<td>**h (heads)**</td>
<td>8</td>
<td>Multi-head attention 헤드 수</td>
</tr>
<tr>
<td>**dropout**</td>
<td>0.1</td>
<td>드롭아웃</td>
</tr>
<tr>
<td>**optimizer**</td>
<td>Adam</td>
<td>β₁=0.9, β₂=0.98</td>
</tr>
<tr>
<td>**learning rate schedule**</td>
<td>warmup=4000</td>
<td>유명한 노란 곡선 스케줄</td>
</tr>
<tr>
<td>**label smoothing**</td>
<td>0.1</td>
<td>정답에 약간의 확률 분산</td>
</tr>
<tr>
<td>**batch size**</td>
<td>약 25k tokens</td>
<td>source 25k + target 25k</td>
</tr>
</table>



## Big Model은 어떻게 다른가?

<table header-row="true">
<tr>
<td>항목</td>
<td>Base</td>
<td>Big</td>
</tr>
<tr>
<td>d_model</td>
<td>**512**</td>
<td>**1024**</td>
</tr>
<tr>
<td>d_ff</td>
<td>**2048**</td>
<td>**4096**</td>
</tr>
<tr>
<td>heads</td>
<td>8</td>
<td>16</td>
</tr>
</table>

### 5.3 Optimizer {toggle="true"}
---
[image omitted: temporary Notion asset]
---


## 핵심

# ⭐ 먼저, 저 문단이 말하는 전체 내용 요약
- Transformer는 **Adam optimizer**라는 알고리즘으로 학습함
- Adam의 설정값은 β₁=0.9, β₂=0.98, ε=10⁻⁹
- 그리고 learning rate는 **고정이 아니라**, 아래 공식에 따라 계속 변함
- 초반에는 step이 올라갈수록 learning rate가 올라가고(warmup 단계)
- 이후에는 step이 증가할수록 learning rate가 점점 줄어듦
- 이 warmup 단계는 4000 step으로 설정됨
이제 개념 하나씩 아주 쉽게 풀어서 설명해줄게.
---
# ⭐ 1. Adam Optimizer가 뭐야?
신경망은 \*\*가중치(파라미터)\*\*를 계속 업데이트하면서 학습한다.
업데이트는 이런 식으로 이루어짐:
```plain text
weight ← weight - learning_rate * gradient


```
근데 이걸 더 똑똑하게 해주는 알고리즘이 Adam임.
✔ Adam =
SGD보다 빠르고 안정적으로 학습하도록 만든 **고급 옵티마이저**.
Adam은 아래 두 통계를 계속 추적함:
- **Momentum(모멘텀)**: 과거 gradient 평균
- **RMSProp 방식**: gradient 제곱값 평균
두 가지를 합쳐서 weight 업데이트를 더 안정적으로 한다.
---
# ⭐ 2. Adam의 β₁, β₂, ε는 뭐야?
이 값들은 Adam 내부 동작을 위한 **하이퍼파라미터**임.
### ✔ β₁ = 0.9
→ 모멘텀(gradient의 이동 평균)을 얼마나 강하게 적용할지
→ 0.9면 “과거 경향을 90% 정도 따라간다”는 뜻
### ✔ β₂ = 0.98
→ gradient 제곱 평균(변동성 추적)을 얼마나 강하게 적용할지
→ 0.98이면 거의 “평균을 천천히 업데이트한다”는 뜻
### ✔ ε = 10⁻⁹
→ 나눗셈에서 0으로 나누는 걸 방지하기 위해 넣는 작은 값
→ 안정성 향상용
이 세 값은 모두 **Adam optimizer 자체의 설정값**이다.
---
# ⭐ 3. Learning Rate(학습률)는 고정이 아니다
Transformer는 유명한 학습률 스케줄 공식이 있음:
lrate=dmodel−0.5⋅min⁡(step−0.5,  step⋅warmup−1.5)lrate = d_\{model\}\^\{-0.5\} \\cdot \\min(step\^\{-0.5\},\\; step \\cdot warmup\^\{-1.5\})
lrate=dmodel−0.5⋅min(step−0.5,step⋅warmup−1.5)
이 공식이 말하는 것:
---
### ⭐ 4. Warmup 단계(초반 학습)가 필요하다
초반에는 learning rate가 너무 크면 모델이 폭주함.
그래서:
- 처음 4000 step은 **선형적으로 증가함**
즉:
```plain text
learning_rate = step * warmup_steps^-1.5


```
---
### ⭐ 5. 그 후에는 학습률이 감소
4000 step 이후부터는 아래 부분이 작아짐:
step−0.5step\^\{-0.5\}
step−0.5
즉 step이 커질수록 **역제곱근**으로 learning rate 줄어듦.
---
# ⭐ 왜 이런 이상한 수식을 쓰는 거야?
아주 간단히:
- 첫 4000 step은 **모델이 안정적으로 방향을 잡도록** 천천히 키우고
- 이후에는 **폭주를 막기 위해** 천천히 줄인다
이 방식은 Transformer에서 매우 성능이 좋은 것으로 알려져 있음.
---
# ⭐ 최종 요약 (진짜 쉽게)
- **Adam optimizer**: 모델을 학습시키는 알고리즘
- β₁=0.9, β₂=0.98 → Adam 내부 설정값
- ε=10⁻⁹ → 안정성용 작은 수
- Learning rate는 아래 규칙을 따름:
✔ 처음 4000 step까지는 점점 커지고(warmup)
✔ 4000 이후부터는 점점 작아진다(step\^-0.5)
이 공식 덕분에 Transformer는 안정적으로, 빠르게 학습된다.

### 5.4 Regularization {toggle="true"}
---
[image omitted: temporary Notion asset]
---


## 핵심

Transformer는 과적합을 막고 더 안정적으로 학습시키기 위해
**3가지 정규화 기법**을 사용한다.
## **① Embedding Dropout**
- 어디?
**Input Embedding + Positional Encoding** 직후
- 역할?
입력 자체에 약간의 랜덤 잡음을 넣어 **일반화 성능 향상**
---
## **② Residual Dropout**
- 어디?
**각 sub-layer(MHA, FFN) 출력 → Add & Norm 직전**
- 역할?
레이어가 깊어지며 발생할 수 있는 **과적합을 방지**
※ 위 두 개는 모두 dropout이지만 **적용 위치와 목적이 다르므로**
논문에서는 **두 개의 regularization**으로 따로 센다.
---
## **③ Label Smoothing**
- 무엇?
정답을 one-hot(1/0) 대신 0.9/0.1로 “부드럽게” 만드는 기법
- 역할?
모델이 정답을 지나치게 확신하지 않게 하여
**과적합 감소 + BLEU 점수 상승**

## 6. Results {toggle="true"}
### 6.1 Machine Translation {toggle="true"}
---
[image omitted: temporary Notion asset]
[image omitted: temporary Notion asset]
---


## 표 2 내용

이 표는 **Transformer가 기존 모델들보다 BLEU 점수는 더 높고, 학습 비용은 훨씬 적다**는 사실을 보여준다.
## ① 기존 모델들 (ByteNet, GNMT, ConvS2S 등)
- BLEU 점수는 **24\~26** 수준
- 훈련 FLOPs는 **10¹⁹\~10²¹** (매우 큼)
- 특히 Ensemble 모델(여러 모델 결합)은 FLOPs가 엄청 큼
## ② Transformer (base)
- EN-DE BLEU = **27.3**
- EN-FR BLEU = **38.1**
- **FLOPs = 3.3 × 10¹⁸**
→ 기존 모델 대비 **수십 배 적은 비용**
## ③ Transformer (big)
- EN-DE BLEU = **28.4**
- EN-FR BLEU = **41.8** (표에서 최고 성능)
- FLOPs = **2.3 × 10¹⁹**
→ 여전히 GNMT/ConvS2S보다 상당히 저렴함
### ✔ 표가 말하는 핵심 메시지
**Transformer는 더 싸고, 더 빠르고, 더 정확하다.**
같은 FLOPs면 BLEU가 더 높고, 같은 BLEU면 FLOPs가 훨씬 적다.



## 핵심

## ✔ (1) English–German 번역 결과
- Transformer(big)는 기존 최고 성능보다 **2 BLEU 이상 높다**
- BLEU = **28.4** (새로운 state-of-the-art)
- 8개의 P100 GPU에서 **3.5일** 학습
- 기존 모델 및 ensemble보다 성능이 더 좋고 **학습 비용도 훨씬 적다**
---
## ✔ (2) English–French 번역 결과
- Transformer(big)는 BLEU **41.0** 달성
- 기존 모든 single model을 뛰어넘음
- 기존 state-of-the-art 모델 대비 **1/4 이하의 비용**으로 달성
- 이 실험에서는 dropout rate=**0.1** 사용
(English–German에서는 0.3 → 여기선 더 작은 0.1 사용)
---
## ✔ (3) Checkpoint Averaging 설명
- base model → 마지막 **5개** checkpoint average
- big model → 마지막 **20개** checkpoint average
→ 이렇게 하면 결과가 더 안정적
---
## ✔ (4) 번역 과정에서 사용한 beam search
- beam size = **4**
- length penalty α = **0.6**
- 최대 출력 길이 = 입력 길이 + 50
---
## ✔ (5) FLOPs 계산 방식
- FLOPs 계산할 때
- 훈련에 사용된 시간
- GPU 수
- GPU의 single-precision FLOP 성능
위 항목들을 고려해 계산했다고 명시



## Checkpoint Averaging

### ✔ 먼저, Checkpoint(체크포인트)란?
모델을 학습할 때 \*\*특정 step마다 저장해 두는 모델 상태(가중치)\*\*임.
예:
- step 90,000 시점 model → checkpoint #1
- step 91,000 시점 model → checkpoint #2
- step 92,000 시점 model → checkpoint #3
…
이렇게 모델의 가중치를 여러 버전으로 저장한다.
---
# ✔ Checkpoint Averaging이란?
> “여러 개의 checkpoint의 가중치를 평균 내서 하나의 모델을 만든다”
예:
```plain text
final_model_weight = (ckpt_1 + ckpt_2 + ckpt_3 + ckpt_4 + ckpt_5) / 5


```
Transformer 논문에서는:
- base model: 마지막 **5개** checkpoint 평균
- big model: 마지막 **20개** checkpoint 평균
---
# ✔ 왜 checkpoint averaging을 하는가?
### 1) 성능이 더 안정적
학습 후반에는 가중치가 요동칠 수 있음.
평균을 내면 **진동이 줄어들고 더 안정된 성능** 나온다.
### 2) 일반화 성능 향상
평균을 내면 마치 **ensemble 효과**가 생긴다.
(여러 모델을 합친 것과 비슷한 효과)
### 3) 실제로 BLEU 등 번역 성능이 올라감
그래서 많은 언어 모델들이 checkpoint averaging을 사용함.



## beam search

# ✔ 1. Beam Search가 뭐야?
디코더는 문장을 **한 단어씩** 생성함.
각 단계에서 “다음 단어 후보들”이 여러 개 나옴:
예)
- you (0.6)
- it (0.2)
- pizza (0.1)
- dogs (0.1)
문장은 여러 단어가 이어지므로
**전체 문장의 확률이 가장 높은 조합을 찾아야 한다.**
그런데 단 하나의 후보만 선택하면
초반 선택 실수 때문에 전체 문장이 망해버릴 수 있음.
그래서:
> beam search = 여러 개의 문장 후보를 동시에 계속 따라가며, 가장 좋은 문장을 선택하는 방법
---
# ✔ 2. beam size = 4
- “항상 가장 가능성 높은 문장 후보 4개를 유지”
- 각 단어를 생성할 때마다
후보 문장을 4개씩 확장 후 → 가장 좋은 4개만 남김
- beam size가 크면 정확도↑, 계산량↑
---
# ✔ 3. length penalty α = 0.6
딥러닝 번역 모델은 종종 **너무 짧은 문장**을 만들어버림
예: “Yes.”처럼 짧고 확률만 높은 문장
그래서:
> 문장이 너무 짧으면 점수를 깎는 보정값이 length penalty
α = 0.6 → 적당히 보정해서 자연스러운 길이 유도
---
# ✔ 4. 최대 출력 길이 = 입력 길이 + 50
모델이 멈추지 않고 너무 길게 생성하는 것을 막기 위해
예:
입력 길이 10 → 최대 출력 60 단어까지
입력 길이 20 → 최대 출력 70 단어까지
---
# ✔ 최종 초간단 요약
- **Beam Search**: 한 문장만 고르지 않고 **여러 후보를 동시에 탐색**하는 번역 알고리즘
- **Beam size 4**: 항상 4개의 문장 후보를 유지
- **Length penalty 0.6**: 너무 짧은 문장을 불리하게 만들어서 자연스러운 문장 생성
- **출력 최대 길이 = 입력 + 50**: 너무 긴 문장을 방지하기 위한 안전장치



## FLOPs(Floating Point Operations)

### ✔ FLOPs란?
모델이 학습하거나 추론할 때 수행해야 하는
**부동소수점 연산(Floating Point Operation)의 총 개수**를 말함.
즉:
- “얼마나 많은 계산을 했는가”를 나타내는 수치
- 모델의 **연산 비용**, **학습 비용**, **추론 비용**을 비교하는 기준

### 6.2 Model Variations {toggle="true"}
---
[image omitted: temporary Notion asset]
[image omitted: temporary Notion asset]
---


## 표 3 설명

# ✔ A. Attention Head 수(h)와 head 차원(dₖ, dᵥ) 변화 실험
### 무엇을 변경?
- attention head 수를 1, 4, 16, 32로 바꿨음
- 그에 맞춰 dk, dv도 변경됨
### 결과(표)
- head가 1개(single-head) → BLEU **낮아짐** (24.9)
- head가 너무 많아도(32개) → BLEU **떨어짐** (25.4)
- 기본값인 8-head가 가장 좋음 (25.8)
### 해석
- head 수가 적으면 다양한 관점을 못 보고
- head 수가 너무 많아도 차원 분할이 너무 작아져서 성능 저하
→ **8-head가 가장 균형 좋음**
---
# ✔ B. Key/Value 차원(dₖ) 줄이기 실험
### 무엇을 변경?
- key dimension dk를 16, 32로 줄여봄
### 결과
- dk 16 → BLEU 25.1
- dk 32 → BLEU 25.4
기본보다 성능 떨어짐
### 해석
- attention에서 Q·K 내적의 품질이 나빠져서 성능 하락
→ 너무 작은 dk는 **좋지 않음**
---
# ✔ C. FFN 내부 차원(d_ff) 변경 + layer 개수(N) 변경
### 무엇을 변경?
- FFN 차원 1024, 4096 등 다양하게 변경
- 레이어 수 N = 2, 4, 8으로 변경
### 결과
- FFN을 크게 하면 BLEU 상승 (26.0\~26.2)
- dropout을 늘리는 경우 오버피팅 방지에 효과
- 레이어 수 증가(2 → 4 → 8)하면 BLEU 상승
### 해석
- FFN 차원이 크고 레이어가 깊을수록 모델 표현력이 좋아짐
→ 일반적인 딥러닝 성능 향상 패턴
---
# ✔ D. Dropout 변화 실험(P_drop)
### 무엇을 변경?
- dropout = 0.0, 0.2로 변경
### 결과
- dropout 0.0 → BLEU **떨어짐** (25.3)
- dropout 0.2 → BLEU **조금 상승** (25.5\~25.7)
### 해석
- dropout이 없으면 과적합
- 0.2 정도면 일반화 효과 좋음
dropout이 상당히 유용하다는 것을 확인한 실험.
---
# ✔ E. Positional Encoding 방식 변경
### 무엇을 변경?
- 기존 sin/cos positional encoding을
**학습형 positional embedding**으로 교체
### 결과
- BLEU 거의 동일 (25.7 vs 25.8)
### 해석
- 학습형 positional encoding도 잘 작동하지만
- sin/cos 방식이 extrapolation(긴 문장 처리)에 유리하기 때문에
논문은 sin/cos 방식을 최종 선택했다고 말함
---
# ✔ big model (마지막 줄)
- d_model = 1024
- d_ff = 4096
- head = 16
- BLEU = **26.4** (표 전체 중 가장 좋음)
- PPL = **4.33** (가장 낮음 → 가장 좋은 언어모델 품질)
이게 실제 논문에서 사용한 “Transformer big” 모델임



## PPL이 뭐야? (Perplexity)

**PPL = Perplexity (언어 모델 품질 지표)**
한 줄로 말하면,
> “모델이 다음 단어를 얼마나 잘 예측하는지 나타내는 점수”
값이 **낮을수록 좋음**.



## positional encoding 두 방식

<table header-row="true">
<tr>
<td>방식</td>
<td>어떻게 생김?</td>
<td>장점</td>
<td>단점</td>
</tr>
<tr>
<td>**Sinusoidal (공식 기반)**</td>
<td>sin/cos로 위치 벡터 생성</td>
<td>훈련보다 긴 문장에도 잘 됨</td>
<td>학습적 유연성 낮음</td>
</tr>
<tr>
<td>**Learned (학습형)**</td>
<td>위치마다 임베딩 학습</td>
<td>데이터 많으면 매우 잘됨</td>
<td>긴 문장 extrapolation 불가</td>
</tr>
</table>
- **Sin/Cos positional encoding**: 수학 공식 기반 → 긴 문장에 강함
- **Learned positional embedding**: 임베딩 학습 방식 → 유연하지만 길이 extrapolation 약함



## 핵심

<table header-row="true">
<tr>
<td>실험 항목</td>
<td>결론</td>
</tr>
<tr>
<td>A. head 수/크기</td>
<td>너무 적어도 많아도 안 좋고 8-head가 최적</td>
</tr>
<tr>
<td>B. d_k 줄이기</td>
<td>성능 떨어짐 → attention 품질 저하</td>
</tr>
<tr>
<td>C. FFN 크기/깊이</td>
<td>커질수록, 깊을수록 성능 좋아짐</td>
</tr>
<tr>
<td>D. dropout</td>
<td>반드시 필요, 성능 향상</td>
</tr>
<tr>
<td>E. positional encoding</td>
<td>학습형도 OK, 하지만 sin/cos이 더 범용적</td>
</tr>
</table>

## A) head 수와 head 차원(d_k, d_v) 변화
- head=1 → BLEU 0.9 감소
- head 너무 많아도 성능 저하
→ **multi-head는 적당한 개수가 최적** (8-head)
---
## (B) key dimension(d_k) 줄이면 성능 떨어짐
- attention은 Q·K 내적 기반인데
dimension이 너무 작으면 품질이 떨어짐
→ dot-product attention이 동작하기 어려움
---
## (C) FFN 확장과 layer 깊이 증가
- FFN 사이즈 크게 하고
- dropout 더 잘 적용하면
→ **커질수록 성능이 좋아짐**
→ FFN의 역할이 매우 중요함
---
## (D) dropout 변화
- dropout 없으면 오버피팅 심해짐
- dropout은 매우 유용함
---
## (E) positional encoding 비교
- sinusoidal vs learned positional embedding
→ 성능 거의 동일
→ 하지만 sin/cos 방식은 extrapolation 가능해 더 선호됨

### 6.3 English Constituency Parsing {toggle="true"}
---
[image omitted: temporary Notion asset]
[image omitted: temporary Notion asset]
---


## 표 4 정리

- **4-layer Transformer**
→ 얕은 Transformer(4층)로 파싱 작업을 수행한 것.
- **WSJ-only(4만 문장)**
→ 적은 데이터만 가지고 학습 → 그래도 기존 모델과 비슷한 성능(91.3).
- **Semi-supervised(반지도 학습)**
→ 정답 없는 1,700만 문장을 추가로 사용
→ Transformer 성능이 92.7까지 크게 상승.
- 결론
→ Transformer는 번역뿐 아니라 **문장 구조 분석 작업에서도 매우 강력한 모델임**
→ 적은 데이터에서도 잘하고, 큰 데이터에서는 거의 SOTA 수준



## 핵심

## 1) Transformer가 번역뿐 아니라 다른 복잡한 NLP 작업에도 잘 되는지 확인하려고
→ “구문 분석(Constituency Parsing)”에 적용해봄.
## 2) WSJ-only로 학습 (4-layer, d_model=1024)
- 데이터: WSJ 4만 문장(정답 있는 데이터)
- 하이퍼파라미터: 번역 모델과 거의 동일
(dropout, beam size 정도만 약간 조정)
- 결과: **91.3 F1**
→ 기존 strong 모델들과 비슷한 수준
## 3) Semi-supervised로 학습 (WSJ + 1700만 문장)
- unlabeled 대규모 텍스트에 parser(Parsing Models)를 써서 의사 정답(pseudo-label)을 붙임
- vocabulary 크기는 더 크게(16K → 32K)
- 결과: **92.7 F1**
→ 기존 semi-supervised 모델들도 앞지름
## 4) 추론(inference) 시 설정
- beam size = 21
- length penalty α = 0.3
- 최대 출력 길이 = 입력 길이 + 300
→ 파싱 결과(트리)를 안정적으로 생성하기 위한 설정
Transformer 디코더는 **문장을 생성할 때 끝없이 단어(토큰)를 만들어낼 위험**이 있음.
즉, 멈출 타이밍을 잘못 잡으면 **출력이 무한히 길어질 수도 있음**.
그래서 **안전장치**가 필요함:
> 출력 문장은 어느 길이를 넘지 못하게 최대 길이를 제한한다.
## 5) 결론
- 특별한 파싱 전용 구조나 튜닝 없이도
**Transformer는 구문 분석에서도 매우 높은 성능**
- 작은 데이터에서도 잘하고
큰 데이터(반지도 학습)에서는 거의 최고 수준(SOTA)에 근접

## 7. Conclusion {toggle="true"}
---
이 연구에서 우리는 Transformer를 제시하였다. Transformer는 전체가 attention에만 기반한 최초의 sequence transduction 모델로, encoder–decoder 구조에서 일반적으로 사용되는 recurrent layer를 multi-headed self-attention으로 대체하였다.
번역 작업에 대해 Transformer는 recurrent layer나 convolutional layer 기반의 구조보다 훨씬 빠르게 훈련될 수 있다. WMT 2014 English-to-German과 WMT 2014 English-to-French 두 번역 작업 모두에서 우리는 새로운 state-of-the-art를 달성하였다. 특히 English-to-German 작업에서는 우리의 최고 모델이 이전에 보고된 모든 ensemble 모델보다도 더 뛰어난 성능을 보였다.
우리는 attention 기반 모델들의 미래를 매우 긍정적으로 보고 있으며, 이를 다른 작업에도 적용할 계획이다. Transformer를 텍스트 이외의 입력과 출력 modality를 포함하는 문제로 확장하고, 이미지·오디오·비디오처럼 큰 입력과 출력에 대해 효율적으로 처리할 수 있도록 local하고 제한된 attention 메커니즘을 연구할 계획이다. 생성 과정의 순차성을 줄이는 방향도 우리의 연구 목표 중 하나이다.
모델을 훈련하고 평가하는 데 사용한 코드는 다음에서 확인할 수 있다:
[https://github.com/tensorflow/tensor2tensor](https://github.com/tensorflow/tensor2tensor)
---
### figure 3 - 장거리 의존성 {toggle="true"}
[image omitted: temporary Notion asset]
그림 3: 6개의 인코더 층 중 5번째 층에서 self-attention이 장거리 의존성을 따라가는 모습을 보여주는 예시이다. 여러 attention head들이 동사 ‘making’과 멀리 떨어진 단어에 주목하여, 문장에서 ‘making ... more difficult’이라는 구를 완성하는 데 필요한 장거리 의존 관계를 포착하고 있다. 그림에서는 ‘making’이라는 단어에 대한 attention만 표시했다. 색깔이 다른 것은 서로 다른 head를 의미한다. 색상이 있는 상태로 볼 때 가장 잘 보인다.<br>
- 이 그림은 self-attention이 ‘making’과 멀리 떨어진 단어들(특히 ‘more difficult’)을 연결해서 본다는 것을 보여주는 예시임.
- 여러 색의 선은 각기 다른 attention head가 ‘making’과 관련된 단어를 찾고 있다는 것을 의미함.
- 즉, Transformer가 문장 내 장거리 의존성을 잘 파악한다는 것을 보여주는 그림임.
### figure 4 - 대명사 찾기 {toggle="true"}
[image omitted: temporary Notion asset]
그림 4: 6개 중 5번째 층에 있는 두 개의 attention head가 대용어(대명사) 해석(anaphora resolution)에 관여하고 있는 것으로 보이는 예시이다. 위쪽은 head 5의 전체 attention을 보여준다. 아래쪽은 단어 ‘its’에 대해서만 attention head 5와 6이 보이는 분리된(attention만 따로 뽑은) attention들을 보여준다. 이 단어에 대한 attention이 \*\*매우 날카롭다(sharp)\*\*는 점에 주목하라.
- 이 그림은 단어 ‘its’가 문장에서 무엇을 가리키는지(self-attention이 어떻게 대명사 참조를 해결하는지) 보여주는 예시임.
- 위쪽 그림은 attention head 5가 문장의 모든 단어에 어떻게 주목하는지 전체 분포를 보여주는 그림임.
- 아래쪽 그림은 ‘its’라는 단어만 기준으로, attention head 5와 6이 특정 단어(예: ‘Law’)에 강하게 집중하는 모습을 따로 보여주는 그림임.
- self-attention이 ‘its’의 지시 대상이 되는 단어를 정확하게 찾아 sharply(attention이 뚜렷하게) 집중한다는 것을 보여주는 그림임.
### figure 5 - Head들은 서로 다른 패턴 학습 {toggle="true"}
[image omitted: temporary Notion asset]
그림 5: 많은 attention head들이 문장의 구조와 관련되어 보이는 행동을 보인다. 우리는 위에서 두 가지 예시를 제시하는데, 이는 6개 중 5번째 인코더 self-attention 층에 있는 서로 다른 두 head에서 가져온 것이다. 이 head들은 명확히 서로 다른 작업을 수행하도록 학습되었다.
Transformer의 서로 다른 attention head들은 문장의 구조를 서로 다른 방식으로 해석하는 각기 다른 “역할”을 배운다는 것을 보여주는 그림이야.
### 좀 더 구체적으로 말하면
- 위(초록색)와 아래(빨간색) 두 그림은 서로 다른 attention head 두 개를 보여줌.
- 두 head 모두 같은 문장을 보고 있지만, 각 head가 집중하는 단어와 연결 방식이 완전히 다름.
- 즉, 어떤 head는 문장의 앞부분—뒷부분을 연결하는 패턴을 배우고,
다른 head는 대명사 관계, 동사-목적어 관계, 문장 구조적 연결 등을 잡아내는 역할을 학습함.

---

# AlexNet

---
## 목차
---
### 논문 전체 요약 {toggle="true"}
> ImageNet 대회에서 120만 장의 이미지를 1000개 클래스로 분류하는 딥러닝 모델 개발
- **🏗️ 네트워크 구조**
- **8개 레이어** (5개 합성곱 + 3개 완전연결)
- **6000만 개 파라미터**, 65만 개 뉴런
- 최종 출력: 1000-way Softmax
- **2개의 GTX 580 GPU**에 분산 학습 (5\~6일 소요)
- **🔑 주요 기술적 기여**
<table header-row="true">

<tr>
<td>**기법**</td>
<td>**효과**</td>
</tr>
<tr>
<td>**ReLU** 활성화 함수</td>
<td>tanh 대비 학습 속도 6배 향상</td>
</tr>
<tr>
<td>**다중 GPU 학습**</td>
<td>Top-1/5 오류율 각 1.7%, 1.2% 감소</td>
</tr>
<tr>
<td>**Local Response Normalization**</td>
<td>Top-1/5 오류율 각 1.4%, 1.2% 감소</td>
</tr>
<tr>
<td>**Overlapping Pooling**</td>
<td>Top-1/5 오류율 각 0.4%, 0.3% 감소</td>
</tr>
<tr>
<td>**Data Augmentation**</td>
<td>이미지 반전·크롭·RGB PCA로 과적합 억제</td>
</tr>
<tr>
<td>**Dropout**</td>
<td>완전연결층에 적용, 과적합 대폭 감소</td>
</tr>
</table>

### Abstract {toggle="true"}
#### 해석
---
우리는 1.2백만 개의 고해상도 이미지를 포함한 ImageNet LSVRC-2010 대회의 1000개의 서로 다른 클래스를 분류하기 위해 큰 규모의 깊은 합성곱 신경망(convolutional neural network)을 훈련시켰습니다. 테스트 데이터에서 우리는 top-1과 top-5 오류율이 각각 37.5%와 17.0%를 달성했으며, 이는 이전의 최신 기술(state-of-the-art)보다 상당히 더 나은 성능입니다.** **이 신경망은 6천만 개의 매개변수와 65만 개의 뉴런으로 구성되어 있으며, 다섯 개의 합성곱 층(그중 일부는 최대 풀링 층이 뒤따름)과 세 개의 완전 연결 층, 그리고 최종적으로 1000방향 소프트맥스(softmax) 층으로 이루어져 있습니다. 학습 속도를 빠르게 하기 위해 우리는 포화되지 않는(non-saturating) 뉴런과 합성곱 연산의 매우 효율적인 GPU 구현을 사용했습니다. 완전 연결 층에서 과적합을 줄이기 위해, 우리는 “드롭아웃(dropout)”이라고 불리는 최근에 개발된 정규화 방법을 사용했으며, 이는 매우 효과적인 것으로 입증되었습니다. 우리는 또한 이 모델의 변형 버전을 ILSVRC-2012 대회에 출전시켰고, 테스트 데이터에서 top-5 오류율 15.3%를 달성했으며, 이는 두 번째로 좋은 참가자가 기록한 26.2%보다 우수한 결과였습니다.
---
#### 요약
---
- **무엇을 했나** :  120만 장 이미지를 1000개 클래스로 분류하는 대형 CNN을 학습시킴
- **모델 스펙**
- 파라미터 6000만 개, 뉴런 65만 개
- 5개 합성곱 레이어 + 3개 완전연결 레이어
- 최종단 1000-way Softmax
- **핵심 기법 3가지**
- **빠른 학습** → 비포화 뉴런(ReLU) + GPU 병렬 연산
- **과적합 방지** → Dropout 적용
- **대규모 처리** → GPU 최적화 구현
---
#### QnA
---


## **모델 스펙 자세히 - 합성곱, 완전연결 등**

[image omitted: temporary Notion asset]



## **Layer 순서**

[image omitted: temporary Notion asset]



## **non-saturationg이란?**

[image omitted: temporary Notion asset]
- **"포화된다"** = 함수의 출력값이 어떤 한계에 딱 달라붙어서 더 이상 변하지 않는 상태
- tanh는 입력이 아무리 크든 작든 출력이 항상 **-1과 +1 사이**에 갇혀버림
- 이 상태에서 기울기가 0에 가까워지면 신경망이 "얼마나 틀렸는지"를 역전파로 전달하지 못해서 학습이 거의 멈춰버림
- = **기울기 소실 문제**

---
### 1. Introduction {toggle="true"}
#### 해석
---
현재의 객체 인식 접근법들은 본질적으로 머신러닝 방법들을 사용합니다. 그들의 성능을 향상시키기 위해, 우리는 더 큰 데이터셋을 수집하고, 더 강력한 모델을 학습시키며, 과적합을 방지하기 위한 더 나은 기법들을 사용할 수 있습니다. 최근까지 라벨이 지정된 이미지 데이터셋들은 비교적 작았으며, 대략 수만 장의 이미지 수준이었습니다(예: NORB [16], Caltech-101/256 [8, 9], 그리고 CIFAR-10/100 [12]). 간단한 인식 과제들은 이러한 크기의 데이터셋으로도 꽤 잘 해결될 수 있으며, 특히 라벨을 유지하는 변환(label-preserving transformations)을 통해 데이터셋을 확장한 경우에는 더욱 그렇습니다. 예를 들어, MNIST 숫자 인식 과제에서 현재 최고 오류율(\<0.3%)은 인간의 성능에 근접합니다 [4]. 그러나 현실적인 환경에서의 객체들은 상당한 변동성을 보이므로, 그것들을 인식하도록 학습하기 위해서는 훨씬 더 큰 학습 세트가 필요합니다. 그리고 실제로, 작은 이미지 데이터셋의 한계점들은 널리 인식되어 왔습니다(예: Pinto et al. [21]). 하지만 라벨이 붙은 수백만 장의 이미지 데이터셋을 수집하는 것이 가능해진 것은 최근에 와서야였습니다. 새롭고 더 큰 데이터셋에는 완전하게 분할된 수십만 장의 이미지를 포함하는 LabelMe [23], 그리고 22,000개 이상의 범주에 걸쳐 1,500만 장 이상의 라벨이 붙은 고해상도 이미지를 포함하는 ImageNet [6]이 있습니다.

과거 단순한 인식 과제는 적은 데이터셋으로 가능, but 현실 세계 객체 인식은 더 큰 데이터셋 필요하고 이게 요즘 나온 ImageNet 데이터셋이 있다

수백만 장의 이미지로부터 수천 개의 객체를 학습하기 위해서는, 우리는 큰 학습 용량을 가진 모델이 필요합니다. 그러나 객체 인식 과제의 엄청난 복잡성은, 이 문제가 ImageNet만큼 큰 데이터셋으로도 완전히 명시될 수 없음을 의미합니다. 따라서, 우리 모델은 우리가 가지고 있지 않은 모든 데이터를 보완하기 위해 많은 사전 지식을 가지고 있어야 합니다. 합성곱 신경망(Convolutional Neural Networks, CNNs)은 이러한 종류의 모델 중 하나를 구성합니다 [16, 11, 13, 18, 15, 22, 26]. 이들의 용량은 깊이(depth)와 너비(breadth)를 조절함으로써 통제할 수 있으며, 또한 이미지의 본질에 대해 강력하고 대부분 올바른 가정을 합니다(즉, 통계의 정상성(stationarity of statistics)과 픽셀 의존성의 국소성(locality of pixel dependencies)). 따라서, 유사한 크기의 층을 가진 표준 피드포워드 신경망과 비교했을 때, CNN은 훨씬 더 적은 연결과 매개변수를 가지며, 그 결과 더 쉽게 학습될 수 있습니다. 반면에, 이론적으로 가능한 최고의 성능은 약간만 더 나쁠 가능성이 있습니다.

데이터셋이 많아지면 뭐해 그걸 돌릴 수 있는 모델이 필요한데 → CNN

CNN의 매력적인 특성들에도 불구하고, 그리고 그들의 국소적 아키텍처의 상대적인 효율성에도 불구하고, 여전히 고해상도 이미지에 대규모로 적용하기에는 비용이 지나치게 많이 들었습니다. 다행히도, 현재의 GPU들은 고도로 최적화된 2차원 합성곱(2D convolution) 구현과 결합되어, 흥미로울 정도로 큰 CNN을 학습할 수 있을 만큼 충분히 강력합니다. 또한, ImageNet과 같은 최근의 데이터셋들은 그러한 모델들을 심각한 과적합 없이 학습시키기에 충분한 수의 라벨이 지정된 예시들을 포함하고 있습니다.

CNN있으면 뭐해, 아직도 고해상도 이미지에 대규모로 적용하기에 비용이 많이 들어!
→ GPU를 사용하자 

이 논문의 구체적인 기여는 다음과 같습니다. 우리는 ILSVRC-2010과 ILSVRC-2012 대회 [2]에서 사용된 ImageNet의 하위집합들에 대해 지금까지 중 가장 큰 합성곱 신경망 중 하나를 학습시켰으며, 이 데이터셋들에서 지금까지 보고된 것 중 가장 뛰어난 결과를 달성했습니다. 우리는 2차원 합성곱과 합성곱 신경망 학습에 내재된 모든 다른 연산들에 대한 고도로 최적화된 GPU 구현을 작성하였으며, 이를 공개적으로 제공했습니다¹. 우리의 네트워크는 성능을 향상시키고 학습 시간을 단축시키는 여러 가지 새롭고 특이한 기능들을 포함하고 있으며, 이러한 내용은 3장에서 자세히 설명됩니다. 우리의 네트워크의 크기는 120만 개의 라벨이 붙은 학습 예제가 있음에도 불구하고 과적합을 중요한 문제로 만들었기 때문에, 우리는 과적합을 방지하기 위한 몇 가지 효과적인 기법들을 사용하였으며, 이는 4장에서 설명됩니다. 우리의 최종 네트워크는 다섯 개의 합성곱 층과 세 개의 완전 연결 층을 포함하고 있으며, 이러한 깊이는 중요한 것으로 보입니다. 우리는 어떤 합성곱 층이라도 제거할 경우(각 층은 모델의 전체 매개변수의 1% 미만만을 포함함) 성능이 저하된다는 것을 발견했습니다.

3장: 성능 향상 및 학습 단축시키는 여러 가지 새롭고 특이한 기능<br>4장: 과적합을 방지하기 위한 몇 가지 효과적인 기법<br>🌟 우리가 만든 네트워크는 5개의 합성곱 층과 3개의 완전 연결층이고 이건 중요해<br>       → 하나라도 없으면 성능 저하

결국, 네트워크의 크기는 주로 현재 GPU에서 사용 가능한 메모리의 양과 우리가 감수할 수 있는 학습 시간의 양에 의해 제한됩니다. 우리의 네트워크는 두 개의 GTX 580 3GB GPU에서 학습하는 데 5일에서 6일 사이가 걸립니다. 우리의 모든 실험은, 더 빠른 GPU와 더 큰 데이터셋이 사용 가능해지기를 단순히 기다림으로써 결과를 향상시킬 수 있음을 시사합니다.

네트워크의 크기는 현재 GPU에서 사용 가능한 메모리의 양과 우리가 감수할 수 있는 학습 시간의 양에 의해 제한돼!

---
#### 요약
---
- **왜 이 연구를 했나?**
기존 이미지 인식 모델들은 NORB, CIFAR, Caltech 같은 **수만 장짜리 소규모 데이터셋**으로 학습했어요. 단순한 문제는 이 정도로도 풀렸지만, 현실의 사물들은 훨씬 복잡하고 다양하기 때문에 한계가 명확했습니다.
그런데 최근 LabelMe, **ImageNet(1500만 장, 22,000개 카테고리)** 같은 초대규모 데이터셋이 등장하면서 상황이 바뀌었어요.
- **큰 데이터엔 큰 모델이 필요**
데이터가 커지면 그걸 소화할 수 있는 **학습 용량이 큰 모델**이 필요합니다. 그 해답으로 저자들이 선택한 게 CNN이에요. CNN은 두 가지 장점이 있습니다.
- 이미지의 특성(픽셀은 주변 픽셀과 연관됨)을 잘 반영하는 구조
- 같은 성능의 일반 신경망보다 **파라미터 수가 훨씬 적어** 학습이 쉬움
- **그럼 왜 지금까지 CNN을 안 썼나?**
고해상도 대규모 이미지에 CNN을 적용하는 건 **계산량이 너무 많아서** 현실적으로 불가능했어요. 그런데 두 가지가 해결해줬습니다.
- **GPU**의 발전 → 대규모 병렬 연산 가능
- **ImageNet** 같은 대규모 데이터 → 과적합 없이 깊은 모델 학습 가능
- **이 논문이 기여한 것**
- ILSVRC 대회에서 **압도적 1위** 달성
- ReLU, Dropout, GPU 병렬학습 등 **새로운 기법들** 도입
- 모델 깊이가 중요하다는 것을 실험으로 증명 (합성곱 레이어 하나만 빼도 성능이 뚝 떨어짐)
- 향후 더 빠른 GPU와 더 큰 데이터셋으로 성능이 계속 개선될 것이라고 전망
---
#### QnA
---


## **통계의 정상성(stationarity of statistics)과 픽셀 의존성의 국소성(locality of pixel dependencies)**

[image omitted: temporary Notion asset]

---
### 2. The Dataset {toggle="true"}
#### 해석
---
ImageNet은 약 22,000개의 범주에 속하는 1,500만 장이 넘는 라벨이 붙은 고해상도 이미지들로 구성된 데이터셋입니다. 이 이미지들은 웹에서 수집되었으며, Amazon의 Mechanical Turk 크라우드소싱 도구를 사용한 인간 라벨러들에 의해 라벨이 지정되었습니다. 2010년부터 Pascal Visual Object Challenge의 일환으로, ImageNet 대규모 시각적 인식 챌린지(ImageNet Large-Scale Visual Recognition Challenge, ILSVRC)라는 연례 대회가 열리고 있습니다. ILSVRC는 ImageNet의 하위집합을 사용하며, 각 1000개의 범주(category)마다 약 1000장의 이미지를 포함합니다. 전체적으로 약 120만 장의 학습 이미지, 5만 장의 검증 이미지, 그리고 15만 장의 테스트 이미지가 있습니다.

대회에서는 Category: 1000개 <br>각 Category마다: 1000장의 이미지<br>= 전체적으로, 약 120만장의 학습 이미지, 5만 장의 검증 이미지, 15만 장의 테스트 이미지

ILSVRC-2010은 테스트 세트의 라벨이 제공되는 유일한 ILSVRC 버전이므로, 우리는 대부분의 실험을 이 버전에서 수행했습니다. 또한 우리는 ILSVRC-2012 대회에도 우리의 모델을 출전시켰기 때문에, 6장에서 테스트 세트 라벨이 제공되지 않는 이 버전의 데이터셋에 대한 우리의 결과도 보고합니다. ImageNet에서는 일반적으로 두 가지 오류율을 보고하는 것이 관례입니다: top-1과 top-5. 여기서 top-5 오류율은 모델이 가장 가능성이 높다고 판단한 다섯 개의 라벨 중에 정답 라벨이 포함되지 않은 테스트 이미지의 비율을 의미합니다.

6장에서는 대회에 대한 실험 결과와 성능 보고가 나올꺼야<br>ImageNet에서는 일반적으로 두 가지 오류율을 보고해: Top-1, Top-5

ImageNet은 해상도가 다양한 이미지들로 구성되어 있지만, 우리의 시스템은 일정한 입력 차원을 필요로 합니다. 따라서 우리는 이미지를 256×256의 고정된 해상도로 다운샘플링했습니다. 직사각형 이미지를 주어졌을 때, 우리는 먼저 짧은 변의 길이가 256이 되도록 이미지를 리스케일한 후, 그 결과 이미지에서 중앙의 256×256 패치를 잘라냈습니다. 우리는 학습 세트 전체에서의 평균 활성(mean activity)을 각 픽셀에서 빼주는 것을 제외하고는, 이미지를 다른 어떤 방식으로도 전처리하지 않았습니다. 따라서 우리는 픽셀의 (중심화된) 원시 RGB 값들로 우리의 네트워크를 학습시켰습니다.

우리는 일정한 입력 차원을 필요로해 → 256 by 256 다운 샘플링<br><br>if) 직각사각형 이미지가 주어지면<br>1. 짧은 변의 길이가 256가 되도록 이미지 리스케일링<br>2. 그 결과 이미지에서 중앙 256 by 256 패치를 잘라냄<br>3. 해당 이미지에서 평균 활성(Mean Activity)을 각 픽셀에서 빼줌 = 픽셀을 0 근처로 중심화

---
#### 요약
---
- **ImageNet이란?**
- 웹에서 수집한 **1500만 장 이상**의 고해상도 이미지, **22,000개** 카테고리
- Amazon Mechanical Turk(크라우드소싱)으로 사람이 직접 라벨링
- **성능 평가 기준**
두 가지 오류율로 평가합니다.
- **Top-1 오류율**: 모델이 가장 높은 확률로 예측한 1개가 틀린 비율
- **Top-5 오류율**: 확률 상위 5개 안에 정답이 없는 비율
- **이미지 전처리**
ImageNet은 이미지 크기가 제각각이라 통일이 필요했어요.
1. 짧은 쪽이 **256픽셀**이 되도록 비율 유지하며 축소
2. 중앙을 **256×256**으로 자름
3. 각 픽셀에서 **학습셋 평균값을 빼줌** (정규화)
4. 그 외 별도 전처리 없이 **RGB 원본값** 그대로 사용
---
#### QnA
---


## **왜 각 픽셀에서 학습셋 평균값을 빼주는가?**

[image omitted: temporary Notion asset]

---
### 3. The Architecture {toggle="true"}
#### 해석
---
우리 네트워크의 구조는 그림 2에 요약되어 있습니다. 이 네트워크는 다섯 개의 합성곱 층과 세 개의 완전 연결 층을 포함하여 총 여덟 개의 학습된 층으로 이루어져 있습니다. 아래에서는 우리 네트워크 구조의 새롭거나 특이한 몇 가지 특징들을 설명합니다. 3.1절부터 3.4절까지는 우리가 판단한 중요도에 따라 정렬되어 있으며, 가장 중요한 항목이 먼저 제시됩니다.
---
### 3.1 ReLU Nonlinearity (Fig.1) {toggle="true"}
#### 해석
---


[image omitted: temporary Notion asset]


[image omitted: temporary Notion asset]


뉴런의 출력 f를 입력 x의 함수로 모델링하는 표준적인 방법은
```math
f(x)=tanh(x)\quad또는\quad f(x)=(1+e^{−x})^{−1}
```
형태를 사용하는 것입니다. 그러나 경사하강법(gradient descent)을 이용한 학습 시간 측면에서, 이러한 포화형 비선형 함수(saturating nonlinearities)는 비포화형 비선형 함수(non-saturating nonlinearity)인 
```math
f(x)=max(0,x)
```
보다 훨씬 느립니다. Nair와 Hinton [20]을 따라, 우리는 이러한 비선형 함수를 사용하는 뉴런을 정류 선형 유닛(Rectified Linear Units, **ReLU**s)이라고 부릅니다. ReLU를 사용하는 심층 합성곱 신경망은 tanh 유닛을 사용하는 네트워크보다 몇 배나 빠르게 학습됩니다. 이는 **그림 1**에서 보여지는데, 해당 그림은 특정 4층 합성곱 신경망이 CIFAR-10 데이터셋에서 훈련 오류 25%에 도달하기 위해 필요한 반복 횟수를 나타냅니다. 이 그래프는 우리가 만약 전통적인 포화형 뉴런 모델을 사용했다면, 본 연구에서 이처럼 큰 신경망들을 실험할 수 없었을 것임을 보여줍니다.

포화형 비선형 함수(sigmoid, tanh)보다 비포화형 비선형 함수(ReLU)가 더 빠르게 학습

우리가 CNN에서 전통적인 뉴런 모델의 대안을 고려한 최초의 연구자는 아닙니다. 예를 들어, Jarrett 등 [11]은 그들의 방식의 대비 정규화(contrast normalization) 뒤에 지역 평균 풀링(local average pooling)을 적용했을 때, 비선형 함수
```math
f(x)=∣tanh⁡(x)∣
```
이 특히 잘 작동한다고 주장했습니다. 그러나 이 데이터셋(Caltech-101)에서는 주요 관심사가 과적합을 방지하는 것이기 때문에, 그들이 관찰한 효과는 우리가 ReLU를 사용할 때 보고한 훈련 세트를 더 빠르게 적합시키는 능력 향상과는 다른 것입니다.
더 빠른 학습은 큰 데이터셋에서 학습되는 대형 모델의 성능에 매우 큰 영향을 미칩니다.

이전 연구는 tanh 같은 함수로 과적합을 줄이는 데 초점을 뒀지만, AlexNet은 ReLU를 사용해 학습 속도를 대폭 높여 대규모 데이터셋에서 더 나은 성능을 얻었다는 점

---
#### 요약
---
- **기존 방식의 문제**
전통적인 뉴런 활성화 함수는 두 가지였어요.
- **tanh(x)** : 출력이 -1 \~ +1 사이로 제한
- **sigmoid (1+e⁻ˣ)⁻¹** : 출력이 0 \~ 1 사이로 제한
둘 다 **포화(saturating) 함수**라서, 입력값이 크거나 작아지면 기울기가 0에 가까워져 학습이 극도로 느려집니다. 이 문제는 네트워크가 깊어질수록 더 심각해져요.
- **해결책: ReLU**
**f(x) = max(0, x)**
Nair와 Hinton의 연구를 따라 이 함수를 쓰는 뉴런을 **ReLU(Rectified Linear Unit)** 라고 부릅니다.
- 입력이 양수면 그냥 통과
- 입력이 음수면 0으로 처리
- 아무리 값이 커져도 **천장이 없음 → 포화 없음**
- **얼마나 빠른가?**
논문에서 CIFAR-10 데이터셋으로 4층 CNN을 실험한 결과, **ReLU가 tanh보다 학습 속도가 6배 빠릅니다.** 훈련 오류율 25%에 도달하는 데 필요한 epoch 수가 압도적으로 적었어요.
- **왜 중요한가?**
> "ReLU가 없었다면 이렇게 큰 네트워크를 실험조차 못했을 것이다"
당시 AlexNet 같은 대규모 네트워크를 tanh로 학습시켰다면 시간이 너무 오래 걸려 실용적이지 않았을 거예요. ReLU 덕분에 깊고 큰 네트워크 실험이 처음으로 가능해졌습니다.
- **다른 연구와의 차이**
Jarrett 등은 Caltech-101에서 **\|tanh(x)\|** 가 잘 된다고 했지만, 그건 **과적합 방지**가 목적인 소규모 데이터셋 상황이었어요. AlexNet이 주목한 건 과적합이 아니라 **대규모 데이터에서의 빠른 학습 속도**로, 맥락이 다릅니다.
---
#### QnA
---


## **대비 정규화(contrast normalization)와 지역 평균 풀링(local average pooling)**

[image omitted: temporary Notion asset]

---
### 3.2 Training on Multiple GPUs {toggle="true"}
#### 해석
---
단일 GTX 580 GPU는 3GB의 메모리만을 가지고 있으며, 이는 그 위에서 학습시킬 수 있는 네트워크의 최대 크기를 제한합니다. 120만 개의 학습 예제는 하나의 GPU에 들어가지 않을 정도로 큰 네트워크를 학습시키기에 충분하다는 것이 밝혀졌습니다. 따라서 우리는 네트워크를 두 개의 GPU에 걸쳐 분산시켰습니다. 현재의 GPU들은 서로의 메모리에 직접 읽기와 쓰기를 수행할 수 있어, 호스트 머신의 메모리를 거치지 않고도 교차 GPU 병렬화에 특히 잘 적합합니다.

데이터셋이 많아질수록 그 데이터를 잘 학습하려면 큰 모델이 필요 <br>→ but,  큰 모델을 한 번에 GPU 한 대에 올리기엔 메모리가 부족 = 두개의 GPU로 분산<br>🌟 GPU끼리 통신 가능 

우리가 사용하는 병렬화 방식은 본질적으로 절반의 커널(또는 뉴런)을 각각의 GPU에 배치하는 것입니다. 여기에 하나의 추가적인 트릭이 있습니다. GPU들은 특정 층에서만 서로 통신합니다. 이는 예를 들어, 3번째 층의 커널들은 2번째 층의 모든 커널 맵으로부터 입력을 받는다는 것을 의미합니다. 그러나 4번째 층의 커널들은 동일한 GPU에 존재하는 3번째 층의 커널 맵으로부터만 입력을 받습니다. 연결 패턴을 선택하는 것은 교차 검증(cross-validation)의 문제이지만, 이러한 방식은 통신량이 계산량에 비해 허용 가능한 비율이 될 때까지 통신의 양을 정밀하게 조정할 수 있게 해줍니다.

GPU들이 항상 통신하는 것 X → 특정 층에서만 서로 통신 <br>통신 시간 많이 소요 → 필요한것들만 통신 (통신량이 계산량에 비해 너무 많지 않도록)

결과적으로 생성된 아키텍처는 Cireşan 등 [5]이 사용한 “columnar” CNN의 구조와 다소 유사하지만, 우리의 컬럼들은 독립적이지 않습니다(그림 2 참조). 이 방식은 각 합성곱 층에서 절반의 커널만을 사용하여 하나의 GPU에서 학습된 네트워크와 비교했을 때, 우리의 top-1과 top-5 오류율을 각각 1.7%와 1.2% 감소시킵니다. 또한 두 개의 GPU를 사용하는 네트워크는 하나의 GPU를 사용하는 네트워크보다 학습 시간이 약간 더 짧습니다².

GPU를 2개 사용하지만 독립적인건 아니고, 일부는 공유 일부는 독립적인 구조야<br>이 구조 vs 네트워크 절반 구조 비교(GPU 1개만 쓰기)  : 해당 구조가 오류율 감소 <br>                                                                                               & 학습 시간도 짧음

---
#### 요약
---
- **왜 GPU를 2개 썼나?**
당시 GTX 580 GPU 한 대의 메모리는 **3GB**뿐이었어요. 그런데 AlexNet은 파라미터가 6000만 개나 되는 거대한 네트워크라 GPU 한 대에 다 올라가지 않았습니다. 그래서 네트워크를 **두 GPU에 절반씩 나눠서** 학습시켰어요.
- **어떻게 나눴나?**
단순히 반씩 쪼갠 게 아니라 영리하게 설계했습니다.
[image omitted: temporary Notion asset]
- **성능 효과**
<table header-row="true">
<tr>
<td>비교</td>
<td>Top-1 오류율</td>
<td>Top-5 오류율</td>
</tr>
<tr>
<td>GPU 1개</td>
<td>+1.7% 더 높음</td>
<td>+1.2% 더 높음</td>
</tr>
<tr>
<td>**GPU 2개**</td>
<td>**기준**</td>
<td>**기준**</td>
</tr>
</table>
단순히 메모리 부족을 해결한 것뿐 아니라 **정확도도 함께 올라갔어요.**
---
#### QnA
---


## **통신량이 계산량에 비해 허용 가능한 비율이 될 때까지 통신의 양을 정밀하게 조정할 수 있게 해줍니다?**

- **매 레이어마다 통신**
[image omitted: temporary Notion asset]
- **통신 없음**
[image omitted: temporary Notion asset]
- **AlexNet 방식**
[image omitted: temporary Notion asset]

---
### 3.3 Local Response Normalization {toggle="true"}
#### 해석
---
[image omitted: temporary Notion asset]

ReLU는 원래 정규화를 안 해도 괜찮지만 Local normalization을 하니까 일반화에 도움
→ 한 위치에서 여러 커널들이 비슷한 특징에 강하게 반응할 수 있는데, 이때 RN은 너무 세게 반응하는 뉴런을 주변 뉴런들이 눌러준다<br><br>= 비슷한 특징을 중복해서 감지하지 않게 도와주는 경쟁 메커니즘<br>& 우리는 local normalization 중에서도 response normalization을 사용

이 방식은 Jarrett 등 [11]의 국소 대비 정규화(local contrast normalization) 방식과 약간의 유사성을 가지지만, 우리는 평균 활성(mean activity)을 빼지 않기 때문에 우리의 방식은 보다 정확하게 “밝기 정규화(brightness normalization)”라고 불릴 수 있습니다. 응답 정규화(response normalization)는 우리의 top-1 및 top-5 오류율을 각각 1.4%와 1.2% 감소시켰습니다. 우리는 또한 CIFAR-10 데이터셋에서 이 방식의 효율성을 검증했습니다. 네 개의 층으로 이루어진 CNN은 정규화를 사용하지 않았을 때 13%의 테스트 오류율을, 정규화를 사용했을 때 11%의 테스트 오류율을 달성했습니다³.

response normalization은 local contrast normalization 방식과 약간 유사하지만 <br>차이점 : 우리는 평균 활성을 빼지 않음 (brightness normalization)<br><br>그리고 response normalization을 하니까 성능이 더 좋아짐!

---
#### 요약
---
- **배경: ReLU는 정규화가 필요 없지만…**
ReLU는 포화가 없어서 입력값을 굳이 정규화하지 않아도 학습이 잘 돼요. 하지만 저자들은 실험해보니 **추가적인 정규화를 하면 성능이 더 올라간다**는 걸 발견했어요.
- **LRN이란?**
**주변 뉴런들끼리 경쟁**시키는 방식이에요. 어떤 뉴런이 매우 강하게 반응하면, 바로 옆 뉴런들의 반응을 억제(약화)시킵니다. 이걸 **측면 억제(lateral inhibition)** 라고 하며, 실제 뇌의 뉴런이 동작하는 방식에서 영감을 받았어요.
[image omitted: temporary Notion asset]
- **LRN vs 대비 정규화 차이**
앞서 나온 대비 정규화와 헷갈릴 수 있는데, 둘은 목적이 달라요.
<table header-row="true">
<tr>
<td>구분</td>
<td>대비 정규화</td>
<td>LRN</td>
</tr>
<tr>
<td>목적</td>
<td>밝기 차이 제거</td>
<td>강한 신호 강조</td>
</tr>
<tr>
<td>대상</td>
<td>픽셀값</td>
<td>커널(필터) 반응값</td>
</tr>
<tr>
<td>방식</td>
<td>평균 빼기</td>
<td>주변 커널끼리 경쟁</td>
</tr>
</table>
- **어디에 적용했나?**
Conv1, Conv2 뒤에 ReLU를 적용한 다음 LRN을 붙였어요. 모든 레이어에 다 쓴 게 아니라 앞쪽 두 레이어에만 선택적으로 적용했습니다.
---
#### QnA
---


## **LRN vs Maxpooling**

[image omitted: temporary Notion asset]



## **LRN을 하는 이유**

- 한 줄로 정리하면:
> 같은 위치에서 여러 필터가 중복으로 반응하는 걸 억제하기 위해서



## **왜 같은 위치에서 여러 필터가 중복으로 반응하는 것이 문제인가**

- 필터는 이미지에서 **특정 패턴을 감지하는 작은 탐지기**예요.
예를 들어
- 필터 1 → 가로 엣지 감지 전문
- 필터 2 → 세로 엣지 감지 전문
- 필터 3 → 파란색 감지 전문
- 필터 4 → 대각선 감지 전문
이런 식으로 각 필터가 서로 다른 특징을 담당
[image omitted: temporary Notion asset]

---
### 3.4 Overlapping Pooling {toggle="true"}
#### 해석
---
[image omitted: temporary Notion asset]
---
#### 요약
---
- **기존 풀링 방식**
- 전통적인 풀링은 **겹치지 않게(non-overlapping)** 구역을 나눠요.
- stride(s) = 2, 구역 크기(z) = 2 → 각 구역이 딱 붙어있고 겹치지 않음
- **AlexNet의 방식**
- stride(s) = 2, 구역 크기(z) = 3 → **구역이 서로 1픽셀씩 겹침**
[image omitted: temporary Notion asset]
- **왜 겹치면 좋은가?**
- 일반 풀링은 구역 경계에 있는 픽셀이 딱 한 구역에만 속해요. 그 구역에서 최댓값이 아니면 그냥 버려지는 거예요.
- 반면 겹치는 풀링은 경계 픽셀이 **여러 구역에 포함**되어 살아남을 기회가 더 많아요. 정보 손실이 줄어들고, 과적합도 약간 줄어드는 효과가 있어요.
[image omitted: temporary Notion asset]
---
### 3.5 Overall Architecture (Fig.2) {toggle="true"}
#### 해석
---
[image omitted: temporary Notion asset]
이제 우리는 우리의 CNN의 전체 구조를 설명할 준비가 되었습니다. 그림 2에 나타난 것처럼, 이 네트워크는 가중치를 가진 여덟 개의 층으로 구성되어 있으며, 처음 다섯 개는 합성곱 층이고 나머지 세 개는 완전 연결 층입니다. 마지막 완전 연결 층의 출력은 1000개의 클래스 라벨에 대한 분포를 생성하는 1000-way 소프트맥스(softmax)에 입력됩니다. 우리의 네트워크는 다항 로지스틱 회귀(multinomial logistic regression) 목적함수를 최대화하며, 이는 예측 분포 하에서 정답 라벨의 로그 확률(log-probability)의 학습 사례 전체에 대한 평균을 최대화하는 것과 동일합니다.
두 번째, 네 번째, 그리고 다섯 번째 합성곱 층의 커널들은 이전 층에서 동일한 GPU에 존재하는 커널 맵들과만 연결되어 있습니다(그림 2 참조). 세 번째 합성곱 층의 커널들은 두 번째 층의 모든 커널 맵들과 연결되어 있습니다. 완전 연결 층의 뉴런들은 이전 층의 모든 뉴런들과 연결되어 있습니다. **응답 정규화(response-normalization) **층은 첫 번째와 두 번째 합성곱 층 뒤에 위치합니다.<br>섹션 3.4에서 설명된 종류의 **최대 풀링(max-pooling) **층은 두 개의 응답 정규화 층과 다섯 번째 합성곱 층 뒤에 위치합니다. **ReLU 비선형 함수**는 모든 합성곱 층과 완전 연결 층의 출력에 적용됩니다.
첫 번째 합성곱 층은 224×224×3 크기의 입력 이미지를 11×11×3 크기의 커널 96개로 필터링하며, 스트라이드는 4픽셀입니다(이는 커널 맵에서 인접한 뉴런들의 수용 영역 중심 사이의 거리입니다). 두 번째 합성곱 층은 첫 번째 합성곱 층의 (응답 정규화되고 풀링된) 출력을 입력으로 받아, 5×5×48 크기의 커널 256개로 필터링합니다.
세 번째, 네 번째, 다섯 번째 합성곱 층들은 그 사이에 풀링이나 정규화 층이 없이 서로 연결되어 있습니다. 세 번째 합성곱 층은 두 번째 합성곱 층의 (정규화되고 풀링된) 출력에 연결된 3×3×256 크기의 커널 384개를 가지고 있습니다. 네 번째 합성곱 층은 3×3×192 크기의 커널 384개를, 다섯 번째 합성곱 층은 3×3×192 크기의 커널 256개를 가지고 있습니다. 완전 연결 층은 각각 4096개의 뉴런을 가지고 있습니다.
---
#### 요약
---
[image omitted: temporary Notion asset]
---
#### QnA
---


## **다항 로지스틱 회귀란?**

[image omitted: temporary Notion asset]



## **로그 확률을 사용하는 이유**

[image omitted: temporary Notion asset]

---
### 4. Reducing Overfitting {toggle="true"}
#### 해석
---
우리의 신경망 구조는 6천만 개의 매개변수를 가지고 있습니다. ILSVRC의 1000개 클래스는 각 학습 예제가 이미지에서 라벨로의 매핑에 대해 10비트의 제약을 부여하지만, 이는 상당한 과적합(overfitting) 없이 그렇게 많은 매개변수를 학습하기에는 충분하지 않다는 것이 드러났습니다. 아래에서는 우리가 과적합에 맞서 싸우기 위해 사용한 두 가지 주요 방법을 설명합니다.

데이터가 주는 정보(라벨 1000개 ≈ 10비트)는 너무 적어서 6천만 개의 파라미터를 안정적으로 학습시키기 어렵기 때문에, AlexNet은 과적합을 막기 위해 **데이터 증강과 드롭아웃**을 적용했다

---
### 4.1 Data Augmentation {toggle="true"}
#### 해석
---
이미지 데이터에서 과적합을 줄이기 위한 가장 쉽고 일반적인 방법은 라벨을 보존하는 변환(label-preserving transformations)을 사용하여 데이터셋을 인위적으로 확장하는 것입니다(예: [25, 4, 5]). 우리는 두 가지 형태의 데이터 증강(data augmentation)을 사용하며, 이 두 방법 모두 원본 이미지로부터 매우 적은 계산만으로 변환된 이미지를 생성할 수 있기 때문에, 변환된 이미지를 디스크에 저장할 필요가 없습니다. 우리의 구현에서는, GPU가 이전 배치의 이미지를 학습하는 동안 CPU에서 Python 코드로 변환된 이미지가 생성됩니다. 따라서 이러한 데이터 증강 방식들은 사실상 계산 비용이 들지 않습니다.
첫 번째 형태의 데이터 증강(data augmentation)은 이미지의 이동(translation)과 수평 반사(horizontal reflection)를 생성하는 것으로 구성됩니다. 우리는 256×256 이미지로부터 무작위로 224×224 패치(그리고 그 수평 반사본)를 추출하고, 이러한 추출된 패치들로 우리의 네트워크를 학습시킵니다⁴. 이 방법은 우리의 학습 세트의 크기를 2048배 증가시키지만, 결과적으로 생성된 학습 예제들은 물론 서로 강하게 상호의존적입니다. 이 방식을 사용하지 않으면, 우리의 네트워크는 심각한 과적합을 겪게 되며, 이는 훨씬 더 작은 네트워크를 사용하도록 강요했을 것입니다. 테스트 시에는, 네트워크가 224×224 크기의 다섯 개의 패치(네 개의 모서리 패치와 하나의 중앙 패치) 및 그 수평 반사본(따라서 총 10개의 패치)을 추출하여, 네트워크의 소프트맥스(softmax) 층이 이 10개의 패치에 대해 생성한 예측값을 평균함으로써 최종 예측을 수행합니다.
[image omitted: temporary Notion asset]
---
#### 요약
---
[image omitted: temporary Notion asset]
---
####  QnA
---


## **두번째 증강에 대한 설명 (PCA)**

[image omitted: temporary Notion asset]

---
### 4.2 Dropout {toggle="true"}
#### 해석
---
여러 다른 모델들의 예측을 결합하는 것은 테스트 오류를 줄이는 매우 성공적인 방법입니다 [1, 3]. 그러나 이미 학습에 여러 날이 걸리는 대규모 신경망의 경우, 이러한 방법은 너무 비용이 많이 드는 것으로 보입니다. 그러나 학습 중에 약 두 배의 비용만 추가되는 매우 효율적인 모델 결합 방식이 존재합니다. 최근에 도입된 **“드롭아웃(dropout)” **[10]이라는 기법은 각 은닉 뉴런의 출력을 0.5의 확률로 0으로 설정하는 것으로 구성됩니다. 이렇게 “드롭아웃”된 뉴런들은 순전파(forward pass)에 기여하지 않으며, 역전파(backpropagation)에도 참여하지 않습니다.
따라서 입력이 주어질 때마다 신경망은 서로 다른 구조를 샘플링하게 되지만, 이 모든 구조들은 가중치를 공유합니다. 이 기법은 뉴런들의 복잡한 공동 적응(co-adaptation)을 감소시키는데, 이는 한 뉴런이 특정 다른 뉴런들의 존재에 의존할 수 없기 때문입니다. 따라서 뉴런은 여러 가지 다른 무작위 부분집합의 뉴런들과 함께 사용될 수 있는 보다 강건한 특징들을 학습하도록 강요됩니다.
테스트 시에는, 우리는 모든 뉴런을 사용하지만 그들의 출력을 0.5배로 줄입니다. 이는 지수적으로 많은 드롭아웃 네트워크들이 생성하는 예측 분포들의 기하평균(geometric mean)을 취하는 것에 대한 합리적인 근사치로 볼 수 있습니다.
우리는 그림 2의 처음 두 개의 완전 연결 층(fully-connected layers)에서 드롭아웃(dropout)을 사용합니다. 드롭아웃을 사용하지 않으면, 우리의 네트워크는 상당한 과적합을 보입니다. 드롭아웃은 수렴(converge)하는 데 필요한 반복 횟수를 대략 두 배로 증가시킵니다.
---
#### 요약
---
- **왜 Dropout이 필요한가?**
- 여러 모델의 예측을 합치면 성능이 올라가요. 
- 하지만 AlexNet처럼 학습에 며칠씩 걸리는 모델을 여러 개 만드는 건 현실적으로 불가능
- Dropout은 이 **"여러 모델을 합치는 효과"를 단 하나의 모델로** 구현하는 방법
- **Dropout이란?**
[image omitted: temporary Notion asset]
---
### 5. Details of learning (Fig.3) {toggle="true"}
#### 해석
---
[image omitted: temporary Notion asset]
우리는 각 층의 가중치를 평균이 0이고 표준편차가 0.01인 가우시안 분포로부터 초기화했습니다. 두 번째, 네 번째, 다섯 번째 합성곱 층과 완전 연결 은닉층의 뉴런 바이어스는 상수 1로 초기화했습니다. 이러한 초기화는 ReLU에 양의 입력을 제공함으로써 학습의 초기 단계를 가속화합니다. 나머지 층들의 뉴런 바이어스는 상수 0으로 초기화했습니다.
우리는 모든 층에 동일한 학습률(learning rate)을 사용했으며, 학습 과정 전반에 걸쳐 이를 수동으로 조정했습니다. 우리가 따른 경험적 기준(heuristic)은 현재의 학습률로 검증 오류율(validation error rate)이 더 이상 개선되지 않을 때 학습률을 10으로 나누는 것이었습니다. 학습률은 처음에 0.01로 설정되었으며, 종료되기 전까지 세 번 감소시켰습니다. 우리는 120만 장의 이미지로 구성된 학습 세트를 약 90회 순환(cycle)하며 네트워크를 학습시켰고, 이는 NVIDIA GTX 580 3GB GPU 두 개에서 5일에서 6일 정도가 걸렸습니다.
그림 3: 224×224×3 크기의 입력 이미지에 대해 첫 번째 합성곱 층이 학습한 11×11×3 크기의 96개의 합성곱 커널. 상단의 48개 커널은 GPU 1에서 학습되었으며, 하단의 48개 커널은 GPU 2에서 학습되었습니다. 자세한 내용은 6.1절을 참조하십시오.
---
#### 요약
---
[image omitted: temporary Notion asset]
---
#### QnA
---


## 기본적인 내용 - SGD, 배치, lr, momenterm, 가중치 감쇠

[image omitted: temporary Notion asset]

---
### 6. Results (Table 1,2) {toggle="true"}
#### 해석
---


[image omitted: temporary Notion asset]


[image omitted: temporary Notion asset]


우리의 ILSVRC-2010 결과는 표 1에 요약되어 있습니다. 우리의 네트워크는 테스트 세트에서 top-1과 top-5 오류율이 각각 37.5%와 17.0%⁵를 달성했습니다. ILSVRC-2010 대회 기간 동안 달성된 최고 성능은 서로 다른 특징(feature)들로 학습된 여섯 개의 희소 부호화(sparse-coding) 모델로부터 생성된 예측값들을 평균한 접근법으로, 각각 47.1%와 28.2%였습니다 [2]. 그 이후 발표된 최고 결과는 두 종류의 밀집 샘플링된 특징으로부터 계산된 피셔 벡터(Fisher Vectors, FVs)에 대해 학습된 두 개의 분류기의 예측값들을 평균한 접근법으로, 45.7%와 25.7%였습니다 [24].
우리는 또한 ILSVRC-2012 대회에 우리 모델을 출전시켰으며, 그 결과를 표 2에 보고합니다. ILSVRC-2012 테스트 세트의 라벨은 공개적으로 제공되지 않기 때문에, 우리가 시도한 모든 모델의 테스트 오류율을 보고할 수는 없습니다. 이 문단의 나머지 부분에서는 검증 오류율(validation error rate)과 테스트 오류율(test error rate)을 구분 없이 사용합니다. 왜냐하면 우리의 경험상 두 값의 차이는 0.1%를 넘지 않기 때문입니다(표 2 참조).
이 논문에서 설명한 CNN은 top-5 오류율 18.2%를 달성했습니다. 다섯 개의 유사한 CNN들의 예측값을 평균하면 오류율은 16.4%가 됩니다. 마지막 풀링 층 위에 여섯 번째 합성곱 층을 추가한 단일 CNN을 사용하여 ImageNet 2011년 가을 전체 릴리스(1500만 장의 이미지, 22,000개 범주)를 분류하도록 학습시킨 후, ILSVRC-2012에 대해 “미세 조정(fine-tuning)”을 수행하면 오류율은 16.6%가 됩니다.
전체 2011년 가을 릴리스에 대해 사전 학습된 두 개의 CNN과 앞서 언급한 다섯 개의 CNN의 예측값을 평균하면 오류율은 15.3%가 됩니다. 두 번째로 높은 성능을 기록한 대회 참가자는 서로 다른 유형의 밀집 샘플링된 특징들(densely-sampled features)로부터 계산된 피셔 벡터(Fisher Vectors, FVs)에 대해 학습된 여러 분류기의 예측값을 평균하는 접근법을 사용했으며, 오류율은 26.2%였습니다 [7].
<br>표 1: ILSVRC-2010 테스트 세트에서의 결과 비교. *이탤릭체*로 표시된 값은 다른 연구자들에 의해 달성된 최고 결과입니다.<br><br>표 2: ILSVRC-2012 검증 세트와 테스트 세트에서의 오류율 비교. *이탤릭체*로 표시된 값은 다른 연구자들에 의해 달성된 최고 결과입니다. 별표(\*)가 붙은 모델들은 ImageNet 2011년 가을 전체 릴리스를 분류하도록 “사전 학습(pre-trained)”되었습니다. 자세한 내용은 6절을 참조하십시오.
---
#### 요약
---
[image omitted: temporary Notion asset]
---
### 6.1 Qualitative Evaluations {toggle="true"}
#### 해석
---
[image omitted: temporary Notion asset]
그림 4의 왼쪽 패널에서 우리는 여덟 개의 테스트 이미지에 대해 네트워크의 상위 5개 예측값(top-5 predictions)을 계산함으로써 네트워크가 무엇을 학습했는지를 정성적으로 평가합니다. 왼쪽 상단의 진드기(mite)와 같이 중심에 있지 않은 객체들도 네트워크에 의해 인식될 수 있음을 주목하십시오. 대부분의 상위 5개 라벨들은 타당해 보입니다. 예를 들어, 표범(leopard)의 경우 가능한 라벨로 고려된 것들은 모두 다른 종류의 고양이들입니다. 어떤 경우들(grille, cherry)에는 사진의 초점이 실제로 어디에 맞추어져 있는지에 대한 모호함이 존재합니다.
네트워크의 시각적 지식을 탐색하는 또 다른 방법은, 이미지가 마지막 4096차원 은닉층(hidden layer)에서 유도하는 특징 활성(feature activation)을 고려하는 것입니다. 두 이미지가 작은 유클리드 거리(Euclidean separation)를 가진 특징 활성 벡터를 생성한다면, 신경망의 높은 수준 계층들이 이 두 이미지를 유사하다고 판단한다고 말할 수 있습니다.
그림 4는 테스트 세트에서 선택된 다섯 개의 이미지와, 이 측정 기준에 따라 각각과 가장 유사한 학습 세트의 여섯 개 이미지를 보여줍니다. 주목할 점은, 픽셀 수준에서는 검색된 학습 이미지들이 첫 번째 열의 질의 이미지들과 L2 거리상으로 가깝지 않다는 것입니다. 예를 들어, 검색된 개(dog)들과 코끼리(elephant)들은 다양한 자세로 나타나 있습니다. 훨씬 더 많은 테스트 이미지에 대한 결과는 부록(supplementary material)에 제시되어 있습니다.
두 개의 4096차원 실수값 벡터(real-valued vectors) 사이의 유클리드 거리(Euclidean distance)를 사용하여 유사도를 계산하는 것은 비효율적입니다. 그러나 이러한 벡터들을 짧은 이진 코드(short binary codes)로 압축하도록 오토인코더(auto-encoder)를 학습시킴으로써 효율적으로 만들 수 있습니다. 이러한 방법은 이미지 라벨을 활용하지 않기 때문에 의미적으로 유사하지 않더라도 비슷한 에지(edge) 패턴을 가진 이미지를 검색하는 경향이 있는, 원시 픽셀(raw pixels)에 오토인코더를 적용하는 방식 [14]보다 훨씬 더 나은 이미지 검색 방법을 만들어낼 것입니다.
그림 3은 네트워크의 두 개의 데이터 연결 층(data-connected layers)에 의해 학습된 합성곱 커널(convolutional kernels)을 보여줍니다. 네트워크는 다양한 주파수(frequency)와 방향(orientation)에 선택적인 커널들뿐 아니라, 여러 가지 색상의 블롭(blobs)들도 학습했습니다. 3.5절에서 설명된 제한된 연결성(restricted connectivity)의 결과로, 두 GPU가 각각 특화된 형태를 보이는 것을 확인할 수 있습니다. GPU 1의 커널들은 주로 색상에 무관(color-agnostic)하며, GPU 2의 커널들은 주로 색상에 특화(color-specific)되어 있습니다. 이러한 형태의 특화(specialization)는 모든 실행(run)에서 발생하며, 특정한 무작위 가중치 초기화(random weight initialization)와는 독립적입니다(GPU의 번호 재지정은 예외)..
---
#### 요약
---
- **이 섹션에서 하는 것**
- 숫자(오류율)가 아니라 **"네트워크가 실제로 뭘 배웠는지"를 눈으로 확인**하는 섹션
- 두 가지 방법으로 살펴봐요.
[image omitted: temporary Notion asset]
---
### 7. Discussion {toggle="true"}
#### 해석
---
우리의 결과는 크고 깊은 합성곱 신경망이 순수한 지도 학습(supervised learning)만을 사용하여 매우 어려운 데이터셋에서 기록적인 성능을 달성할 수 있음을 보여줍니다. 주목할 만한 점은, 하나의 합성곱 층을 제거하면 네트워크의 성능이 저하된다는 것입니다. 예를 들어, 중간 층들 중 어느 하나를 제거하면 네트워크의 top-1 성능이 약 2% 감소합니다. 따라서 깊이(depth)는 우리의 결과를 달성하는 데 있어 실제로 중요한 요소입니다.
우리의 실험을 단순화하기 위해, 우리는 비지도 사전 학습(unsupervised pre-training)을 사용하지 않았습니다. 그러나 우리는 그것이 도움이 될 것이라고 예상합니다. 특히 라벨이 지정된 데이터의 양을 늘리지 않고도 네트워크의 크기를 크게 확장할 만큼 충분한 계산 능력을 확보할 수 있다면 더욱 그렇습니다. 지금까지 우리의 결과는 네트워크의 크기를 키우고 학습 시간을 늘릴수록 개선되어 왔지만, 인간 시각 시스템의 하측두(infero-temporal) 경로에 도달하기 위해서는 여전히 수많은 단계가 남아 있습니다. 궁극적으로 우리는 시간적 구조(temporal structure)가 정적 이미지에서는 결여되어 있거나 덜 명확한 유용한 정보를 제공하는 비디오 시퀀스에 대해 매우 크고 깊은 합성곱 신경망을 사용하고자 합니다.
---

---

# ResNet

---
## 목차
---
### 논문 전체 요약 {toggle="true"}
- **핵심 문제의식**
딥러닝에서 레이어를 단순히 더 쌓으면 성능이 좋아질 것 같지만, 실제로는 **오히려 정확도가 떨어지는 "degradation problem"** 이 발생합니다. 이는 과적합(overfitting) 때문이 아니라, 최적화(optimization) 자체가 어려워지기 때문입니다. 56-layer plain network가 20-layer보다 훈련 오류가 더 높은 것이 대표적인 예입니다.
- **핵심 아이디어: Residual Learning**
기존 방식이 레이어에게 목표 출력 **H(x)** 를 직접 학습시키는 반면, ResNet은 레이어가 **잔차(residual) F(x) = H(x) − x** 만 학습하도록 재구성합니다.
`출력 = F(x) + x`
직관적으로, 만약 identity mapping(입력을 그대로 통과)이 최적이라면, 잔차를 0으로 만드는 것이 새로운 함수를 처음부터 학습하는 것보다 훨씬 쉽습니다. 이를 구현하는 것이 바로 **shortcut connection** 으로, 추가 파라미터 없이 입력을 출력에 더해줍니다.
- **네트워크 구조**
**기본 Building Block (ResNet-34 이하)**
`입력 x → [3×3 conv] → [ReLU] → [3×3 conv] → (+x) → ReLU`
**Bottleneck Block (ResNet-50 이상)**
`입력 x → [1×1 conv] → [3×3 conv] → [1×1 conv] → (+x) → ReLU`
1×1 conv로 차원을 줄였다가 복원해서 연산량을 줄이는 구조입니다.
**주요 모델 스펙**
<table header-row="true">
<tr>
<td>모델</td>
<td>레이어 수</td>
<td>FLOPs</td>
</tr>
<tr>
<td>ResNet-18</td>
<td>18</td>
<td>1.8B</td>
</tr>
<tr>
<td>ResNet-34</td>
<td>34</td>
<td>3.6B</td>
</tr>
<tr>
<td>ResNet-50</td>
<td>50</td>
<td>3.8B</td>
</tr>
<tr>
<td>ResNet-101</td>
<td>101</td>
<td>7.6B</td>
</tr>
<tr>
<td>ResNet-152</td>
<td>152</td>
<td>11.3B</td>
</tr>
</table>
ResNet-152는 VGG-16/19(15\~19.6B FLOPs)보다 복잡도가 낮으면서도 훨씬 깊습니다.
- **실험 결과**
**ImageNet 분류 성능 비교**
<table header-row="true">
<tr>
<td>모델</td>
<td>Top-5 Error</td>
</tr>
<tr>
<td>VGG (ILSVRC'14)</td>
<td>8.43%</td>
</tr>
<tr>
<td>GoogLeNet</td>
<td>7.89%</td>
</tr>
<tr>
<td>ResNet-152 (단일)</td>
<td>4.49%</td>
</tr>
<tr>
<td>**ResNet 앙상블**</td>
<td>**3.57%**</td>
</tr>
</table>
→ ILSVRC 2015 **1위 달성**
**Plain vs ResNet 비교 (ImageNet)**
<table header-row="true">
<tr>
<td></td>
<td>Plain-18</td>
<td>Plain-34</td>
<td>ResNet-18</td>
<td>ResNet-34</td>
</tr>
<tr>
<td>Top-1 Error</td>
<td>27.94%</td>
<td>28.54%</td>
<td>27.88%</td>
<td>**25.03%**</td>
</tr>
</table>
Plain network는 깊어질수록 오히려 나빠지는 반면, ResNet은 깊어질수록 개선됩니다.
**CIFAR-10 결과**<br>110-layer ResNet이 단 1.7M 파라미터로 **6.43% 오류율** 달성. 심지어 1202-layer 모델도 학습 오류 0.1% 미만으로 수렴에 성공했습니다.
**객체 검출 (Faster R-CNN 기반)**<br>VGG-16 대비 ResNet-101로 교체 시 COCO 데이터셋에서 **28% 상대적 성능 향상**.
- **왜 잘 되는가?**
논문에서는 residual function의 응답값 표준편차(std)를 분석했는데, ResNet의 레이어 응답이 plain network보다 전반적으로 **작은 값**을 가집니다. 이는 각 레이어가 identity mapping에서 작은 변화만 학습하면 되므로 최적화가 훨씬 용이하다는 것을 시사합니다.
---
#### QnA
---


## **Shortcut Connection**

= 입력값을 몇 개 레이어를 건너뛰어서 출력에 그대로 더해주는 것
[image omitted: temporary Notion asset]
- **일반 네트워크**는 레이어가 H(x) 자체를 처음부터 통째로 학습해야 합니다. 레이어가 많아질수록 이게 점점 어려워지는 거죠.
- **ResNet의 shortcut connection**은 입력 x를 레이어를 건너뛰어서 출력에 그냥 더해버립니다. 그러면 레이어는 H(x) 전체가 아니라 **변화분(잔차) F(x) = H(x) - x 만 학습**하면 됩니다.
- 비유하자면, 선생님이 "0점짜리 답안지에서 100점 만들어" 하는 것보다, "이미 90점짜리 답안에서 틀린 10점만 고쳐" 하는 게 훨씬 쉬운 것과 같습니다. 
- 그리고 만약 그 레이어가 아무것도 안 해도 된다면? F(x)를 그냥 0으로 만들면 x가 그대로 통과되니까 최소한 성능이 나빠지지는 않습니다.

---
### Abstract {toggle="true"}
#### 해석
---
더 깊은 신경망은 학습하기가 더 어렵습니다. 우리는 이전보다 훨씬 **더 깊은 네트워크를 학습하는 과정을 쉽게 만들기 위해 잔차(residual) 학습 프레임워크를 제안**합니다. 이 방법에서는 층들이 입력을 직접 변환하는 함수를 학습하도록 하는 대신, 입력 대비 변화량(잔차 함수) 를 학습하도록 층들을 재정의했습니다. 이를 통해 잔차 네트워크는 최적화가 쉬워지고, 깊이가 크게 증가해도 정확도가 향상된다는 실험적 근거를 제시합니다.
ImageNet 데이터셋에서 우리는 **152층**에 달하는 잔차 네트워크를 평가했으며, 이는 VGG 네트워크보다 8배 깊지만 더 낮은 연산 복잡도를 가집니다. 이러한 잔차 네트워크 앙상블은 ImageNet 테스트 세트에서 3.57% 오류율을 기록했으며, ILSVRC 2015 분류 과제에서 1위를 달성했습니다. 또한 CIFAR-10 데이터셋에서 100층 및 1000층 규모의 네트워크 분석 결과도 제시합니다.
**표현의 깊이는 다양한 시각 인식 작업에서 매우 중요**합니다. 우리의 매우 깊은 표현 덕분에 COCO 객체 검출 데이터셋에서는 28%의 상대적 성능 향상을 얻었습니다. 딥 잔차 네트워크는 ILSVRC 및 COCO 2015 대회에서 우리가 제출한 모델들의 기반이 되었으며, ImageNet detection, ImageNet localization, COCO detection, COCO segmentation 과제에서도 모두 1위를 차지했습니다.
---
### 1. Introduction {toggle="true"}
#### 해석
---
딥 컨볼루션 신경망은 이미지 분류에서 여러 돌파구를 만들어냈다. 이러한 네트워크는 저·중·고 수준의 특징들과 분류기를 end-to-end 방식의 다층 구조로 자연스럽게 통합하며, 쌓인 layer(깊이)의 수가 많아질수록 특징의 “수준”은 더 풍부해질 수 있다. 최근 연구는 **네트워크 깊이가 매우 중요**하다는 사실을 보여주었고, 도전적인 ImageNet 데이터셋에서 최고 성능을 낸 모델들은 모두 깊이가 16층에서 30층에 이르는 “매우 깊은” 구조를 사용한다. 또한 많은 다른 시각 인식 과제에서도 매우 깊은 모델은 큰 성능 향상을 가져왔다.
이 깊이의 중요성으로 인해 자연스럽게 다음 질문이 제기된다:
더 나은 네트워크를 학습하는 것은 단순히 layer를 더 쌓는 것만으로 해결될까?
이 질문에 답하기 어려웠던 이유는 잘 알려진 **gradient vanishing / gradient explosion** 문제였고, 이는 학습 초기부터 수렴을 방해한다. 그러나 **normalized initialization**과 **Batch Normalization**이 도입되면서 이 문제는 상당 부분 해결되었고, 덕분에 수십 개 layer를 가진 네트워크도 **SGD + backpropagation**으로 학습을 시작할 수 있게 되었다.
하지만 더 깊은 네트워크가 학습을 시작할 수 있게 되자, 또 하나의 문제가 드러났다. 바로 **degradation problem**이다.
네트워크의 깊이가 증가하면 정확도는 어느 지점에서 포화되고(놀랍지 않을 수 있음), 이후 급격하게 떨어진다. 더욱 놀라운 점은, 이러한 성능 저하가 **overfitting 때문이 아니라는 것**이다. 이미 충분히 깊은 모델에 layer를 더 추가하면 오히려 **training error가 증가**한다는 사실이 보고되었고, 우리의 실험에서도 철저히 확인되었다. 그림 1은 이 전형적인 예를 보여준다.
[image omitted: temporary Notion asset]
이러한 training accuracy의 저하는 모든 구조가 똑같이 최적화되기 쉬운 것은 아니라는 사실을 의미한다.
얕은 네트워크와, 그 위에 layer를 더 추가한 더 깊은 네트워크를 생각해보자. 더 깊은 구조에는 구성적으로 하나의 해가 존재한다:
- 추가된 layer들은 **항등 함수(identity mapping)** 를 수행하고
- 나머지 layer들은 학습된 얕은 네트워크에서 그대로 복사한다.
이렇게 구성 가능한 해가 존재한다는 사실은, 더 깊은 네트워크가 얕은 네트워크보다 더 높은 training error를 내서는 안 된다는 의미다.
하지만 실험은, 우리가 사용하는 최적화 방법들이 이러한 해와 비슷한 수준의 solution조차 찾지 못한다는 것을 보여준다(혹은 현실적인 시간 안에는 찾지 못한다).
[image omitted: temporary Notion asset]
이 논문에서 우리는 이러한 degradation 문제를 해결하기 위해 **deep residual learning** 프레임워크를 제안한다. 여러 layer가 원하는 mapping을 직접 학습하도록 두는 대신, 우리는 이 layer들이 **residual mapping**을 학습하도록 한다.
원하는 mapping을 H(x)라고 하면, residual은 다음과 같다:
```math
F(x) = H(x) − x
```
즉, 원래의 mapping은 F(x) + x로 다시 표현된다.
이 residual mapping을 최적화하는 것이, 참조 정보가 없는 원래 mapping을 직접 최적화하는 것보다 더 쉽다는 것이 우리의 가설이다. 극단적인 경우, 최적의 함수가 identity mapping이라면, 여러 비선형 layer로 identity를 직접 학습하는 것보다 residual을 0으로 만드는 편이 훨씬 쉽다. **F(x) + x 구조는 shortcut connection을 가진 feedforward 네트워크로 구현**된다.
Shortcut connection은 하나 이상의 layer를 건너뛰는 연결이며, 우리의 경우 shortcut은 단순한 identity mapping을 수행하고, 그 출력은 쌓인 layer 출력과 더해진다.
이 identity shortcut은 파라미터를 추가하지 않으며 계산량도 증가시키지 않는다.
네트워크 전체는 여전히 기존의 **SGD + backpropagation**으로 end-to-end 학습이 가능하고, Caffe 같은 라이브러리에서 solver를 수정하지 않고 구현할 수 있다.
우리는 ImageNet에서 degradation 문제와 제안 방법을 검증하기 위한 실험을 수행했다. 그 결과:
1. 매우 깊은 residual network는 최적화가 쉽지만, 단순히 layer를 쌓은 plain network는 깊어질수록 training error가 증가한다.
2. residual network는 깊이가 크게 증가해도 정확도를 쉽게 향상시킬 수 있으며, 이전 네트워크보다 훨씬 좋은 결과를 낸다.
CIFAR-10에서도 같은 현상이 나타났으며, 이는 최적화의 어려움과 residual learning의 효과가 특정 데이터셋에 국한되지 않음을 보여준다. 우리는 100층이 넘는 모델을 성공적으로 학습했으며, 1000층 이상의 모델도 탐색했다.
ImageNet 분류에서는 매우 깊은 residual network로 뛰어난 성능을 얻었다. 152-layer ResNet은 ImageNet에서 발표된 모델 중 가장 깊은 네트워크이며, 복잡도는 VGG보다 낮다. 
우리의 앙상블은 ImageNet test set에서 top-5 error 3.57%를 달성해 ILSVRC 2015 분류 과제에서 1위를 차지했다. 또한 이러한 매우 깊은 표현은 다른 시각 인식 문제에서도 뛰어난 일반화 성능을 보였고, ImageNet detection, localization, COCO detection, COCO segmentation에서도 모두 1위를 기록했다. 이는 residual learning이 일반적으로 효과적이며, 비전뿐 아니라 다른 다양한 문제에도 적용 가능함을 보여준다.
---
#### 요약
---
- **배경 — 깊이의 중요성**
- 네트워크 깊이는 성능에 직결됨
- ImageNet 상위권 모델들 모두 16\~30층의 깊은 구조 사용
- 깊은 네트워크일수록 저수준→고수준 특징을 계층적으로 학습 가능
- **문제 1 — 기울기 소실/폭발**
- 역전파 시 기울기가 레이어를 거칠수록 너무 작아지거나 커짐 → 학습 불가
- **해결됨**: 정규화된 초기화 + 배치 정규화(Batch Normalization)로 상당 부분 해결
- 덕분에 수십 층도 학습 가능해짐
- **문제 2 — ****열화(Degradation) 문제**** ★핵심**
- 깊어질수록 정확도가 포화되다가 **오히려 급격히 떨어지는 현상**
- **과적합이 아님** → 테스트 오류뿐 아니라 학습 오류 자체가 높아짐
- **이론적으로는 있어서는 안 되는 문제**
- 깊은 모델은 얕은 모델의 해를 포함하는 더 넓은 해 공간을 가짐
- 추가된 레이어가 항등 함수(입력을 그대로 통과)만 해도 얕은 모델과 동일해야 함
- 그런데 현실에선 최적화 방법이 이 해를 찾지 못함
- **결론**: 최적화 알고리즘의 근본적인 한계
- **해결책 —**** 잔차 학습(Residual Learning)**
- 기존: 레이어들이 **H(x) 전체**를 직접 학습
- ResNet: 레이어들이 **F(x) = H(x) - x, 즉 잔차만** 학습
- 출력: F(x) + x = H(x) → shortcut 연결로 x를 그대로 더해줌
- **왜 더 쉬운가?**
- 항등 함수가 최적이라면 F(x)를 0으로 만들기만 하면 됨
- 가중치를 0으로 밀면 되니까 H(x)=x를 직접 학습하는 것보다 훨씬 쉬움
- **shortcut 연결의 장점**
- 추가 파라미터 없음
- 추가 계산 복잡도 없음
- 기존 SGD 역전파로 그대로 학습 가능
- 구현 간단
---
#### QnA
---


## **깊어질수록 기울기 소실/폭발 생기는 이유**

**역전파할 때 기울기가 레이어를 거칠 때마다 계속 곱해지기 때문이야.**
- 0.9 같은 숫자가 50번 곱해지면 거의 0 → **기울기 소실**
- 1.1 같은 숫자가 50번 곱해지면 엄청 커짐 → **기울기 폭발**
즉, 레이어가 많아지면 숫자를 계속 곱해야 해서 문제가 생김.



## **normalized initialization과 Batch Normalization**

- Normalized Initialization
- 뭐지? 
- 학습 시작 전 가중치의 초깃값을 설정하는 방법
- 너무 크거나 작지 않은 **적당한 범위**로 초기화
- 왜 필요함?
- 가중치가 너무 크면 → 신호가 레이어를 거칠수록 폭발적으로 커짐 → 기울기 폭발
- 가중치가 너무 작으면 → 신호가 레이어를 거칠수록 0에 수렴 → 기울기 소실
- 초깃값을 적절히 설정해야 신호가 레이어를 통과해도 **적당한 크기를 유지**
- Batch Normalization 
- 뭐지?
- 학습 **도중** 각 레이어의 출력값을 정규화하는 기법
- 미니배치 단위로 평균을 빼고 분산으로 나눠서 출력을 0 중심으로 맞춰줌
- 왜 필요함?
- 학습이 진행되면서 앞 레이어 가중치가 바뀌면 뒷 레이어가 받는 입력 분포도 계속 바뀜
- 이걸 \*\*내부 공변량 변화(Internal Covariate Shift)\*\*라고 함
- 레이어마다 입력 분포가 달라지면 학습이 불안정해지고 느려짐
- 어떻게 함?
- 배치 내 데이터의 평균을 빼고 표준편차로 나눔 → 출력이 평균 0, 분산 1이 됨
- 그 다음 γ, β라는 학습 가능한 파라미터로 스케일과 이동을 조정



## **degradation problem의 원인**

> 깊은 네트워크에서 **"아무것도 안 하기(항등 함수)"를 학습하는 게 최적화 알고리즘에겐 너무 어렵기 때문**이에요.
- **왜 항등 함수 학습이 어렵냐?**
레이어는 구조상 **뭔가를 변환하도록** 설계되어 있어요. 가중치 행렬, 활성화 함수, 편향이 전부 입력을 바꾸는 방향으로 작동해요.
이 구조로 "아무것도 안 하기"를 표현하려면 가중치들이 아주 특수한 조합을 이뤄야 하는데, SGD가 수많은 파라미터 공간에서 그 특수한 지점을 찾는 게 현실적으로 매우 어려운 거예요.

---
### 2. Related Work {toggle="true"}
#### 해석
---
### Residual Representations.
이미지 인식에서, VLAD는 사전(dictionary)에 대해 residual vector들을 인코딩하는 표현이고, Fisher Vector는 VLAD의 확률적 버전으로 볼 수 있다. 이 두 방법은 이미지 검색과 분류에서 강력한 얕은(shallow) 표현 방식이다.
벡터 양자화(vector quantization) 분야에서는, residual vector를 인코딩하는 것이 원래 벡터를 인코딩하는 것보다 더 효과적이라는 것이 알려져 있다.
저수준 비전과 컴퓨터 그래픽스에서는, 편미분방정식(PDE)을 푸는 데 사용되는 널리 알려진 Multigrid 방법이 존재한다. 이 방법은 시스템을 여러 스케일의 하위 문제(sub-problem)로 재구성하며, 각 하위 문제는 더 거친(coarser) 스케일과 더 세밀한(finer) 스케일 사이의 residual 해(solution) 를 담당한다.
Multigrid의 대안으로는 hierarchical basis preconditioning이 있는데, 이는 두 스케일 사이의 residual vector를 나타내는 변수들에 기반한다. 이러한 기법들은 residual의 특성을 고려하지 않는 기존 solver보다 훨씬 빠르게 수렴한다는 것이 알려져 있다. 이는 적절한 재구성(formulation) 또는 preconditioning이 최적화를 단순화할 수 있음을 시사한다.
### Shortcut Connections.
shortcut connection에 대한 실천적 기법과 이론은 오랫동안 연구되어 왔다.
🔽 초기 multi-layer perceptron(MLP) 연구에서는 입력을 출력으로 직접 연결하는 linear layer를 추가 <br>      하는 방식이 사용되었다.
🔽또한, 일부 연구에서는 vanishing/exploding gradient 문제를 완화하기 위해 중간 layer를 auxiliary <br>     classifier에 직접 연결하는 구조를 제안한 바 있다.
🔽다른 연구들에서는 layer의 반응(response), gradient, error를 안정화시키기 위해 shortcut <br>     connection을 이용해 중심화(centering)하는 방법이 소개되었다.
Inception 구조 또한 shortcut branch와 더 깊은 branch들을 함께 구성한 형태라고 볼 수 있다.
우리의 연구와 거의 동시에, highway network가 제안되었다.
highway network의 shortcut connection은 게이트(gate) 함수를 포함하며 이 게이트에는 학습 가능한 파라미터가 존재한다. 이는 우리의 **항등 shortcut(identity shortcut)** 과 다르다.
Highway network에서는 게이트가 닫히면(shortcut이 0에 가까워지면), layer는 residual function이 아닌 일반적인 변환을 수행하게 된다. 반면, 우리의 구조에서는 shortcut이 항상 열려 있으며, 모든 정보가 shortcut을 통해 흐른다. 네트워크는 반드시 residual function을 학습하도록 되어 있다.
또한 highway network는 매우 깊은(depth 100 이상) 구조에서의 정확도 향상을 보여주지 못했다는 한계가 있다.
---
#### 요약
---
1. ** 잔차 표현 (Residual Representations) **
- 이미지 인식 분야에서 잔차 벡터를 인코딩하는 방식이 이미 존재했음
- **VLAD**: 사전(dictionary)에 대한 잔차 벡터로 이미지를 표현하는 방법
- **Fisher Vector**: VLAD의 확률적 버전
- 둘 다 이미지 검색과 분류에서 강력한 성능을 보인 얕은 표현 방법
- 벡터 양자화에서도 잔차 벡터 인코딩이 원본 벡터 인코딩보다 효과적임이 증명됨
- **컴퓨터 그래픽스에서도 유사한 개념** 존재
- 편미분 방정식(PDE) 풀 때 쓰는 Multigrid 방법
- 각 서브문제를 서로 다른 스케일의 잔차로 분해해서 풀음
- 잔차 기반 solver가 일반 solver보다 훨씬 빠르게 수렴함
- **핵심 시사점**: 잔차를 학습하는 게 원본을 직접 학습하는 것보다 최적화가 쉽다는 걸 다른 분야에서도 이미 확인함
2. **Shortcut 연결 (Shortcut Connections)**
- Shortcut 연결 아이디어는 오래전부터 연구됨
- **초기 연구들**: MLP 학습에서 입력을 출력에 직접 연결하는 선형 레이어 추가
- **기울기 소실 해결용**: 중간 레이어를 보조 분류기에 직접 연결해서 기울기 전달
- **레이어 응답, 기울기, 전파 오류를 중심화**하는 방법들도 shortcut으로 구현됨
- **GoogLeNet의 Inception 레이어**: shortcut 브랜치와 깊은 브랜치를 합친 구조
- **Highway Networks**: shortcut 연결에 \*\*게이팅 함수(gating function)\*\*를 추가한 방법
- 게이트가 데이터에 따라 열리고 닫히며 정보 흐름 조절
- 게이트 파라미터가 있어서 추가 파라미터 필요
- 게이트가 닫히면(0에 가까워지면) 비잔차 함수가 됨
- **100층 이상에서 정확도 향상을 보여주지 못함**
- **ResNet과의 차이**
- ResNet의 shortcut은 **항등 함수(identity mapping)** → 파라미터 없음
- 게이트가 없어서 항상 열려있음 → 정보가 항상 통과
- 모든 정보가 shortcut을 통해 그대로 전달되고 잔차만 추가로 학습
- Highway Networks와 달리 **극단적으로 깊은 네트워크에서도 정확도 향상** 달성
---
#### QnA
---


## ResNet에서 Shortcut = identity mapping ?

> ResNet의 shortcut 연결은 입력 x를 받아서 **아무런 변환 없이 그대로** 출력 쪽으로 전달
```bash
입력 x
  │
  ├──→ 가중치 레이어들 → F(x)
  │                         │
  └─────────────────────────┤ (+)
           shortcut         │
           (x 그대로)        ↓
                         F(x) + x
```
shortcut 경로에는 가중치도 없고, 곱셈도 없고, 활성화 함수도 없어요. 그냥 x를 그대로 가져다 더해줄 뿐이에요.

---
### 3. Deep Residual Learning {toggle="true"}
### 3.1. Residual Learning {toggle="true"}
#### 해석
---
H(x)를 몇 개의 스택된 층들(전체 네트워크일 필요는 없음)이 맞춰야 하는 기본적인 매핑이라고 생각하자.\{=H(x)는 목표함수다\} 여기서 x는 이 층들 중 첫 번째 층에 입력되는 값을 나타낸다.
만약 여러 개의 비선형 층들이 복잡한 함수들을 점근적으로 근사할 수 있다고 가정한다면, 그들은 residual 함수들, 즉 H(x) − x 또한 점근적으로 근사할 수 있다고 가정하는 것과 동일하다(입력과 출력의 차원이 동일하다고 가정하면). 
= 복잡한 함수도 학습할 수 있으니까 그보다 쉬운 residual 함수는 껌이다
따라서 스택된 층들이 H(x)를 근사하도록 기대하는 대신, 우리는 이 층들이 **residual 함수 F(x) := H(x) − x 를 근사하도록 명시적으로 설정**한다. **그러면 원래의 함수는 F(x) + x **가 된다. 두 형태 모두 (가정에 따르면) 원하는 함수를 점근적으로 근사할 수 있어야 하지만, 학습의 용이성은 서로 다를 수 있다.
이러한 재공식화는 degradation 문제(그림 1, 왼쪽)에서의 직관적이지 않은 현상으로부터 동기부여된다. 도입부에서 논의했듯이, 추가된 층들이 identity mapping(항등 매핑)으로 구성될 수 있다면, 더 깊은 모델은 더 얕은 모델보다 훈련 오류가 더 커질 이유가 없다. 
degradation 문제는, 여러 비선형 층들로 identity mapping을 근사하는 데 solver가 어려움을 겪을 수 있음을 시사한다. residual learning 재공식화를 사용하면, 만약 identity mapping이 최적이라면, solver는 identity mapping에 접근하기 위해 여러 비선형 층들의 weight를 단순히 0에 가깝게 만들기만 하면 된다.
실제의 경우에는 identity mapping이 최적일 가능성은 낮지만, 우리의 재공식화는 문제를 precondition하는 데 도움이 될 수 있다. 만약 최적의 함수가 0 매핑보다는 identity mapping에 더 가깝다면, solver는 함수를 새롭고 독립적인 형태로 학습하기보다는 identity mapping을 기준으로 작은 변화(perturbation)를 찾는 것이 더 쉬울 것이다. 우리는 실험(그림 7)을 통해 학습된 residual 함수들이 일반적으로 작은 반응 값을 가진다는 것을 보여주며, 이는 identity mapping이 합리적인 preconditioning을 제공함을 시사한다.
---
#### 요약
---
- **핵심 아이디어**
- 몇 개 레이어 묶음이 H(x)를 학습해야 한다고 할 때
- 기존: H(x) 직접 학습
- ResNet: **F(x) = H(x) - x 를 학습** → 출력에 x를 더해서 H(x) 복원
- 둘 다 표현력은 동일 → 하지만 **최적화 난이도가 다름**
- **왜 잔차 학습이 더 쉬운가?**
- 열화 문제의 원인: 깊은 네트워크에서 추가 레이어들이 **항등 함수를 학습하지 못함**
- 기존 방식: H(x) = x를 직접 학습 → 가중치들이 특수한 조합을 이뤄야 해서 **어려움**
- ResNet: F(x) = 0만 만들면 됨 → 가중치를 0으로 밀면 되니까 **쉬움**
- 최적값이 항등 함수와 가깝다면 → **0 근처에서 출발해서 조금만 움직이면 됨**
- **Preconditioning 효과**
- 항등 함수가 항상 최적은 아님 → F(x) = 0이 최적인 경우는 드묾
- 그래도 최적값이 0 근처라면 → 0에서 출발하는 게 훨씬 유리
- **실험(Fig.7) 결과**: F(x) 값이 실제로 작게 나옴 → 가정이 맞았음을 확인
---
#### QnA
---


## **residual 함수들이 일반적으로 작응 반응 값을 가진다?**

- **ResNet의 가정**이 뭐였냐면 이거예요.
> 최적값이 항등 함수(H(x) = x)에 가까울 것이다. 그러면 F(x) = H(x) - x는 0에 가까운 작은 값일 것이다.
**실험(Fig.7) 결과**를 보니까 실제로 F(x) 값들이 작게 나왔어요.
- 왜 이게 중요한가?
F(x)가 작다는 건 두 가지를 의미해요 :
- **첫째**, 레이어들이 입력을 크게 바꾸지 않아도 됨 → 항등 함수가 기본 베이스라는 가정이 맞았음
- **둘째**, preconditioning이 실제로 효과가 있었음 → 최적값이 0 근처에 있으니까 학습이 쉬웠음

---
### 3.2. Identity Mapping by Shortcuts {toggle="true"}
#### 해석
---
우리는 residual learning을 몇 개의 스택된 층마다 적용한다. 하나의 building block은 그림 2에 나타나 있다. 형식적으로, 이 논문에서 우리는 다음과 같이 정의되는 building block을 고려한다:
```plain text
y = F(x, {Wi}) + x   (식 1)
```
여기서 x와 y는 해당 층들이 받는 입력 벡터와 출력 벡터이다. F(x, \{Wi\}) 함수는 학습되어야 하는 residual mapping을 나타낸다. 그림 2의 예시처럼 두 개의 층을 가진 경우, F = W₂ σ(W₁ x) 이고
여기서 σ는 ReLU [29]를 나타내며, 단순화를 위해 bias는 표기에서 생략한다.
[image omitted: temporary Notion asset]
F + x 연산은 shortcut connection과 element-wise addition(요소별 덧셈)에 의해 수행된다. 우리는 addition 이후에 두 번째 비선형성(즉, σ(y))을 적용한다(그림 2 참고). 이 식 (1)의 shortcut connection은 **추가 파라미터나 추가 계산량을 발생시키지 않는다**. 이는 실제적으로 매력적일 뿐 아니라, plain 네트워크와 residual 네트워크를 비교할 때 중요한 점이다.
우리는 파라미터 수, 깊이, 폭, 계산 비용이 동일한 plain/residual 네트워크를 공정하게 비교할 수 있다(미미한 element-wise addition 계산량을 제외하고). 식 (1)에서 x와 F의 차원은 동일해야 한다.
만약 그렇지 않은 경우(예: input/output 채널 수가 달라지는 경우), shortcut connection에서 선형 투영 Wₛ 를 적용하여 차원을 맞출 수 있다:
```plain text
y = F(x, {Wi}) + Wₛ x   (식 2)
```
우리는 식 (1)에 제곱 행렬(square matrix) Wₛ를 사용하는 것도 가능하다. 그러나 실험을 통해 identity mapping이 degradation 문제를 해결하는 데 충분하며 경제적임을 보여줄 것이며, 따라서 Wₛ 는 **오직 차원을 맞춰야 할 때만 사용된다**.
Residual function F의 형태는 유연하다. 이 논문의 실험에서는 F가 두 개 또는 세 개의 층을 가진 경우(Fig. 5)를 사용하며, 더 많은 층을 사용하는 것도 가능하다.
[image omitted: temporary Notion asset]
하지만 만약 F가 단일 층만 가진다면, 식 (1)은 다음과 같은 선형 층과 유사하게 된다:
```plain text
y = W₁ x + x
```
이 경우에서는 특별한 장점을 관찰하지 못했다. 또한 위의 표기들은 단순성을 위해 fully-connected layer 기준으로 설명했지만, 동일한 개념이 convolutional layer에도 적용된다. F(x, \{Wi\}) 함수는 여러 개의 convolutional layer를 나타낼 수 있다. Element-wise addition은 두 개의 feature map에 대해 채널 단위로 수행된다.
---
#### 요약
---
- **핵심 공식**
- **기본 building block**: y = F(x, \{Wi\}) + x
- x: 입력
- F(x, \{Wi\}): 잔차 함수 (레이어들이 학습)
- y: 출력
- shortcut: x를 그대로 더함
- **shortcut의 두 가지 경우**
**경우 1 — 입출력 차원이 같을 때**
- y = F(x, \{Wi\}) + x
- x를 그대로 더함
- **추가 파라미터 없음**
- **추가 계산 복잡도 없음**
**경우 2 — 입출력 차원이 다를 때**
- y = F(x, \{Wi\}) + Wsx
- Ws: 차원을 맞춰주는 선형 변환
- **차원 맞출 때만 사용** → 불필요한 복잡도 방지
- 1×1 합성곱으로 구현
- **Ws를 항등 변환에도 쓸 수 있지 않나?**
- 이론적으로 Ws를 항상 쓸 수도 있음
- 하지만 실험으로 **항등 shortcut으로 충분함**을 확인
- Ws를 남발하면 파라미터만 늘어나고 이득이 없음
- 그래서 **차원이 같을 땐 항등 shortcut만 사용**
- **F의 형태는 유연함**
- **2개 레이어 묶음**: F = W₂σ(W₁x) (σ는 ReLU)
- **3개 레이어 묶음**: bottleneck 구조에서 사용
- **1개 레이어면 의미 없음**: y = W₁x + x → 그냥 선형 레이어와 동일
- 완전연결층, 합성곱층 모두 적용 가능
- 합성곱층에서는 **같은 위치, 같은 채널끼리** element-wise 덧셈
- **shortcut의 핵심 장점**
- 파라미터 추가 없음 → **모델 크기 그대로**
- 계산 복잡도 추가 없음 → **연산량 그대로**
- Plain net과 **공정한 비교 가능** (파라미터 수, 깊이, 너비, 연산량 동일)
- 역전파 시 기울기가 shortcut을 통해 **그대로 전달** → 기울기 소실 완화
---
#### QnA
---


## **Bottleneck **

- **단어 뜻부터**
> Bottleneck = 병목 현상. 병의 목처럼 **중간이 좁아지는 구조**예요.
- **ResNet에서 Bottleneck이란?**
3개 레이어를 이렇게 구성하는 구조예요.
```bash
입력 (256 채널)
      ↓
1×1 conv (64 채널)   ← 채널 수를 확 줄임 (압축)
      ↓
3×3 conv (64 채널)   ← 줄어든 채널에서 연산
      ↓
1×1 conv (256 채널)  ← 채널 수를 다시 늘림 (복원)
      ↓
출력 (256 채널)
```
중간에 채널이 256 → 64 → 256으로 **좁아졌다가 다시 넓어지는** 모양이 병목처럼 생겼다고 해서 bottleneck이에요.
- **왜 이렇게 하나?**
- 3×3 conv를 바로 256 채널에 적용하면 계산량이 엄청 커요.
- 1×1 conv로 먼저 채널을 줄이고 → 3×3 conv 적용 → 다시 늘리면 **계산량을 대폭 줄이면서도 비슷한 성능**을 낼 수 있어요.
<table header-row="true">

<tr>
<td>방식</td>
<td>계산량</td>
</tr>
<tr>
<td>3×3 conv 2개 (일반)</td>
<td>많음</td>
</tr>
<tr>
<td>Bottleneck (1×1 → 3×3 → 1×1)</td>
<td>훨씬 적음</td>
</tr>
</table>
- **언제 쓰나?**
- **얕은 ResNet** (ResNet-18, 34): 2개 레이어 일반 블록 사용
- **깊은 ResNet** (ResNet-50, 101, 152): Bottleneck 블록 사용
- 깊어질수록 계산량 부담이 커지니까 bottleneck으로 효율화

---
### 3.3. Network Architectures {toggle="true"}
#### 해석
---


[image omitted: temporary Notion asset]


우리는 다양한 plain/residual 네트워크를 테스트했으며, 일관된 현상을 관찰하였다. 
논의를 위한 구체적인 예시를 제공하기 위해, 우리는 ImageNet용 두 가지 모델을 다음과 같이 설명한다. 먼저 **Plain Network**는 (그림 3 가운데) 주로 VGG 네트워크(그림 3 왼쪽)의 철학에 의해 영향을 받았다. 
이 네트워크의 convolution layer는 대부분 3×3 필터를 사용하며, 두 가지 단순한 설계 규칙을 따른다: 
1. 같은 출력 feature map 크기를 갖는 층들은 같은 수의 필터를 사용한다 
2. **feature map의 공간 크기가 절반으로 줄어들면, 각 층의 시간적 계산 복잡도를 유지하기 위해 필터 수는 두 배로 늘린다. **
다운샘플링은 stride 2를 갖는 convolution layer를 통해 직접 수행된다. 
네트워크의 마지막은 global average pooling layer와 softmax를 갖는 1000-way fully-connected layer로 끝난다. 그림 3 (가운데)의 모델에서 가중치를 갖는 층의 총 개수는 34개이다. 우리의 모델은 VGG 네트워크(그림 3 왼쪽)보다 더 적은 필터 수와 더 낮은 복잡도를 가진다는 점을 주목할 만하다. 우리의 34-layer baseline은 3.6 billion FLOPs(multiply-adds)를 가지며, 이는 VGG-19의 19.6 billion FLOPs의 18%에 불과하다.


**Residual Network**는 위에서 설명한 plain network를 기반으로 하며, 여기에 shortcut connection(그림 3 오른쪽)을 삽입하여 대응되는 residual 버전으로 만든 것이다. 
입력과 출력의 차원이 동일할 때(그림 3의 실선 shortcut), identity shortcut(식 1)은 그대로 사용할 수 있다. 차원이 증가하는 경우(그림 3의 점선 shortcut)에는 두 가지 선택지를 고려한다: 
1. shortcut이 여전히 identity mapping을 수행하지만, 차원을 증가시키기 위해 0을 패딩하여 추가한다. 이 옵션은 추가 파라미터를 전혀 도입하지 않는다. 
2. 식 (2)의 projection shortcut을 사용하여 차원을 맞추며, 이는 1×1 convolution으로 구현된다.
두 옵션 모두 shortcut이 두 가지 다른 크기의 feature map을 가로지를 때 stride 2로 수행된다.
---
#### 요약
---
> 전체 구조 — Plain Network와 Residual Network 두 가지를 비교
- **Plain Network (일반 네트워크)**
- **VGGNet 철학**에서 영감을 받아 설계
- 설계 원칙 두 가지
- 출력 특징맵 크기가 같으면 → **필터 수 동일**
- 출력 특징맵 크기가 절반으로 줄면 → **필터 수 두 배** (연산량 유지)
- 대부분 **3×3 conv** 사용
- 크기 줄일 때 **stride 2** conv 사용 (풀링 대신)
- 마지막: **global average pooling → fc 1000**
- 총 34개 가중치 레이어
- **VGG-19보다 훨씬 효율적**
- ResNet-34: 3.6 billion FLOPs
- VGG-19: 19.6 billion FLOPs
- ResNet-34가 VGG-19의 **18%** 연산량
- **Residual Network (잔차 네트워크)**
- Plain Network에 **shortcut 연결만 추가**한 구조
- 차원이 같으면 → **항등 shortcut** (파라미터 없음)
- 차원이 다를 때 두 가지 옵션
- **옵션 A**: 0 패딩으로 차원 맞춤 → 추가 파라미터 없음
- **옵션 B**: 1×1 conv(Ws)로 차원 맞춤 → 파라미터 있음
---
#### QnA
---


## **Fig.3의 숫자의 의미**

<table header-row="true">

<tr>
<td>부분</td>
<td>의미</td>
</tr>
<tr>
<td>**3×3**</td>
<td>필터(커널) 크기. 이미지의 3×3 픽셀 영역을 한 번에 봄</td>
</tr>
<tr>
<td>**conv**</td>
<td>합성곱 레이어</td>
</tr>
<tr>
<td>**64**</td>
<td>필터 개수 = 출력 채널 수. 64가지 특징을 추출</td>
</tr>
</table>

---
### 3.4. Implementation {toggle="true"}
#### 해석
---
ImageNet 실험을 위한 우리의 구현은 [21, 40]의 방식을 따른다. 이미지는 scale augmentation을 위해 짧은 변(shorter side)을 256\~480 범위에서 무작위로 선택하여 리사이즈한다. 그 후 224×224 크기의 crop을 원본 이미지 또는 그 수평 뒤집기(horizontal flip)에서 무작위로 추출하고, per-pixel mean을 빼준다 [21]. 또한 [21]의 표준 컬러 증가(color augmentation) 기법을 사용한다.
각 convolution 뒤, activation(활성함수) 전에 batch normalization(BN) [16]을 적용하며, 이는 [16]의 방식을 따른 것이다. 가중치 초기화는 [12]의 방법을 사용하며, plain/residual 네트워크는 모두 처음부터(scratch) 학습한다.
최적화는 mini-batch 크기 256의 SGD를 사용하며, 초기 learning rate는 0.1이고, 에러가 더 이상 줄어들지 않는 plateau 지점에서 10배씩 감소시킨다. 전체 모델은 최대 60×10⁴ iterations까지 학습한다. weight decay는 0.0001, momentum은 0.9를 사용하며, [16]의 관례에 따라 dropout [13]은 사용하지 않는다.
테스트 단계에서는 비교 실험을 위해 [21]의 standard 10-crop testing을 사용한다. 최고 성능을 위해서는 [40, 12]에서 사용된 fully convolutional 형태를 채택하며, 여러 스케일에서 점수를 평균한다. 이때 이미지의 짧은 변은 \{224, 256, 384, 480, 640\} 중 하나가 되도록 리사이즈된다.
---
#### 요약
---
- **데이터 전처리 (학습 시)**
- 짧은 쪽이 **256\~480** 사이가 되도록 랜덤하게 리사이즈 (scale augmentation)
- 리사이즈된 이미지에서 **224×224 랜덤 크롭**
- **좌우 반전** 적용
- **픽셀 평균값 빼기** (AlexNet과 동일)
- **PCA 색상 변화** 적용 (AlexNet과 동일)
- **정규화**
- 각 conv 후, 활성화 함수 전에 **Batch Normalization(BN)** 적용
- Dropout **사용 안 함** → BN이 정규화 역할을 대신
- **가중치 초기화**
- He 초기화 방법 사용
- Plain Network와 Residual Network 모두 **처음부터 학습** (사전학습 없음)
- **학습 설정**
- 옵티마이저: **SGD**
- 미니배치 크기: **256**
- 초기 학습률: **0.1**
- 오류가 정체되면 **÷10** (AlexNet과 동일한 방식)
- Weight decay: **0.0001**
- Momentum: **0.9**
- 총 **60만 번** iteration 학습
- **테스트 방법**
- **10-crop testing** 적용 (AlexNet과 동일)
- 4귀퉁이 + 중앙 5개 크롭 × 좌우 반전 = 10개 예측 평균
- 더 좋은 성능을 위해 **fully-convolutional** 방식도 사용
- 여러 스케일로 리사이즈 후 점수 평균
- 짧은 쪽을 \{224, 256, 384, 480, 640\}으로 리사이즈
---
#### QnA
---


## **fully-convolutional**

- **기존 10-crop testing의 한계**
- 이미지에서 **딱 10개 위치**만 잘라서 예측
- 나머지 위치 정보는 버려짐
- 10개 평균이라 어느 정도 좋지만 **놓치는 정보가 있음**
- **Fully-Convolutional 방식이란?**
크롭 없이 **이미지 전체를 한 번에** 네트워크에 통과시키는 방법이에요.
FC층을 conv층으로 바꿔서 어떤 크기의 이미지든 입력 가능하게 만들어요. 그러면 이미지 전체에서 **모든 위치의 정보**를 다 활용할 수 있어요.
- **여러 스케일로 테스트하는 이유**
같은 고양이 사진이라도 크게 찍혔을 수도, 작게 찍혔을 수도 있어요. 하나의 크기로만 테스트하면 특정 크기에서만 잘 인식할 수 있어요.
그래서 짧은 쪽을 **\{224, 256, 384, 480, 640\}** 다섯 가지 크기로 리사이즈한 후 각각 예측하고 평균을 내요.
```bash
원본 이미지
    ↓ 5가지 크기로 리사이즈
224, 256, 384, 480, 640
    ↓ 각각 예측
5개 결과 평균
    ↓
최종 예측
```

---
### 4. Experiments {toggle="true"}
### 4.1. ImageNet Classification {toggle="true"}
#### 해석
---
[image omitted: temporary Notion asset]


[image omitted: temporary Notion asset]
[image omitted: temporary Notion asset]


[image omitted: temporary Notion asset]


우리는 1000개의 클래스들로 구성된 ImageNet 2012 분류 데이터셋 [35]에서 우리의 방법을 평가한다. 모델들은 128만 개의 학습 이미지에서 학습되며, 5만 개의 검증 이미지에서 평가된다. 또한 10만 개의 테스트 이미지에 대해 테스트 서버가 보고하는 최종 결과도 얻는다. 우리는 top-1과 top-5 오류율을 모두 평가한다.
**Plain Networks.** 
우리는 먼저 18-layer 및 34-layer plain 네트워크들을 평가한다. 34-layer plain 네트워크는 그림 3(가운데)에 있다. 18-layer plain 네트워크도 유사한 형태이다. 자세한 아키텍처는 표 1을 참조하라.
표 2의 결과는 더 깊은 34-layer plain 네트워크가 더 얕은 18-layer plain 네트워크보다 더 높은 검증 오류를 가진다는 것을 보여준다. 그 이유를 밝히기 위해, 그림 4(왼쪽)에서 학습 과정 동안 그들의 학습/검증 오류를 비교한다. 우리는** degradation 문제**를 관찰하였다 — 34-layer plain 네트워크는 전체 학습 절차 동안 더 높은 training error를 보이는데, 이는 18-layer plain 네트워크의 해 공간(solution space)이 34-layer의 해 공간의 부분집합임에도 불구하고 그렇다.
우리는 이러한 최적화 어려움이 gradient vanishing에 의해 발생했다고 보기는 어렵다고 주장한다. 이러한 plain 네트워크들은 BN[16]을 사용하여 학습되며, 이는 forward로 전파되는 신호들이 0이 아닌 분산을 갖도록 보장한다. 우리는 backward로 전파되는 gradient들도 건강한(norm이 정상적인) 값을 갖는다는 것을 확인하였다. 따라서 forward 신호나 backward 신호가 사라지지는 않는다. 실제로 34-layer plain 네트워크는 여전히 경쟁력 있는 정확도(표 3)를 달성할 수 있는데, 이는 solver가 어느 정도는 작동하고 있음을 시사한다. 우리는 깊은 plain 네트워크들이 지수적으로 낮은 수렴률을 가질 수 있으며, 이것이 training error 감소에 영향을 미친다고 추측한다(각주 3). 이러한 최적화 어려움의 원인은 향후 연구될 것이다.
**Residual Networks.** 
다음으로 우리는 18-layer 및 34-layer residual 네트워크(ResNets)를 평가한다. 기본 아키텍처는 위의 plain 네트워크와 동일하되, 그림 3(오른쪽)과 같이 각 3×3 필터 쌍마다 shortcut connection이 추가되어 있다. 첫 번째 비교(표 2 및 그림 4 오른쪽)에서는 모든 shortcut에 대해 identity mapping을 사용하고, 차원이 증가하는 경우 zero-padding(옵션 A)을 사용한다. 따라서 plain 네트워크 대비 추가 파라미터가 없다.
표 2와 그림 4에서 우리는 세 가지 주요 관찰을 얻는다. 
첫째, residual learning을 적용하면 상황이 반전된다 — 34-layer ResNet은 18-layer ResNet보다 더 좋다(top-1 기준 2.8% 향상). 더 중요한 점은, 34-layer ResNet이 상당히 낮은 training error를 보이며 검증 데이터에서도 일반화된다는 것이다. 이는 **degradation 문제가 이 설정에서 잘 해결**되었으며, 깊이가 증가함으로써 정확도 향상을 얻을 수 있음을 의미한다.
둘째, plain 네트워크와 비교하면, 34-layer ResNet은 top-1 에러를 3.5% 줄이며(표 2), 이는 성공적으로 낮아진 training error(그림 4 오른쪽 vs 왼쪽)에서 비롯된다. 이 비교는 극도로 깊은 시스템에서 residual learning의 효과를 검증한다. → plain보다도 좋다
마지막으로, 18-layer plain/residual 네트워크는 비슷한 정확도를 보이지만(표 2), 18-layer ResNet이 더 빠르게 수렴한다(그림 4 오른쪽 vs 왼쪽). 네트워크가 “지나치게 깊지 않은”(여기서는 18-layer) 경우에는 현재의 SGD solver가 plain 네트워크에 대해서도 여전히 좋은 해법을 찾을 수 있다. 이 경우 ResNet은 초기 단계에서 더 빠른 수렴을 제공함으로써 최적화를 돕는다.
**Identity vs. Projection Shortcuts.** 
우리는 파라미터가 없는 identity shortcut이 학습에 도움이 된다는 것을 보였다. 다음으로 projection shortcut(Eq.(2))을 조사한다. 표 3에서는 세 가지 옵션을 비교한다: 
(A) 차원이 증가할 때 zero-padding shortcut을 사용하며 모든 shortcut은 파라미터가 없음(표 2와 <br>      그림 4 오른쪽과 동일)
(B) 차원 증가 시 projection shortcut을 사용하고, 나머지는 identity
(C) 모든 shortcut을 projection으로 사용.
표 3은 세 옵션 모두 plain 네트워크보다 상당히 더 좋다는 것을 보여준다. B가 A보다 약간 더 좋은데, 이는 A에서 **zero-padding된 차원들은 실제 residual learning이 일어나지 않기 때문**이라고 본다. C는 B보다 근소하게 더 좋은데, 이는 여러(13개) projection shortcut이 도입하는 **추가 파라미터 때문**이라고 본다. 그러나 A/B/C의 **작은 차이는 degradation 문제를 해결하는 데 projection shortcut이 필수적이지 않다는 것을 나타낸다**. 따라서 우리는 이후 실험에서 모델 크기와 메모리/시간 복잡도를 줄이기 위해 옵션 C를 사용하지 않는다. Identity shortcut은 이후 소개하는 bottleneck 아키텍처에서 복잡도를 증가시키지 않는 데 특히 중요하다.
**Deeper Bottleneck Architectures.** 
이제 우리는 ImageNet을 위한 더 깊은 네트워크를 설명한다. 우리가 감당할 수 있는 학습 시간에 대한 우려 때문에, building block을 bottleneck 구조로 수정한다. 각 residual 함수 F는 2층이 아닌 3층(그림 5)으로 구성된다. 세 층은 1×1, 3×3, 1×1 convolution이며, 1×1 층들은 차원을 줄이고 늘리는 역할을 하여, 3×3 층이 더 작은 입력/출력 차원을 갖는 bottleneck 역할을 맡는다. 그림 5는 두 설계가 유사한 시간 복잡도를 갖는 예를 보여준다.
파라미터가 없는 identity shortcut은 bottleneck 아키텍처에서 특히 중요하다. 그림 5(오른쪽)에서 identity shortcut을 projection으로 대체하면, shortcut이 고차원 양 끝에 연결되므로 시간 복잡도와 모델 크기가 두 배가 된다. 따라서 bottleneck 설계에서는 identity shortcut이 더 효율적인 모델을 만든다.
**50-layer ResNet:** 우리는 34-layer 네트워크의 각 2층 블록을 이 3층 bottleneck 블록으로 대체하여 50-layer ResNet(표 1)을 만든다. 차원이 증가하는 경우 option B를 사용한다. 이 모델은 3.8 billion FLOPs를 가진다.
**101-layer 및 152-layer ResNet:** 우리는 더 많은 3층 블록을 사용하여 101-layer 및 152-layer ResNet을 구성한다(표 1). 주목할 만한 점은, 깊이가 상당히 증가했음에도 불구하고 152-layer ResNet(11.3 billion FLOPs)은 여전히 VGG-16/19(15.3/19.6 billion FLOPs)보다 낮은 복잡도를 가진다는 것이다.
50/101/152-layer ResNet들은 34-layer 모델보다 상당한 폭으로 더 정확하다(표 3 및 4). 우리는 degradation 문제를 관찰하지 않았으며, 깊이가 크게 증가함에 따라 의미 있는 정확도 향상을 얻는다. 깊이의 이점은 모든 평가 지표에서 확인된다(표 3 및 4).
**최첨단 기법들과의 비교:** 표 4에서 이전 최고 single-model 결과들과 비교한다. 우리의 34-layer ResNet baseline은 매우 경쟁력 있는 정확도를 달성했다. 우리의 152-layer ResNet은 단일 모델 top-5 검증 오류가 4.49%이다. 이 single-model 결과는 이전의 모든 ensemble 결과들을 능가한다(표 5). 우리는 깊이가 다른 6개의 모델을 결합하여 ensemble을 구성한다(논문 제출 시점에는 그중 2개만 152-layer였다). 이것은 테스트 세트에서 3.57% top-5 error를 달성했다(표 5). 이 entry는 ILSVRC 2015에서 1위를 차지했다.
---
#### 요약
---
- 실험 설정
- ImageNet 2012 데이터셋 (1000 클래스)
- 학습: **128만 장**
- 검증: **5만 장**
- 테스트: **10만 장**
- Top-1, Top-5 오류율로 평가
- 실험 1 — Plain Network vs ResNet
**Plain Network 결과**
- 34층 plain이 18층 plain보다 **오류율이 더 높음**
- 열화 문제 확인
- 기울기 소실 때문이 아님 (BN으로 확인)
- 수렴 속도가 지수적으로 느려지는 것으로 추측
**ResNet 결과**
- 34층 ResNet이 18층 ResNet보다 **오류율이 더 낮음**
- 열화 문제 해결 확인
- 34층 ResNet이 plain-34보다 Top-1 오류율 **3.5% 감소**
- 18층에서도 ResNet이 plain보다 **초반 수렴 속도 빠름**
- 실험 2 — Shortcut 옵션 비교 (Table 3)
세 가지 옵션 비교했어요.
<table header-row="true">
<tr>
<td>옵션</td>
<td>방식</td>
<td>Top-1 오류율</td>
</tr>
<tr>
<td>**A**</td>
<td>차원 증가 시 0 패딩 shortcut</td>
<td>25.03%</td>
</tr>
<tr>
<td>**B**</td>
<td>차원 증가 시만 projection shortcut</td>
<td>24.52%</td>
</tr>
<tr>
<td>**C**</td>
<td>모든 shortcut에 projection</td>
<td>24.19%</td>
</tr>
</table>
- A \< B \< C 순으로 성능 향상
- **B가 A보다 좋은 이유**: 0 패딩된 차원은 잔차 학습을 못하기 때문
- **C가 B보다 좋은 이유**: projection shortcut 파라미터가 추가되기 때문
- 하지만 **A, B, C 차이가 매우 작음** → projection shortcut이 핵심이 아님
- 이후 실험에서 **옵션 B 사용** (복잡도와 성능의 균형)
- 실험 3 — Deeper Bottleneck Architecture
**50층, 101층, 152층 ResNet**
- 깊어질수록 계산량이 너무 커져서 **Bottleneck 구조** 도입
- 1×1 → 3×3 → 1×1 conv 3개 레이어 묶음
- 1×1 conv로 채널 줄이고 → 3×3 conv 연산 → 1×1 conv로 복원
- **파라미터 없는 항등 shortcut이 특히 중요**
- Bottleneck에서 Ws 쓰면 복잡도와 크기가 2배로 커짐
**성능 결과 (Table 3, 4)**
<table header-row="true">
<tr>
<td>모델</td>
<td>Top-1 오류율</td>
<td>Top-5 오류율</td>
</tr>
<tr>
<td>ResNet-34 B</td>
<td>24.52%</td>
<td>7.46%</td>
</tr>
<tr>
<td>ResNet-50</td>
<td>22.85%</td>
<td>6.71%</td>
</tr>
<tr>
<td>ResNet-101</td>
<td>21.75%</td>
<td>6.05%</td>
</tr>
<tr>
<td>ResNet-152</td>
<td>21.43%</td>
<td>5.71%</td>
</tr>
</table>
- 깊어질수록 꾸준히 성능 향상
- 열화 문제 없이 깊이의 이점을 온전히 누림
- 최종 성능 비교 (Table 4, 5)
**Single model 결과**
- ResNet-152: Top-5 오류율 **4.49%**
- 이전 최고 PReLU-net(5.71%)을 능가
- **단일 모델로 이전 앙상블 결과를 뛰어넘음**
**앙상블 결과**
- 6개 모델 앙상블: Top-5 오류율 **3.57%**
- ILSVRC 2015 **1위** 달성
- 2위와 비교해도 압도적 차이
---
### 4.2. CIFAR-10 and Analysis {toggle="true"}
#### 해석
---
[image omitted: temporary Notion asset]


[image omitted: temporary Notion asset]


[image omitted: temporary Notion asset]


우리는 50k개의 훈련 이미지와 10k개의 테스트 이미지(10개 클래스)로 구성된 CIFAR-10 데이터셋[20]에 대해 더 많은 연구를 진행하였다. 우리는 훈련 세트에서 학습하고 테스트 세트에서 평가한 실험을 제시한다. 우리의 초점은 최신 성능을 밀어붙이는 것이 아니라, **극도로 깊은 네트워크들의 동작 특성**에 있으므로 의도적으로 단순한 아키텍처를 사용한다.
plain 모델과 residual 모델의 아키텍처는 그림 3(가운데/오른쪽)의 형태를 따른다. 네트워크 입력은 32×32 이미지이며, 픽셀별 평균을 뺀다. 첫 번째 레이어는 3×3 합성곱이다. 그다음 특징 맵 크기가 \{32, 16, 8\}일 때 각각 3×3 합성곱으로 구성된 6n개의 레이어 스택을 사용하며, 각 크기마다 2n개의 레이어를 사용한다. 필터 개수는 순서대로 \{16, 32, 64\}이다. 서브샘플링은 stride 2의 합성곱으로 수행한다. 네트워크는 global average pooling, 10-way fully-connected layer, softmax로 끝난다. 총 6n+2개의 가중치를 가진 레이어가 있다. 
아키텍처 요약은 다음과 같다:
<table header-row="true">
<tr>
<td>출력 맵 크기</td>
<td>32×32</td>
<td>16×16</td>
<td>8×8</td>
</tr>
<tr>
<td># 레이어</td>
<td>1+2n</td>
<td>2n</td>
<td>2n</td>
</tr>
<tr>
<td># 필터</td>
<td>16</td>
<td>32</td>
<td>64</td>
</tr>
</table>
shortcut connection을 사용할 때는 3×3 레이어 쌍마다 연결되며(총 3n개의 shortcut), 이 데이터셋에서는 모든 경우 identity shortcut(옵션 A)를 사용한다. 따라서 residual 모델은 plain 모델과 깊이, 폭, 파라미터 수가 완전히 동일하다.
우리는 weight decay 0.0001, momentum 0.9를 사용하며, [12]의 weight initialization과 BN[16]을 사용하지만 dropout은 사용하지 않는다. 모델은 2개의 GPU에서 minibatch 크기 128로 훈련된다. 학습률은 0.1에서 시작하여 32k, 48k iteration에서 10배씩 줄이고, 64k iteration에서 학습을 종료한다. 이 스케줄은 45k/5k 훈련/검증 분할에서 결정되었다. 
훈련 시에는 [24]의 간단한 데이터 증강을 따른다: 각 변에 4픽셀 패딩 후 32×32 크롭을 패딩된 이미지 또는 수평 뒤집기에서 무작위로 샘플링한다. 테스트 시에는 원본 32×32 이미지 단일 뷰만 평가한다.
우리는 n = \{3, 5, 7, 9\}을 비교하여 20, 32, 44, 56-layer 네트워크를 얻었다. 그림 6(왼쪽)은 plain 네트워크의 동작을 보여준다. 깊은 plain 네트워크는 깊이가 증가할수록 학습 에러가 상승하는 현상을 보인다. 이 현상은 ImageNet(Fig. 4 왼쪽)이나 MNIST([41])에서도 나타나며, 이러한 최적화 어려움이 근본적인 문제임을 시사한다.
그림 6(가운데)은 ResNet의 동작을 보여준다. ImageNet 실험(Fig. 4 오른쪽)과 유사하게 ResNet은 최적화의 어려움을 극복하며, 깊이가 증가할수록 정확도 향상을 보여준다.
우리는 n = 18인 경우도 탐구했는데, 이는 110-layer ResNet이 된다. 이 경우 초기 학습률 0.1은 수렴을 시작하기에 약간 큰 것으로 나타났다. 그래서 학습 초반 약 400 iteration 동안 학습 오류가 80% 아래로 떨어질 때까지 학습률 0.01로 warm-up한 뒤 다시 0.1로 올려 학습을 지속했다. 나머지 학습 스케줄은 동일하다. 이 110-layer 네트워크는 잘 수렴한다(Fig. 6 가운데). 이 모델은 FitNet[34]이나 Highway[41] 같은 다른 얇고 깊은 모델보다 파라미터 수가 적지만(Table 6), 여전히 state-of-the-art 수준(6.43%)이다.
**Layer Response 분석.**
그림 7은 레이어 반응의 표준편차(std)를 보여준다. 이 반응은 각 3×3 layer의 출력이며, BN 이후이지만 ReLU/더하기 전에 측정한다. ResNet에서는 이 분석이 residual function의 반응 강도를 보여준다.
그림 7은 ResNet이 plain 네트워크에 비해 전반적으로 layer response가 더 작다는 것을 보여준다. 이는 residual function이 일반적으로 0에 더 가깝다는 우리의 기본 동기(Sec. 3.1)를 지지한다. 또한 그림에서 ResNet-20, 56, 110을 비교하면, 더 깊은 ResNet일수록 개별 레이어의 반응 크기가 더 작다. 즉, 레이어가 입력 신호를 수정하는 정도가 깊어질수록 줄어드는 경향이 있다.
**1000 레이어 이상 탐구.**
우리는 1000 레이어를 넘는 매우 깊은 모델도 탐구했다. n = 200을 설정하여 1202-layer 네트워크를 만들고, 앞서 설명한 방식으로 훈련했다. 우리의 방식은 최적화에 문제가 없었고, 이 1202-layer 네트워크는 학습 에러 \< 0.1% (Fig. 6 오른쪽)를 달성하였다. 테스트 에러도 비교적 괜찮은 7.93%(Table 6)이다.
하지만 이렇게 극단적으로 깊은 모델에는 여전히 해결되지 않은 문제가 있다. 이 1202-layer 모델의 테스트 결과는 110-layer 모델보다 좋지 않은데, 둘의 학습 오류는 거의 동일하다. 우리는 이를 **overfitting** 때문이라고 본다. CIFAR-10처럼 작은 데이터셋에서는 19.4M 파라미터의 1202-layer 모델이 지나치게 클 수 있다. maxout[9]이나 dropout[13] 같은 강한 regularization이 이 데이터셋에서 최상의 결과를 내는 데 자주 사용된다([9, 25, 24, 34]). 본 논문에서는 이러한 강한 regularization을 사용하지 않고, 최적화 문제에 집중하기 위해 설계적으로 깊고 얇은 네트워크를 regularization처럼 사용했을 뿐이다. 더 강한 regularization을 결합하면 성능이 개선될 수도 있으며, 이는 향후 연구로 남겨둔다.
---
#### 요약
---
### 실험 설정
- CIFAR-10 데이터셋 (10개 클래스)
- 학습: **5만 장**, 테스트: **1만 장**
- 목적: 극단적으로 깊은 네트워크 동작 분석 (최고 성능 달성이 목적이 아님)
- 모든 shortcut에 **항등 shortcut(옵션 A)** 사용
---
### 네트워크 구조
- 입력 **32×32** 이미지
- 3단계 구성 (32×32 → 16×16 → 8×8)
- 각 단계마다 2n개 레이어, 필터 수 \{16, 32, 64\}
- 총 레이어 수: **6n+2**
- n = \{3, 5, 7, 9, 18\} → 20, 32, 44, 56, 110층
---
### 전처리 및 학습 설정
**데이터 증강**
- 각 면에 4픽셀 패딩 후 32×32 랜덤 크롭
- 좌우 반전
**학습 설정**
- 배치 크기: **128** (GPU 2개)
- 초기 학습률: **0.1** → 32k/48k iter에서 ÷10 → 64k iter에서 종료
- Weight decay: **0.0001**, Momentum: **0.9**
- BN 사용, Dropout **없음**
---
### 실험 1 — Plain vs ResNet
**Plain Network**
- 깊어질수록 오류율 높아짐 → 열화 문제 확인
- plain-110은 오류율 60% 이상 → 그래프에 표시도 못함
- ImageNet과 동일한 현상 → 특정 데이터셋만의 문제가 아님
**ResNet**
- 깊어질수록 오류율 낮아짐 → 열화 문제 해결
- n이 커질수록 꾸준히 성능 향상
---
### 실험 2 — 110층 ResNet
- n=18 → **110층 ResNet**
- 초기 학습률 0.1이 너무 커서 **warm-up** 적용
- 처음 400 iter: 학습률 0.01로 예열
- 학습 오류 80% 아래로 내려가면 0.1로 복귀
- 결과: **6.43%** 오류율
- FitNet(8.39%), Highway(7.54%)보다 파라미터 적으면서 성능 더 좋음
<table header-row="true">
<tr>
<td>모델</td>
<td>파라미터</td>
<td>오류율</td>
</tr>
<tr>
<td>FitNet</td>
<td>2.5M</td>
<td>8.39%</td>
</tr>
<tr>
<td>Highway</td>
<td>2.3M</td>
<td>7.54%</td>
</tr>
<tr>
<td>**ResNet-110**</td>
<td>**1.7M**</td>
<td>**6.43%**</td>
</tr>
</table>
---
### 실험 3 — 레이어 반응 분석 (Fig.7)
- 각 3×3 레이어 출력의 **표준편차(std)** 측정
- BN 후, ReLU 전에 측정 → F(x)의 순수한 반응 강도
- **ResNet이 plain보다 std가 전반적으로 작음**
- 레이어가 입력을 조금씩만 수정 → 잔차 학습 동작 확인
- **깊어질수록 std가 더 작아짐**
- ResNet-20 \> ResNet-56 \> ResNet-110 순으로 감소
- 더 깊을수록 각 레이어가 더 세밀하게 보정
---
### 실험 4 — 1202층 ResNet 탐구
- n=200 → **1202층 ResNet**
- 학습 오류 **0.1% 이하** → 학습 자체는 완벽
- 테스트 오류 **7.93%** → 110층(6.43%)보다 오히려 나쁨
- 원인: **과적합**
- CIFAR-10 같은 작은 데이터셋에 비해 모델이 너무 큼
- 1202층은 파라미터 19.4M → 데이터 대비 과도함
- Dropout, maxout 같은 강한 정규화 없이는 역효과
---
#### QnA
---


## **response std란 무엇인가**

### 먼저 "반응(response)"이 뭔가?
각 레이어가 입력을 받아서 계산한 **출력값**이에요.
`입력 x → 3×3 conv → BN → [여기서 측정] → ReLU → 다음 레이어`
BN 직후, ReLU 전에 측정해요. ReLU를 거치기 전이라 음수값도 포함된 순수한 출력이에요.
---
### "표준편차(std)"가 뭔가?
값들이 **얼마나 퍼져있는지**를 나타내요.
- std가 크다 = 값들이 크게 변함 = 레이어가 입력을 많이 변환함
- std가 작다 = 값들이 거의 비슷함 = 레이어가 입력을 조금만 변환함
---
### 그래서 이 그래프가 말하는 건?
**ResNet의 F(x) 값이 plain보다 전반적으로 작다**는 걸 보여줘요.
`plain network:  std 크다 → 레이어가 값을 크게 바꿈<br>ResNet:         std 작다 → 레이어가 값을 조금만 바꿈`
---
### 왜 이게 중요하냐?
ResNet에서 F(x)가 작다는 건 이걸 의미해요.
> "레이어들이 입력을 크게 바꾸지 않고 **조금씩만 수정(잔차)하고 있다**"
즉 shortcut으로 전달되는 x가 대부분을 담당하고, 레이어는 작은 보정만 해요. 이게 바로 **잔차 학습이 제대로 동작하고 있다는 증거**예요.
---
### 또 하나 — 깊어질수록 std가 더 작아짐
ResNet-20 \< ResNet-56 \< ResNet-110 순으로 갈수록 std가 작아져요.
> 더 깊어질수록 각 레이어가 더 조금씩만 변환 → 더 세밀하게 학습



## **Warm-up**

### 첫 번째 — 왜 학습률을 0.01로 시작했냐?
보통 초기 학습률 0.1로 시작하는데, 110층처럼 **극단적으로 깊은 네트워크**는 초반에 0.1이 너무 커요.
학습 초반엔 가중치가 아직 엉망인 상태라서 0.1로 시작하면 기울기가 너무 크게 업데이트되어 학습이 불안정해져요.
---
### Warm-up이 뭔가?
`처음 400 iter:  학습률 0.01  ← 천천히 시작 (예열)<br>                    ↓<br>학습 오류 80% 아래로 내려가면<br>                    ↓<br>이후:           학습률 0.1   ← 다시 빠르게<br>                    ↓<br>나머지 스케줄:  오류 정체 시 ÷10`
마치 차 엔진을 바로 고속으로 돌리지 않고 **천천히 예열**하는 것과 같아요.
---
### 두 번째 — FitNet, Highway보다 파라미터 적은데 더 좋다?
당시 깊은 네트워크 연구들이 있었어요.
<table header-row="true">
<tr>
<td>모델</td>
<td>파라미터 수</td>
<td>오류율</td>
</tr>
<tr>
<td>FitNet</td>
<td>2.5M</td>
<td>8.39%</td>
</tr>
<tr>
<td>Highway</td>
<td>2.3M</td>
<td>7.54%</td>
</tr>
<tr>
<td>**ResNet-110**</td>
<td>**1.7M**</td>
<td>**6.43%**</td>
</tr>
</table>
ResNet-110이 파라미터가 **가장 적은데** 성능은 **가장 좋아요.**
파라미터가 적다 = 모델이 더 작고 효율적이라는 뜻이에요.



## 2n

### 정리하면
<table header-row="true">
<tr>
<td>구간</td>
<td>레이어 수</td>
<td>이유</td>
</tr>
<tr>
<td>32×32</td>
<td>**1+2n**</td>
<td>맨 첫 conv 1개 + residual 블록 n개(2n)</td>
</tr>
<tr>
<td>16×16</td>
<td>**2n**</td>
<td>residual 블록 n개만</td>
</tr>
<tr>
<td>8×8</td>
<td>**2n**</td>
<td>residual 블록 n개만</td>
</tr>
</table>
---
### 그래서 총 레이어 수가
`(1+2n) + 2n + 2n + 1(fc)<br>= 1 + 6n + 1<br>= 6n + 2`

---
### 4.3. Object Detection on PASCAL and MS COCO {toggle="true"}
#### 해석
---
우리 방법은 다른 인식(task)들에서도 좋은 일반화 성능을 보인다.
표 7과 표 8은 PASCAL VOC 2007, 2012 [5]와 COCO [26]에서의 객체 탐지 기준(baseline) 결과를 보여준다.
우리는 탐지 방법으로 Faster R-CNN [32]을 사용하였다.
여기서 우리가 관심 있는 부분은 **기존의 VGG-16 [40]을 ResNet-101으로 교체했을 때 향상되는 성능**이다.
두 모델을 사용할 때의 탐지 파이프라인 구현은(appendix 참고) 완전히 동일하므로,
성능 향상은 오직 **더 나은 네트워크 표현력(표현 학습)** 때문이라고 볼 수 있다.
가장 주목할 점은 어려운 데이터셋인 COCO에서,
COCO의 표준 평가 지표(mAP@[.5, .95])가 **6.0% 절대 향상**, 즉 **28% 상대 향상**을 보였다는 점이다.
이 성능 향상은 전적으로 **학습된 표현(레프리젠테이션)이 더 좋아졌기 때문**이다.
딥 레지듀얼 네트워크를 기반으로 하여, 우리는 ILSVRC & COCO 2015 대회의 여러 트랙에서 1위를 차지했다:
ImageNet detection, ImageNet localization, COCO detection, COCO segmentation.
자세한 내용은 부록(appendix)에 나와 있다.
---

---

# Attention is All you Need

---
## 목차
---
### Abstract {toggle="true"}
#### 해석
---
지배적인 시퀀스 변환 모델들은 인코더와 디코더를 포함하는 복잡한 순환 신경망 또는 합성곱 신경망에 기반을 두고 있다. 가장 성능이 좋은 모델들은 또한 주의(attention) 메커니즘을 통해 인코더와 디코더를 연결한다. 우리는 Transformer라고 불리는 새로운 단순한 네트워크 구조를 제안하는데, 이는 전적으로 attention 메커니즘에만 기반하고, 순환과 합성 연산을 완전히 없앤다.
두 가지 기계 번역 과제에서의 실험은, 이 모델들이 품질 면에서 더 우수하면서도 더 높은 병렬성을 가지며, 학습에 필요한 시간도 상당히 더 적게 든다는 것을 보여준다.
우리 모델은 WMT 2014 영어→독일어 번역 과제에서 28.4 BLEU를 달성하여, 앙상블을 포함한 기존 최고 결과보다 2 BLEU 이상 향상시킨다. WMT 2014 영어→프랑스어 번역 과제에서는, 8개의 GPU로 3.5일 동안 학습한 후 단일 모델 기준의 새로운 최첨단 BLEU 점수 41.8을 기록하는데, 이는 문헌에서 보고된 최고 모델들의 학습 비용에 비해 극히 작은 수준이다.
우리는 Transformer가 다른 작업에도 잘 일반화됨을 보이기 위해, 대규모 및 소규모 학습 데이터 모두에서 영어 구성 구문 분석(English constituency parsing)에 성공적으로 적용함으로써 이를 보여준다.
---
<table header-row="true">

<tr>
<td>시대</td>
<td>구조</td>
<td>특징</td>
</tr>
<tr>
<td>옛날</td>
<td>RNN Encoder → RNN Decoder</td>
<td>느림, 병렬화 불가능</td>
</tr>
<tr>
<td>중간</td>
<td>RNN Encoder → RNN Decoder + Attention</td>
<td>성능 좋아짐</td>
</tr>
<tr>
<td>RNN 한계 극복</td>
<td>CNN Encoder → CNN Decoder (+ Attention)</td>
<td>빠르지만 구조 복잡</td>
</tr>
<tr>
<td>Transformer</td>
<td>**Self-Attention Encoder → Self-Attention Decoder**</td>
<td>RNN/CNN 없이 빠르고 정확</td>
</tr>
</table>
✅ **BLEU** = 기계 번역이 사람 번역과 얼마나 비슷한지를 수치로 나타낸 점수
---
#### 요약
---
- 핵심 주장<br>기존 sequence 변환 모델들은 RNN이나 CNN 기반의 복잡한 encoder-decoder 구조에 의존했는데, 이 논문은 오직 Attention mechanism만으로 이를 완전히 대체한 새로운 아키텍처 **Transformer**를 제안합니다.
- 기존 방식의 한계<br>Recurrent 또는 Convolutional 신경망 기반의 구조를 사용하며, Encoder와 Decoder를 Attention으로 연결하는 방식에 머물러 있었습니다.
- Transformer의 장점
<table header-row="true">

<tr>
<td>항목</td>
<td>내용</td>
</tr>
<tr>
<td>구조</td>
<td>Recurrence & Convolution **완전 제거**, Attention만 사용</td>
</tr>
<tr>
<td>병렬화</td>
<td>훨씬 더 뛰어난 병렬 처리 가능</td>
</tr>
<tr>
<td>학습 속도</td>
<td>8개의 P100 GPU로 **단 3.5일** 학습</td>
</tr>
</table>
- 성능 결과
<table header-row="true">
<tr>
<td>번역 방향</td>
<td>데이터셋</td>
<td>BLEU 점수</td>
<td>비고</td>
</tr>
<tr>
<td>영어→독일어</td>
<td>WMT 2014</td>
<td>**28.4**</td>
<td>기존 최고 모델 대비 +2 BLEU 초과</td>
</tr>
<tr>
<td>영어→프랑스어</td>
<td>WMT 2014</td>
<td>**41.0**</td>
<td>단일 모델 기준 당시 SOTA</td>
</tr>
</table>
---
#### QnA
---


## **Attention이란?**

## Attention이란?
한 마디로, **"어떤 단어를 볼 때 다른 어떤 단어에 집중해야 하는가"** 를 계산하는 메커니즘입니다.
예를 들어 "The animal didn't cross the street because **it** was too tired" 라는 문장에서 **it**이 무엇을 가리키는지 알려면 **animal**을 봐야 한다는 걸 학습하는 것입니다.
---
## 핵심 재료 3가지 (Q, K, V)
Attention은 3가지 벡터로 동작합니다.
<table header-row="true">

<tr>
<td>벡터</td>
<td>이름</td>
<td>역할</td>
<td>비유</td>
</tr>
<tr>
<td>Q</td>
<td>Query</td>
<td>내가 찾고 싶은 것</td>
<td>검색어</td>
</tr>
<tr>
<td>K</td>
<td>Key</td>
<td>각 단어의 식별자</td>
<td>검색 인덱스</td>
</tr>
<tr>
<td>V</td>
<td>Value</td>
<td>실제 정보</td>
<td>검색 결과 내용</td>
</tr>
</table>
구글 검색으로 비유하면, **Q**(내가 검색한 키워드)와 **K**(웹페이지 제목)를 비교해서 관련성을 계산하고, 관련성이 높은 **V**(웹페이지 내용)를 가져오는 것과 같습니다.
---
## 계산 과정
[image omitted: temporary Notion asset]
복잡해 보이지만 단계별로 보면 간단합니다.
**1단계 : Q와 K를 곱한다 **
- 각 단어 쌍의 **유사도(관련성 점수)** 를 계산
- 예) "it"과 "animal"의 점수 = 높음, "it"과 "street"의 점수 = 낮음
**2단계 : dk로 나눈다**
- 점수가 너무 커지는 것을 방지하기 위한 **스케일링**
**3단계 : Softmax를 취한다**
- 점수들을 **확률(합=1)** 로 변환
- 예) animal: 0.7, street: 0.1, the: 0.05 ...
**4단계 : V를 곱한다**
- 각 단어의 정보(V)를 확률 가중치로 **가중합산**
- 관련성 높은 단어의 정보가 더 많이 반영됨
---
## 왜 병렬처리가 되냐면
이 계산 전체가 결국 **행렬곱** 입니다. 모든 단어의 Q, K, V를 한 번에 행렬로 묶어서 한 번에 계산하기 때문에, RNN처럼 순서를 기다릴 필요가 전혀 없습니다.

---
### 1. Introduction {toggle="true"}
#### 해석
---
순환 신경망(Recurrent Neural Networks), 특히 장단기 메모리(Long Short-Term Memory) [13]와 게이트드 순환 신경망(Gated Recurrent Neural Networks) 은, 언어 모델링과 기계 번역과 같은 시퀀스 모델링 및 변환 문제에서 최첨단(state-of-the-art) 접근법으로 확고히 자리 잡아 왔다. 그 이후로 수많은 시도가 순환 언어 모델과 인코더-디코더 구조의 한계를 계속 확장해 왔다.
> RNN(LSTM/GRU)이 언어 모델과 번역에서 오랫동안 최고의 방법이었고, 많은 연구가 그것을 더 개선하려고 해왔다”라는 의미
순환 모델들은 일반적으로 입력 및 출력 시퀀스의 심볼 위치에 따라 계산을 분해한다. 위치들을 계산 시간의 단계와 정렬시키면서, 이들은 이전 은닉 상태 ht−1과 위치 t의 입력에 대한 함수로서 은닉 상태 hₜ의 시퀀스를 생성한다. 이러한 본질적으로 순차적인 특성은 학습 예제 내부의 병렬화를 불가능하게 하며, 이는 시퀀스 길이가 길어질 때 문제가 되는데, 그 이유는 메모리 제약이 예제 간 배치를 제한하기 때문이다.
> 1️⃣ RNN은 시퀀스를 앞에서부터 위치별로 순서대로 처리해야 한다
2️⃣ 각 위치는 이전 은닉 상태에 의존하므로 병렬 처리가 불가능하다
3️⃣ 그래서 시퀀스가 길어지면 계산이 매우 느려진다
4️⃣ 길어질수록 메모리를 많이 써서 batch 크기도 줄어든다
→ 결과적으로 학습 효율이 크게 떨어진다.
최근 연구들은 요인 분해 기법(factorization tricks)과 조건부 계산(conditional computation)을 통해 계산 효율성을 크게 개선했으며, 후자의 경우에는 모델 성능 또한 향상시켰다. 그러나 그럼에도 불구하고 순차적 계산이라는 근본적인 제약은 그대로 남아 있다.
주의(attention) 메커니즘은 다양한 작업에서 설득력 있는 시퀀스 모델링과 변환 모델의 필수적인 부분이 되었으며, 입력 또는 출력 시퀀스 내에서 위치 간의 거리에 관계없이 의존성을 모델링할 수 있게 한다. 하지만 몇 가지 경우 [27]를 제외하면, 이러한 attention 메커니즘은 순환 신경망과 함께 결합되어 사용된다.
이 연구에서 우리는** Transformer를 제안**하는데, 이 모델 구조는 순환을 배제(eschew)하고, 대신 입력과 출력 간의 전역적 의존성을 포착하기 위해 전적으로 attention 메커니즘에만 의존한다. Transformer는 매우 높은 병렬화를 가능하게 하며, 8개의 P100 GPU에서 단 12시간 학습한 후 번역 품질에서 새로운 최첨단 성능에 도달할 수 있다.
---
#### 요약
---
- **기존 연구 흐름**
- RNN, LSTM, GRU 등이 sequence 모델링과 기계번역 분야에서 SOTA를 차지
- 이후 recurrent 언어모델과 encoder-decoder 구조를 발전시키려는 연구들이 꾸준히 이어짐
- **RNN의 구조적 한계**
- ht 계산 시 반드시 ht−1 필요 → **병렬화 불가능**
- 시퀀스가 길어질수록 메모리 제약으로 배치 처리도 어려워짐
- factorization trick, conditional computation 등으로 개선 시도했지만 **근본적인 한계는 미해결**
- **Attention의 기존 역할**
- 입출력 시퀀스 내 거리에 **상관없이** dependency 모델링 가능
- 그러나 기존에는 RNN과 함께 **보조적으로만** 사용
- **Transformer 제안**
<table header-row="true">

<tr>
<td>항목</td>
<td>내용</td>
</tr>
<tr>
<td>핵심 아이디어</td>
<td>Recurrence 완전 제거, **Attention만** 사용</td>
</tr>
<tr>
<td>장점</td>
<td>병렬화 대폭 향상</td>
</tr>
<tr>
<td>학습 환경</td>
<td>8개의 P100 GPU</td>
</tr>
<tr>
<td>학습 시간</td>
<td>**단 12시간**</td>
</tr>
<tr>
<td>결과</td>
<td>번역 분야 새로운 **SOTA** 달성</td>
</tr>
</table>
---
### 2. Background {toggle="true"}
#### 해석
---
순차적 계산을 줄이려는 목표는 Extended Neural GPU, ByteNet, 그리고 ConvS2S의 기반을 이루기도 하는데, 이들 모두는 합성곱 신경망을 기본 구성 요소로 사용하며, 모든 입력 및 출력 위치에 대해 은닉 표현을 병렬로 계산한다. 이러한 모델들에서, 임의의 두 입력 또는 출력 위치의 신호를 서로 관련짓기 위해 요구되는 연산 수는 그 위치들 사이의 거리에 따라 증가하며, ConvS2S의 경우 선형적으로, ByteNet의 경우 로그 형태로(logarithmically) 증가한다. **이는 먼 위치들 사이의 의존성을 학습하는 것을 더 어렵게 만든다. **
> 1️⃣ CNN 기반 모델(Extended Neural GPU, ByteNet, ConvS2S)은 RNN의 순차성을 줄이려고         나왔다.
         → 그래서 병렬 계산이 가능하다.
2️⃣ 하지만 CNN은 멀리 떨어진 두 단어를 연결하려면 여러 층을 거쳐야 한다.
3️⃣ 그래서 멀리 떨어진 단어 관계를 학습하기가 어렵다.
Transformer에서는 이러한 연산 수가 상수(constant)로 줄어드는데, 이는 attention 가중치가 적용된 위치들을 평균함으로 인해 효과적인 해상도가 감소하는 비용을 수반하며, 우리는 3.2절에서 설명한 멀티-헤드 어텐션(Multi-Head Attention)을 사용하여 이 효과를 상쇄한다.
> Transformer는 멀리 있는 단어도 한 번에 연결할 수 있지만 정보가 흐려지는 문제가 생기므로, 이를 Multi-Head Attention으로 보완한다.
Self-attention, 때때로 intra-attention이라고도 불리는 것은 하나의 시퀀스 내의 서로 다른 위치들을 서로 연관지어 그 시퀀스의 표현을 계산하는 attention 메커니즘이다. Self-attention은 읽기 이해, 추상적 요약, 텍스트 함의(textual entailment), 과제-독립적 문장 표현 학습을 포함한 다양한 작업들에서 성공적으로 사용되어 왔다.
> Self-attention은 한 문장 내부의 단어 관계를 계산해 표현을 만드는 방식이며, 여러 NLP 작업에서 매우 잘 동작한다.
End-to-end memory networks는 시퀀스 정렬된 순환 대신 순환적 attention 메커니즘에 기반하며, 단순한 언어의 질문응답 및 언어 모델링 작업에서 좋은 성능을 보이는 것으로 알려져 있다.
그러나 우리의 지식으로는, Transformer는 입력 및 출력의 표현을 계산하기 위해 시퀀스 정렬된 RNN이나 합성곱을 사용하지 않고 전적으로 self-attention에만 의존하는 최초의 변환(transduction) 모델이다. 다음 절들에서는 Transformer를 설명하고, self-attention의 동기를 제시하며, [17], [18], [9]와 같은 모델들에 비해 가지는 장점에 대해 논의할 것이다.
> ✅ End-to-End Memory Network는 RNN처럼 순서대로 처리하지 않고, attention을 반복해          서 사용하는 모델이다.
✅ 하지만 Transformer는 그보다 더 나아가, RNN도 안 쓰고 CNN도 안 쓰고, 오직 self-attention<br>     만으로 입력·출력 표현을 계산하는 최초의 모델이다.
---
#### 요약
---
- **순차적 계산 줄이기 (기존 연구)**
- Extended Neural GPU, ByteNet, ConvS2S 등이 CNN을 기반으로 병렬 처리 시도
- 하지만 멀리 떨어진 두 위치를 연결하는 데 필요한 연산 수가 거리에 비례해서 증가
<table header-row="true">

<tr>
<td>모델</td>
<td>멀리 떨어진 위치 연결에 필요한 연산 수</td>
</tr>
<tr>
<td>ConvS2S</td>
<td>O(n) - 선형적으로 증가</td>
</tr>
<tr>
<td>ByteNet</td>
<td>O(log n) - 로그적으로 증가</td>
</tr>
<tr>
<td>**Transformer**</td>
<td>**O(1) - 항상 상수**</td>
</tr>
</table>
- **Self-Attention**
- 하나의 시퀀스 내 **서로 다른 위치들 간의 관계**를 계산하는 Attention
- 독해, 요약, 문장 표현 학습 등 다양한 태스크에서 이미 성공적으로 사용됨
- **End-to-End Memory Network**
- 순차적 recurrence 대신 **recurrent attention mechanism** 기반
- 간단한 질의응답, 언어 모델링 태스크에서 좋은 성능을 보임
- **Transformer의 차별점**
- 기존 연구들과 달리 **RNN이나 CNN을 전혀 사용하지 않음**
- 오직 Self-Attention만으로 입력과 출력의 표현을 계산하는 **최초의 모델**
---
#### QnA
---


## **attention 가중치가 적용된 위치들을 평균함으로 인해 효과적인 해상도가 감소하는 비용을 수반?**

"The animal didn't cross the street because **it** was too tired"
"it"이 무엇을 가리키는지 Attention을 계산하면
```bash
animal: 0.6
street: 0.3
tired:  0.1
```
---
## 실제로 뭉개지는 과정
최종 출력 벡터는 이 가중치로 V를 **가중합산**합니다.
`output = 0.6 × V(animal) + 0.3 × V(street) + 0.1 × V(tired)`
결과적으로 나오는 벡터는 **animal도 아니고, street도 아닌** 세 단어가 섞인 벡터입니다.
---
## 왜 이게 문제냐면
<table header-row="true">
<tr>
<td>상황</td>
<td>문제</td>
</tr>
<tr>
<td>"it" = animal이 명확한 경우</td>
<td>street(0.3)의 정보가 불필요하게 섞임</td>
</tr>
<tr>
<td>두 가지 의미를 동시에 파악해야 할 경우</td>
<td>하나의 평균 벡터로는 두 의미를 동시에 담기 어려움</td>
</tr>
</table>
사진으로 비유하면, 여러 사진을 겹쳐서 **흐릿하게 합성**한 것과 같습니다. animal 사진과 street 사진을 6:3으로 겹치면 어느 쪽도 선명하지 않은 흐릿한 사진이 되는 것처럼요.
---
## Multi-Head Attention이 이를 해결하는 방식
단일 Attention 대신 여러 Head가 **각자 독립적으로** Attention을 계산합니다.
`Head 1 → "it"을 문법적 관점으로 봄 → animal에 집중<br>Head 2 → "it"을 의미적 관점으로 봄 → street에 집중`
각 Head가 서로 다른 측면에 집중한 뒤 결과를 합치기 때문에, 단일 Attention처럼 정보가 하나로 뭉개지지 않고 **다양한 관점의 정보를 선명하게** 유지할 수 있습니다.

---
### 3. Model Architecture {toggle="true"}
#### 해석
---


[image omitted: temporary Notion asset]


대부분의 경쟁력 있는 신경 시퀀스 변환 모델들은 인코더-디코더 구조를 가진다. 여기에서 인코더는 심볼 표현들로 이루어진 입력 시퀀스 (x1, ..., xn)를 연속적인 표현들의 시퀀스 z = (z1, ..., zn)로 매핑한다. z가 주어지면, 디코더는 출력 시퀀스 (y1, ..., ym)를 한 번에 하나의 심볼씩 생성한다.
각 단계에서 모델은 **자가회귀(auto-regressive)** 방식으로 동작하며, 다음 심볼을 생성할 때 이전에 생성된 심볼들을 추가 입력으로 사용한다.
Transformer는 이러한 전체 구조를 따르며, 인코더와 디코더 모두에 대해 여러 층으로 쌓은(self-attention) 자기-주의와 위치별(point-wise) 완전연결층을 사용한다. 이는 각각 Figure 1의 왼쪽 절반과 오른쪽 절반에 나타나 있다.


---
#### QnA
---


## **인코더와 디코더의 하는 일**

## 심볼이란?
그냥 **단어(또는 토큰)** 라고 이해하면 됩니다.
`"I love you" → [I, love, you] 이 각각이 심볼`
---
## 입력 시퀀스를 시퀀스로 바꾼다?
언뜻 보면 이상하지만, **표현 형태가 바뀌는 것**입니다.
<table header-row="true">
<tr>
<td></td>
<td>형태</td>
<td>예시</td>
</tr>
<tr>
<td>입력 (x1,...,xn)(x_1, ..., x_n)<br>(x1,...,xn)</td>
<td>단어 그 자체</td>
<td>"I", "love", "you"</td>
</tr>
<tr>
<td>출력 z=(z1,...,zn)z = (z_1, ..., z_n)<br>z=(z1,...,zn)</td>
<td>각 단어의 **의미를 담은 벡터**</td>
<td>[0.2, 0.8, ...], [0.5, 0.1, ...]</td>
</tr>
</table>
즉 단어를 숫자 벡터로 변환하되, **문장 전체의 문맥을 반영한** 벡터로 만드는 것입니다. "love"라는 단어도 "I love you"에서의 love와 "I love pizza"에서의 love가 **다른 벡터**로 표현됩니다.
---
## 디코더가 한 번에 하나씩 생성한다?
번역을 예시로 들면
`입력: "I love you" (영어)<br>출력: "나는 너를 사랑해" (한국어)`
디코더는 이 출력을 한꺼번에 만드는 게 아니라
`1단계: "나는" 생성<br>2단계: "나는" 보고 → "너를" 생성<br>3단계: "나는 너를" 보고 → "사랑해" 생성`
이렇게 **이전에 생성한 단어들을 참고하면서** 다음 단어를 하나씩 만들어냅니다. 이게 바로 **auto-regressive** 방식입니다.



## **Feed Forward (완전연결층)이 하는 일**

```bash
Attention  → 문장 전체를 보면서 "단어 간 관계" 학습
    ↓
   FFN     → 단어 하나하나를 독립적으로 보면서
             비선형 변환을 통해 "각 단어의 의미" 를 더 깊게 학습
```

---
### 3.1 Encoder and Decoder Stacks {toggle="true"}
#### 해석
---
**Encoder:**
인코더는 N = 6개의 동일한 층으로 이루어진 스택으로 구성된다. 각 층은 두 개의 서브레이어를 가진다. 첫 번째 서브레이어는 멀티-헤드 자기-어텐션(multi-head self-attention) 메커니즘이고, 두 번째는 위치별(position-wise) 완전연결(feed-forward) 네트워크이다.
우리는 두 서브레이어 각각을 둘러싸는 **잔차 연결(residual connection)** [11]을 사용하고, 그 뒤에 레이어 정규화(layer normalization) [1]를 적용한다. 즉, 각 서브레이어의 출력은 다음과 같다:
```math
LayerNorm(x + Sublayer(x))
```
여기서 Sublayer(x)는 해당 서브레이어가 구현하는 함수이다. 이러한 잔차 연결을 가능하게 하기 위해, 모델의 모든 서브레이어와 임베딩 층은 출력 차원이 d_model = 512가 되도록 한다.
**Decoder:**
디코더도 N = 6개의 동일한 층으로 구성된 스택이다. 디코더의 각 층에는 인코더 층과 동일한 두 개의 서브레이어에 더해, 세 번째 서브레이어가 추가된다. 이 서브레이어는 인코더 스택의 출력에 대해 멀티-헤드 어텐션을 수행한다.
인코더와 마찬가지로, 우리는 디코더의 각 서브레이어 주위에 잔차 연결을 적용하고, 그 뒤에 레이어 정규화를 수행한다.
또한 디코더 스택에서의 self-attention 서브레이어는 현재 위치가 이후 위치를 참조하지 못하도록(masking) 수정된다. 이 마스킹은 출력 임베딩이 한 위치(offset)만큼 이동되어 있다는 사실과 결합되어, 위치 i에 대한 예측이 i보다 작은 위치의 이미 알려진 출력에만 의존하도록 한다.
---
#### QnA
---


## **인코더 - sublayer가 무엇인가**

```bash
Encoder 블록
┌─────────────────┐
│   Add & Norm    │
│  Feed Forward   │  ← 서브레이어 2
│   Add & Norm    │
│ Multi-Head      │
│   Attention     │  ← 서브레이어 1
└─────────────────┘
```



## **인코더 - 식에대한 설명**

## 1. Residual Connection: x+Sublayer(x)
x는 입력, Sublayer(x)Sublayer(x)<br>Sublayer(x)는 Attention이나 FFN을 통과한 출력입니다.
`x (원본 입력)<br>    +<br>Sublayer(x) (변환된 출력)`
즉 변환된 결과만 쓰는게 아니라 **원본을 더해서** 같이 씁니다.
왜냐하면 층이 깊어질수록 gradient가 소실되는 문제가 있는데, 원본을 더해줌으로써 **gradient가 층을 건너뛰어 직접 전달**될 수 있기 때문입니다. ResNet 논문에서 나온 개념이에요!
---
## 2. Layer Normalization
더한 결과값이 **너무 크거나 작아지지 않도록** 정규화해주는 것입니다.
`평균 = 0, 분산 = 1 이 되도록 스케일 조정`
학습을 **안정적으로** 만들어주는 역할입니다.



## **디코더 - 각 레이어가 하는 일**

## 전체 목표
`입력: "I love you" (영어)<br>출력: "나는 너를 사랑해" (한국어)`
---
## Decoder의 3개 서브레이어
- 1번째: Masked Multi-Head Self-Attention**지금까지 생성한 출력 단어들끼리** 관계를 파악합니다.
`    "나는 너를" 까지 생성한 상태에서<br>    "나는" ↔ "너를" 관계 파악<br>    → 다음 단어 생성을 위한 준비`
- `*Masked**인 이유는 미래 단어를 미리 보면 안 되기 때문입니다.`
`    "나는" 생성할 때 → "너를", "사랑해" 못 봄 ❌<br>    "너를" 생성할 때 → "나는" 만 볼 수 있음 ✅`
- 2번째: Multi-Head Attention (Encoder-Decoder Attention)<br>여기서 Encoder 출력을 받습니다!
<table header-row="true">
<tr>
<td></td>
<td>출처</td>
</tr>
<tr>
<td>Q (Query)</td>
<td>Decoder (내가 생성하려는 것)</td>
</tr>
<tr>
<td>K, V (Key, Value)</td>
<td>**Encoder 출력** (입력 문장 정보)</td>
</tr>
</table>
`    "사랑해"를 생성하려 할 때<br>    Q = "사랑해" 관련 정보<br>    K, V = "I", "love", "you" 의 정보<br>    → "love"에 가장 높은 Attention → "사랑해" 생성`
`즉 **"입력 문장의 어떤 단어를 참고해서 다음 단어를 생성할지"** 결정하는 단계입니다.`
- 3번째: Feed Forward<br>앞선 두 Attention에서 수집한 정보를 **각 단어별로 최종 변환**합니다.
---
## 한 줄 요약
> 1번째 Attention은 **"내가 지금까지 뭘 생성했는지"** 파악, 2번째 Attention은 **"입력 문장에서 뭘 참고할지"** 결정, FFN은 **"최종 표현으로 변환"** 합니다.



## **디코더 - linear와 softmax**

## 문제 상황
Decoder의 FFN까지 통과하면 출력은 **512차원 벡터**입니다.
`FFN 출력 → [0.2, 0.8, 0.1, ...]  (512차원)`
이걸 그대로는 "다음 단어가 뭔지" 알 수 없습니다.
---
## Linear와 Softmax의 역할
- Linear<br>512차원 벡터를 **단어 사전 크기만큼** 늘립니다.
`    [512차원] → [30000차원]  (단어 사전이 3만개라면)`
`각 차원이 **단어 하나하나에 대응**되는 점수(logit)가 됩니다.`
- Softmax<br>그 점수들을 **확률로 변환**합니다.
`    "나는":  0.05<br>    "너를":  0.03<br>    "사랑해": 0.87  ← 가장 높은 확률<br>    "먹었다": 0.01<br>    ...`
---
## 전체 흐름
`Decoder FFN 출력 (512차원)<br>    ↓<br>Linear → 단어 사전 크기(30000차원)로 변환<br>    ↓<br>Softmax → 각 단어의 확률로 변환<br>    ↓<br>가장 높은 확률의 단어 선택 → "사랑해" ✅`
---
## 한 줄 요약
> Decoder 내부의 벡터를 **실제 단어로 변환**하기 위해 Linear로 차원을 늘리고, Softmax로 확률을 계산해서 **가장 확률 높은 단어를 선택**합니다.

---
### 3.2 Attention {toggle="true"}
#### 해석
---
[image omitted: temporary Notion asset]
어텐션 함수는 하나의 쿼리(query)와 키-값(key-value) 쌍들의 집합을 출력으로 매핑하는 함수로 설명될 수 있으며, 이때 쿼리, 키, 값, 그리고 출력은 모두 벡터이다. 
출력은 값들(values)의 가중합(weighted sum)으로 계산되며, 각 값에 할당되는 가중치는 쿼리와 해당 키의 호환성(comparability)을 나타내는 함수에 의해 계산된다.
<table header-row="true">

<tr>
<td></td>
<td>역할</td>
<td>비유</td>
</tr>
<tr>
<td>Q (Query)</td>
<td>내가 찾고 싶은 것</td>
<td>검색창에 입력한 키워드</td>
</tr>
<tr>
<td>K (Key)</td>
<td>각 문서의 제목/태그</td>
<td>웹페이지 제목</td>
</tr>
<tr>
<td>V (Value)</td>
<td>실제 내용</td>
<td>웹페이지 본문</td>
</tr>
</table>
---
### 3.2.1 Scaled Dot-Product Attention {toggle="true"}
#### 해석
---
[image omitted: temporary Notion asset]
---
### 3.2.2 Multi-Head Attention {toggle="true"}
#### 해석
---
[image omitted: temporary Notion asset]
---
## 문제 상황
8개의 head 결과를 Concat하면
```bash
head1 출력: 64차원 (문법적 관계에 집중)
head2 출력: 64차원 (의미적 관계에 집중)
...
head8 출력: 64차원 (또 다른 관계에 집중)
    ↓
Concat: 512차원 (그냥 이어붙인 것)
```
근데 이렇게 **그냥 이어붙이기만 하면** 8개의 head가 각자 따로 놀고 있는 상태입니다.
---
## Linear가 하는 일
```bash
Concat (512차원)
    ↓
Linear (512 × 512)
    ↓
512차원 출력
```
각 head의 결과들을 **서로 섞어서 통합**하는 역할
## Multi-Head를 사용하는 이유
Single Attention은 **하나의 관점**으로만 봅니다.
Multi-Head는 **여러 관점**으로 동시에 볼 수 있습니다.
```bash
head1 → 문법적 관계 집중
head2 → 의미적 관계 집중
head3 → 위치적 관계 집중
...
```
### 3.2.3 Applications of Attention in our Model {toggle="true"}
#### 해석
---
Transformer는 멀티-헤드 어텐션을 세 가지 방식으로 사용한다:
- 인코더-디코더 어텐션 층에서는, query는 이전 디코더 층에서 오고, memory key와 value는 인코더의 출력에서 온다. 이를 통해 디코더의 모든 위치가 입력 시퀀스의 모든 위치에 주의를 기울일 수 있다. 이는 [38, 2, 9]와 같은 시퀀스-투-시퀀스 모델에서 전형적으로 사용되는 인코더-디코더 어텐션 메커니즘을 그대로 따른다.
- 인코더는 self-attention 층을 포함한다. self-attention 층에서는 key, value, query가 모두 동일한 곳, 즉 인코더 이전 층의 출력에서 온다. 인코더의 각 위치는 이전 인코더 층의 모든 위치에 주의를 기울일 수 있다.
- 디코더의 self-attention 층도 마찬가지로, 디코더의 각 위치가 자기 자신을 포함해 그 이전의 모든 위치에 주의를 기울일 수 있게 한다. 우리는 auto-regressive 성질을 유지하기 위해 디코더에서 왼쪽 방향으로의 정보 흐름을 막아야 한다. 이를 위해 scaled dot-product attention 내부에서, softmax 입력 중 불가능한 연결에 해당하는 값들을 마스킹하여( setting to −∞ ) 제거한다. 그림 2를 참고하라.
---
- Encoder Self-Attention
- Q, K, V 모두 **Encoder 이전 레이어**에서 옴
- 입력 문장의 모든 단어가 **서로를 참고**할 수 있음
- Decoder Masked Self-Attention
- Q, K, V 모두 **Decoder 이전 레이어**에서 옴
- 미래 단어를 **−∞\\infty<br>−∞로 마스킹<br>**해서 현재 위치 이전 단어만 참고 가능
- Encoder-Decoder Attention
- Q는 **Decoder**, K와 V는 **Encoder 출력**에서 옴
- Decoder가 출력 단어를 생성할 때 **입력 문장 전체를 참고**할 수 있음
- 번역에서 "사랑해" 생성 시 → "love"에 높은 Attention
### 3.3 Position-wise Feed-Forward Networks {toggle="true"}
#### 해석
---
[image omitted: temporary Notion asset]
---
#### QnA
---


## **식에 대한 설명**

## 단계별 해석
- 1단계: xW1+b1 (첫 번째 Linear)
- 입력 x(512차원)를 W1으로 **확장**
- W1 크기: (512 × 2048)
```bash
    512차원 → 2048차원으로 확장
```
- 2단계: max⁡(0,...) (ReLU)
- 음수값을 **0으로 제거**
```bash
    [-0.5, 0.3, -0.1, 0.8] → [0, 0.3, 0, 0.8]
```
- `비선형성을 추가해서 **더 복잡한 패턴** 학습 가능`
- 3단계: W2+b2 (두 번째 Linear)
- 2048차원을 다시 **512차원으로 압축**
- W2 크기: (2048 × 512)
```bash
    2048차원 → 512차원으로 압축
```
---
## 전체 흐름
```bash
x (512차원)
    ↓
W1 (512×2048): 확장
    ↓
ReLU: 비선형성 추가
    ↓
W2 (2048×512): 압축
    ↓
출력 (512차원)
```
---
## 왜 한번 늘렸다가 줄이냐?
차원을 늘렸다가 줄이는 과정에서 **더 복잡한 패턴을 학습**할 수 있기 때문입니다. 512차원에서 바로 변환하는 것보다 2048차원으로 넓혀서 **더 풍부한 표현 공간**에서 처리하는 것입니다!

### 3.4 Embeddings and Softmax {toggle="true"}
#### 해석
---
[image omitted: temporary Notion asset]
---
- Embedding이란?
= 각 단어의 인덱스를 의미를 담은 512차원의 벡터로 변환하는게 embedding
- 컴퓨터는 텍스트를 그대로 이해 못하므로 단어를 먼저 **숫자 인덱스**로 변환
- 하지만 인덱스만으로는 단어 간 **의미적 관계** 표현 불가
```bash
    "love" → 5312
    "like" → 5313  ← love랑 비슷한데 숫자는 전혀 다름
```
- 그래서 Embedding을 통해 \*\*\$d_\{model\}\$ = 512차원 벡터\*\*로 변환
```bash
    "love" → [0.2, 0.8, 0.1, ...]  ← 의미가 담긴 벡터
    "like" → [0.2, 0.7, 0.1, ...]  ← 비슷한 벡터!
    "hate" → [0.9, 0.1, 0.8, ...]  ← 다른 벡터!
```
- Embedding은 언제 하냐?
- 단어가 모델에 **처음 입력될 때** 딱 한 번 수행
```bash
    Encoder: "I love you" → Embedding → 512차원 벡터들 → Encoder 통과
    Decoder: "나는 너를"  → Embedding → 512차원 벡터들 → Decoder 통과
```
- Weight Matrix 공유
- 모델 안에 Embedding이 두 개 있고, Decoder 마지막에 Pre-softmax Linear가 있음
<table header-row="true">

<tr>
<td>위치</td>
<td>역할</td>
</tr>
<tr>
<td>Encoder 입력 Embedding</td>
<td>"I love you" → 512차원 벡터</td>
</tr>
<tr>
<td>Decoder 입력 Embedding</td>
<td>"나는 너를" → 512차원 벡터</td>
</tr>
<tr>
<td>Pre-softmax Linear</td>
<td>512차원 벡터 → 단어 확률</td>
</tr>
</table>
- 이 세 곳이 **동일한 가중치 행렬 공유**
- 이유 1: Embedding **(단어→벡터)** 과 Pre-softmax Linear **(벡터→단어)** 는 **정확히 반대 방향**의 같은 작업 → 전치해서 재사용 가능
- 이유 2: 하나의 행렬만 학습하면 되므로 **파라미터 절약**
- sqrt\{d_\{model\}\} 곱하는 이유
- Embedding 벡터는 학습 과정에서 값이 **매우 작아지는 경향**이 있음
- 반면 Positional Encoding은 **sin/cos 함수**라 값이 -1\~1 사이
```bash
    그냥 더하면:
    Embedding (매우 작음) + Positional Encoding (-1~1)
    → Positional Encoding이 너무 커서 Embedding 정보가 묻혀버림 ❌
    
    √512 ≈ 22.6을 곱하면:
    Embedding × √512 + Positional Encoding
    → 두 정보가 균형있게 더해짐 ✅
```
### 3.5 Positional Encoding {toggle="true"}
#### 해석
---
[image omitted: temporary Notion asset]
---
- 왜 필요하냐?
- Transformer는 모든 단어를 **동시에** 처리해서 순서 정보가 없음
- "I love you" 와 "you love I" 를 구별 못함 ❌
- 그래서 위치 정보를 **Embedding 벡터에 더해서** 직접 주입
- 어떻게 동작하냐?
- **pos**: 단어의 위치 (0, 1, 2, ...)
- **i**: 벡터의 차원 인덱스
- 짝수 차원 → sin, 홀수 차원 → cos
- 왜 차원마다 다른 주기를 쓰냐?
- 모든 차원이 같은 주기면 정보가 **중복**됨
- 차원마다 다른 주기를 써서 각 위치가 **고유한 벡터**를 갖도록 함
```bash
    2i/d_model = 0 → 10000^0 = 1     (가장 빠른 주기)
    2i/d_model = 1 → 10000^1 = 10000 (가장 느린 주기)
```
- 왜 sin/cos이냐?
- 값이 **1\~1 사이**로 안정적 → Embedding과 더하기 적합
- **주기함수**라서 위치 간 상대적 거리 표현에 유리
- 학습 때 보지 못한 **긴 시퀀스에도 일반화** 가능
- 학습된 Positional Embedding과 비교
- 둘 다 실험했을 때 성능은 **거의 동일**
- sin/cos 방식 선택 이유: 학습 데이터보다 **긴 시퀀스에도 일반화** 가능하기 때문
### 4. Why Self-Attention {toggle="true"}
#### 해석
---
[image omitted: temporary Notion asset]
[image omitted: temporary Notion asset]
---
이 섹션은 Self-Attention을 왜 쓰는지를 **3가지 기준**으로 RNN, CNN과 비교합니다.
---
- 비교 기준 3가지
<table header-row="true">
<tr>
<td>기준</td>
<td>의미</td>
</tr>
<tr>
<td>1. 층당 계산 복잡도</td>
<td>한 번 계산하는데 얼마나 많은 연산이 필요한가</td>
</tr>
<tr>
<td>2. 병렬화 가능성</td>
<td>동시에 얼마나 많이 계산할 수 있는가</td>
</tr>
<tr>
<td>3. Long-range dependency</td>
<td>멀리 떨어진 단어 간 관계를 얼마나 잘 학습하는가</td>
</tr>
</table>
---
- 각 모델 비교표
<table header-row="true">
<tr>
<td></td>
<td>Self-Attention</td>
<td>RNN</td>
<td>CNN</td>
</tr>
<tr>
<td>층당 복잡도</td>
<td>O(n2⋅d)</td>
<td>O(n⋅d2)</td>
<td>O(k⋅n⋅d2)</td>
</tr>
<tr>
<td>병렬화</td>
<td>✅ O(1)</td>
<td>❌ O(n)</td>
<td>✅ O(1)</td>
</tr>
<tr>
<td>최대 경로 길이</td>
<td>✅ O(1)</td>
<td>❌ O(n)</td>
<td>❌ O(logk(n))</td>
</tr>
</table>
---
- 각 기준별 설명
- 1. 층당 계산 복잡도
- Self-Attention: O(n2⋅d)
- 모든 단어 쌍의 유사도를 계산하기 때문에 n2
n2n\^2
- 문장이 길어질수록 급격히 커짐
- RNN: O(n⋅d2)
- 단어를 하나씩 처리하므로 n
- 하지만 hidden state 차원 d2 이 큼
- **실제로는 문장 길이 n이 차원 d보다 작은 경우가 많아서 Self-Attention이 유리**
- 2. 병렬화
- Self-Attention: 모든 단어를 **동시에** 처리 → O(1)
- RNN: ht 계산에 ht−1 필요 →**순차적** → O(n)
- CNN: 필터 연산을 **동시에** 처리 → O(1)
- 3. Long-range dependency (핵심!)
- 멀리 떨어진 두 단어를 연결하는 데 필요한 **경로 길이**
```bash
`"The cat that ... sat" 에서
        "cat"과 "sat"이 20단어 떨어져 있다면?`

    `| | 경로 길이 | 의미 |
    |------|------|------|
    | Self-Attention | $O(1)$ | 한 번에 직접 연결 ✅ |
    | RNN | $O(n)$ | 20번 거쳐야 연결 ❌ |
    | CNN | $O(log_k(n))$ | 여러 층 거쳐야 연결 ❌ |`
```
---
- 추가 장점: 해석 가능성
- Attention 가중치를 시각화하면 **모델이 어떤 단어에 집중하는지** 볼 수 있음
- RNN, CNN에 비해 모델의 동작을 **이해하기 쉬움**
## 일반 Self-Attention vs Restricted Self-Attention
<table header-row="true">
<tr>
<td></td>
<td>일반 Self-Attention</td>
<td>Restricted Self-Attention</td>
</tr>
<tr>
<td>범위</td>
<td>문장 **전체** 참고</td>
<td>**주변 r개** 단어만 참고</td>
</tr>
<tr>
<td>복잡도</td>
<td>O(n2⋅d)</td>
<td>O(r⋅n⋅d)</td>
</tr>
<tr>
<td>경로 길이</td>
<td>O(1)</td>
<td>O(n/r)</td>
</tr>
</table>
---
## 왜 필요하냐?
문장이 **매우 길어지면** 일반 Self-Attention의 O(n2)O(n\^2)<br>O(n2) 복잡도가 너무 커집니다.
`n = 1000이면<br>n^2 = 1,000,000 번 계산 ❌`
그래서 멀리 있는 단어는 무시하고 **가까운 r개만** 보는 것입니다.
`r = 10이면<br>"love" → 주변 10개 단어만 참고`
---
## 단점
범위를 제한하면 복잡도는 줄지만 **멀리 떨어진 단어를 직접 연결 못함** → 경로 길이가 O(n/r)O(n/r)<br>O(n/r)로 늘어납니다.
### 5. Training {toggle="true"}
> 이 절에서는 우리의 모델들을 위한 훈련 방식(training regime)을 설명한다.
### 5.1 Training Data and Batching {toggle="true"}
#### 해석
---
우리는 약 450만 개의 문장 쌍으로 이루어진 표준 WMT 2014 English–German 데이터셋에서 훈련을 수행하였다. 문장들은 byte-pair encoding [3]을 사용해 인코딩되었으며, 약 37000개의 source–target 공유 vocabulary를 가진다. English–French의 경우, 훨씬 더 큰 규모인 3600만 개 문장으로 이루어진 WMT 2014 English–French 데이터셋을 사용하였고, 토큰은 32000개의 word-piece vocabulary [38]로 분할하였다.
문장 쌍들은 시퀀스 길이가 비슷하도록 묶어 batch를 구성하였다. 각 training batch는 약 25000개의 source token과 25000개의 target token을 포함하는 문장 쌍 집합으로 구성되었다.
---
## 1. 학습 데이터
<table header-row="true">
<tr>
<td>번역 방향</td>
<td>데이터셋</td>
<td>문장 쌍 수</td>
<td>Vocabulary</td>
</tr>
<tr>
<td>영어→독일어</td>
<td>WMT 2014</td>
<td>450만 쌍</td>
<td>37,000개</td>
</tr>
<tr>
<td>영어→프랑스어</td>
<td>WMT 2014</td>
<td>3,600만 쌍</td>
<td>32,000개</td>
</tr>
</table>
---
## 2. Byte-pair Encoding이란?
단어를 \*\*더 작은 단위(subword)\*\*로 쪼개는 방법입니다.
```bash
"playing" → "play" + "ing"
"unhappy" → "un" + "happy"
```
- 모르는 단어가 나와도 **조각으로 쪼개서** 처리 가능
- Vocabulary 크기를 **줄일 수 있음**
---
## 3. Batch 구성
- 길이가 비슷한 문장끼리 묶어서 batch 구성
- 각 batch는 **약 25,000개의 토큰**으로 구성
왜 길이가 비슷한 것끼리 묶냐면
```bash
짧은 문장: "I love you"        (3 토큰)
긴 문장:  "I love you so much" (5 토큰)
```
길이가 다르면 짧은 문장에 \*\*빈 칸(padding)\*\*을 채워야 해서 낭비가 생깁니다. 비슷한 길이끼리 묶으면 **padding을 최소화**할 수 있습니다!
### 5.2 Hardware and Schedule {toggle="true"}
#### 해석
---
우리는 하나의 머신에서 8개의 NVIDIA P100 GPU를 사용하여 모델들을 훈련하였다. 논문 전반에서 설명한 하이퍼파라미터를 사용하는 base model의 경우, 각 training step은 약 0.4초가 걸렸다. 우리는 base model을 총 100,000 step, 즉 약 12시간 동안 훈련하였다.
table 3의 마지막 줄에 설명된 big model의 경우, step당 걸리는 시간은 1.0초였으며, big model은 300,000 step(약 3.5일) 동안 훈련되었다.
---
## 학습 환경 및 시간
<table header-row="true">
<tr>
<td></td>
<td>Base Model</td>
<td>Big Model</td>
</tr>
<tr>
<td>GPU</td>
<td>8개 NVIDIA P100</td>
<td>8개 NVIDIA P100</td>
</tr>
<tr>
<td>Step당 시간</td>
<td>0.4초</td>
<td>1.0초</td>
</tr>
<tr>
<td>총 Step</td>
<td>100,000</td>
<td>300,000</td>
</tr>
<tr>
<td>총 학습 시간</td>
<td>**12시간**</td>
<td>**3.5일**</td>
</tr>
</table>
---
## 핵심 메시지
기존 SOTA 모델들과 비교했을 때 Transformer는 **훨씬 적은 학습 시간**으로도 더 좋은 성능을 냈다는 것을 강조하는 것입니다.
```bash
기존 모델들: 수일 ~ 수주 학습 필요
Transformer Base: 단 12시간 ✅
Transformer Big:  단 3.5일  ✅
```
### 5.3 Optimizer {toggle="true"}
#### 해석
---
[image omitted: temporary Notion asset]
---
### 5.4 Regularization {toggle="true"}
#### 해석
---
[image omitted: temporary Notion asset]
---
- 1. Residual Dropout<br>Dropout이란?
- 각 서브레이어의 출력에 **Dropout** 적용
- Embedding + Positional Encoding 합산 결과에도 적용
- Base model에서 Pdrop=0.1 사용
Pdrop=0.1P_\{drop\} = 0.1
```bash
    학습 중 랜덤하게 일부 뉴런을 꺼버림
    → 특정 뉴런에 과도하게 의존하지 않도록 방지
    → 더 robust한 표현 학습
```
- 2. Label Smoothing<br>예를 들면
- 학습 중 정답 레이블을 **100%** 확신하지 않도록 살짝 부드럽게 만듦
- ls=0.1l_s = 0.1<br>ls=0.1 사용
```bash
    일반적인 경우:
    정답 "사랑해": 1.0
    나머지:        0.0

    Label Smoothing 적용:
    정답 "사랑해": 0.9
    나머지:        0.1 (조금씩 분배)
```
```bash
단점: 모델이 **덜 확신**하게 되어 perplexity는 나빠짐
장점: **accuracy와 BLEU 점수는 향상**
이유: 모델이 너무 확신하면 **과적합**되기 쉬운데, 살짝 불확실하게 만들어서 방지
```
- 3. (이미 배운) Residual Connection + Layer Normalization
- 각 서브레이어마다 LayerNorm(x+Sublayer(x)) 적용
LayerNorm(x+Sublayer(x))LayerNorm(x + Sublayer(x))
- 학습을 **안정적**으로 만들어줌
### 6. Results {toggle="true"}
### 6.1 Machine Translation {toggle="true"}
#### 해석
---
[image omitted: temporary Notion asset]
[image omitted: temporary Notion asset]
---
- 영어→독일어 결과
<table header-row="true">
<tr>
<td>모델</td>
<td>BLEU 점수</td>
</tr>
<tr>
<td>기존 최고 앙상블 모델</td>
<td>26.36</td>
</tr>
<tr>
<td>Transformer (base)</td>
<td>27.3</td>
</tr>
<tr>
<td>**Transformer (big)**</td>
<td>**28.4**</td>
</tr>
</table>
- Transformer (big)이 기존 모든 모델 **(앙상블 포함)** 을 **+2 BLEU** 차이로 압도
- 학습 비용은 기존 모델들의 **극히 일부**에 불과
- 영어→프랑스어 결과
<table header-row="true">
<tr>
<td>모델</td>
<td>BLEU 점수</td>
</tr>
<tr>
<td>기존 최고 단일 모델</td>
<td>40.46</td>
</tr>
<tr>
<td>**Transformer (big)**</td>
<td>**41.0**</td>
</tr>
</table>
- 단일 모델 기준 **SOTA** 달성
- 기존 SOTA 모델 학습 비용의 **1/4 이하**로 달성
- 학습 방식
- Base model: 마지막 **5개 체크포인트** 평균
- Big model: 마지막 **20개 체크포인트** 평균
- **Beam search** 사용 (beam size = 4, length penalty α = 0.6)
<table header-row="true">

<tr>
<td>역할</td>
<td>언제 사용</td>
</tr>
<tr>
<td>**Checkpoint**</td>
<td>학습 중 모델 저장</td>
</tr>
<tr>
<td>**Beam Search**</td>
<td>단어 생성 시 후보 탐색</td>
</tr>
</table>

## Checkpoint
학습 중간중간 모델 상태를 저장한 것으로, 마지막 여러 개를 **평균내서** 더 안정적인 최종 모델을 만듭니다.
## Beam Search
단어 생성 시 매 단계마다 **확률 높은 후보 k개를 동시에 유지**하면서 탐색해서 가장 좋은 번역을 찾는 방법입니다.
- 핵심 메시지
- 더 **적은 학습 비용**으로 더 **높은 성능** 달성
- RNN, CNN 없이 **Attention만으로** SOTA 달성 가능함을 증명 ✅
### 6.2 Model Variations {toggle="true"}
#### 해석
---
[image omitted: temporary Notion asset]
[image omitted: temporary Notion asset]
---
- (A) Head 수 변화
<table header-row="true">
<tr>
<td>head 수</td>
<td>BLEU</td>
<td>이유</td>
</tr>
<tr>
<td>1</td>
<td>24.9</td>
<td>한 가지 관점만 봐서 표현력 부족</td>
</tr>
<tr>
<td>4</td>
<td>25.5</td>
<td>관점이 늘어나면서 성능 향상</td>
</tr>
<tr>
<td>**8**</td>
<td>**25.8**</td>
<td>성능과 효율의 최적 균형점 ✅</td>
</tr>
<tr>
<td>16</td>
<td>25.8</td>
<td>비슷한 성능이지만 비용 증가</td>
</tr>
<tr>
<td>32</td>
<td>25.4</td>
<td>head당 차원(16)이 너무 작아져서 오히려 하락</td>
</tr>
</table>
- (B) dk 크기 변화<br>Q와 K를 곱해서 나오는 값이 바로 **compatibility 점수**
<table header-row="true">
<tr>
<td>dk</td>
<td>성능</td>
<td>이유</td>
</tr>
<tr>
<td>작음</td>
<td>하락</td>
<td>Q와 K의 유사도 계산 공간이 좁아져서 **compatibility 계산이 어려워짐**</td>
</tr>
<tr>
<td>큼</td>
<td>향상</td>
<td>더 넓은 공간에서 유사도 계산 가능</td>
</tr>
</table>
차원이 작을수록 단어의 **정보가 적게 담겨있어서** 두 단어가 얼마나 관련있는지 정확하게 계산하기 어렵습니다.<br>
- (C) 모델 크기 변화
<table header-row="true">
<tr>
<td>크기</td>
<td>성능</td>
<td>이유</td>
</tr>
<tr>
<td>작음</td>
<td>하락</td>
<td>표현할 수 있는 패턴이 제한됨</td>
</tr>
<tr>
<td>**기본**</td>
<td>**좋음**</td>
<td>성능과 효율의 균형 ✅</td>
</tr>
<tr>
<td>큼</td>
<td>더 좋음</td>
<td>더 복잡한 패턴 학습 가능, 단 학습 비용 증가</td>
</tr>
</table>
- (D) Dropout 효과
<table header-row="true">
<tr>
<td>Dropout</td>
<td>성능</td>
<td>이유</td>
</tr>
<tr>
<td>없음</td>
<td>하락</td>
<td>과적합 발생 → 학습 데이터에만 잘 맞춰짐 ❌</td>
</tr>
<tr>
<td>**있음**</td>
<td>**향상**</td>
<td>과적합 방지 → 더 일반화된 표현 학습 ✅</td>
</tr>
</table>
- (E) Positional Encoding 비교
<table header-row="true">
<tr>
<td>방식</td>
<td>성능</td>
<td>이유</td>
</tr>
<tr>
<td>sin/cos</td>
<td>25.7</td>
<td>긴 시퀀스에도 일반화 가능 ✅</td>
</tr>
<tr>
<td>학습된 방식</td>
<td>25.7</td>
<td>성능은 같지만 긴 시퀀스에 취약 ❌</td>
</tr>
</table>
---
- 핵심 메시지
- **head 수**: 너무 적으면 표현력 부족, 너무 많으면 head당 차원이 작아져서 오히려 하락
- **모델 크기**: 클수록 좋지만 학습 비용과 **트레이드오프**
- **Dropout**: 과적합 방지에 **필수적**
- **Positional Encoding**: sin/cos 방식이 **일반화 측면에서 유리**


## PPL이 뭐야? (Perplexity)

**PPL = Perplexity (언어 모델 품질 지표)**
한 줄로 말하면,
> “모델이 다음 단어를 얼마나 잘 예측하는지 나타내는 점수”
값이 **낮을수록 좋음**.

### 7. Conclusion {toggle="true"}
#### 해석
---
이 연구에서 우리는 Transformer를 제시하였다. Transformer는 전체가 attention에만 기반한 최초의 sequence transduction 모델로, encoder–decoder 구조에서 일반적으로 사용되는 recurrent layer를 multi-headed self-attention으로 대체하였다.
번역 작업에 대해 Transformer는 recurrent layer나 convolutional layer 기반의 구조보다 훨씬 빠르게 훈련될 수 있다. WMT 2014 English-to-German과 WMT 2014 English-to-French 두 번역 작업 모두에서 우리는 새로운 state-of-the-art를 달성하였다. 특히 English-to-German 작업에서는 우리의 최고 모델이 이전에 보고된 모든 ensemble 모델보다도 더 뛰어난 성능을 보였다.
우리는 attention 기반 모델들의 미래를 매우 긍정적으로 보고 있으며, 이를 다른 작업에도 적용할 계획이다. Transformer를 텍스트 이외의 입력과 출력 modality를 포함하는 문제로 확장하고, 이미지·오디오·비디오처럼 큰 입력과 출력에 대해 효율적으로 처리할 수 있도록 local하고 제한된 attention 메커니즘을 연구할 계획이다. 생성 과정의 순차성을 줄이는 방향도 우리의 연구 목표 중 하나이다.
---
